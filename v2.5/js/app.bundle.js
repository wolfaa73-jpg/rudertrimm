/* Generated by scripts/build-classic-bundle.mjs from js/app.mjs. Do not edit directly. */
(() => {
  // js/core.mjs
  var SCHEMA_VERSION = 4;
  var MAX_NAME_LENGTH = 80;
  function truncateCodePoints(value, maxLength = MAX_NAME_LENGTH) {
    if (typeof value !== "string") throw new TypeError("Expected a string");
    if (!Number.isSafeInteger(maxLength) || maxLength < 0) throw new RangeError("maxLength must be a non-negative safe integer");
    return [...value].slice(0, maxLength).join("");
  }
  var MAX_IMPORT_ITEMS = 250;
  var RIGS = Object.freeze(["skull", "riemen"]);
  var BLADES = Object.freeze(["big", "mac"]);
  var LEGACY_SEATS = Object.freeze(["s1", "s2"]);
  var CAPACITY_STATUSES = Object.freeze(["preset", "confirmed", "legacy-assumed"]);
  var SEAT_ROLES = Object.freeze(["single", "bow", "crew", "stroke"]);
  function deepFreeze(value) {
    if (value && typeof value === "object" && !Object.isFrozen(value)) {
      Object.freeze(value);
      for (const child of Object.values(value)) deepFreeze(child);
    }
    return value;
  }
  var CATCH_MODEL = deepFreeze({
    kneeDeg: 58,
    leanDeg: 16,
    sweepShoulderRotationDeg: 16,
    defaultFa: { skull: 32.5, riemen: 50 },
    search: { minDeg: 20, maxDeg: 88, stepDeg: 0.5, refineIterations: 14 }
  });
  var PRESETS = deepFreeze({
    "1x": { rig: "skull", DA: 159, IH: 88, Lbig: 288, Lmac: 298, a: 15, single: true, seatCount: 1 },
    "2x": { rig: "skull", DA: 159, IH: 88, Lbig: 288, Lmac: 300, a: 15, seatCount: 2 },
    // 3x/6x sind ausdrücklich gewünschte Vereinsklassen. Ihre Startwerte
    // übernehmen transparent den 4x-Grundtrimm; sie sind keine World-Rowing-Klasse.
    "3x": { rig: "skull", DA: 158, IH: 87, Lbig: 289, Lmac: 300, a: 15, seatCount: 3, clubClass: true },
    "4x": { rig: "skull", DA: 158, IH: 87, Lbig: 289, Lmac: 300, a: 15, seatCount: 4 },
    "6x": { rig: "skull", DA: 158, IH: 87, Lbig: 289, Lmac: 300, a: 15, seatCount: 6, clubClass: true },
    "2-": { rig: "riemen", DA: 85, IH: 115, Lbig: 374, Lmac: 383, a: 17, seatCount: 2 },
    "4-": { rig: "riemen", DA: 84, IH: 114, Lbig: 375, Lmac: 382, a: 17, seatCount: 4 },
    "4+": { rig: "riemen", DA: 84, IH: 114, Lbig: 375, Lmac: 382, a: 17, seatCount: 4, coxed: true },
    "8+": { rig: "riemen", DA: 83, IH: 113.5, Lbig: 375, Lmac: 384, a: 17, seatCount: 8, coxed: true },
    gigS: { rig: "skull", DA: 160, IH: 88, Lbig: 289, Lmac: 298, a: 15, seatCount: null },
    gigR: { rig: "riemen", DA: 84, IH: 114, Lbig: 375, Lmac: 384, a: 17, seatCount: null },
    wmM1x: { rig: "skull", DA: 160, IH: 88.5, Lbig: 288.5, Lmac: 298, a: 16, single: true, seatCount: 1 },
    wmW1x: { rig: "skull", DA: 160, IH: 88, Lbig: 286.5, Lmac: 296, a: 16, single: true, seatCount: 1 },
    wmM2x: { rig: "skull", DA: 159, IH: 88.5, Lbig: 289, Lmac: 299, a: 16, seatCount: 2 },
    wmW2x: { rig: "skull", DA: 159.5, IH: 87.5, Lbig: 287.5, Lmac: 297, a: 16, seatCount: 2 },
    wmM4x: { rig: "skull", DA: 159, IH: 88, Lbig: 289.5, Lmac: 299, a: 16, seatCount: 4 },
    wmW4x: { rig: "skull", DA: 159, IH: 88, Lbig: 288, Lmac: 298, a: 16, seatCount: 4 },
    "wmM2-": { rig: "riemen", DA: 86, IH: 116, Lbig: 375.5, Lmac: 384, a: 17, seatCount: 2 },
    "wmW2-": { rig: "riemen", DA: 86.5, IH: 116, Lbig: 373, Lmac: 382, a: 17, seatCount: 2 },
    "wmM4-": { rig: "riemen", DA: 85, IH: 115, Lbig: 376, Lmac: 385, a: 17, seatCount: 4 },
    "wmW4-": { rig: "riemen", DA: 85, IH: 115.5, Lbig: 373, Lmac: 382, a: 17, seatCount: 4 },
    "wmM8+": { rig: "riemen", DA: 83.5, IH: 114, Lbig: 377, Lmac: 386, a: 17, seatCount: 8, coxed: true },
    "wmW8+": { rig: "riemen", DA: 84, IH: 114, Lbig: 373, Lmac: 382, a: 17, seatCount: 8, coxed: true }
  });
  var RANGES = deepFreeze({
    skull: { DA: [150, 168], IH: [82, 94], L: [265, 306] },
    riemen: { DA: [78, 92], IH: [106, 122], L: [355, 392] },
    seat: {
      d: [1, 3],
      handGap: [14, 22],
      a: [11, 21],
      anlage: [0, 8],
      aussen: [0, 3],
      dBB: [-2, 3],
      stemmW: [36, 48],
      rollL: [68, 80],
      rueh: [-5, 12]
    },
    rower: {
      legLen: [70, 105],
      torsoLen: [75, 110],
      wingspan: [150, 215],
      SB: [32, 48],
      weight: [45, 120],
      stemmX: [26, 56]
    },
    boat: { phiA: [40, 85], phiR: [20, 60], c: [5, 10], seatOffset: [3, 8] },
    current: { kg: [-20, 20], t: [0, 100] }
  });
  var SCHEMAS = deepFreeze({
    rower: {
      kind: "rower",
      fields: ["schemaVersion", "kind", "externalRef", "name", "legLen", "torsoLen", "wingspan", "SB", "weight", "stemmX"]
    },
    seat: {
      kind: "seat",
      fields: ["schemaVersion", "kind", "id", "trimId", "externalRef", "position", "role", "label", "rig", "DA", "IH", "L", "d", "handGap", "a", "anlage", "aussen", "dBB", "stemmW", "rollL", "rueh", "stemmX", "rowerRef"]
    },
    boat: {
      kind: "boat",
      fields: ["schemaVersion", "kind", "externalRef", "name", "preset", "blade", "rig", "strokeSide", "phiA", "phiR", "c", "seatOffset", "cox", "capacityStatus", "seats", "legacyRigTemplate"]
    },
    crewAssignment: {
      kind: "crewAssignment",
      fields: ["schemaVersion", "kind", "seatId", "trimId", "rowerRef", "rower"]
    },
    currentConfig: {
      kind: "currentConfig",
      fields: ["schemaVersion", "kind", "boatRef", "boat", "crew", "editSeatId", "referenceSeatId", "mode", "heightRef", "kg", "t", "recovery"]
    },
    importKinds: ["rudertrimm.rowers", "rudertrimm.boats", "rudertrimm.current-config"]
  });
  function seatRoleForPosition(position, seatCount) {
    if (!Number.isSafeInteger(position) || !Number.isSafeInteger(seatCount) || seatCount < 1 || position < 1 || position > seatCount) {
      throw new RangeError("seat position must be inside a positive real-seat count");
    }
    if (seatCount === 1) return "single";
    if (position === 1) return "bow";
    if (position === seatCount) return "stroke";
    return "crew";
  }
  function seatLabelForPosition(position, seatCount) {
    const role = seatRoleForPosition(position, seatCount);
    if (role === "single") return "Platz 1 · Einer";
    if (role === "bow") return "Platz 1 · Bug";
    if (role === "stroke") return `Platz ${position} · Schlag`;
    return `Platz ${position}`;
  }
  function planSeatLayoutByRole(priorSeats, targetCount) {
    if (!Array.isArray(priorSeats)) throw new TypeError("priorSeats must be an array");
    if (!Number.isSafeInteger(targetCount) || targetCount < 1 || targetCount > 8) {
      throw new RangeError("targetCount must be an integer between 1 and 8");
    }
    const used = /* @__PURE__ */ new Set();
    const take = (predicate) => {
      const seat = priorSeats.find((candidate) => !used.has(candidate) && predicate(candidate));
      if (seat) used.add(seat);
      return seat ?? null;
    };
    const sources = Array(targetCount).fill(null);
    if (targetCount === 1) {
      sources[0] = take((seat) => seat?.role === "single") ?? take((seat) => seat?.role === "stroke") ?? take((seat) => seat?.position === 1);
    } else {
      sources[0] = take((seat) => seat?.role === "bow") ?? take((seat) => seat?.position === 1 && seat?.role !== "single");
      sources[targetCount - 1] = take((seat) => seat?.role === "stroke") ?? take((seat) => seat?.role === "single");
      for (let position = 2; position < targetCount; position += 1) {
        sources[position - 1] = take((seat) => seat?.position === position && seat?.role === "crew");
      }
    }
    return Object.freeze({
      sources: Object.freeze(sources),
      removed: Object.freeze(priorSeats.filter((seat) => !used.has(seat)))
    });
  }
  function isFiniteNumber(value) {
    return typeof value === "number" && Number.isFinite(value);
  }
  function isInRange(value, min, max) {
    return isFiniteNumber(value) && isFiniteNumber(min) && isFiniteNumber(max) && min <= max && value >= min && value <= max;
  }
  function requireFinite(value, label = "value") {
    if (!isFiniteNumber(value)) throw new TypeError(`${label} must be a finite number`);
    return value;
  }
  function requireRange(value, min, max, label = "value") {
    requireFinite(min, `${label}.min`);
    requireFinite(max, `${label}.max`);
    if (min > max) throw new RangeError(`${label} has an inverted range`);
    requireFinite(value, label);
    if (value < min || value > max) throw new RangeError(`${label} must be in [${min}, ${max}]`);
    return value;
  }
  var STROKE_CYCLE = deepFreeze({ durationMs: 2800, driveShare: 0.45 });
  function normalizedCycleProgress(value) {
    requireFinite(value, "cycleProgress");
    return (value % 1 + 1) % 1;
  }
  function smoothStep(value) {
    const bounded = Math.max(0, Math.min(1, value));
    return bounded * bounded * (3 - 2 * bounded);
  }
  function inverseSmoothStep(value) {
    const bounded = requireRange(value, 0, 1, "smoothStepValue");
    if (bounded === 0 || bounded === 1) return bounded;
    let low = 0;
    let high = 1;
    for (let iteration = 0; iteration < 32; iteration += 1) {
      const middle = (low + high) / 2;
      if (smoothStep(middle) < bounded) low = middle;
      else high = middle;
    }
    return (low + high) / 2;
  }
  function strokePoseAtCycleProgress(cycleProgress) {
    const progress = normalizedCycleProgress(cycleProgress);
    if (progress < STROKE_CYCLE.driveShare) {
      return Object.freeze({
        t: 100 * smoothStep(progress / STROKE_CYCLE.driveShare),
        recovery: false
      });
    }
    return Object.freeze({
      t: 100 * (1 - smoothStep((progress - STROKE_CYCLE.driveShare) / (1 - STROKE_CYCLE.driveShare))),
      recovery: true
    });
  }
  function cycleProgressFromStrokePose({ t, recovery }) {
    requireRange(t, 0, 100, "t");
    if (typeof recovery !== "boolean") throw new TypeError("recovery must be a boolean");
    if (!recovery) return STROKE_CYCLE.driveShare * inverseSmoothStep(t / 100);
    return STROKE_CYCLE.driveShare + (1 - STROKE_CYCLE.driveShare) * inverseSmoothStep((100 - t) / 100);
  }
  function advanceStrokeCycle(cycleProgress, elapsedMs, speed) {
    requireFinite(elapsedMs, "elapsedMs");
    requireFinite(speed, "speed");
    if (elapsedMs < 0) throw new RangeError("elapsedMs must not be negative");
    if (speed <= 0) throw new RangeError("speed must be positive");
    return normalizedCycleProgress(
      normalizedCycleProgress(cycleProgress) + elapsedMs * speed / STROKE_CYCLE.durationMs
    );
  }
  function clamp(value, min, max) {
    requireFinite(value);
    requireFinite(min, "min");
    requireFinite(max, "max");
    if (min > max) throw new RangeError("min must not exceed max");
    return Math.max(min, Math.min(max, value));
  }
  function findHighestReachableAngle({
    minDeg,
    maxDeg,
    stepDeg = 0.5,
    refineIterations = 14,
    isReachable
  }) {
    requireFinite(minDeg, "minDeg");
    requireFinite(maxDeg, "maxDeg");
    requireFinite(stepDeg, "stepDeg");
    if (minDeg >= maxDeg) throw new RangeError("minDeg must be smaller than maxDeg");
    if (stepDeg <= 0) throw new RangeError("stepDeg must be positive");
    if (!Number.isSafeInteger(refineIterations) || refineIterations < 0 || refineIterations > 30) {
      throw new RangeError("refineIterations must be an integer in [0, 30]");
    }
    if (typeof isReachable !== "function") throw new TypeError("isReachable must be a function");
    const check = (angle) => {
      const result = isReachable(angle);
      if (typeof result !== "boolean") throw new TypeError("isReachable must return a boolean");
      return result;
    };
    if (check(maxDeg)) return Object.freeze({ angleDeg: maxDeg, reachable: true, limited: false });
    const sampleCount = Math.ceil((maxDeg - minDeg) / stepDeg);
    let upper = maxDeg;
    for (let index = 1; index <= sampleCount; index += 1) {
      const candidate = Math.max(minDeg, maxDeg - index * stepDeg);
      if (check(candidate)) {
        let low = candidate;
        let high = upper;
        for (let iteration = 0; iteration < refineIterations; iteration += 1) {
          const middle = (low + high) / 2;
          if (check(middle)) low = middle;
          else high = middle;
        }
        return Object.freeze({ angleDeg: low, reachable: true, limited: true });
      }
      upper = candidate;
      if (candidate === minDeg) break;
    }
    return Object.freeze({ angleDeg: minDeg, reachable: false, limited: true });
  }
  function requireRig(rig) {
    if (!RIGS.includes(rig)) throw new RangeError(`Unknown rig: ${String(rig)}`);
    return rig;
  }
  function defaultFaForRig(rig) {
    return CATCH_MODEL.defaultFa[requireRig(rig)];
  }
  function deriveBodySegments({ legLen, torsoLen, wingspan, SB }) {
    for (const [key, value] of Object.entries({ legLen, torsoLen, wingspan, SB })) requireFinite(value, key);
    if (legLen <= 0 || torsoLen <= 0 || wingspan <= 0 || SB <= 0) throw new RangeError("Body measurements must be positive");
    const reach = Math.max(24, (wingspan - SB) / 2 - 8);
    const HEAD = 24;
    const NECK = Math.max(6, torsoLen * 0.09);
    return Object.freeze({
      OS: legLen / 2,
      US: legLen / 2,
      T: Math.max(30, torsoLen - HEAD - NECK),
      OA: reach * 0.48,
      UA: reach * 0.52,
      HEAD,
      NECK,
      height: legLen + torsoLen
    });
  }
  function solveNaturalCatchAngle({ rig, DA, inboardFromPin, outboardFromPin, a, c, kg, rower }) {
    requireRig(rig);
    for (const [key, value] of Object.entries({ DA, inboardFromPin, outboardFromPin, a, c, kg })) requireFinite(value, key);
    if (DA <= 0 || inboardFromPin <= 0 || outboardFromPin <= 0) throw new RangeError("Rig distances must be positive");
    if (!rower || typeof rower !== "object" || Array.isArray(rower)) throw new TypeError("rower must be an object");
    for (const key of ["legLen", "torsoLen", "wingspan", "SB", "stemmX", "rollL", "rueh"]) requireFinite(rower[key], `rower.${key}`);
    if (rower.rollL <= 0) throw new RangeError("rower.rollL must be positive");
    const rad = (degrees) => degrees * Math.PI / 180;
    const smoothStep2 = (value) => {
      const x = Math.max(0, Math.min(1, value));
      return x * x * (3 - 2 * x);
    };
    const segments = deriveBodySegments(rower);
    const foot = { x: rower.stemmX, y: c - 18 };
    const hipY = c + 4;
    const dyLeg = hipY - foot.y;
    const legDistance = (angle) => Math.sqrt(segments.OS ** 2 + segments.US ** 2 - 2 * segments.OS * segments.US * Math.cos(angle));
    const hipForKnee = (angle) => foot.x - Math.sqrt(Math.max(1, legDistance(angle) ** 2 - dyLeg ** 2));
    const nominalHipX = hipForKnee(rad(CATCH_MODEL.kneeDeg));
    const seatReferenceOffset = 5;
    const anatomyMin = hipForKnee(rad(172));
    const anatomyMax = hipForKnee(rad(45));
    const rawMin = Math.max(anatomyMin, rower.rueh - rower.rollL + seatReferenceOffset);
    const rawMax = Math.min(anatomyMax, rower.rueh + seatReferenceOffset);
    const hipMin = Math.min(rawMin, rawMax);
    const hipMax = Math.max(rawMin, rawMax);
    const hipX = Math.max(hipMin, Math.min(hipMax, nominalHipX));
    const trackLimited = rawMin > rawMax || Math.abs(hipX - nominalHipX) > 0.05;
    const lean = rad(CATCH_MODEL.leanDeg);
    const flex = segments.T * 0.42 * smoothStep2(lean / rad(CATCH_MODEL.leanDeg));
    const shoulder = {
      x: hipX + segments.T * Math.sin(lean) + 0.35 * flex,
      y: hipY + segments.T * Math.cos(lean) - 0.9 * flex
    };
    const DAr = rig === "skull" ? DA / 2 : DA;
    const waterline = kg * 0.1;
    const pinY = c + a;
    const bladeLiftAtCatch = -8;
    const sinBlade = Math.max(-0.5, Math.min(0.5, (pinY - waterline - bladeLiftAtCatch) / outboardFromPin));
    const handY = pinY + inboardFromPin * sinBlade;
    const armLength3D = segments.OA + segments.UA;
    let evaluations = 0;
    const residual = (angleDeg2) => {
      evaluations += 1;
      const theta = rad(angleDeg2);
      const wristZ = DAr - inboardFromPin * Math.cos(theta);
      const lateral = rig === "skull" ? Math.abs(wristZ - rower.SB / 2) : Math.abs(wristZ + Math.cos(rad(CATCH_MODEL.sweepShoulderRotationDeg)) * rower.SB / 2);
      const sagittalArm = Math.sqrt(Math.max((armLength3D * 0.55) ** 2, armLength3D ** 2 - lateral ** 2));
      const vertical = rig === "skull" ? Math.max(Math.abs(shoulder.y - (handY - 1.5)), Math.abs(shoulder.y - (handY + 1.5))) : Math.abs(shoulder.y - handY);
      const horizontalSquared = sagittalArm ** 2 - vertical ** 2;
      const achievedX = shoulder.x + (horizontalSquared <= 4 ? 0 : Math.sqrt(horizontalSquared));
      const requiredX = inboardFromPin * Math.sin(theta);
      return requiredX - achievedX;
    };
    const { minDeg, maxDeg, stepDeg, refineIterations } = CATCH_MODEL.search;
    const sampleCount = Math.round((maxDeg - minDeg) / stepDeg);
    let previous = null;
    let best = { angle: minDeg, value: residual(minDeg) };
    let bracket = null;
    for (let index = 0; index <= sampleCount; index += 1) {
      const angle = Math.min(maxDeg, minDeg + index * stepDeg);
      const value = index === 0 ? best.value : residual(angle);
      if (Math.abs(value) < Math.abs(best.value)) best = { angle, value };
      if (previous && previous.value * value <= 0) {
        const score = Math.min(Math.abs(previous.value), Math.abs(value));
        if (!bracket || score < bracket.score) bracket = { lo: previous.angle, hi: angle, flo: previous.value, score };
      }
      previous = { angle, value };
    }
    let angleDeg;
    const bracketed = Boolean(bracket);
    if (bracket) {
      let { lo, hi, flo } = bracket;
      for (let iteration = 0; iteration < refineIterations; iteration += 1) {
        const middle = (lo + hi) / 2;
        const value = residual(middle);
        if (flo * value <= 0) hi = middle;
        else {
          lo = middle;
          flo = value;
        }
      }
      angleDeg = (lo + hi) / 2;
    } else {
      let lo = Math.max(minDeg, best.angle - stepDeg);
      let hi = Math.min(maxDeg, best.angle + stepDeg);
      for (let iteration = 0; iteration < refineIterations; iteration += 1) {
        const left = (2 * lo + hi) / 3;
        const right = (lo + 2 * hi) / 3;
        if (Math.abs(residual(left)) <= Math.abs(residual(right))) hi = right;
        else lo = left;
      }
      angleDeg = (lo + hi) / 2;
    }
    const finalResidual = residual(angleDeg);
    return Object.freeze({
      angleDeg,
      residualCm: finalResidual,
      hipX,
      trackLimited,
      bracketed,
      evaluations,
      modelStatus: "needsCalibration"
    });
  }
  function derivedGeometry({ rig, DA, IH, L, d, a, phiA, phiR, t = 0, c = 8, kg = 0 }) {
    requireRig(rig);
    for (const [key, value] of Object.entries({ DA, IH, L, d, a, phiA, phiR, t, c, kg })) {
      requireFinite(value, key);
    }
    requireRange(t, 0, 100, "t");
    const inb = IH + d;
    const outb = L - IH - d;
    if (inb <= 0) throw new RangeError("IH + d must be positive");
    if (outb <= 0) throw new RangeError("L - IH - d must be positive");
    const skull = rig === "skull";
    const overlap = skull ? 2 * inb - DA : inb - DA;
    const outsideLever = L - IH;
    const strokeWidth = phiA + phiR;
    if (strokeWidth <= 0) throw new RangeError("phiA + phiR must be positive");
    const shareBeforeOrthogonal = 100 * phiA / strokeWidth;
    const pinHeightAboveWater = a + c - kg * 0.1;
    const targetIH = skull ? DA / 2 + 8 : DA + 30;
    const theta = phiA - t / 100 * strokeWidth;
    const DAr = skull ? DA / 2 : DA;
    return Object.freeze({
      rig,
      skull,
      inb,
      outb,
      inboardFromPin: inb,
      outboardFromPin: outb,
      overlap,
      U: overlap,
      outsideLever,
      AH: outsideLever,
      strokeWidth,
      SW: strokeWidth,
      shareBeforeOrthogonal,
      antV: shareBeforeOrthogonal,
      pinHeightAboveWater,
      dWL: pinHeightAboveWater,
      targetIH,
      IHsoll: targetIH,
      theta,
      DA,
      DAr,
      phiA
    });
  }
  function forceRatio({ L, IH, d }) {
    for (const [key, value] of Object.entries({ L, IH, d })) requireFinite(value, key);
    const inb = IH + d;
    const outb = L - IH - d;
    if (inb <= 0 || outb <= 0) throw new RangeError("Moment arms must both be positive");
    return outb / inb;
  }
  function solveInboardForRatio({ L, d, targetRatio, range, step = 0.5 }) {
    requireFinite(L, "L");
    requireFinite(d, "d");
    requireFinite(targetRatio, "targetRatio");
    requireFinite(step, "step");
    if (L <= 0 || d < 0 || targetRatio <= 0 || step < 0) throw new RangeError("Invalid solve parameters");
    const rawIH = L / (targetRatio + 1) - d;
    let IH = step > 0 ? Math.round(rawIH / step) * step : rawIH;
    let clamped = false;
    if (range !== void 0) {
      if (!Array.isArray(range) || range.length !== 2) throw new TypeError("range must be [min, max]");
      const [min, max] = range;
      requireFinite(min, "range[0]");
      requireFinite(max, "range[1]");
      if (min > max) throw new RangeError("range is inverted");
      const bounded = clamp(IH, min, max);
      clamped = bounded !== IH;
      IH = bounded;
    }
    const achievedRatio = forceRatio({ L, IH, d });
    return Object.freeze({
      IH,
      rawIH,
      targetRatio,
      achievedRatio,
      delta: achievedRatio - targetRatio,
      rounded: step > 0 && Math.abs(IH - rawIH) > 1e-12,
      clamped,
      status: clamped ? "clamped" : "ok"
    });
  }
  var ORTHOGONAL_POLICY = deepFreeze({
    kneeDeg: { target: 165, min: 160, max: 170 },
    rollwayPct: { target: 75, min: 70, max: 80 }
  });
  var ARM_ANGLE_POLICY = deepFreeze({
    definition: "Signed shoulder-to-hand angle in the sagittal x/y plane relative to horizontal; positive when the hand is below the shoulder.",
    targetDeg: 6,
    toleranceDeg: 3,
    calibrated: false,
    source: "DRV Trimmhandbuch figure reference; exact operational tolerance still requires calibration."
  });
  function isPlainRecord(value) {
    return value !== null && typeof value === "object" && !Array.isArray(value) && (Object.getPrototypeOf(value) === Object.prototype || Object.getPrototypeOf(value) === null);
  }
  function addError(errors, path, code, message) {
    errors.push(Object.freeze({ path, code, message }));
  }
  function exactRecord(value, fields, path, errors) {
    if (!isPlainRecord(value)) {
      addError(errors, path, "type", "Expected a plain object");
      return false;
    }
    const allowed = new Set(fields);
    for (const key of Object.keys(value)) {
      if (!allowed.has(key)) addError(errors, `${path}.${key}`, "unknownField", "Unknown field");
    }
    for (const key of fields) {
      if (!Object.hasOwn(value, key)) addError(errors, `${path}.${key}`, "required", "Required field is missing");
    }
    return true;
  }
  function checkConst(value, expected, path, errors) {
    if (value !== expected) addError(errors, path, "const", `Expected ${JSON.stringify(expected)}`);
  }
  function checkEnum(value, allowed, path, errors) {
    if (!allowed.includes(value)) addError(errors, path, "enum", `Expected one of: ${allowed.join(", ")}`);
  }
  function checkBoolean(value, path, errors) {
    if (typeof value !== "boolean") addError(errors, path, "type", "Expected a boolean");
  }
  function checkNumber(value, range, path, errors) {
    if (!isFiniteNumber(value)) {
      addError(errors, path, "finite", "Expected a finite number");
    } else if (range && !isInRange(value, range[0], range[1])) {
      addError(errors, path, "range", `Expected a value in [${range[0]}, ${range[1]}]`);
    }
  }
  var FORBIDDEN_NAME_CHARS = /[<>\u0000-\u001f\u007f\u202a-\u202e\u2066-\u2069]/u;
  function hasInvalidUnicodeScalar(value) {
    for (let index = 0; index < value.length; index += 1) {
      const first = value.charCodeAt(index);
      let codePoint = first;
      if (first >= 55296 && first <= 56319) {
        const second = value.charCodeAt(index + 1);
        if (!(second >= 56320 && second <= 57343)) return true;
        codePoint = 65536 + (first - 55296) * 1024 + (second - 56320);
        index += 1;
      } else if (first >= 56320 && first <= 57343) {
        return true;
      }
      if (codePoint >= 64976 && codePoint <= 65007 || (codePoint & 65535) >= 65534) return true;
    }
    return false;
  }
  function checkName(value, path, errors) {
    if (typeof value !== "string") {
      addError(errors, path, "type", "Expected a string");
      return;
    }
    const length = [...value].length;
    if (value !== value.trim()) addError(errors, path, "whitespace", "Leading or trailing whitespace is not allowed");
    if (length < 1 || length > MAX_NAME_LENGTH) addError(errors, path, "length", `Name must contain 1-${MAX_NAME_LENGTH} characters`);
    if (FORBIDDEN_NAME_CHARS.test(value)) addError(errors, path, "unsafeText", "Markup, controls, and bidi controls are not allowed in names");
    if (hasInvalidUnicodeScalar(value)) addError(errors, path, "unicode", "Lone surrogates and Unicode noncharacters are not allowed in names");
  }
  function validationResult(value, errors) {
    const frozenErrors = Object.freeze(errors);
    return Object.freeze({ ok: errors.length === 0, value: errors.length === 0 ? value : null, errors: frozenErrors });
  }
  var LEGACY_SCHEMA_VERSION = 2;
  var PREVIOUS_SCHEMA_VERSION = 3;
  var STABLE_ID_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;
  var LEGACY_ROWER_FIELDS = Object.freeze([
    "schemaVersion",
    "kind",
    "name",
    "legLen",
    "torsoLen",
    "wingspan",
    "SB",
    "weight",
    "stemmX"
  ]);
  var LEGACY_SEAT_FIELDS = Object.freeze([
    "schemaVersion",
    "kind",
    "rig",
    "DA",
    "IH",
    "L",
    "d",
    "handGap",
    "a",
    "anlage",
    "aussen",
    "dBB",
    "stemmW",
    "rollL",
    "rueh"
  ]);
  var LEGACY_BOAT_FIELDS = Object.freeze([
    "schemaVersion",
    "kind",
    "name",
    "preset",
    "blade",
    "rig",
    "strokeSide",
    "phiA",
    "phiR",
    "c",
    "seatOffset",
    "s1",
    "s2"
  ]);
  var LEGACY_CURRENT_FIELDS = Object.freeze([
    "schemaVersion",
    "kind",
    "boat",
    "crew",
    "editSeat",
    "mode",
    "heightRef",
    "kg",
    "t",
    "recovery"
  ]);
  var PREVIOUS_SEAT_FIELDS = Object.freeze(SCHEMAS.seat.fields.filter((field) => field !== "role"));
  var PREVIOUS_BOAT_FIELDS = Object.freeze(SCHEMAS.boat.fields.filter((field) => field !== "cox"));
  function checkStableId(value, path, errors) {
    if (typeof value !== "string" || !STABLE_ID_PATTERN.test(value)) {
      addError(errors, path, "id", "Expected a stable 1-128 character local id");
    }
  }
  function checkPositiveRevision(value, path, errors) {
    if (!Number.isSafeInteger(value) || value < 1) {
      addError(errors, path, "revision", "Expected a positive safe-integer revision");
    }
  }
  function checkExternalRef(value, path, errors) {
    if (value === null) return;
    if (!exactRecord(value, ["system", "scope", "id"], path, errors)) return;
    for (const key of ["system", "scope", "id"]) {
      checkName(value[key], `${path}.${key}`, errors);
    }
  }
  function checkCox(value, path, errors) {
    if (value === null) return;
    if (!exactRecord(value, ["name"], path, errors)) return;
    checkName(value.name, `${path}.name`, errors);
  }
  function checkRecordRef(value, path, errors, { nullable = true } = {}) {
    if (value === null && nullable) return;
    if (!exactRecord(value, ["id", "revision"], path, errors)) return;
    checkStableId(value.id, `${path}.id`, errors);
    checkPositiveRevision(value.revision, `${path}.revision`, errors);
  }
  function recordRefEqual(left, right) {
    if (left === null || right === null) return left === right;
    if (!isPlainRecord(left) || !isPlainRecord(right)) return false;
    return left.id === right.id && left.revision === right.revision;
  }
  function canonicalJson(value) {
    if (value === null || typeof value !== "object") return JSON.stringify(value);
    if (Array.isArray(value)) return `[${value.map(canonicalJson).join(",")}]`;
    const keys = Object.keys(value).sort();
    return `{${keys.map((key) => `${JSON.stringify(key)}:${canonicalJson(value[key])}`).join(",")}}`;
  }
  function stableLegacyId(prefix, value) {
    const text = canonicalJson(value);
    let high = 2654435769;
    let low = 2166136261;
    for (let index = 0; index < text.length; index += 1) {
      const code = text.charCodeAt(index);
      low = Math.imul(low ^ code, 16777619) >>> 0;
      high = Math.imul(high ^ code + index, 2246822507) >>> 0;
    }
    return `${prefix}-${high.toString(16).padStart(8, "0")}${low.toString(16).padStart(8, "0")}`;
  }
  function copyExternalRef(value) {
    return value === null ? null : { system: value.system, scope: value.scope, id: value.id };
  }
  function copyRecordRef(value) {
    return value === null ? null : { id: value.id, revision: value.revision };
  }
  function validateLegacyRower(value, path = "rower") {
    const errors = [];
    if (!exactRecord(value, LEGACY_ROWER_FIELDS, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, LEGACY_SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "rower", `${path}.kind`, errors);
    checkName(value.name, `${path}.name`, errors);
    for (const key of ["legLen", "torsoLen", "wingspan", "SB", "weight", "stemmX"]) {
      checkNumber(value[key], RANGES.rower[key], `${path}.${key}`, errors);
    }
    if (isFiniteNumber(value.SB) && isFiniteNumber(value.wingspan) && value.SB >= value.wingspan) {
      addError(errors, `${path}.SB`, "geometry", "Shoulder width must be smaller than wingspan");
    }
    return validationResult(value, errors);
  }
  function validateLegacySeat(value, path = "seat") {
    const errors = [];
    if (!exactRecord(value, LEGACY_SEAT_FIELDS, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, LEGACY_SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "seat", `${path}.kind`, errors);
    checkEnum(value.rig, RIGS, `${path}.rig`, errors);
    const rigRanges = RANGES[value.rig];
    for (const key of ["DA", "IH", "L"]) checkNumber(value[key], rigRanges?.[key], `${path}.${key}`, errors);
    for (const key of Object.keys(RANGES.seat)) checkNumber(value[key], RANGES.seat[key], `${path}.${key}`, errors);
    if (isFiniteNumber(value.L) && isFiniteNumber(value.IH) && isFiniteNumber(value.d) && value.L - value.IH - value.d <= 0) {
      addError(errors, `${path}.L`, "geometry", "Outboard moment arm must be positive");
    }
    return validationResult(value, errors);
  }
  function validateLegacyBoat(value, path = "boat") {
    const errors = [];
    if (!exactRecord(value, LEGACY_BOAT_FIELDS, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, LEGACY_SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "boat", `${path}.kind`, errors);
    checkName(value.name, `${path}.name`, errors);
    checkEnum(value.preset, Object.keys(PRESETS), `${path}.preset`, errors);
    checkEnum(value.blade, BLADES, `${path}.blade`, errors);
    checkEnum(value.rig, RIGS, `${path}.rig`, errors);
    if (value.strokeSide !== -1 && value.strokeSide !== 1) addError(errors, `${path}.strokeSide`, "enum", "Expected -1 or 1");
    for (const key of ["phiA", "phiR", "c", "seatOffset"]) checkNumber(value[key], RANGES.boat[key], `${path}.${key}`, errors);
    for (const key of LEGACY_SEATS) {
      errors.push(...validateLegacySeat(value[key], `${path}.${key}`).errors);
      if (isPlainRecord(value[key]) && RIGS.includes(value.rig) && value[key].rig !== value.rig) {
        addError(errors, `${path}.${key}.rig`, "coherence", "Seat rig must match boat rig");
      }
    }
    if (PRESETS[value.preset] && RIGS.includes(value.rig) && PRESETS[value.preset].rig !== value.rig) {
      addError(errors, `${path}.rig`, "coherence", "Preset and boat rig do not match");
    }
    return validationResult(value, errors);
  }
  function validateLegacyCurrentConfig(value, path = "config") {
    const errors = [];
    if (!exactRecord(value, LEGACY_CURRENT_FIELDS, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, LEGACY_SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "currentConfig", `${path}.kind`, errors);
    errors.push(...validateLegacyBoat(value.boat, `${path}.boat`).errors);
    if (exactRecord(value.crew, ["s1", "s2"], `${path}.crew`, errors)) {
      errors.push(...validateLegacyRower(value.crew.s1, `${path}.crew.s1`).errors);
      const single = isPlainRecord(value.boat) && PRESETS[value.boat.preset]?.single;
      if (!(single && value.crew.s2 === null)) {
        errors.push(...validateLegacyRower(value.crew.s2, `${path}.crew.s2`).errors);
      }
    }
    checkEnum(value.editSeat, LEGACY_SEATS, `${path}.editSeat`, errors);
    checkEnum(value.mode, ["werkstatt", "wasser"], `${path}.mode`, errors);
    checkEnum(value.heightRef, ["sitz", "schiene"], `${path}.heightRef`, errors);
    checkNumber(value.kg, RANGES.current.kg, `${path}.kg`, errors);
    checkNumber(value.t, RANGES.current.t, `${path}.t`, errors);
    checkBoolean(value.recovery, `${path}.recovery`, errors);
    if (isPlainRecord(value.boat) && PRESETS[value.boat.preset]?.single && value.editSeat === "s2") {
      addError(errors, `${path}.editSeat`, "coherence", "A single boat cannot edit seat s2");
    }
    return validationResult(value, errors);
  }
  function validateRower(value, path = "rower") {
    const errors = [];
    if (!exactRecord(value, SCHEMAS.rower.fields, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "rower", `${path}.kind`, errors);
    checkExternalRef(value.externalRef, `${path}.externalRef`, errors);
    checkName(value.name, `${path}.name`, errors);
    for (const key of ["legLen", "torsoLen", "wingspan", "SB", "weight", "stemmX"]) {
      checkNumber(value[key], RANGES.rower[key], `${path}.${key}`, errors);
    }
    if (isFiniteNumber(value.SB) && isFiniteNumber(value.wingspan) && value.SB >= value.wingspan) {
      addError(errors, `${path}.SB`, "geometry", "Shoulder width must be smaller than wingspan");
    }
    return validationResult(value, errors);
  }
  function validateSeat(value, path = "seat") {
    const errors = [];
    if (!exactRecord(value, SCHEMAS.seat.fields, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "seat", `${path}.kind`, errors);
    checkStableId(value.id, `${path}.id`, errors);
    checkStableId(value.trimId, `${path}.trimId`, errors);
    checkExternalRef(value.externalRef, `${path}.externalRef`, errors);
    if (!Number.isSafeInteger(value.position) || value.position < 1 || value.position > 64) {
      addError(errors, `${path}.position`, "range", "Expected a seat position in [1, 64]");
    }
    checkEnum(value.role, SEAT_ROLES, `${path}.role`, errors);
    checkName(value.label, `${path}.label`, errors);
    checkEnum(value.rig, RIGS, `${path}.rig`, errors);
    const rigRanges = RANGES[value.rig];
    for (const key of ["DA", "IH", "L"]) checkNumber(value[key], rigRanges?.[key], `${path}.${key}`, errors);
    for (const key of Object.keys(RANGES.seat)) checkNumber(value[key], RANGES.seat[key], `${path}.${key}`, errors);
    checkNumber(value.stemmX, RANGES.rower.stemmX, `${path}.stemmX`, errors);
    checkRecordRef(value.rowerRef, `${path}.rowerRef`, errors);
    if (isFiniteNumber(value.L) && isFiniteNumber(value.IH) && isFiniteNumber(value.d) && value.L - value.IH - value.d <= 0) {
      addError(errors, `${path}.L`, "geometry", "Outboard moment arm must be positive");
    }
    return validationResult(value, errors);
  }
  function validateBoat(value, path = "boat") {
    const errors = [];
    if (!exactRecord(value, SCHEMAS.boat.fields, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "boat", `${path}.kind`, errors);
    checkExternalRef(value.externalRef, `${path}.externalRef`, errors);
    checkName(value.name, `${path}.name`, errors);
    checkEnum(value.preset, Object.keys(PRESETS), `${path}.preset`, errors);
    checkEnum(value.blade, BLADES, `${path}.blade`, errors);
    checkEnum(value.rig, RIGS, `${path}.rig`, errors);
    checkEnum(value.capacityStatus, CAPACITY_STATUSES, `${path}.capacityStatus`, errors);
    if (value.strokeSide !== -1 && value.strokeSide !== 1) addError(errors, `${path}.strokeSide`, "enum", "Expected -1 or 1");
    for (const key of ["phiA", "phiR", "c", "seatOffset"]) checkNumber(value[key], RANGES.boat[key], `${path}.${key}`, errors);
    checkCox(value.cox, `${path}.cox`, errors);
    if (value.cox !== null && !PRESETS[value.preset]?.coxed) {
      addError(errors, `${path}.cox`, "coherence", "Cox metadata is allowed only for a coxed boat class");
    }
    if (PRESETS[value.preset] && RIGS.includes(value.rig) && PRESETS[value.preset].rig !== value.rig) {
      addError(errors, `${path}.rig`, "coherence", "Preset and boat rig do not match");
    }
    if (!Array.isArray(value.seats)) {
      addError(errors, `${path}.seats`, "type", "Expected an ordered seat array");
    } else {
      if (value.seats.length < 1 || value.seats.length > 64) {
        addError(errors, `${path}.seats`, "length", "Expected 1-64 real seats");
      }
      const expectedCount = PRESETS[value.preset]?.seatCount;
      if (Number.isSafeInteger(expectedCount) && value.seats.length !== expectedCount) {
        addError(errors, `${path}.seats`, "capacity", `Preset ${String(value.preset)} requires ${expectedCount} seats`);
      }
      if (expectedCount === null && value.capacityStatus === "preset") {
        addError(errors, `${path}.capacityStatus`, "coherence", "Gig capacity must be confirmed or marked legacy-assumed");
      }
      const seatIds = /* @__PURE__ */ new Set();
      const trimIds = /* @__PURE__ */ new Set();
      const rowerIds = /* @__PURE__ */ new Set();
      for (let index = 0; index < value.seats.length; index += 1) {
        const seat = value.seats[index];
        errors.push(...validateSeat(seat, `${path}.seats[${index}]`).errors);
        if (isPlainRecord(seat)) {
          if (seat.position !== index + 1) {
            addError(errors, `${path}.seats[${index}].position`, "order", "Seat positions must be ordered and contiguous");
          }
          const expectedRole = seatRoleForPosition(index + 1, value.seats.length);
          if (seat.role !== expectedRole) {
            addError(errors, `${path}.seats[${index}].role`, "coherence", `Expected ${expectedRole} for position ${index + 1}`);
          }
          const expectedLabel = seatLabelForPosition(index + 1, value.seats.length);
          if (seat.label !== expectedLabel) {
            addError(errors, `${path}.seats[${index}].label`, "coherence", `Expected ${expectedLabel} for position ${index + 1}`);
          }
          if (seatIds.has(seat.id)) addError(errors, `${path}.seats[${index}].id`, "duplicate", "Seat id must be unique");
          if (trimIds.has(seat.trimId)) addError(errors, `${path}.seats[${index}].trimId`, "duplicate", "Trim id must be unique");
          seatIds.add(seat.id);
          trimIds.add(seat.trimId);
          if (RIGS.includes(value.rig) && seat.rig !== value.rig) {
            addError(errors, `${path}.seats[${index}].rig`, "coherence", "Seat rig must match boat rig");
          }
          if (isPlainRecord(seat.rowerRef)) {
            if (rowerIds.has(seat.rowerRef.id)) {
              addError(errors, `${path}.seats[${index}].rowerRef.id`, "duplicate", "One rower cannot occupy two seats");
            }
            rowerIds.add(seat.rowerRef.id);
          }
        }
      }
    }
    if (value.legacyRigTemplate !== null) {
      errors.push(...validateLegacySeat(value.legacyRigTemplate, `${path}.legacyRigTemplate`).errors);
      if (!PRESETS[value.preset]?.single) {
        addError(errors, `${path}.legacyRigTemplate`, "coherence", "Only 1x may retain the hidden legacy rig template");
      } else if (isPlainRecord(value.legacyRigTemplate) && value.legacyRigTemplate.rig !== value.rig) {
        addError(errors, `${path}.legacyRigTemplate.rig`, "coherence", "Legacy rig template must match the boat rig");
      } else if (Array.isArray(value.seats) && isPlainRecord(value.seats[0])) {
        const fields = LEGACY_SEAT_FIELDS.filter((key) => key !== "schemaVersion" && key !== "kind");
        const redundant = fields.every((key) => value.legacyRigTemplate[key] === value.seats[0][key]);
        if (redundant) {
          addError(errors, `${path}.legacyRigTemplate`, "redundant", "Legacy rig template is allowed only when it preserves a distinct hidden v2 rig");
        }
      }
    }
    return validationResult(value, errors);
  }
  var CURRENT_FIELDS = SCHEMAS.currentConfig.fields;
  function validateCrewAssignment(value, path = "assignment") {
    const errors = [];
    if (!exactRecord(value, SCHEMAS.crewAssignment.fields, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "crewAssignment", `${path}.kind`, errors);
    checkStableId(value.seatId, `${path}.seatId`, errors);
    checkStableId(value.trimId, `${path}.trimId`, errors);
    checkRecordRef(value.rowerRef, `${path}.rowerRef`, errors);
    errors.push(...validateRower(value.rower, `${path}.rower`).errors);
    return validationResult(value, errors);
  }
  function validateCurrentConfig(value, path = "config") {
    const errors = [];
    if (!exactRecord(value, CURRENT_FIELDS, path, errors)) return validationResult(value, errors);
    checkConst(value.schemaVersion, SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    checkConst(value.kind, "currentConfig", `${path}.kind`, errors);
    checkRecordRef(value.boatRef, `${path}.boatRef`, errors);
    const boat = validateBoat(value.boat, `${path}.boat`);
    errors.push(...boat.errors);
    if (!Array.isArray(value.crew)) {
      addError(errors, `${path}.crew`, "type", "Expected an ordered occupied-seat array");
    } else if (isPlainRecord(value.boat) && Array.isArray(value.boat.seats)) {
      const seatRecords = value.boat.seats.filter(isPlainRecord);
      const seatsById = new Map(seatRecords.map((seat) => [seat.id, seat]));
      const crewSeatIds = /* @__PURE__ */ new Set();
      const crewRowerIds = /* @__PURE__ */ new Set();
      let lastPosition = 0;
      for (let index = 0; index < value.crew.length; index += 1) {
        const assignment = value.crew[index];
        errors.push(...validateCrewAssignment(assignment, `${path}.crew[${index}]`).errors);
        if (!isPlainRecord(assignment)) continue;
        const seat = seatsById.get(assignment.seatId);
        if (!seat) {
          addError(errors, `${path}.crew[${index}].seatId`, "reference", "Assignment seat does not exist");
          continue;
        }
        if (crewSeatIds.has(assignment.seatId)) {
          addError(errors, `${path}.crew[${index}].seatId`, "duplicate", "A seat may have only one assignment");
        }
        if (isPlainRecord(assignment.rowerRef) && crewRowerIds.has(assignment.rowerRef.id)) {
          addError(errors, `${path}.crew[${index}].rowerRef.id`, "duplicate", "One rower cannot occupy two seats");
        }
        crewSeatIds.add(assignment.seatId);
        if (isPlainRecord(assignment.rowerRef)) crewRowerIds.add(assignment.rowerRef.id);
        if (seat.position <= lastPosition) {
          addError(errors, `${path}.crew[${index}]`, "order", "Crew snapshots must follow seat order");
        }
        lastPosition = seat.position;
        if (assignment.trimId !== seat.trimId) {
          addError(errors, `${path}.crew[${index}].trimId`, "reference", "Assignment trim id does not match the seat");
        }
        if (!recordRefEqual(assignment.rowerRef, seat.rowerRef)) {
          addError(errors, `${path}.crew[${index}].rowerRef`, "reference", "Assignment and seat rower references differ");
        }
      }
      for (const seat of seatRecords) {
        if (seat.rowerRef !== null && !crewSeatIds.has(seat.id)) {
          continue;
        }
      }
    }
    checkStableId(value.editSeatId, `${path}.editSeatId`, errors);
    checkStableId(value.referenceSeatId, `${path}.referenceSeatId`, errors);
    if (isPlainRecord(value.boat) && Array.isArray(value.boat.seats)) {
      const seatIds = new Set(value.boat.seats.filter(isPlainRecord).map((seat) => seat.id));
      if (!seatIds.has(value.editSeatId)) addError(errors, `${path}.editSeatId`, "reference", "Edit seat does not exist");
      if (!seatIds.has(value.referenceSeatId)) addError(errors, `${path}.referenceSeatId`, "reference", "Reference seat does not exist");
    }
    checkEnum(value.mode, ["werkstatt", "wasser"], `${path}.mode`, errors);
    checkEnum(value.heightRef, ["sitz", "schiene"], `${path}.heightRef`, errors);
    checkNumber(value.kg, RANGES.current.kg, `${path}.kg`, errors);
    checkNumber(value.t, RANGES.current.t, `${path}.t`, errors);
    checkBoolean(value.recovery, `${path}.recovery`, errors);
    return validationResult(value, errors);
  }
  function hasHiddenSingleSeatProfile(value) {
    return isPlainRecord(value) && value.schemaVersion === LEGACY_SCHEMA_VERSION && isPlainRecord(value.boat) && PRESETS[value.boat.preset]?.single === true && isPlainRecord(value.crew) && value.crew.s2 !== null;
  }
  function minimizeHiddenSingleSeatProfile(value) {
    const migration = migrateCurrentConfigToCurrent(value);
    return Object.freeze({ changed: migration.migrated, config: migration.value });
  }
  function validateImportObject(value, path = "import") {
    const errors = [];
    if (!isPlainRecord(value)) {
      addError(errors, path, "type", "Expected a versioned import object");
      return validationResult(value, errors);
    }
    const importKind = value.kind;
    if (!SCHEMAS.importKinds.includes(importKind)) {
      addError(errors, `${path}.kind`, "enum", `Unknown import kind: ${String(importKind)}`);
      return validationResult(value, errors);
    }
    const fields = importKind === "rudertrimm.current-config" ? ["schemaVersion", "kind", "config"] : ["schemaVersion", "kind", "items"];
    exactRecord(value, fields, path, errors);
    checkConst(value.schemaVersion, SCHEMA_VERSION, `${path}.schemaVersion`, errors);
    if (importKind === "rudertrimm.current-config") {
      errors.push(...validateCurrentConfig(value.config, `${path}.config`).errors);
    } else if (!Array.isArray(value.items)) {
      addError(errors, `${path}.items`, "type", "Expected an array");
    } else {
      if (value.items.length > MAX_IMPORT_ITEMS) addError(errors, `${path}.items`, "length", `At most ${MAX_IMPORT_ITEMS} items are allowed`);
      const validator = importKind === "rudertrimm.rowers" ? validateRower : validateBoat;
      for (let index = 0; index < value.items.length; index += 1) {
        errors.push(...validator(value.items[index], `${path}.items[${index}]`).errors);
      }
    }
    return validationResult(value, errors);
  }
  var DomainValidationError = class extends TypeError {
    constructor(label, errors) {
      super(`${label} is invalid: ${errors.map((error) => `${error.path} ${error.message}`).join("; ")}`);
      this.name = "DomainValidationError";
      this.errors = errors;
    }
  };
  var UnsupportedSchemaVersionError = class extends RangeError {
    constructor(label, actual) {
      super(`${label} schema version ${String(actual)} is newer than supported version ${SCHEMA_VERSION}`);
      this.name = "UnsupportedSchemaVersionError";
      this.code = "unsupported-schema-version";
      this.actual = actual;
      this.supported = SCHEMA_VERSION;
    }
  };
  function requireValid(result, label) {
    if (!result.ok) throw new DomainValidationError(label, result.errors);
  }
  function requireMigratableVersion(value, label) {
    const actual = value?.schemaVersion;
    if (Number.isSafeInteger(actual) && actual > SCHEMA_VERSION) {
      throw new UnsupportedSchemaVersionError(label, actual);
    }
    if (actual !== LEGACY_SCHEMA_VERSION && actual !== PREVIOUS_SCHEMA_VERSION && actual !== SCHEMA_VERSION) {
      throw new DomainValidationError(label, [Object.freeze({
        path: `${label}.schemaVersion`,
        code: "version",
        message: `Expected schema version ${LEGACY_SCHEMA_VERSION}, ${PREVIOUS_SCHEMA_VERSION}, or ${SCHEMA_VERSION}`
      })]);
    }
    return actual;
  }
  function buildLegacySeatDTO(source) {
    const dto = {
      schemaVersion: LEGACY_SCHEMA_VERSION,
      kind: "seat",
      rig: source?.rig,
      DA: source?.DA,
      IH: source?.IH,
      L: source?.L,
      d: source?.d,
      handGap: source?.handGap,
      a: source?.a,
      anlage: source?.anlage,
      aussen: source?.aussen,
      dBB: source?.dBB,
      stemmW: source?.stemmW,
      rollL: source?.rollL,
      rueh: source?.rueh
    };
    requireValid(validateLegacySeat(dto), "legacy seat DTO");
    return dto;
  }
  function defaultSeatSource(presetKey, blade) {
    const preset = PRESETS[presetKey];
    if (!preset) return {};
    return {
      rig: preset.rig,
      DA: preset.DA,
      IH: preset.IH,
      L: blade === "mac" ? preset.Lmac : preset.Lbig,
      d: 2,
      handGap: 18,
      a: preset.a,
      anlage: 4,
      aussen: 0,
      dBB: 0.5,
      stemmW: 42,
      rollL: 75,
      rueh: 5,
      stemmX: defaultFaForRig(preset.rig)
    };
  }
  function buildRowerDTO(source) {
    const dto = {
      schemaVersion: SCHEMA_VERSION,
      kind: "rower",
      externalRef: source?.externalRef == null ? null : copyExternalRef(source.externalRef),
      name: source?.name,
      legLen: source?.legLen,
      torsoLen: source?.torsoLen,
      wingspan: source?.wingspan,
      SB: source?.SB,
      weight: source?.weight,
      stemmX: source?.stemmX
    };
    requireValid(validateRower(dto), "rower DTO");
    return deepFreeze(dto);
  }
  function buildSeatDTO(source, defaults = {}) {
    const position = source?.position ?? defaults.position ?? 1;
    const rig = source?.rig ?? defaults.rig;
    const dto = {
      schemaVersion: SCHEMA_VERSION,
      kind: "seat",
      id: source?.id ?? defaults.id ?? `seat-${position}`,
      trimId: source?.trimId ?? defaults.trimId ?? `trim-${position}`,
      externalRef: source?.externalRef == null ? null : copyExternalRef(source.externalRef),
      position,
      role: source?.role ?? defaults.role,
      label: source?.label ?? defaults.label ?? `Platz ${position}`,
      rig,
      DA: source?.DA ?? defaults.DA,
      IH: source?.IH ?? defaults.IH,
      L: source?.L ?? defaults.L,
      d: source?.d ?? defaults.d,
      handGap: source?.handGap ?? defaults.handGap,
      a: source?.a ?? defaults.a,
      anlage: source?.anlage ?? defaults.anlage,
      aussen: source?.aussen ?? defaults.aussen,
      dBB: source?.dBB ?? defaults.dBB,
      stemmW: source?.stemmW ?? defaults.stemmW,
      rollL: source?.rollL ?? defaults.rollL,
      rueh: source?.rueh ?? defaults.rueh,
      stemmX: source?.stemmX ?? defaults.stemmX ?? defaultFaForRig(rig),
      rowerRef: source?.rowerRef == null ? null : copyRecordRef(source.rowerRef)
    };
    requireValid(validateSeat(dto), "seat DTO");
    return deepFreeze(dto);
  }
  function buildBoatDTO(source, { idSeed } = {}) {
    const presetKey = source?.preset;
    const blade = source?.blade;
    const expectedCount = PRESETS[presetKey]?.seatCount;
    const hasSeatArray = Array.isArray(source?.seats);
    const seatCount = hasSeatArray ? source.seats.length : expectedCount ?? 2;
    const seed = idSeed ?? stableLegacyId("boat", {
      name: source?.name,
      preset: presetKey,
      rig: source?.rig,
      seats: source?.seats ?? [source?.s1, source?.s2]
    });
    const presetSeat = defaultSeatSource(presetKey, blade);
    const seats = Array.from({ length: seatCount }, (_, index) => {
      const position = index + 1;
      const sourceSeat = hasSeatArray ? source.seats[index] : seatCount === 1 ? source?.s1 : position === seatCount ? source?.s1 : position === seatCount - 1 ? source?.s2 ?? presetSeat : presetSeat;
      return buildSeatDTO(sourceSeat, {
        ...presetSeat,
        position,
        id: `${seed}-seat-${position}`,
        trimId: `${seed}-trim-${position}`,
        role: seatRoleForPosition(position, seatCount),
        label: seatLabelForPosition(position, seatCount)
      });
    });
    let legacyRigTemplate = null;
    if (source?.legacyRigTemplate != null) {
      legacyRigTemplate = buildLegacySeatDTO(source.legacyRigTemplate);
    } else if (!hasSeatArray && PRESETS[presetKey]?.single && source?.s2) {
      const first = buildLegacySeatDTO(source.s1);
      const second = buildLegacySeatDTO(source.s2);
      if (canonicalJson(first) !== canonicalJson(second)) legacyRigTemplate = second;
    }
    const dto = {
      schemaVersion: SCHEMA_VERSION,
      kind: "boat",
      externalRef: source?.externalRef == null ? null : copyExternalRef(source.externalRef),
      name: source?.name,
      preset: presetKey,
      blade,
      rig: source?.rig,
      strokeSide: source?.strokeSide,
      phiA: source?.phiA,
      phiR: source?.phiR,
      c: source?.c,
      seatOffset: source?.seatOffset,
      cox: source?.cox == null ? null : { name: source.cox.name },
      capacityStatus: source?.capacityStatus ?? (expectedCount === null ? "legacy-assumed" : "preset"),
      seats,
      legacyRigTemplate
    };
    requireValid(validateBoat(dto), "boat DTO");
    return deepFreeze(dto);
  }
  function buildCurrentConfigDTO(source, { boatIdSeed } = {}) {
    const sourceHadLegacySeats = !Array.isArray(source?.boat?.seats);
    let boat = buildBoatDTO(source?.boat, { idSeed: boatIdSeed });
    const assignments = [];
    if (Array.isArray(source?.crew)) {
      for (const candidate of source.crew) {
        const seat = boat.seats.find((item) => item.id === candidate?.seatId);
        const rower = buildRowerDTO(candidate?.rower);
        const rowerRef = candidate?.rowerRef == null ? null : copyRecordRef(candidate.rowerRef);
        assignments.push({
          schemaVersion: SCHEMA_VERSION,
          kind: "crewAssignment",
          seatId: candidate?.seatId,
          trimId: candidate?.trimId ?? seat?.trimId,
          rowerRef,
          rower
        });
      }
    } else {
      for (const [index, key] of LEGACY_SEATS.entries()) {
        const seat = boat.seats[boat.seats.length === 1 ? 0 : boat.seats.length - 1 - index];
        const candidate = source?.crew?.[key];
        if (!seat || candidate == null) continue;
        if (index > 0 && PRESETS[boat.preset]?.single) continue;
        const rower = buildRowerDTO(candidate);
        const explicitRef = source?.rowerRefs?.[key] ?? null;
        assignments.push({
          schemaVersion: SCHEMA_VERSION,
          kind: "crewAssignment",
          seatId: seat.id,
          trimId: seat.trimId,
          rowerRef: explicitRef == null ? null : copyRecordRef(explicitRef),
          rower
        });
      }
    }
    const assignmentsBySeat = new Map(assignments.map((assignment) => [assignment.seatId, assignment]));
    boat = buildBoatDTO({
      ...boat,
      seats: boat.seats.map((seat) => {
        const assignment = assignmentsBySeat.get(seat.id);
        if (!assignment) return seat;
        return {
          ...seat,
          rowerRef: assignment.rowerRef,
          // V2 only had profile Fa; migration promotes it to the actual per-seat
          // setting once, while current seat values remain independently editable.
          stemmX: sourceHadLegacySeats ? assignment.rower.stemmX : seat.stemmX
        };
      })
    }, { idSeed: boatIdSeed });
    const seatOrder = new Map(boat.seats.map((seat) => [seat.id, seat.position]));
    assignments.sort((left, right) => seatOrder.get(left.seatId) - seatOrder.get(right.seatId));
    const editSeatId = source?.editSeatId ?? (source?.editSeat === "s2" ? boat.seats[Math.max(0, boat.seats.length - 2)]?.id : boat.seats[boat.seats.length - 1]?.id);
    const dto = {
      schemaVersion: SCHEMA_VERSION,
      kind: "currentConfig",
      boatRef: source?.boatRef == null ? null : copyRecordRef(source.boatRef),
      boat,
      crew: assignments,
      editSeatId,
      referenceSeatId: source?.referenceSeatId ?? boat.seats[boat.seats.length - 1]?.id,
      mode: source?.mode,
      heightRef: source?.heightRef,
      kg: source?.kg,
      t: source?.t,
      recovery: source?.recovery
    };
    requireValid(validateCurrentConfig(dto), "current-config DTO");
    return deepFreeze(dto);
  }
  function migrateRowerV2(source) {
    requireValid(validateLegacyRower(source), "v2 rower");
    return buildRowerDTO(source);
  }
  function migrateBoatV2(source, context = {}) {
    requireValid(validateLegacyBoat(source), "v2 boat");
    const idSeed = stableLegacyId("boat", {
      recordId: context?.record?.id ?? null,
      boat: source
    });
    return buildBoatDTO(source, { idSeed });
  }
  function migrateCurrentConfigV2(source, context = {}) {
    requireValid(validateLegacyCurrentConfig(source), "v2 current config");
    const boatIdSeed = stableLegacyId("boat", {
      recordId: context?.record?.id ?? null,
      boat: source.boat
    });
    return buildCurrentConfigDTO(source, { boatIdSeed });
  }
  function requirePreviousRecord(value, fields, label, kind) {
    const errors = [];
    if (exactRecord(value, fields, label, errors)) {
      checkConst(value.schemaVersion, PREVIOUS_SCHEMA_VERSION, `${label}.schemaVersion`, errors);
      checkConst(value.kind, kind, `${label}.kind`, errors);
    }
    requireValid(validationResult(value, errors), label);
  }
  function migrateRowerV3(source) {
    requirePreviousRecord(source, SCHEMAS.rower.fields, "v3 rower", "rower");
    return buildRowerDTO({ ...source, schemaVersion: SCHEMA_VERSION });
  }
  function migrateSeatV3(source, position, seatCount) {
    requirePreviousRecord(source, PREVIOUS_SEAT_FIELDS, "v3 seat", "seat");
    return buildSeatDTO({
      ...source,
      schemaVersion: SCHEMA_VERSION,
      position,
      role: seatRoleForPosition(position, seatCount),
      label: seatLabelForPosition(position, seatCount)
    });
  }
  function migrateBoatV3(source) {
    requirePreviousRecord(source, PREVIOUS_BOAT_FIELDS, "v3 boat", "boat");
    if (!Array.isArray(source.seats)) {
      throw new DomainValidationError("v3 boat", [Object.freeze({ path: "v3 boat.seats", code: "type", message: "Expected an ordered seat array" })]);
    }
    const seatCount = source.seats.length;
    const seats = source.seats.slice().reverse().map((seat, index) => migrateSeatV3(seat, index + 1, seatCount));
    return buildBoatDTO({ ...source, schemaVersion: SCHEMA_VERSION, cox: null, seats });
  }
  function migrateCurrentConfigV3(source) {
    requirePreviousRecord(source, SCHEMAS.currentConfig.fields, "v3 current config", "currentConfig");
    if (!Array.isArray(source.crew)) {
      throw new DomainValidationError("v3 current config", [Object.freeze({ path: "v3 current config.crew", code: "type", message: "Expected an assignment array" })]);
    }
    const boat = migrateBoatV3(source.boat);
    const crew = source.crew.map((assignment, index) => {
      requirePreviousRecord(assignment, SCHEMAS.crewAssignment.fields, `v3 assignment[${index}]`, "crewAssignment");
      return {
        ...assignment,
        schemaVersion: SCHEMA_VERSION,
        rower: migrateRowerV3(assignment.rower)
      };
    });
    return buildCurrentConfigDTO({ ...source, schemaVersion: SCHEMA_VERSION, boat, crew });
  }
  function migrateRowerToCurrent(source, context = {}) {
    const version = requireMigratableVersion(source, "rower");
    if (version === SCHEMA_VERSION) {
      requireValid(validateRower(source), "current rower");
      return Object.freeze({ value: source, migrated: false });
    }
    if (version === PREVIOUS_SCHEMA_VERSION) {
      return Object.freeze({ value: migrateRowerV3(source, context), migrated: true });
    }
    return Object.freeze({ value: migrateRowerV2(source, context), migrated: true });
  }
  function migrateBoatToCurrent(source, context = {}) {
    const version = requireMigratableVersion(source, "boat");
    if (version === SCHEMA_VERSION) {
      requireValid(validateBoat(source), "current boat");
      return Object.freeze({ value: source, migrated: false });
    }
    if (version === PREVIOUS_SCHEMA_VERSION) {
      return Object.freeze({ value: migrateBoatV3(source, context), migrated: true });
    }
    return Object.freeze({ value: migrateBoatV2(source, context), migrated: true });
  }
  function migrateCurrentConfigToCurrent(source, context = {}) {
    const version = requireMigratableVersion(source, "current config");
    if (version === SCHEMA_VERSION) {
      requireValid(validateCurrentConfig(source), "current config");
      return Object.freeze({ value: source, migrated: false });
    }
    if (version === PREVIOUS_SCHEMA_VERSION) {
      return Object.freeze({ value: migrateCurrentConfigV3(source, context), migrated: true });
    }
    return Object.freeze({ value: migrateCurrentConfigV2(source, context), migrated: true });
  }
  function buildTestielDemoConfig() {
    const preset = PRESETS["1x"];
    const seat = {
      rig: preset.rig,
      DA: preset.DA,
      IH: preset.IH,
      L: preset.Lbig,
      d: 2,
      handGap: 18,
      a: preset.a,
      anlage: 4,
      aussen: 0,
      dBB: 0.5,
      stemmW: 42,
      rollL: 75,
      rueh: 5
    };
    return buildCurrentConfigDTO({
      boat: {
        name: "Testiel · Demo-Boot",
        preset: "1x",
        blade: "big",
        rig: preset.rig,
        strokeSide: 1,
        phiA: 66,
        phiR: 44,
        c: 8,
        seatOffset: 5,
        s1: seat,
        s2: seat
      },
      crew: {
        s1: {
          name: "Testiel",
          legLen: 90,
          torsoLen: 95,
          wingspan: 188,
          SB: 40,
          weight: 80,
          stemmX: defaultFaForRig(preset.rig)
        },
        s2: null
      },
      editSeat: "s1",
      mode: "werkstatt",
      heightRef: "sitz",
      kg: 0,
      t: 0,
      recovery: false
    });
  }
  function buildTestielComparisonDemoConfig() {
    const preset = PRESETS["4x"];
    const seat = {
      rig: preset.rig,
      DA: preset.DA,
      IH: preset.IH,
      L: preset.Lbig,
      d: 2,
      handGap: 18,
      a: preset.a,
      anlage: 4,
      aussen: 0,
      dBB: 0.5,
      stemmW: 42,
      rollL: 75,
      rueh: 5
    };
    const comparisonSeat = { ...seat, IH: preset.IH - 3, anlage: 3.5 };
    return buildCurrentConfigDTO({
      boat: {
        name: "Testiel · Vergleichs-Demo-Boot",
        preset: "4x",
        blade: "big",
        rig: preset.rig,
        strokeSide: 1,
        phiA: 66,
        phiR: 44,
        c: 8,
        seatOffset: 5,
        s1: seat,
        s2: comparisonSeat
      },
      crew: {
        s1: {
          name: "Testiel",
          legLen: 90,
          torsoLen: 95,
          wingspan: 188,
          SB: 40,
          weight: 80,
          stemmX: defaultFaForRig(preset.rig)
        },
        s2: {
          name: "Testiel 2",
          legLen: 84,
          torsoLen: 90,
          wingspan: 176,
          SB: 38,
          weight: 72,
          stemmX: defaultFaForRig(preset.rig)
        }
      },
      editSeat: "s1",
      mode: "werkstatt",
      heightRef: "sitz",
      kg: 0,
      t: 0,
      recovery: false
    });
  }

  // js/storage.mjs
  var STORAGE_SCHEMA_VERSION = 3;
  var EXCHANGE_SCHEMA_VERSION = 2;
  var STORAGE_FORMAT = "rudertrimm.storage";
  var EXCHANGE_FORMAT = "rudertrimm.exchange";
  var STORAGE_KINDS = Object.freeze({
    rowers: "rudertrimm.rowers",
    boats: "rudertrimm.boats",
    efaCandidates: "rudertrimm.efa-candidates",
    workspace: "rudertrimm.current-workspace"
  });
  var STORAGE_KEYS = Object.freeze({
    rowers: "rudertrimm:v2:rowers",
    boats: "rudertrimm:v2:boats",
    efaCandidates: "rudertrimm:v2:efa-candidates",
    workspace: "rudertrimm:v2:current-workspace",
    quarantinePrefix: "rudertrimm:v2:quarantine:"
  });
  var STORAGE_LIMITS = Object.freeze({
    maxBytes: 1048576,
    // Storage v3 keeps the current DTO and a local baseline snapshot. The larger
    // internal ceiling lets every formerly valid 1 MiB v2 envelope migrate
    // atomically; exchange/import files deliberately remain capped at maxBytes.
    maxStorageBytes: 2621440,
    maxRecords: 250,
    maxHistoryEntries: 500,
    maxHistoryPerEntity: 20,
    maxHistoryFloors: 500,
    maxNameLength: 80,
    maxDepth: 32,
    maxNodes: 2e4
  });
  var COLLECTION_KINDS = /* @__PURE__ */ new Set([STORAGE_KINDS.rowers, STORAGE_KINDS.boats, STORAGE_KINDS.efaCandidates]);
  var FORBIDDEN_WORKSPACE_KEYS = /* @__PURE__ */ new Set(["db", "databases", "rowers", "boats", "records"]);
  var DANGEROUS_KEYS = /* @__PURE__ */ new Set(["__proto__", "prototype", "constructor"]);
  var HISTORY_OPERATIONS = /* @__PURE__ */ new Set(["baseline", "create", "update", "import", "migration", "delete"]);
  var HISTORY_SOURCES = /* @__PURE__ */ new Set(["local-ui", "json-import", "efa-csv", "migration", "system"]);
  var fallbackIdCounter = 0;
  var StorageValidationError = class extends TypeError {
    constructor(message, details = []) {
      super(message);
      this.name = "StorageValidationError";
      this.code = "validation";
      this.details = Object.freeze([...details]);
    }
  };
  var RevisionConflictError = class extends Error {
    constructor(message = "The persisted revision changed") {
      super(message);
      this.name = "RevisionConflictError";
      this.code = "revision-conflict";
    }
  };
  var NoSelectionError = class extends Error {
    constructor(message = "An explicit record id is required") {
      super(message);
      this.name = "NoSelectionError";
      this.code = "no-selection";
    }
  };
  var UnsafeRecoveryError = class extends Error {
    constructor(message = "Persisting is blocked because invalid source bytes could not be backed up safely") {
      super(message);
      this.name = "UnsafeRecoveryError";
      this.code = "unsafe-recovery";
    }
  };
  var MigrationPendingError = class extends Error {
    constructor(message = "A supported data migration must be committed before other writes") {
      super(message);
      this.name = "MigrationPendingError";
      this.code = "migration-pending";
    }
  };
  var UnsupportedSchemaError = class extends StorageValidationError {
    constructor(message = "Stored data uses a newer unsupported schema version", details = []) {
      super(message, details);
      this.name = "UnsupportedSchemaError";
      this.code = "unsupported-schema";
    }
  };
  function isPlainObject(value) {
    if (value === null || typeof value !== "object" || Array.isArray(value)) return false;
    const prototype = Object.getPrototypeOf(value);
    return prototype === Object.prototype || prototype === null;
  }
  function exactKeys(value, expected, label) {
    if (!isPlainObject(value)) throw new StorageValidationError(`${label} must be a plain object`);
    const actual = Object.keys(value).sort();
    const wanted = [...expected].sort();
    if (actual.length !== wanted.length || actual.some((key, index) => key !== wanted[index])) {
      throw new StorageValidationError(`${label} has unexpected or missing fields`, [{ actual, expected: wanted }]);
    }
  }
  function assertRevision(value, label, { allowZero = true } = {}) {
    if (!Number.isSafeInteger(value) || value < (allowZero ? 0 : 1)) {
      throw new StorageValidationError(`${label} must be a ${allowZero ? "non-negative" : "positive"} safe integer`);
    }
  }
  function assertTimestamp(value, label) {
    const date = typeof value === "string" ? new Date(value) : null;
    if (!date || !Number.isFinite(date.getTime()) || date.toISOString() !== value) {
      throw new StorageValidationError(`${label} must be an ISO-compatible timestamp`);
    }
  }
  function assertId(value, label = "id") {
    if (typeof value !== "string" || !/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u.test(value)) {
      throw new StorageValidationError(`${label} is not a safe stable id`);
    }
  }
  function assertNameLimit(value, label, limits) {
    if (!isPlainObject(value) || !Object.hasOwn(value, "name")) {
      throw new StorageValidationError(`${label}.name is required`);
    }
    if (typeof value.name !== "string") throw new StorageValidationError(`${label}.name must be a string`);
    const length = [...value.name].length;
    if (length < 1 || length > limits.maxNameLength) {
      throw new StorageValidationError(`${label}.name must contain 1-${limits.maxNameLength} characters`);
    }
  }
  function assertJsonSafe(value, limits, label = "value") {
    const ancestors = /* @__PURE__ */ new WeakSet();
    let nodes = 0;
    function visit(current, path, depth) {
      nodes += 1;
      if (nodes > limits.maxNodes) throw new StorageValidationError(`${label} contains too many values`);
      if (depth > limits.maxDepth) throw new StorageValidationError(`${label} exceeds the maximum nesting depth`);
      if (current === null || typeof current === "string" || typeof current === "boolean") return;
      if (typeof current === "number") {
        if (!Number.isFinite(current)) throw new StorageValidationError(`${path} must be finite`);
        return;
      }
      if (typeof current !== "object") throw new StorageValidationError(`${path} is not JSON-safe`);
      if (ancestors.has(current)) throw new StorageValidationError(`${path} contains a cycle`);
      ancestors.add(current);
      if (Array.isArray(current)) {
        current.forEach((entry, index) => visit(entry, `${path}[${index}]`, depth + 1));
      } else {
        if (!isPlainObject(current)) throw new StorageValidationError(`${path} must be a plain object`);
        for (const key of Object.keys(current)) {
          if (DANGEROUS_KEYS.has(key)) throw new StorageValidationError(`${path}.${key} is forbidden`);
          visit(current[key], `${path}.${key}`, depth + 1);
        }
      }
      ancestors.delete(current);
    }
    visit(value, label, 0);
  }
  function assertWorkspaceIsolation(value, path = "workspace") {
    if (Array.isArray(value)) {
      value.forEach((entry, index) => assertWorkspaceIsolation(entry, `${path}[${index}]`));
      return;
    }
    if (!isPlainObject(value)) return;
    for (const [key, child] of Object.entries(value)) {
      if (FORBIDDEN_WORKSPACE_KEYS.has(key)) {
        throw new StorageValidationError(`${path}.${key} is forbidden in the current workspace`);
      }
      assertWorkspaceIsolation(child, `${path}.${key}`);
    }
  }
  function utf8ByteLength(text) {
    if (typeof text !== "string") throw new TypeError("Expected text");
    if (typeof TextEncoder === "function") return new TextEncoder().encode(text).byteLength;
    return unescape(encodeURIComponent(text)).length;
  }
  function quarantineFingerprint(text) {
    let hash = 0xcbf29ce484222325n;
    for (let index = 0; index < text.length; index += 1) {
      hash ^= BigInt(text.charCodeAt(index));
      hash = BigInt.asUintN(64, hash * 0x100000001b3n);
    }
    return `${utf8ByteLength(text)}-${hash.toString(16).padStart(16, "0")}`;
  }
  function enforceTextLimit(text, limits, label, maxBytes = limits.maxBytes) {
    const bytes = utf8ByteLength(text);
    if (bytes > maxBytes) {
      throw new StorageValidationError(`${label} exceeds ${maxBytes} UTF-8 bytes`, [{ bytes, maxBytes }]);
    }
    return bytes;
  }
  function jsonClone(value, limits, label = "value") {
    assertJsonSafe(value, limits, label);
    return JSON.parse(JSON.stringify(value));
  }
  function deepFreeze2(value) {
    if (value && typeof value === "object" && !Object.isFrozen(value)) {
      Object.freeze(value);
      for (const child of Object.values(value)) deepFreeze2(child);
    }
    return value;
  }
  function normalizeLimits(overrides = {}) {
    const limits = { ...STORAGE_LIMITS, ...overrides };
    if (Object.hasOwn(overrides, "maxBytes") && !Object.hasOwn(overrides, "maxStorageBytes")) {
      limits.maxStorageBytes = limits.maxBytes;
    }
    for (const key of [
      "maxBytes",
      "maxStorageBytes",
      "maxRecords",
      "maxHistoryEntries",
      "maxHistoryPerEntity",
      "maxHistoryFloors",
      "maxNameLength",
      "maxDepth",
      "maxNodes"
    ]) {
      if (!Number.isSafeInteger(limits[key]) || limits[key] < 1) throw new RangeError(`${key} must be a positive safe integer`);
    }
    if (limits.maxStorageBytes < limits.maxBytes) throw new RangeError("maxStorageBytes must not be lower than maxBytes");
    return Object.freeze(limits);
  }
  function nowIso(clock) {
    const value = clock();
    const date = value instanceof Date ? value : new Date(value);
    if (!Number.isFinite(date.getTime())) throw new RangeError("clock must return a valid date");
    return date.toISOString();
  }
  function monotonicStoreTimestamp(clock, state) {
    const timestamps = [nowIso(clock), state?.updatedAt];
    for (const record of state?.records ?? []) timestamps.push(record.updatedAt);
    for (const entry of state?.history?.entries ?? []) timestamps.push(entry.changedAt);
    return timestamps.filter(Boolean).sort().at(-1);
  }
  function validatorResult(validator, value, path) {
    if (typeof validator !== "function") return value;
    const result = validator(value, path);
    if (result === true || result === void 0) return value;
    if (result === false) throw new StorageValidationError(`${path} failed validation`);
    if (isPlainObject(result) && typeof result.ok === "boolean") {
      if (!result.ok) throw new StorageValidationError(`${path} failed validation`, result.errors ?? []);
      return result.value ?? value;
    }
    return result;
  }
  function assertStorageSchemaVersion(value, label = "envelope.schemaVersion", { allowPrevious = false } = {}) {
    if (Number.isSafeInteger(value) && value > STORAGE_SCHEMA_VERSION) {
      throw new UnsupportedSchemaError(`${label} ${value} is newer than supported version ${STORAGE_SCHEMA_VERSION}`, [{
        actual: value,
        supported: STORAGE_SCHEMA_VERSION
      }]);
    }
    if (value !== STORAGE_SCHEMA_VERSION && !(allowPrevious && value === 2)) {
      throw new StorageValidationError("Unsupported storage schema version");
    }
  }
  function assertExchangeSchemaVersion(value, label = "envelope.schemaVersion") {
    if (Number.isSafeInteger(value) && value > EXCHANGE_SCHEMA_VERSION) {
      throw new UnsupportedSchemaError(`${label} ${value} is newer than supported exchange version ${EXCHANGE_SCHEMA_VERSION}`, [{
        actual: value,
        supported: EXCHANGE_SCHEMA_VERSION
      }]);
    }
    if (value !== EXCHANGE_SCHEMA_VERSION) throw new StorageValidationError("Unsupported exchange schema version");
  }
  function migrationMetadata({ recordIds = [], historyBaselineIds = [], storageEnvelope = false, workspace = false } = {}) {
    const ids = Object.freeze([...recordIds]);
    const baselineIds = Object.freeze([...historyBaselineIds]);
    const metadata = {
      migrated: ids.length > 0 || baselineIds.length > 0 || storageEnvelope || workspace,
      recordIds: ids,
      workspace
    };
    Object.defineProperties(metadata, {
      historyBaselineIds: { value: baselineIds },
      storageEnvelope: { value: storageEnvelope }
    });
    return Object.freeze(metadata);
  }
  var NO_MIGRATION = migrationMetadata();
  function applyValueMigration(migrate, value, { limits, path, context }) {
    if (typeof migrate !== "function") return { value, migrated: false };
    const candidate = jsonClone(value, limits, `${path} migration input`);
    let result;
    try {
      result = migrate(candidate, Object.freeze({ ...context, path }));
    } catch (error) {
      if (error?.code === "unsupported-schema-version") {
        throw new UnsupportedSchemaError(error.message, [{
          path,
          actual: error.actual,
          supported: error.supported
        }]);
      }
      throw error;
    }
    let migratedValue;
    let explicitlyMigrated = null;
    if (isPlainObject(result) && Object.keys(result).length === 2 && Object.hasOwn(result, "value") && typeof result.migrated === "boolean") {
      migratedValue = result.value;
      explicitlyMigrated = result.migrated;
    } else {
      migratedValue = result === void 0 ? candidate : result;
    }
    assertJsonSafe(migratedValue, limits, `${path} migrated value`);
    const valueChanged = JSON.stringify(migratedValue) !== JSON.stringify(value);
    const migrated = explicitlyMigrated === true || valueChanged;
    return { value: migratedValue, migrated };
  }
  function uuidFromBytes(bytes) {
    bytes[6] = bytes[6] & 15 | 64;
    bytes[8] = bytes[8] & 63 | 128;
    const hex = [...bytes].map((byte) => byte.toString(16).padStart(2, "0"));
    return `${hex.slice(0, 4).join("")}-${hex.slice(4, 6).join("")}-${hex.slice(6, 8).join("")}-${hex.slice(8, 10).join("")}-${hex.slice(10).join("")}`;
  }
  function createStableId({ cryptoObject = globalThis.crypto, now = Date.now, random = Math.random } = {}) {
    if (cryptoObject && typeof cryptoObject.randomUUID === "function") return cryptoObject.randomUUID();
    const bytes = new Uint8Array(16);
    if (cryptoObject && typeof cryptoObject.getRandomValues === "function") {
      cryptoObject.getRandomValues(bytes);
      return uuidFromBytes(bytes);
    }
    let seed = Number(now()) + ++fallbackIdCounter * 2654435761;
    for (let index = 0; index < bytes.length; index += 1) {
      seed = seed * 1664525 + 1013904223 >>> 0;
      bytes[index] = (seed ^ Math.floor(random() * 256)) & 255;
    }
    return uuidFromBytes(bytes);
  }
  function normalizeAudit(audit, defaults) {
    const source = audit?.source ?? defaults.source;
    const reason = audit?.reason ?? defaults.reason;
    if (!HISTORY_SOURCES.has(source)) throw new StorageValidationError(`Unknown history source: ${String(source)}`);
    if (typeof reason !== "string" || !/^[a-z0-9][a-z0-9._-]{0,63}$/u.test(reason)) {
      throw new StorageValidationError("History reason must be a short controlled code");
    }
    return Object.freeze({ source, reason });
  }
  function historyEntry({ entityId, revision: revision2, changedAt, operation, source, reason, snapshot }, limits) {
    assertId(entityId, "history.entityId");
    assertRevision(revision2, "history.revision", { allowZero: false });
    assertTimestamp(changedAt, "history.changedAt");
    if (!HISTORY_OPERATIONS.has(operation)) throw new StorageValidationError(`Unknown history operation: ${String(operation)}`);
    normalizeAudit({ source, reason }, { source: "system", reason: "unknown" });
    if (snapshot !== null) {
      if (!isPlainObject(snapshot)) throw new StorageValidationError("history.snapshot must be a plain object or null");
      assertJsonSafe(snapshot, limits, "history.snapshot");
    }
    if (operation === "delete" && snapshot !== null) throw new StorageValidationError("Delete history must not retain a snapshot");
    if (operation !== "delete" && snapshot === null) throw new StorageValidationError("Non-delete history requires a snapshot");
    return {
      entityId,
      revision: revision2,
      changedAt,
      operation,
      source,
      reason,
      snapshot: snapshot === null ? null : jsonClone(snapshot, limits, "history.snapshot")
    };
  }
  function historyFloor(value) {
    exactKeys(value, ["entityId", "throughRevision"], "history floor");
    assertId(value.entityId, "history floor entityId");
    assertRevision(value.throughRevision, "history floor revision", { allowZero: false });
    return { entityId: value.entityId, throughRevision: value.throughRevision };
  }
  function emptyHistory() {
    return { entries: [], floors: [] };
  }
  function normalizeHistory(value, {
    records,
    limits,
    kind,
    validateRecord,
    migrateRecord
  }) {
    exactKeys(value, ["entries", "floors"], "history");
    if (!Array.isArray(value.entries) || !Array.isArray(value.floors)) {
      throw new StorageValidationError("history entries and floors must be arrays");
    }
    if (value.entries.length > limits.maxHistoryEntries) throw new StorageValidationError("History entry limit exceeded");
    if (value.floors.length > limits.maxHistoryFloors) throw new StorageValidationError("History floor limit exceeded");
    const floors = value.floors.map(historyFloor);
    const floorIds = /* @__PURE__ */ new Set();
    for (const floor of floors) {
      if (floorIds.has(floor.entityId)) throw new StorageValidationError(`Duplicate history floor: ${floor.entityId}`);
      floorIds.add(floor.entityId);
    }
    const projectedSnapshots = /* @__PURE__ */ new Map();
    const entries = value.entries.map((entry, index) => {
      exactKeys(entry, ["entityId", "revision", "changedAt", "operation", "source", "reason", "snapshot"], "history entry");
      const normalized = historyEntry(entry, limits);
      if (normalized.snapshot !== null) {
        const path = `history.entries[${index}].snapshot`;
        const projected = typeof migrateRecord === "function" ? applyValueMigration(migrateRecord, normalized.snapshot, {
          limits,
          path,
          context: { kind, storage: true, history: true }
        }).value : normalized.snapshot;
        const validated = validatorResult(validateRecord, projected, path);
        assertJsonSafe(validated, limits, path);
        assertNameLimit(validated, path, limits);
        if (JSON.stringify(validated) !== JSON.stringify(projected)) {
          throw new StorageValidationError(`history.entries[${index}].snapshot is not a canonical current-domain DTO`);
        }
        projectedSnapshots.set(normalized, projected);
      }
      return normalized;
    });
    const latest = /* @__PURE__ */ new Map();
    const counts = /* @__PURE__ */ new Map();
    for (const entry of entries) {
      const previous = latest.get(entry.entityId);
      const floor = floors.find((candidate) => candidate.entityId === entry.entityId)?.throughRevision ?? 0;
      if (!previous && entry.revision <= floor || previous && entry.revision <= previous.revision) {
        throw new StorageValidationError(`History revisions are not strictly increasing for ${entry.entityId}`);
      }
      if (previous && entry.changedAt < previous.changedAt) {
        throw new StorageValidationError(`History timestamps move backwards for ${entry.entityId}`);
      }
      if (previous?.operation === "delete") {
        throw new StorageValidationError(`History continues after deletion for ${entry.entityId}`);
      }
      latest.set(entry.entityId, entry);
      counts.set(entry.entityId, (counts.get(entry.entityId) ?? 0) + 1);
    }
    for (const [entityId, count] of counts) {
      if (count > limits.maxHistoryPerEntity) throw new StorageValidationError(`History limit exceeded for ${entityId}`);
    }
    const recordsById = new Map(records.map((record) => [record.id, record]));
    for (const record of records) {
      const entry = latest.get(record.id);
      if (!entry) throw new StorageValidationError(`Current record ${record.id} has no history baseline`);
      if (entry.operation === "delete") throw new StorageValidationError(`Current record ${record.id} ends in a delete tombstone`);
      if (entry.revision !== record.revision || JSON.stringify(projectedSnapshots.get(entry)) !== JSON.stringify(record.value)) {
        throw new StorageValidationError(`Current record ${record.id} does not match its latest history snapshot`);
      }
    }
    for (const [entityId, entry] of latest) {
      if (!recordsById.has(entityId) && entry.operation !== "delete") {
        throw new StorageValidationError(`History for missing record ${entityId} lacks a delete tombstone`);
      }
      if (recordsById.has(entityId) && entry.operation === "delete") {
        throw new StorageValidationError(`Delete tombstone conflicts with current record ${entityId}`);
      }
    }
    return { entries, floors };
  }
  function setHistoryFloor(history, entityId, throughRevision) {
    const existing = history.floors.find((floor) => floor.entityId === entityId);
    if (existing) existing.throughRevision = Math.max(existing.throughRevision, throughRevision);
    else history.floors.push({ entityId, throughRevision });
  }
  function compactHistory(history, records, limits) {
    const activeIds = new Set(records.map((record) => record.id));
    const byEntity = /* @__PURE__ */ new Map();
    for (const entry of history.entries) {
      if (!byEntity.has(entry.entityId)) byEntity.set(entry.entityId, []);
      byEntity.get(entry.entityId).push(entry);
    }
    for (const [entityId, entries] of byEntity) {
      while (entries.length > limits.maxHistoryPerEntity) {
        const removed = entries.shift();
        const index = history.entries.indexOf(removed);
        if (index >= 0) history.entries.splice(index, 1);
        setHistoryFloor(history, entityId, removed.revision);
      }
    }
    while (history.entries.length > limits.maxHistoryEntries) {
      const latestByEntity = /* @__PURE__ */ new Map();
      for (const entry of history.entries) latestByEntity.set(entry.entityId, entry);
      const removable = history.entries.filter((entry) => !(activeIds.has(entry.entityId) && latestByEntity.get(entry.entityId) === entry)).sort((left, right) => left.changedAt.localeCompare(right.changedAt) || left.entityId.localeCompare(right.entityId) || left.revision - right.revision)[0];
      if (!removable) throw new StorageValidationError("History cannot be compacted without losing current baselines");
      history.entries.splice(history.entries.indexOf(removable), 1);
      setHistoryFloor(history, removable.entityId, removable.revision);
    }
    if (history.floors.length > limits.maxHistoryFloors) throw new StorageValidationError("History floor limit exceeded");
    return history;
  }
  function compactHistoryToStorageBytes(envelope, limits, stringify) {
    if (!COLLECTION_KINDS.has(envelope.kind) || !envelope.history) return envelope;
    const activeIds = new Set(envelope.records.map((record) => record.id));
    const byteLength = () => {
      const text = stringify(envelope);
      if (typeof text !== "string") throw new StorageValidationError("stringify callback must return text");
      return utf8ByteLength(text);
    };
    while (byteLength() > limits.maxStorageBytes) {
      const latestByEntity = /* @__PURE__ */ new Map();
      for (const entry of envelope.history.entries) latestByEntity.set(entry.entityId, entry);
      const removable = envelope.history.entries.filter((entry) => !(activeIds.has(entry.entityId) && latestByEntity.get(entry.entityId) === entry)).sort((left, right) => left.changedAt.localeCompare(right.changedAt) || left.entityId.localeCompare(right.entityId) || left.revision - right.revision)[0];
      if (!removable) {
        throw new StorageValidationError(`Storage envelope exceeds ${limits.maxStorageBytes} UTF-8 bytes and no obsolete history snapshot can be removed`);
      }
      envelope.history.entries.splice(envelope.history.entries.indexOf(removable), 1);
      setHistoryFloor(envelope.history, removable.entityId, removable.revision);
      if (envelope.history.floors.length > limits.maxHistoryFloors) {
        throw new StorageValidationError("History floor limit exceeded during byte retention");
      }
    }
    return envelope;
  }
  function appendHistory(history, record, operation, timestamp2, audit, records, limits) {
    history.entries.push(historyEntry({
      entityId: record.id,
      revision: record.revision,
      changedAt: timestamp2,
      operation,
      source: audit.source,
      reason: audit.reason,
      snapshot: record.value
    }, limits));
    return compactHistory(history, records, limits);
  }
  function privacyDeleteHistory(history, record, timestamp2, audit, records, limits) {
    history.entries = history.entries.filter((entry) => entry.entityId !== record.id);
    setHistoryFloor(history, record.id, record.revision);
    history.entries.push(historyEntry({
      entityId: record.id,
      revision: record.revision + 1,
      changedAt: timestamp2,
      operation: "delete",
      source: audit.source,
      reason: audit.reason,
      snapshot: null
    }, limits));
    return compactHistory(history, records, limits);
  }
  function emptyCollectionEnvelope(kind, timestamp2) {
    return deepFreeze2({
      format: STORAGE_FORMAT,
      schemaVersion: STORAGE_SCHEMA_VERSION,
      kind,
      revision: 0,
      updatedAt: timestamp2,
      records: [],
      history: emptyHistory()
    });
  }
  function emptyWorkspaceEnvelope(timestamp2) {
    return deepFreeze2({
      format: STORAGE_FORMAT,
      schemaVersion: STORAGE_SCHEMA_VERSION,
      kind: STORAGE_KINDS.workspace,
      revision: 0,
      updatedAt: timestamp2,
      workspace: null
    });
  }
  function normalizeRecord(record, { kind, validateRecord, migrateRecord, limits, path, storage }) {
    exactKeys(record, ["id", "revision", "updatedAt", "value"], path);
    assertId(record.id, `${path}.id`);
    assertRevision(record.revision, `${path}.revision`, { allowZero: false });
    assertTimestamp(record.updatedAt, `${path}.updatedAt`);
    assertJsonSafe(record.value, limits, `${path}.value`);
    const migration = applyValueMigration(migrateRecord, record.value, {
      limits,
      path: `${path}.value`,
      context: {
        kind,
        storage,
        record: Object.freeze({ id: record.id, revision: record.revision, updatedAt: record.updatedAt })
      }
    });
    const value = validatorResult(validateRecord, migration.value, `${path}.value`);
    assertJsonSafe(value, limits, `${path}.value`);
    assertNameLimit(value, `${path}.value`, limits);
    return {
      record: {
        id: record.id,
        revision: record.revision,
        updatedAt: record.updatedAt,
        value: jsonClone(value, limits, `${path}.value`)
      },
      migrated: migration.migrated
    };
  }
  function normalizeCollectionEnvelope(envelope, {
    kind,
    validateRecord,
    migrateRecord,
    migrateHistoryRecord = migrateRecord,
    limits,
    storage = true
  }) {
    const sourceSchema = envelope?.schemaVersion;
    if (isPlainObject(envelope) && Object.hasOwn(envelope, "schemaVersion")) {
      if (storage) assertStorageSchemaVersion(sourceSchema, void 0, { allowPrevious: true });
      else assertExchangeSchemaVersion(sourceSchema);
    }
    const fields = storage ? sourceSchema === 2 ? ["format", "schemaVersion", "kind", "revision", "updatedAt", "records"] : ["format", "schemaVersion", "kind", "revision", "updatedAt", "records", "history"] : ["format", "schemaVersion", "kind", "exportedAt", "records"];
    exactKeys(envelope, fields, storage ? "storage envelope" : "exchange envelope");
    const expectedFormat = storage ? STORAGE_FORMAT : EXCHANGE_FORMAT;
    if (envelope.format !== expectedFormat) throw new StorageValidationError(`Expected format ${expectedFormat}`);
    if (storage) assertStorageSchemaVersion(sourceSchema, void 0, { allowPrevious: true });
    else assertExchangeSchemaVersion(sourceSchema);
    if (envelope.kind !== kind || !COLLECTION_KINDS.has(kind)) throw new StorageValidationError("Unexpected collection kind");
    if (storage) {
      assertRevision(envelope.revision, "envelope.revision");
      assertTimestamp(envelope.updatedAt, "envelope.updatedAt");
    } else {
      assertTimestamp(envelope.exportedAt, "envelope.exportedAt");
    }
    if (!Array.isArray(envelope.records)) throw new StorageValidationError("envelope.records must be an array");
    if (envelope.records.length > limits.maxRecords) throw new StorageValidationError(`At most ${limits.maxRecords} records are allowed`);
    const ids = /* @__PURE__ */ new Set();
    const migratedRecordIds = [];
    const records = envelope.records.map((record, index) => {
      const normalized = normalizeRecord(record, {
        kind,
        validateRecord,
        migrateRecord,
        limits,
        path: `records[${index}]`,
        storage
      });
      if (ids.has(normalized.record.id)) throw new StorageValidationError(`Duplicate record id: ${normalized.record.id}`);
      ids.add(normalized.record.id);
      if (normalized.migrated) migratedRecordIds.push(normalized.record.id);
      return normalized.record;
    });
    const historyBaselineIds = storage && sourceSchema === 2 ? records.map((record) => record.id) : [];
    const history = storage ? sourceSchema === 2 ? {
      entries: records.map((record) => historyEntry({
        entityId: record.id,
        revision: record.revision,
        changedAt: record.updatedAt,
        operation: "baseline",
        source: "migration",
        reason: "history-start",
        snapshot: record.value
      }, limits)),
      floors: []
    } : normalizeHistory(envelope.history, {
      records,
      limits,
      kind,
      validateRecord,
      migrateRecord: migrateHistoryRecord
    }) : null;
    const normalizedEnvelope = storage ? {
      format: STORAGE_FORMAT,
      schemaVersion: STORAGE_SCHEMA_VERSION,
      kind,
      revision: envelope.revision,
      updatedAt: envelope.updatedAt,
      records,
      history
    } : {
      format: EXCHANGE_FORMAT,
      schemaVersion: EXCHANGE_SCHEMA_VERSION,
      kind,
      exportedAt: envelope.exportedAt,
      records
    };
    return { envelope: normalizedEnvelope, migration: migrationMetadata({
      recordIds: migratedRecordIds,
      historyBaselineIds,
      storageEnvelope: storage && sourceSchema === 2
    }) };
  }
  function normalizeWorkspaceEnvelope(envelope, { validateWorkspace, migrateWorkspace, limits, storage = true }) {
    const fields = storage ? ["format", "schemaVersion", "kind", "revision", "updatedAt", "workspace"] : ["format", "schemaVersion", "kind", "exportedAt", "workspace"];
    if (isPlainObject(envelope) && Object.hasOwn(envelope, "schemaVersion")) {
      if (storage) assertStorageSchemaVersion(envelope.schemaVersion, void 0, { allowPrevious: true });
      else assertExchangeSchemaVersion(envelope.schemaVersion);
    }
    exactKeys(envelope, fields, storage ? "workspace storage envelope" : "workspace exchange envelope");
    if (envelope.format !== (storage ? STORAGE_FORMAT : EXCHANGE_FORMAT)) throw new StorageValidationError("Unexpected workspace format");
    if (storage) assertStorageSchemaVersion(envelope.schemaVersion, void 0, { allowPrevious: true });
    else assertExchangeSchemaVersion(envelope.schemaVersion);
    if (envelope.kind !== STORAGE_KINDS.workspace) throw new StorageValidationError("Unexpected workspace kind");
    if (storage) {
      assertRevision(envelope.revision, "envelope.revision");
      assertTimestamp(envelope.updatedAt, "envelope.updatedAt");
    } else {
      assertTimestamp(envelope.exportedAt, "envelope.exportedAt");
    }
    let workspace = envelope.workspace;
    let migrated = false;
    if (workspace !== null) {
      assertJsonSafe(workspace, limits, "workspace");
      const migration = applyValueMigration(migrateWorkspace, workspace, {
        limits,
        path: "workspace",
        context: { kind: STORAGE_KINDS.workspace, storage }
      });
      workspace = migration.value;
      migrated = migration.migrated;
      assertWorkspaceIsolation(workspace);
      workspace = validatorResult(validateWorkspace, workspace, "workspace");
      assertJsonSafe(workspace, limits, "workspace");
      assertWorkspaceIsolation(workspace);
      workspace = jsonClone(workspace, limits, "workspace");
    }
    const normalizedEnvelope = storage ? {
      format: STORAGE_FORMAT,
      schemaVersion: STORAGE_SCHEMA_VERSION,
      kind: STORAGE_KINDS.workspace,
      revision: envelope.revision,
      updatedAt: envelope.updatedAt,
      workspace
    } : {
      format: EXCHANGE_FORMAT,
      schemaVersion: EXCHANGE_SCHEMA_VERSION,
      kind: STORAGE_KINDS.workspace,
      exportedAt: envelope.exportedAt,
      workspace
    };
    return { envelope: normalizedEnvelope, migration: migrationMetadata({
      storageEnvelope: storage && envelope.schemaVersion === 2,
      workspace: migrated
    }) };
  }
  function parseEnvelopeText(text, {
    kind,
    parse,
    validateRecord,
    validateWorkspace,
    migrateRecord,
    migrateWorkspace,
    limits,
    storage
  }) {
    if (typeof text !== "string") throw new TypeError("Envelope input must be text");
    enforceTextLimit(
      text,
      limits,
      storage ? "stored envelope" : "import file",
      storage ? limits.maxStorageBytes : limits.maxBytes
    );
    const parsed = parse(text);
    if (storage && parsed?.schemaVersion === 2) {
      enforceTextLimit(text, limits, "stored v2 envelope", limits.maxBytes);
    }
    return kind === STORAGE_KINDS.workspace ? normalizeWorkspaceEnvelope(parsed, { validateWorkspace, migrateWorkspace, limits, storage }) : normalizeCollectionEnvelope(parsed, { kind, validateRecord, migrateRecord, limits, storage });
  }
  function parseImportEnvelope(text, {
    expectedKind,
    parse = JSON.parse,
    validateRecord,
    validateWorkspace,
    migrateRecord,
    migrateWorkspace,
    limits: limitOverrides
  } = {}) {
    if (!Object.values(STORAGE_KINDS).includes(expectedKind)) throw new RangeError("expectedKind is required");
    const limits = normalizeLimits(limitOverrides);
    try {
      const normalized = parseEnvelopeText(text, {
        kind: expectedKind,
        parse,
        validateRecord,
        validateWorkspace,
        migrateRecord,
        migrateWorkspace,
        limits,
        storage: false
      });
      return Object.freeze({
        ok: true,
        envelope: deepFreeze2(normalized.envelope),
        migration: normalized.migration,
        errors: Object.freeze([])
      });
    } catch (error) {
      return Object.freeze({ ok: false, envelope: null, migration: NO_MIGRATION, errors: Object.freeze([error]) });
    }
  }
  function serializeEnvelope(envelope, { stringify, limits, label, maxBytes = limits.maxBytes }) {
    assertJsonSafe(envelope, limits, label);
    const text = stringify(envelope);
    if (typeof text !== "string") throw new StorageValidationError("stringify callback must return text");
    enforceTextLimit(text, limits, label, maxBytes);
    return text;
  }
  function assertStorageAdapter(storage) {
    for (const method of ["getItem", "setItem", "removeItem"]) {
      if (!storage || typeof storage[method] !== "function") throw new TypeError(`storage.${method} is required`);
    }
  }
  function createMemoryStorage(initial = {}) {
    const values = new Map(Object.entries(initial).map(([key, value]) => [String(key), String(value)]));
    return {
      get length() {
        return values.size;
      },
      key(index) {
        return [...values.keys()][index] ?? null;
      },
      getItem(key) {
        return values.has(String(key)) ? values.get(String(key)) : null;
      },
      setItem(key, value) {
        values.set(String(key), String(value));
      },
      removeItem(key) {
        values.delete(String(key));
      },
      clear() {
        values.clear();
      },
      dump() {
        return Object.fromEntries(values);
      }
    };
  }
  async function withExclusiveRepositoryWrite(repository, action, {
    lockManager = globalThis.navigator?.locks,
    shared = true
  } = {}) {
    if (!repository || typeof repository.key !== "string" || typeof repository.reloadFromExternal !== "function") {
      throw new TypeError("A versioned repository is required");
    }
    if (typeof action !== "function") throw new TypeError("action must be a function");
    const run = async () => {
      repository.reloadFromExternal("write-lock");
      return action();
    };
    if (!shared) return run();
    if (!lockManager || typeof lockManager.request !== "function") {
      const error = new Error("Web Locks are required for safe persistent repository writes");
      error.code = "coordination-unavailable";
      throw error;
    }
    return lockManager.request(
      `rudertrimm:v2:repository-write:${repository.key}`,
      { mode: "exclusive" },
      run
    );
  }
  function createChannel(channelFactory, name) {
    if (channelFactory === false) return null;
    if (typeof channelFactory === "function") return channelFactory(name);
    if (typeof BroadcastChannel === "function") return new BroadcastChannel(name);
    return null;
  }
  var VersionedStore = class {
    constructor({
      kind,
      key,
      storage,
      validateRecord,
      validateWorkspace,
      migrateRecord,
      migrateWorkspace,
      parse = JSON.parse,
      stringify = JSON.stringify,
      limits,
      clock = () => /* @__PURE__ */ new Date(),
      idFactory = () => createStableId(),
      channelFactory,
      storageEventTarget = typeof window === "object" ? window : null
    }) {
      assertStorageAdapter(storage);
      if (!Object.values(STORAGE_KINDS).includes(kind)) throw new RangeError("Unknown store kind");
      if (typeof key !== "string" || !key) throw new TypeError("A storage key is required");
      this.kind = kind;
      this.key = key;
      this.storage = storage;
      this.validateRecord = validateRecord;
      this.validateWorkspace = validateWorkspace;
      this.migrateRecord = migrateRecord;
      this.migrateWorkspace = migrateWorkspace;
      this.parse = parse;
      this.stringify = stringify;
      this.limits = normalizeLimits(limits);
      this.clock = clock;
      this.idFactory = idFactory;
      this.listeners = /* @__PURE__ */ new Set();
      this.instanceId = createStableId();
      const timestamp2 = nowIso(clock);
      this.state = kind === STORAGE_KINDS.workspace ? emptyWorkspaceEnvelope(timestamp2) : emptyCollectionEnvelope(kind, timestamp2);
      this.persistedRaw = null;
      this.recovery = null;
      this.pendingMigration = null;
      this.channel = createChannel(channelFactory, "rudertrimm:v2:storage");
      this.storageEventTarget = storageEventTarget;
      this.onChannelMessage = (event) => {
        const message = event?.data;
        if (message?.type === "rudertrimm-storage-change" && message.key === this.key && message.sender !== this.instanceId) {
          this.reloadFromExternal("broadcast");
        }
      };
      this.onStorageEvent = (event) => {
        const eventKey = event?.key;
        const sameArea = !event?.storageArea || event.storageArea === this.storage;
        if (sameArea && (eventKey === this.key || eventKey === null)) {
          this.reloadFromExternal("storage-event");
        }
      };
      if (this.channel?.addEventListener) this.channel.addEventListener("message", this.onChannelMessage);
      else if (this.channel) this.channel.onmessage = this.onChannelMessage;
      this.storageEventTarget?.addEventListener?.("storage", this.onStorageEvent);
    }
    defaultEnvelope() {
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      return this.kind === STORAGE_KINDS.workspace ? emptyWorkspaceEnvelope(timestamp2) : emptyCollectionEnvelope(this.kind, timestamp2);
    }
    normalizeStored(envelope, { migrate = false } = {}) {
      return this.kind === STORAGE_KINDS.workspace ? normalizeWorkspaceEnvelope(envelope, {
        validateWorkspace: this.validateWorkspace,
        migrateWorkspace: migrate ? this.migrateWorkspace : void 0,
        limits: this.limits,
        storage: true
      }) : normalizeCollectionEnvelope(envelope, {
        kind: this.kind,
        validateRecord: this.validateRecord,
        migrateRecord: migrate ? this.migrateRecord : void 0,
        // Historical snapshots are immutable legacy facts. They still need a
        // current-domain projection on every validation pass, including the
        // migration commit and later reloads, but are never rewritten in place.
        migrateHistoryRecord: this.migrateRecord,
        limits: this.limits,
        storage: true
      });
    }
    normalizeForPersistence(envelope) {
      const normalized = this.normalizeStored(envelope).envelope;
      if (!COLLECTION_KINDS.has(this.kind)) return normalized;
      const fitted = jsonClone(normalized, this.limits, "storage retention");
      compactHistoryToStorageBytes(fitted, this.limits, this.stringify);
      return this.normalizeStored(fitted).envelope;
    }
    quarantine(raw, error) {
      const result = { stored: false, deduplicated: false, key: null, error: null };
      if (typeof raw !== "string" || utf8ByteLength(raw) > this.limits.maxStorageBytes) {
        result.error = new StorageValidationError("Invalid payload was too large to duplicate into quarantine");
        return Object.freeze(result);
      }
      try {
        const baseKey = `${STORAGE_KEYS.quarantinePrefix}${encodeURIComponent(this.kind)}:${quarantineFingerprint(raw)}`;
        const existing = this.storage.getItem(baseKey);
        if (existing === raw) {
          result.stored = true;
          result.deduplicated = true;
          result.key = baseKey;
          return Object.freeze(result);
        }
        const key = existing === null ? baseKey : `${baseKey}:${createStableId()}`;
        this.storage.setItem(key, raw);
        result.stored = true;
        result.key = key;
      } catch (quarantineError) {
        result.error = quarantineError;
      }
      return Object.freeze(result);
    }
    adoptRaw(raw, { quarantine = true } = {}) {
      if (raw === null) {
        this.state = this.defaultEnvelope();
        this.persistedRaw = null;
        this.recovery = null;
        this.pendingMigration = null;
        return Object.freeze({
          ok: true,
          recovered: false,
          rawPresent: false,
          state: this.snapshot(),
          migration: NO_MIGRATION,
          quarantine: null,
          error: null
        });
      }
      try {
        const normalized = parseEnvelopeText(raw, {
          kind: this.kind,
          parse: this.parse,
          validateRecord: this.validateRecord,
          validateWorkspace: this.validateWorkspace,
          migrateRecord: this.migrateRecord,
          migrateWorkspace: this.migrateWorkspace,
          limits: this.limits,
          storage: true
        });
        this.state = deepFreeze2(normalized.envelope);
        this.persistedRaw = raw;
        this.recovery = null;
        this.pendingMigration = normalized.migration.migrated ? Object.freeze({
          sourceRaw: raw,
          sourceRevision: normalized.envelope.revision,
          recordIds: normalized.migration.recordIds,
          historyBaselineIds: normalized.migration.historyBaselineIds,
          storageEnvelope: normalized.migration.storageEnvelope,
          workspace: normalized.migration.workspace
        }) : null;
        return Object.freeze({
          ok: true,
          recovered: false,
          rawPresent: true,
          state: this.snapshot(),
          migration: normalized.migration,
          quarantine: null,
          error: null
        });
      } catch (error) {
        if (error?.code === "unsupported-schema") {
          this.state = this.defaultEnvelope();
          this.persistedRaw = raw;
          this.pendingMigration = null;
          this.recovery = Object.freeze({
            raw,
            error,
            quarantine: null,
            readFailure: false,
            unsupportedSchema: true
          });
          return Object.freeze({
            ok: false,
            recovered: false,
            rawPresent: true,
            unsupportedSchema: true,
            state: this.snapshot(),
            migration: NO_MIGRATION,
            quarantine: null,
            error
          });
        }
        const prior = this.recovery?.readFailure === false && this.recovery.raw === raw ? this.recovery : null;
        this.state = this.defaultEnvelope();
        this.persistedRaw = raw;
        this.pendingMigration = null;
        const quarantineResult = quarantine ? prior?.quarantine ?? this.quarantine(raw, error) : null;
        this.recovery = Object.freeze({ raw, error, quarantine: quarantineResult, readFailure: false });
        return Object.freeze({
          ok: false,
          recovered: true,
          rawPresent: true,
          state: this.snapshot(),
          migration: NO_MIGRATION,
          quarantine: quarantineResult,
          error
        });
      }
    }
    markReadFailure(error, { resetState = false } = {}) {
      if (resetState) this.state = this.defaultEnvelope();
      this.pendingMigration = null;
      this.recovery = Object.freeze({ raw: null, error, quarantine: null, readFailure: true });
      return Object.freeze({
        ok: false,
        recovered: true,
        rawPresent: null,
        state: this.snapshot(),
        migration: NO_MIGRATION,
        quarantine: null,
        error
      });
    }
    load({ quarantine = true } = {}) {
      let raw;
      try {
        raw = this.storage.getItem(this.key);
      } catch (error) {
        return this.markReadFailure(error, { resetState: true });
      }
      return this.adoptRaw(raw, { quarantine });
    }
    reloadFromExternal(source) {
      const before = this.state.revision;
      try {
        const raw = this.storage.getItem(this.key);
        if (raw === this.persistedRaw && this.recovery?.readFailure !== true) return false;
        const result = this.adoptRaw(raw);
        this.emit({ type: "external-sync", source, beforeRevision: before, revision: this.state.revision, result });
        return true;
      } catch (error) {
        this.markReadFailure(error);
        this.emit({ type: "external-sync-error", source, beforeRevision: before, error });
        return false;
      }
    }
    snapshot() {
      return deepFreeze2(jsonClone(this.state, this.limits, "snapshot"));
    }
    constraints() {
      return this.limits;
    }
    migrationStatus() {
      const pending = this.pendingMigration;
      return Object.freeze({
        pending: pending !== null,
        recordIds: pending?.recordIds ?? Object.freeze([]),
        workspace: pending?.workspace ?? false
      });
    }
    subscribe(listener) {
      if (typeof listener !== "function") throw new TypeError("listener must be a function");
      this.listeners.add(listener);
      return () => this.listeners.delete(listener);
    }
    emit(event) {
      const frozenEvent = Object.freeze({ ...event });
      for (const listener of this.listeners) {
        try {
          listener(frozenEvent);
        } catch {
        }
      }
    }
    assertFresh(expectedRevision) {
      if (expectedRevision !== void 0 && expectedRevision !== this.state.revision) {
        throw new RevisionConflictError(`Expected revision ${expectedRevision}, current revision is ${this.state.revision}`);
      }
      const currentRaw = this.storage.getItem(this.key);
      if (currentRaw !== this.persistedRaw) throw new RevisionConflictError("Storage changed in another context");
    }
    persist(next, { expectedRevision, reason }) {
      if (this.pendingMigration) throw new MigrationPendingError();
      if (this.recovery?.error?.code === "unsupported-schema") throw this.recovery.error;
      if (this.recovery && this.recovery.quarantine?.stored !== true) {
        throw new UnsafeRecoveryError();
      }
      this.assertFresh(expectedRevision);
      const normalized = this.normalizeForPersistence(next);
      const raw = serializeEnvelope(normalized, {
        stringify: this.stringify,
        limits: this.limits,
        label: "storage envelope",
        maxBytes: this.limits.maxStorageBytes
      });
      this.storage.setItem(this.key, raw);
      this.state = deepFreeze2(normalized);
      this.persistedRaw = raw;
      this.recovery = null;
      this.emit({ type: "commit", reason, revision: normalized.revision });
      try {
        this.channel?.postMessage?.({
          type: "rudertrimm-storage-change",
          key: this.key,
          revision: normalized.revision,
          sender: this.instanceId
        });
      } catch (error) {
        this.emit({ type: "sync-notification-error", revision: normalized.revision, error });
      }
      return this.snapshot();
    }
    /**
     * Persist a previously validated migration as one compare-and-write commit.
     * Source bytes remain authoritative until setItem succeeds; retries therefore
     * cannot lose the old value or increment revisions twice after quota failure.
     */
    commitPendingMigration({ expectedRevision } = {}) {
      const pending = this.pendingMigration;
      if (!pending) {
        return Object.freeze({
          changed: false,
          revision: this.state.revision,
          recordIds: Object.freeze([]),
          workspace: false
        });
      }
      if (expectedRevision !== void 0 && expectedRevision !== pending.sourceRevision) {
        throw new RevisionConflictError(`Expected revision ${expectedRevision}, current migration source is ${pending.sourceRevision}`);
      }
      const currentRaw = this.storage.getItem(this.key);
      if (currentRaw !== pending.sourceRaw) throw new RevisionConflictError("Migration source bytes changed in another context");
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      const next = jsonClone(this.state, this.limits, "pending migration");
      assertRevision(next.revision + 1, "migrated envelope revision");
      next.revision += 1;
      next.updatedAt = timestamp2;
      if (this.kind !== STORAGE_KINDS.workspace) {
        const migratedIds = new Set(pending.recordIds);
        const baselineIds = new Set(pending.historyBaselineIds);
        next.records = next.records.map((record) => {
          if (!migratedIds.has(record.id)) return record;
          assertRevision(record.revision + 1, `migrated record ${record.id} revision`, { allowZero: false });
          return { ...record, revision: record.revision + 1, updatedAt: timestamp2 };
        });
        for (const id of migratedIds) {
          const record = next.records.find((candidate) => candidate.id === id);
          if (!record) throw new StorageValidationError(`Migrated record ${id} disappeared before commit`);
          if (baselineIds.has(id)) {
            const baseline = next.history.entries.find((entry) => entry.entityId === id);
            if (!baseline) throw new StorageValidationError(`History baseline for ${id} disappeared before commit`);
            baseline.revision = record.revision;
            baseline.changedAt = timestamp2;
            baseline.snapshot = jsonClone(record.value, this.limits, "migrated history baseline");
          } else {
            appendHistory(
              next.history,
              record,
              "migration",
              timestamp2,
              { source: "migration", reason: "domain-schema-migration" },
              next.records,
              this.limits
            );
          }
        }
      }
      const normalized = this.normalizeForPersistence(next);
      const raw = serializeEnvelope(normalized, {
        stringify: this.stringify,
        limits: this.limits,
        label: "migrated storage envelope",
        maxBytes: this.limits.maxStorageBytes
      });
      this.storage.setItem(this.key, raw);
      this.state = deepFreeze2(normalized);
      this.persistedRaw = raw;
      this.pendingMigration = null;
      this.recovery = null;
      this.emit({ type: "commit", reason: "migration", revision: normalized.revision });
      try {
        this.channel?.postMessage?.({
          type: "rudertrimm-storage-change",
          key: this.key,
          revision: normalized.revision,
          sender: this.instanceId
        });
      } catch (error) {
        this.emit({ type: "sync-notification-error", revision: normalized.revision, error });
      }
      return Object.freeze({
        changed: true,
        revision: normalized.revision,
        recordIds: pending.recordIds,
        workspace: pending.workspace
      });
    }
    exportEnvelope() {
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      if (this.kind === STORAGE_KINDS.workspace) {
        return deepFreeze2(normalizeWorkspaceEnvelope({
          format: EXCHANGE_FORMAT,
          schemaVersion: EXCHANGE_SCHEMA_VERSION,
          kind: this.kind,
          exportedAt: timestamp2,
          workspace: this.state.workspace
        }, { validateWorkspace: this.validateWorkspace, limits: this.limits, storage: false }).envelope);
      }
      return deepFreeze2(normalizeCollectionEnvelope({
        format: EXCHANGE_FORMAT,
        schemaVersion: EXCHANGE_SCHEMA_VERSION,
        kind: this.kind,
        exportedAt: timestamp2,
        records: this.state.records
      }, { kind: this.kind, validateRecord: this.validateRecord, limits: this.limits, storage: false }).envelope);
    }
    exportText() {
      return serializeEnvelope(this.exportEnvelope(), { stringify: this.stringify, limits: this.limits, label: "export file" });
    }
    importText(text, { expectedRevision, migratedRecordIds = [], audit } = {}) {
      if (!Array.isArray(migratedRecordIds) || migratedRecordIds.some((id) => typeof id !== "string" || id.length === 0)) {
        throw new TypeError("migratedRecordIds must be an array of non-empty ids");
      }
      const parsed = parseImportEnvelope(text, {
        expectedKind: this.kind,
        parse: this.parse,
        validateRecord: this.validateRecord,
        validateWorkspace: this.validateWorkspace,
        migrateRecord: this.migrateRecord,
        migrateWorkspace: this.migrateWorkspace,
        limits: this.limits
      });
      if (!parsed.ok) throw parsed.errors[0];
      const importedIds = this.kind === STORAGE_KINDS.workspace ? /* @__PURE__ */ new Set() : new Set(parsed.envelope.records.map((record) => record.id));
      if (migratedRecordIds.some((id) => !importedIds.has(id))) {
        throw new StorageValidationError("migratedRecordIds contains an id outside the import envelope");
      }
      if (this.kind !== STORAGE_KINDS.workspace) {
        const activeIds = new Set(this.state.records.map((record) => record.id));
        const retiredIds = new Set([
          ...this.state.history.entries.map((entry) => entry.entityId),
          ...this.state.history.floors.map((floor) => floor.entityId)
        ].filter((id) => !activeIds.has(id)));
        const resurrected = [...importedIds].find((id) => retiredIds.has(id));
        if (resurrected) {
          throw new StorageValidationError(`Import must not reuse retired entity id ${resurrected}; preview through the merge adapter to remap it`);
        }
      }
      const committedMigrationIds = /* @__PURE__ */ new Set([...parsed.migration.recordIds, ...migratedRecordIds]);
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      const next = this.kind === STORAGE_KINDS.workspace ? {
        format: STORAGE_FORMAT,
        schemaVersion: STORAGE_SCHEMA_VERSION,
        kind: this.kind,
        revision: this.state.revision + 1,
        updatedAt: timestamp2,
        workspace: parsed.envelope.workspace
      } : (() => {
        const auditMeta = normalizeAudit(audit, { source: "json-import", reason: "collection-import" });
        const currentById = new Map(this.state.records.map((record) => [record.id, record]));
        const records = parsed.envelope.records.map((incoming) => {
          const current = currentById.get(incoming.id);
          if (current) {
            if (JSON.stringify(current.value) === JSON.stringify(incoming.value)) return current;
            return { ...incoming, revision: current.revision + 1, updatedAt: timestamp2 };
          }
          if (!committedMigrationIds.has(incoming.id)) return incoming;
          assertRevision(incoming.revision + 1, `imported migrated record ${incoming.id} revision`, { allowZero: false });
          return { ...incoming, revision: incoming.revision + 1, updatedAt: timestamp2 };
        });
        const history = jsonClone(this.state.history, this.limits, "import history");
        const recordsById = new Map(records.map((record) => [record.id, record]));
        for (const record of records) {
          const current = currentById.get(record.id);
          if (!current || JSON.stringify(current.value) !== JSON.stringify(record.value)) {
            appendHistory(history, record, "import", timestamp2, auditMeta, records, this.limits);
          }
        }
        for (const current of this.state.records) {
          if (!recordsById.has(current.id)) {
            privacyDeleteHistory(history, current, timestamp2, {
              source: auditMeta.source,
              reason: "collection-import-delete"
            }, records, this.limits);
          }
        }
        return {
          format: STORAGE_FORMAT,
          schemaVersion: STORAGE_SCHEMA_VERSION,
          kind: this.kind,
          revision: this.state.revision + 1,
          updatedAt: timestamp2,
          records,
          history
        };
      })();
      return this.persist(next, { expectedRevision, reason: "import" });
    }
    close() {
      if (this.channel?.removeEventListener) this.channel.removeEventListener("message", this.onChannelMessage);
      else if (this.channel) this.channel.onmessage = null;
      this.channel?.close?.();
      this.storageEventTarget?.removeEventListener?.("storage", this.onStorageEvent);
      this.listeners.clear();
    }
  };
  var CollectionRepository = class extends VersionedStore {
    constructor(options) {
      if (!COLLECTION_KINDS.has(options?.kind)) throw new RangeError("Unknown collection kind");
      super(options);
    }
    list() {
      return this.snapshot().records;
    }
    history(entityId) {
      if (entityId !== void 0) assertId(entityId, "history entity id");
      const entries = entityId === void 0 ? this.state.history.entries : this.state.history.entries.filter((entry) => entry.entityId === entityId);
      return deepFreeze2(jsonClone(entries, this.limits, "history entries"));
    }
    historyFloors() {
      return deepFreeze2(jsonClone(this.state.history.floors, this.limits, "history floors"));
    }
    /**
     * Active ids plus history/tombstone floors. Privacy-deleted ids stay reserved so
     * an import or delayed cross-repository save cannot resurrect their identity.
     */
    reservedIds() {
      return Object.freeze([.../* @__PURE__ */ new Set([
        ...this.state.records.map((record) => record.id),
        ...this.state.history.entries.map((entry) => entry.entityId),
        ...this.state.history.floors.map((floor) => floor.entityId)
      ])]);
    }
    select(id) {
      if (typeof id !== "string" || id.length === 0) {
        return Object.freeze({ ok: false, code: "no-selection", record: null });
      }
      const record = this.state.records.find((entry) => entry.id === id);
      return record ? Object.freeze({ ok: true, code: "ok", record: deepFreeze2(jsonClone(record, this.limits, "record")) }) : Object.freeze({ ok: false, code: "not-found", record: null });
    }
    create(value, { expectedRevision, audit } = {}) {
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      assertJsonSafe(value, this.limits, "record.value");
      assertNameLimit(value, "record.value", this.limits);
      const validated = validatorResult(this.validateRecord, value, "record.value");
      assertJsonSafe(validated, this.limits, "record.value");
      assertNameLimit(validated, "record.value", this.limits);
      let id;
      const reservedIds = new Set(this.reservedIds());
      for (let attempt = 0; attempt < 8; attempt += 1) {
        const candidate = this.idFactory();
        assertId(candidate);
        if (!reservedIds.has(candidate)) {
          id = candidate;
          break;
        }
      }
      if (!id) throw new StorageValidationError("Could not allocate a unique record id");
      const record = {
        id,
        revision: 1,
        updatedAt: timestamp2,
        value: jsonClone(validated, this.limits, "record.value")
      };
      const next = {
        ...jsonClone(this.state, this.limits, "working copy"),
        revision: this.state.revision + 1,
        updatedAt: timestamp2,
        records: [...this.state.records, record]
      };
      const auditMeta = normalizeAudit(audit, { source: "local-ui", reason: "created" });
      appendHistory(next.history, record, "create", timestamp2, auditMeta, next.records, this.limits);
      if (next.records.length > this.limits.maxRecords) throw new StorageValidationError(`At most ${this.limits.maxRecords} records are allowed`);
      this.persist(next, { expectedRevision, reason: "create" });
      return this.select(record.id).record;
    }
    update(id, value, { expectedRevision, expectedRecordRevision, audit } = {}) {
      if (typeof id !== "string" || id.length === 0) throw new NoSelectionError();
      const index = this.state.records.findIndex((entry) => entry.id === id);
      if (index < 0) throw new NoSelectionError("The selected record does not exist");
      const current = this.state.records[index];
      if (expectedRecordRevision !== void 0 && expectedRecordRevision !== current.revision) {
        throw new RevisionConflictError(`Expected record revision ${expectedRecordRevision}, current revision is ${current.revision}`);
      }
      assertJsonSafe(value, this.limits, "record.value");
      assertNameLimit(value, "record.value", this.limits);
      const validated = validatorResult(this.validateRecord, value, "record.value");
      assertJsonSafe(validated, this.limits, "record.value");
      assertNameLimit(validated, "record.value", this.limits);
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      const next = jsonClone(this.state, this.limits, "working copy");
      next.records[index] = {
        id,
        revision: current.revision + 1,
        updatedAt: timestamp2,
        value: jsonClone(validated, this.limits, "record.value")
      };
      next.revision = this.state.revision + 1;
      next.updatedAt = timestamp2;
      appendHistory(
        next.history,
        next.records[index],
        "update",
        timestamp2,
        normalizeAudit(audit, { source: "local-ui", reason: "updated" }),
        next.records,
        this.limits
      );
      this.persist(next, { expectedRevision, reason: "update" });
      return this.select(id).record;
    }
    delete(id, { expectedRevision, expectedRecordRevision, audit } = {}) {
      if (typeof id !== "string" || id.length === 0) throw new NoSelectionError();
      const index = this.state.records.findIndex((entry) => entry.id === id);
      if (index < 0) throw new NoSelectionError("The selected record does not exist");
      const current = this.state.records[index];
      if (expectedRecordRevision !== void 0 && expectedRecordRevision !== current.revision) {
        throw new RevisionConflictError(`Expected record revision ${expectedRecordRevision}, current revision is ${current.revision}`);
      }
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      const next = jsonClone(this.state, this.limits, "working copy");
      next.records.splice(index, 1);
      next.revision = this.state.revision + 1;
      next.updatedAt = timestamp2;
      privacyDeleteHistory(
        next.history,
        current,
        timestamp2,
        normalizeAudit(audit, { source: "local-ui", reason: "privacy-delete" }),
        next.records,
        this.limits
      );
      this.persist(next, { expectedRevision, reason: "delete" });
      return deepFreeze2(jsonClone(current, this.limits, "deleted record"));
    }
  };
  var WorkspaceRepository = class extends VersionedStore {
    constructor(options) {
      super({ ...options, kind: STORAGE_KINDS.workspace });
    }
    get() {
      return this.snapshot().workspace;
    }
    save(workspace, { expectedRevision } = {}) {
      assertJsonSafe(workspace, this.limits, "workspace");
      assertWorkspaceIsolation(workspace);
      const validated = validatorResult(this.validateWorkspace, workspace, "workspace");
      assertJsonSafe(validated, this.limits, "workspace");
      assertWorkspaceIsolation(validated);
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      const next = {
        format: STORAGE_FORMAT,
        schemaVersion: STORAGE_SCHEMA_VERSION,
        kind: STORAGE_KINDS.workspace,
        revision: this.state.revision + 1,
        updatedAt: timestamp2,
        workspace: jsonClone(validated, this.limits, "workspace")
      };
      this.persist(next, { expectedRevision, reason: "save-workspace" });
      return this.get();
    }
    clear({ expectedRevision } = {}) {
      const timestamp2 = monotonicStoreTimestamp(this.clock, this.state);
      return this.persist({
        format: STORAGE_FORMAT,
        schemaVersion: STORAGE_SCHEMA_VERSION,
        kind: STORAGE_KINDS.workspace,
        revision: this.state.revision + 1,
        updatedAt: timestamp2,
        workspace: null
      }, { expectedRevision, reason: "clear-workspace" });
    }
  };
  function createRowerRepository(options) {
    return new CollectionRepository({ ...options, kind: STORAGE_KINDS.rowers, key: options?.key ?? STORAGE_KEYS.rowers });
  }
  function createBoatRepository(options) {
    return new CollectionRepository({ ...options, kind: STORAGE_KINDS.boats, key: options?.key ?? STORAGE_KEYS.boats });
  }
  function createEfaCandidateRepository(options) {
    return new CollectionRepository({
      ...options,
      kind: STORAGE_KINDS.efaCandidates,
      key: options?.key ?? STORAGE_KEYS.efaCandidates
    });
  }
  function createWorkspaceRepository(options) {
    return new WorkspaceRepository({ ...options, key: options?.key ?? STORAGE_KEYS.workspace });
  }

  // js/import-adapter.mjs
  var defaultClock = () => /* @__PURE__ */ new Date();
  function isoNow(clock) {
    const value = clock();
    const date = value instanceof Date ? value : new Date(value);
    if (!Number.isFinite(date.getTime())) throw new RangeError("clock must return a valid date");
    return date.toISOString();
  }
  function validationMessage(result) {
    return result.errors.slice(0, 5).map((error) => `${error.path}: ${error.message}`).join("\n");
  }
  function canonicalJson2(value) {
    if (value === null || typeof value !== "object") return JSON.stringify(value);
    if (Array.isArray(value)) return `[${value.map(canonicalJson2).join(",")}]`;
    return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonicalJson2(value[key])}`).join(",")}}`;
  }
  function collectionExchange(kind, values, builder, { clock = defaultClock, idFactory = createStableId } = {}) {
    if (!Array.isArray(values)) throw new TypeError("values must be an array");
    if (typeof builder !== "function") throw new TypeError("builder must be a function");
    const timestamp2 = isoNow(clock);
    return {
      format: EXCHANGE_FORMAT,
      schemaVersion: EXCHANGE_SCHEMA_VERSION,
      kind,
      exportedAt: timestamp2,
      records: values.map((value) => ({
        id: idFactory(),
        revision: 1,
        updatedAt: timestamp2,
        value: builder(value)
      }))
    };
  }
  function migrateCollection(repository, kind, values, builder, options = {}) {
    if (!values.length) return false;
    repository.importText(JSON.stringify(collectionExchange(kind, values, builder, options)), {
      expectedRevision: repository.snapshot().revision
    });
    return true;
  }
  function uniqueId(ids, idFactory) {
    for (let attempt = 0; attempt < 16; attempt += 1) {
      const id = idFactory();
      if (!ids.has(id)) return id;
    }
    throw new Error("Import-ID conflict could not be resolved");
  }
  var IMPORT_PREVIEW_FIELDS = Object.freeze([
    "kind",
    "existing",
    "incoming",
    "added",
    "duplicatesSkipped",
    "remapped",
    "migrated",
    "referencesDetached",
    "total",
    "maxRecords",
    "capacityExceeded",
    "legacy",
    "baseRevision",
    "planFingerprint"
  ]);
  function sameCollectionImportPreview(left, right) {
    return !!left && !!right && IMPORT_PREVIEW_FIELDS.every((field) => Object.is(left[field], right[field]));
  }
  function detachImportedBoatRowerReferences(value) {
    if (value === null || typeof value !== "object" || Array.isArray(value) || value.kind !== "boat" || !Array.isArray(value.seats)) {
      throw new TypeError("detachImportedBoatRowerReferences requires a validated boat DTO");
    }
    let referencesDetached = 0;
    const seats = value.seats.map((seat) => {
      if (seat?.rowerRef === null) return seat;
      referencesDetached += 1;
      return { ...seat, rowerRef: null };
    });
    return Object.freeze({
      value: referencesDetached === 0 ? value : { ...value, seats },
      referencesDetached
    });
  }
  function prepareCollectionImport({
    repository,
    text,
    validateRecord,
    builder,
    migrateRecord,
    migrateLegacyImport,
    transformIncomingValue,
    clock = defaultClock,
    idFactory = createStableId
  }) {
    if (!repository || typeof repository.importText !== "function") throw new TypeError("repository is required");
    if (typeof repository.constraints !== "function") throw new TypeError("repository constraints are required");
    if (typeof text !== "string") throw new TypeError("text must be a string");
    if (typeof validateRecord !== "function" || typeof builder !== "function") throw new TypeError("validator and builder are required");
    if (migrateRecord !== void 0 && typeof migrateRecord !== "function") throw new TypeError("migrateRecord must be a function");
    if (migrateLegacyImport !== void 0 && typeof migrateLegacyImport !== "function") throw new TypeError("migrateLegacyImport must be a function");
    if (transformIncomingValue !== void 0 && typeof transformIncomingValue !== "function") throw new TypeError("transformIncomingValue must be a function");
    const limits = repository.constraints();
    if (!limits || typeof limits !== "object") throw new TypeError("repository constraints are invalid");
    if (!Number.isSafeInteger(limits.maxBytes) || limits.maxBytes < 1) throw new RangeError("repository maxBytes constraint is invalid");
    const importBytes = utf8ByteLength(text);
    if (importBytes > limits.maxBytes) {
      throw new StorageValidationError(
        `import file exceeds ${limits.maxBytes} UTF-8 bytes`,
        [{ bytes: importBytes, maxBytes: limits.maxBytes }]
      );
    }
    let parsed = parseImportEnvelope(text, { expectedKind: repository.kind, validateRecord, migrateRecord, limits });
    let legacy = false;
    const legacyMigratedRecordIds = /* @__PURE__ */ new Set();
    if (!parsed.ok) {
      const futureDomain = parsed.errors.find((error) => error?.code === "unsupported-schema-version");
      if (futureDomain) throw futureDomain;
      const normalizedFuture = parsed.errors.find((error) => error?.code === "unsupported-schema" && error.details?.some((detail) => detail?.supported === SCHEMA_VERSION && String(detail?.path).endsWith(".value")));
      if (normalizedFuture) {
        const detail = normalizedFuture.details.find((candidate) => candidate?.supported === SCHEMA_VERSION && String(candidate?.path).endsWith(".value"));
        throw new UnsupportedSchemaVersionError(detail.path, detail.actual);
      }
      let oldEnvelope;
      try {
        oldEnvelope = JSON.parse(text);
      } catch {
        throw parsed.errors[0];
      }
      const migratedEnvelope = migrateLegacyImport ? migrateLegacyImport(oldEnvelope, { expectedKind: repository.kind }) : oldEnvelope;
      const validation = validateImportObject(migratedEnvelope);
      if (!validation.ok || migratedEnvelope.kind !== repository.kind) {
        throw new TypeError(validation.ok ? `Expected ${repository.kind}` : validationMessage(validation));
      }
      const legacyExchange = collectionExchange(repository.kind, migratedEnvelope.items, builder, { clock, idFactory });
      if (Array.isArray(oldEnvelope?.items) && oldEnvelope.items.length === migratedEnvelope.items.length) {
        for (let index = 0; index < migratedEnvelope.items.length; index += 1) {
          if (canonicalJson2(oldEnvelope.items[index]) !== canonicalJson2(migratedEnvelope.items[index])) {
            legacyMigratedRecordIds.add(legacyExchange.records[index].id);
          }
        }
      }
      parsed = parseImportEnvelope(
        JSON.stringify(legacyExchange),
        { expectedKind: repository.kind, validateRecord, migrateRecord, limits }
      );
      if (!parsed.ok) throw parsed.errors[0];
      legacy = true;
    }
    const migratedSourceIds = /* @__PURE__ */ new Set([...parsed.migration.recordIds, ...legacyMigratedRecordIds]);
    let referencesDetached = 0;
    if (transformIncomingValue) {
      const transformedRecords = parsed.envelope.records.map((record, index) => {
        const transformed = transformIncomingValue(record.value, Object.freeze({
          kind: repository.kind,
          recordId: record.id,
          index
        }));
        if (transformed === null || typeof transformed !== "object" || Array.isArray(transformed) || !Object.hasOwn(transformed, "value") || !Number.isSafeInteger(transformed.referencesDetached) || transformed.referencesDetached < 0) {
          throw new TypeError("transformIncomingValue must return {value, referencesDetached}");
        }
        referencesDetached += transformed.referencesDetached;
        return { ...record, value: transformed.value };
      });
      const transformedParsed = parseImportEnvelope(JSON.stringify({
        ...parsed.envelope,
        records: transformedRecords
      }), { expectedKind: repository.kind, validateRecord, limits });
      if (!transformedParsed.ok) throw transformedParsed.errors[0];
      parsed = Object.freeze({
        ...transformedParsed,
        migration: parsed.migration
      });
    }
    const current = repository.exportEnvelope();
    const baseRevision = repository.snapshot().revision;
    const planFingerprint = canonicalJson2({ base: current.records, source: parsed.envelope.records });
    const fingerprints = new Set(current.records.map((record) => canonicalJson2(record.value)));
    let duplicatesSkipped = 0;
    const uniqueRecords = parsed.envelope.records.filter((record) => {
      const fingerprint = canonicalJson2(record.value);
      if (fingerprints.has(fingerprint)) {
        duplicatesSkipped += 1;
        return false;
      }
      fingerprints.add(fingerprint);
      return true;
    });
    const ids = new Set(typeof repository.reservedIds === "function" ? repository.reservedIds() : current.records.map((record) => record.id));
    const migratedRecordIds = /* @__PURE__ */ new Set();
    let remapped = 0;
    const incoming = uniqueRecords.map((record) => {
      let id = record.id;
      if (ids.has(id)) {
        id = uniqueId(ids, idFactory);
        remapped += 1;
      }
      ids.add(id);
      if (!migratedSourceIds.has(record.id)) return { ...record, id };
      if (!Number.isSafeInteger(record.revision) || record.revision < 1 || record.revision >= Number.MAX_SAFE_INTEGER) {
        throw new RangeError("Migrierte Importrevision kann nicht sicher erhöht werden");
      }
      migratedRecordIds.add(id);
      return { ...record, id };
    });
    const migrated = migratedRecordIds.size;
    legacy || (legacy = migrated > 0);
    const maxRecords = limits.maxRecords;
    if (!Number.isSafeInteger(maxRecords) || maxRecords < 1) throw new RangeError("repository maxRecords constraint is invalid");
    const total = current.records.length + incoming.length;
    const preview = Object.freeze({
      kind: repository.kind,
      existing: current.records.length,
      incoming: parsed.envelope.records.length,
      added: incoming.length,
      duplicatesSkipped,
      remapped,
      migrated,
      referencesDetached,
      total,
      maxRecords,
      capacityExceeded: total > maxRecords,
      legacy,
      baseRevision,
      planFingerprint
    });
    if (preview.capacityExceeded) {
      throw new RangeError(`Import würde ${total} Datensätze erzeugen; maximal ${maxRecords} sind erlaubt`);
    }
    return { preview, current, incoming, migratedRecordIds: [...migratedRecordIds] };
  }
  function previewCollectionImport(options) {
    return prepareCollectionImport(options).preview;
  }
  function mergeCollectionImport(options) {
    const { repository, approve = () => true, clock = defaultClock, audit } = options ?? {};
    if (typeof approve !== "function") throw new TypeError("approve must be a function");
    const { preview, current, incoming, migratedRecordIds } = prepareCollectionImport(options);
    if (!approve(preview)) return Object.freeze({ ...preview, added: 0, cancelled: true });
    if (incoming.length === 0) return Object.freeze({ ...preview, cancelled: false });
    repository.importText(JSON.stringify({
      ...current,
      exportedAt: isoNow(clock),
      records: [...current.records, ...incoming]
    }), { expectedRevision: repository.snapshot().revision, migratedRecordIds, audit });
    return Object.freeze({ ...preview, cancelled: false });
  }

  // js/ui-session.mjs
  function nonEmptyString(value, label) {
    if (typeof value !== "string" || !value.trim()) throw new TypeError(`${label} must be a non-empty string`);
    return value;
  }
  function revision(value, label, { allowZero = false } = {}) {
    if (!Number.isSafeInteger(value) || value < (allowZero ? 0 : 1)) throw new RangeError(`${label} must be a valid revision`);
    return value;
  }
  var VIEW_STATES = /* @__PURE__ */ new Set(["new", "available", "synced"]);
  var STORAGE_MODES = /* @__PURE__ */ new Set(["local", "session", "memory"]);
  var WORKFLOW_STEP_IDS = Object.freeze(["boat", "seat", "profile", "result", "save"]);
  function workflowGuideState({ boatReady, seatReady, profileReady, resultReady, dirty, viewState }) {
    for (const [label, value] of Object.entries({ boatReady, seatReady, profileReady, resultReady, dirty })) {
      if (typeof value !== "boolean") throw new TypeError(`${label} must be a boolean`);
    }
    if (!VIEW_STATES.has(viewState)) throw new RangeError("unknown workspace view state");
    const raw = [
      boatReady,
      boatReady && seatReady,
      boatReady && seatReady && profileReady,
      boatReady && seatReady && profileReady && resultReady,
      boatReady && seatReady && profileReady && resultReady && viewState === "synced" && !dirty
    ];
    const currentIndex = raw.indexOf(false);
    const steps = WORKFLOW_STEP_IDS.map((id, index) => Object.freeze({
      id,
      status: raw[index] ? "done" : index === currentIndex ? "current" : "open"
    }));
    return Object.freeze({
      steps: Object.freeze(steps),
      completedCount: raw.filter(Boolean).length,
      currentStep: currentIndex < 0 ? null : WORKFLOW_STEP_IDS[currentIndex]
    });
  }
  function workspaceStateLabel({ dirty, viewState, storageMode }) {
    if (typeof dirty !== "boolean") throw new TypeError("dirty must be a boolean");
    if (!VIEW_STATES.has(viewState)) throw new RangeError("unknown workspace view state");
    if (!STORAGE_MODES.has(storageMode)) throw new RangeError("unknown storage mode");
    if (dirty) return "Ungespeicherte Änderungen";
    if (viewState === "available") return "Gespeicherter Stand verfügbar · noch nicht geladen";
    if (viewState === "new") return "Noch nicht gespeichert";
    if (storageMode === "local") return "Arbeitsstand gespeichert";
    if (storageMode === "session") return "Nur in diesem Tab gespeichert";
    return "Nur flüchtig gespeichert";
  }
  function workspaceSavePolicy({ viewState, canCommit }) {
    if (!VIEW_STATES.has(viewState)) throw new RangeError("unknown workspace view state");
    if (typeof canCommit !== "boolean") throw new TypeError("canCommit must be a boolean");
    if (!canCommit) {
      return Object.freeze({
        code: "external-conflict",
        canAttempt: false,
        requiresConfirmation: false,
        message: "Speichern gesperrt: Der Arbeitsstand wurde extern geändert. Zuerst exportieren oder den gespeicherten Stand laden."
      });
    }
    if (viewState === "available") {
      return Object.freeze({
        code: "unloaded-existing",
        canAttempt: true,
        requiresConfirmation: true,
        message: "Ein gespeicherter Arbeitsstand ist vorhanden, aber noch nicht in die Formulare geladen."
      });
    }
    return Object.freeze({ code: "ready", canAttempt: true, requiresConfirmation: false, message: "" });
  }
  function workspaceSaveCompletionPolicy({
    savedChangeVersion,
    currentChangeVersion,
    demoAtStart,
    currentDemo,
    playing
  }) {
    revision(savedChangeVersion, "saved change version", { allowZero: true });
    revision(currentChangeVersion, "current change version", { allowZero: true });
    if (typeof playing !== "boolean") throw new TypeError("playing must be a boolean");
    const stateUnchanged = savedChangeVersion === currentChangeVersion && demoAtStart === currentDemo;
    return Object.freeze({
      clearDemo: stateUnchanged && demoAtStart !== null,
      dirty: !stateUnchanged || playing
    });
  }
  function profileCommitPolicy({ savedContext, activeContext, selectionUnchanged, savedVersion, currentVersion }) {
    nonEmptyString(savedContext, "saved context");
    nonEmptyString(activeContext, "active context");
    if (typeof selectionUnchanged !== "boolean") throw new TypeError("selectionUnchanged must be a boolean");
    revision(savedVersion, "saved draft version", { allowZero: true });
    revision(currentVersion, "current draft version", { allowZero: true });
    return Object.freeze({
      adoptSelection: savedContext === activeContext && selectionUnchanged,
      clearDraft: savedVersion === currentVersion
    });
  }
  function createContextualSelection() {
    let current = Object.freeze({ id: "", revision: null, context: null });
    const snapshot = () => Object.freeze({ ...current });
    return Object.freeze({
      snapshot,
      adopt({ id, revision: recordRevision, context }) {
        current = Object.freeze({
          id: nonEmptyString(id, "selection id"),
          revision: revision(recordRevision, "selection revision"),
          context: nonEmptyString(context, "selection context")
        });
        return snapshot();
      },
      clear() {
        current = Object.freeze({ id: "", revision: null, context: null });
        return snapshot();
      },
      isFor(context) {
        return current.id !== "" && current.context === context;
      },
      matches({ id, revision: recordRevision, context }) {
        return current.id !== "" && current.id === id && current.revision === recordRevision && current.context === context;
      }
    });
  }
  function createObservedRevision(initialRevision = 0) {
    let current = Object.freeze({
      observedRevision: revision(initialRevision, "initial revision", { allowZero: true }),
      externalRevision: null,
      stale: false
    });
    const snapshot = () => Object.freeze({ ...current, canCommit: !current.stale });
    return Object.freeze({
      snapshot,
      adopt(nextRevision) {
        current = Object.freeze({
          observedRevision: revision(nextRevision, "observed revision", { allowZero: true }),
          externalRevision: null,
          stale: false
        });
        return snapshot();
      },
      markExternal(nextRevision) {
        current = Object.freeze({
          observedRevision: current.observedRevision,
          externalRevision: revision(nextRevision, "external revision", { allowZero: true }),
          stale: true
        });
        return snapshot();
      }
    });
  }

  // js/recommendations.mjs
  var finite = (value) => typeof value === "number" && Number.isFinite(value);
  var outside = (value, min, max) => value < min ? min - value : value > max ? value - max : 0;
  function freezePlan(value) {
    if (value && typeof value === "object" && !Object.isFrozen(value)) {
      Object.freeze(value);
      for (const child of Object.values(value)) freezePlan(child);
    }
    return value;
  }
  function completeSeat(seat) {
    return seat && typeof seat === "object" && typeof seat.seatId === "string" && seat.seatId.length > 0 && Number.isSafeInteger(seat.position) && seat.position > 0 && ["skull", "riemen"].includes(seat.rig) && typeof seat.naturalResolved === "boolean" && typeof seat.reachable === "boolean" && typeof seat.trackLimited === "boolean" && ["IH", "IHsoll", "anlage", "stemmX", "knee90", "roll90"].every((key) => finite(seat[key]));
  }
  function seatScope(seat) {
    return Object.freeze({
      type: "seat",
      seatId: seat.seatId,
      position: seat.position,
      label: String(seat.label || `Platz ${seat.position}`),
      rowerName: String(seat.rowerName || "Profil")
    });
  }
  function buildTrimActionPlan({ seats, referenceSeatId = null, totalSeatCount = seats?.length ?? 0, maxActions = 3 } = {}) {
    if (!Array.isArray(seats)) throw new TypeError("seats must be an array");
    if (!Number.isSafeInteger(totalSeatCount) || totalSeatCount < seats.length) throw new RangeError("totalSeatCount must include every diagnostic seat");
    if (!Number.isSafeInteger(maxActions) || maxActions < 1 || maxActions > 10) throw new RangeError("maxActions must be in [1, 10]");
    const valid = seats.filter(completeSeat);
    const unavailable = seats.length - valid.length;
    if (valid.length === 0) {
      return freezePlan({
        status: "missing",
        summary: "Für eine Empfehlung fehlt mindestens ein vollständiges Rudererprofil mit gültigen Mess- und Trimmwerten.",
        actions: [],
        evaluatedSeats: 0,
        unavailableSeats: totalSeatCount
      });
    }
    const candidates = [];
    const safetySeats = /* @__PURE__ */ new Set();
    for (const seat of valid) {
      const issues = [];
      if (!seat.naturalResolved) issues.push("Natural-Catch-Gleichung ohne Lösung");
      if (!seat.reachable) issues.push("Griffziele im 3D-Prüfmodell nicht erreichbar");
      if (seat.trackLimited) issues.push("vorläufige Sitz-/Rollbahngrenze berührt");
      if (issues.length) {
        safetySeats.add(seat.seatId);
        candidates.push({
          id: `safety:${seat.seatId}`,
          priority: 0,
          score: issues.length,
          severity: "change",
          scope: seatScope(seat),
          field: "stemmX",
          parameter: "Stemmbrettposition (Fa)",
          current: { value: seat.stemmX, unit: "cm" },
          target: { text: "Messdaten und Bewegungsgrenzen zuerst plausibilisieren" },
          direction: "zuerst prüfen, nicht blind verstellen",
          reason: issues.join(" · "),
          effect: "Verhindert eine Empfehlung auf Basis einer geometrisch nicht belastbaren Pose.",
          uncertainty: "Körperkinematik und Reichweite sind unkalibrierte Prüfmodelle; Trainer- und Messfreigabe offen."
        });
      }
    }
    if (valid.length > 1) {
      const min = Math.min(...valid.map((seat) => seat.anlage));
      const max = Math.max(...valid.map((seat) => seat.anlage));
      if (max - min > 1e-9) {
        const reference = valid.find((seat) => seat.seatId === referenceSeatId) ?? valid.slice().sort((a, b) => b.position - a.position)[0];
        const focusSeat = valid.slice().sort((left, right) => Math.abs(right.anlage - reference.anlage) - Math.abs(left.anlage - reference.anlage) || left.position - right.position || left.seatId.localeCompare(right.seatId))[0];
        candidates.push({
          id: "crew:anlage",
          priority: 1,
          score: max - min,
          severity: "change",
          scope: Object.freeze({ type: "crew", seatId: null, position: null, label: "Ganzes Boot", rowerName: null }),
          focusSeatId: focusSeat.seatId,
          field: "anlage",
          parameter: "Dollen-Neigung (Anlage)",
          current: { min, max, unit: "°" },
          target: { value: reference.anlage, unit: "°", text: `gemeinsamer Prüfwert der Referenz ${reference.label || `Platz ${reference.position}`}` },
          direction: "abweichende Plätze auf einen gemeinsam bestätigten Wert abstimmen",
          reason: "Die Anlage ist platzbezogen gespeichert, soll innerhalb der Mannschaft aber gleich eingestellt sein.",
          effect: "Reduziert einen vermeidbaren Rigg-Unterschied zwischen den belegten Plätzen.",
          uncertainty: "Der gemeinsame Wert bleibt bis Trainer-/Messfreigabe ein Prüfwert; die App gleicht nichts automatisch an."
        });
      }
    }
    for (const seat of valid) {
      if (safetySeats.has(seat.seatId)) continue;
      const delta = seat.IHsoll - seat.IH;
      if (Math.abs(delta) > 1) {
        candidates.push({
          id: `inboard:${seat.seatId}`,
          priority: 2,
          score: Math.abs(delta),
          severity: Math.abs(delta) > 2 ? "change" : "check",
          scope: seatScope(seat),
          field: "IH",
          parameter: "Innenhebel IH",
          current: { value: seat.IH, unit: "cm" },
          target: { min: seat.IHsoll - 1, max: seat.IHsoll + 1, unit: "cm" },
          direction: delta > 0 ? "Innenhebel erhöhen" : "Innenhebel verringern",
          reason: `Die vorhandene ${seat.rig === "skull" ? "Skull-Formel DA/2 + 8" : "Riemen-Formel DA + 30"} ergibt ${seat.IHsoll} cm.`,
          effect: "Nähert das Hebelverhältnis dem ausgewiesenen Rigg-Zielkorridor.",
          uncertainty: "Modellvorschlag; Materialgrenzen prüfen und anschließend auf dem Wasser sowie mit Trainer gegenprüfen."
        });
      }
      const kneeDistance = outside(seat.knee90, 160, 170);
      const rollDistance = outside(seat.roll90, 70, 80);
      if (kneeDistance > 0 || rollDistance > 0) {
        candidates.push({
          id: `footstretcher:${seat.seatId}`,
          priority: 3,
          score: kneeDistance / 6 + rollDistance / 5,
          severity: kneeDistance > 6 || rollDistance > 5 ? "change" : "check",
          scope: seatScope(seat),
          field: "stemmX",
          parameter: "Stemmbrettposition (Fa)",
          current: { value: seat.stemmX, unit: "cm" },
          target: { text: "bei 90° gleichzeitig Knie 160–170° und genutzter Rollweg 70–80 %" },
          direction: "Fa in kleinen Schritten verändern und beide Prüfwerte erneut kontrollieren",
          reason: `Ist bei 90°: Knie ${seat.knee90}° · Rollweg ${seat.roll90} %.`,
          effect: "Nähert Knie- und Rollweg-Prüfwert gemeinsam dem ausgewiesenen Korridor.",
          uncertainty: "Unkalibriertes 90°-Körpermodell; keine automatische Korrektur und keine Trainerfreigabe."
        });
      }
    }
    candidates.sort((left, right) => left.priority - right.priority || right.score - left.score || (left.scope.position ?? 0) - (right.scope.position ?? 0) || left.id.localeCompare(right.id));
    const actions = candidates.slice(0, maxActions).map(({ score, ...action }) => action);
    const status = actions.some((action) => action.severity === "change") ? "change" : actions.length ? "check" : "ok";
    const free = Math.max(0, totalSeatCount - valid.length - unavailable);
    const summary = status === "ok" ? `Kein akuter Handlungsbedarf aus den geprüften Regeln für ${valid.length} ${valid.length === 1 ? "belegten Platz" : "belegte Plätze"}.` : status === "change" ? `${actions.length} priorisierte${actions.length === 1 ? " Maßnahme" : " Maßnahmen"} – zuerst bewusst prüfen, dann manuell ändern.` : `${actions.length} Prüfpunkt${actions.length === 1 ? "" : "e"} vor einer Änderung.`;
    return freezePlan({ status, summary, actions, evaluatedSeats: valid.length, unavailableSeats: unavailable, freeSeats: free });
  }

  // js/legacy-v1.mjs
  var V1_PRESETS = Object.freeze([
    "1x",
    "2x",
    "4x",
    "2-",
    "4-",
    "8+",
    "gigS",
    "gigR",
    "wmM1x",
    "wmW1x",
    "wmM2x",
    "wmW2x",
    "wmM4x",
    "wmW4x",
    "wmM2-",
    "wmW2-",
    "wmM4-",
    "wmW4-",
    "wmM8+",
    "wmW8+"
  ]);
  var BLADES2 = Object.freeze(["big", "mac"]);
  var RIGS2 = Object.freeze(["skull", "riemen"]);
  var MODES = Object.freeze(["werkstatt", "wasser"]);
  var HEIGHT_REFS = Object.freeze(["sitz", "schiene"]);
  var EDIT_SEATS = Object.freeze(["s1", "s2"]);
  var SEED_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,95}$/u;
  var V1_PROFILE_FIELDS = Object.freeze([
    "name",
    "legLen",
    "torsoLen",
    "wingspan",
    "SB",
    "weight",
    "stemmX"
  ]);
  var V1_SEAT_FIELDS = Object.freeze([
    "DA",
    "IH",
    "L",
    "d",
    "handGap",
    "a",
    "anlage",
    "aussen",
    "dBB",
    "stemmW",
    "rollL",
    "rueh"
  ]);
  var V1_CREW_FIELDS = Object.freeze([...V1_PROFILE_FIELDS, ...V1_SEAT_FIELDS]);
  var V1_BOAT_FIELDS = Object.freeze([
    "name",
    "preset",
    "blade",
    "rig",
    "strokeSide",
    "phiA",
    "phiR",
    "c",
    "seatOffset",
    "s1",
    "s2"
  ]);
  var V1_STATE_FIELDS = Object.freeze([
    "rig",
    "strokeSide",
    "phiA",
    "phiR",
    "t",
    "recovery",
    "c",
    "kg",
    "mode",
    "heightRef",
    "seatOffset",
    "crew",
    "editSeat",
    "db",
    "dbIdx",
    "boats"
  ]);
  var V1_WORKSPACE_FIELDS = Object.freeze(["state", "preset", "blade"]);
  var LegacyV1ValidationError = class extends TypeError {
    constructor(path, message) {
      super(`${path}: ${message}`);
      this.name = "LegacyV1ValidationError";
      this.path = path;
    }
  };
  function fail(path, message) {
    throw new LegacyV1ValidationError(path, message);
  }
  function isPlainRecord2(value) {
    if (value === null || typeof value !== "object" || Array.isArray(value)) return false;
    const proto = Object.getPrototypeOf(value);
    return proto === Object.prototype || proto === null;
  }
  function exactRecord2(value, fields, path) {
    if (!isPlainRecord2(value)) fail(path, "erwartet wurde ein einfaches Objekt");
    const allowed = new Set(fields);
    for (const key of Object.keys(value)) if (!allowed.has(key)) fail(`${path}.${key}`, "unbekanntes Feld");
    for (const key of fields) if (!Object.hasOwn(value, key)) fail(`${path}.${key}`, "Pflichtfeld fehlt");
    return value;
  }
  function finiteRange(value, range, path) {
    if (typeof value !== "number" || !Number.isFinite(value)) fail(path, "erwartet wurde eine endliche Zahl");
    if (value < range[0] || value > range[1]) fail(path, `Wert muss in [${range[0]}, ${range[1]}] liegen`);
    return value;
  }
  function oneOf(value, allowed, path) {
    if (!allowed.includes(value)) fail(path, `erwartet wurde einer von: ${allowed.join(", ")}`);
    return value;
  }
  function bool(value, path) {
    if (typeof value !== "boolean") fail(path, "erwartet wurde ein Boolean");
    return value;
  }
  function safeInteger(value, min, max, path) {
    if (!Number.isSafeInteger(value) || value < min || value > max) fail(path, `erwartet wurde eine Ganzzahl in [${min}, ${max}]`);
    return value;
  }
  function array(value, path) {
    if (!Array.isArray(value)) fail(path, "erwartet wurde ein Array");
    if (value.length > MAX_IMPORT_ITEMS) fail(path, `höchstens ${MAX_IMPORT_ITEMS} Einträge sind erlaubt`);
    for (let index = 0; index < value.length; index += 1) {
      if (!Object.hasOwn(value, index)) fail(`${path}[${index}]`, "Array-Lücken sind nicht erlaubt");
    }
    return value;
  }
  function parseJsonValue(raw, path) {
    if (typeof raw !== "string") return raw;
    try {
      return JSON.parse(raw);
    } catch (error) {
      fail(path, `ungültiges JSON (${error instanceof Error ? error.message : "Parsefehler"})`);
    }
  }
  function requireSeed(seed) {
    if (typeof seed !== "string" || !SEED_PATTERN.test(seed)) {
      fail("options.seed", "erwartet wurde eine stabile Kennung aus 1–96 sicheren Zeichen");
    }
    return seed;
  }
  function v2Rower(profile, path) {
    exactRecord2(profile, V1_PROFILE_FIELDS, path);
    const source = {
      schemaVersion: 2,
      kind: "rower",
      name: profile.name,
      legLen: finiteRange(profile.legLen, RANGES.rower.legLen, `${path}.legLen`),
      torsoLen: finiteRange(profile.torsoLen, RANGES.rower.torsoLen, `${path}.torsoLen`),
      wingspan: finiteRange(profile.wingspan, RANGES.rower.wingspan, `${path}.wingspan`),
      SB: finiteRange(profile.SB, RANGES.rower.SB, `${path}.SB`),
      weight: finiteRange(profile.weight, RANGES.rower.weight, `${path}.weight`),
      stemmX: finiteRange(profile.stemmX, RANGES.rower.stemmX, `${path}.stemmX`)
    };
    return source;
  }
  function currentRower(profile, path) {
    return migrateRowerToCurrent(v2Rower(profile, path), { storage: false, path }).value;
  }
  function v2Seat(seat, rig, path) {
    exactRecord2(seat, V1_SEAT_FIELDS, path);
    const rigRanges = RANGES[rig];
    const source = { schemaVersion: 2, kind: "seat", rig };
    for (const key of V1_SEAT_FIELDS) {
      const range = rigRanges[key] ?? RANGES.seat[key];
      source[key] = finiteRange(seat[key], range, `${path}.${key}`);
    }
    return source;
  }
  function v2Boat(boat, path) {
    exactRecord2(boat, V1_BOAT_FIELDS, path);
    const preset = oneOf(boat.preset, V1_PRESETS, `${path}.preset`);
    const rig = oneOf(boat.rig, RIGS2, `${path}.rig`);
    if (PRESETS[preset]?.rig !== rig) fail(`${path}.rig`, "Rigg passt nicht zur Bootsklasse");
    if (boat.strokeSide !== -1 && boat.strokeSide !== 1) fail(`${path}.strokeSide`, "erwartet wurde -1 oder 1");
    const firstSeat = v2Seat(boat.s1, rig, `${path}.s1`);
    const secondSeat = v2Seat(boat.s2, rig, `${path}.s2`);
    return {
      schemaVersion: 2,
      kind: "boat",
      name: boat.name,
      preset,
      blade: oneOf(boat.blade, BLADES2, `${path}.blade`),
      rig,
      strokeSide: boat.strokeSide,
      phiA: finiteRange(boat.phiA, RANGES.boat.phiA, `${path}.phiA`),
      phiR: finiteRange(boat.phiR, RANGES.boat.phiR, `${path}.phiR`),
      c: finiteRange(boat.c, RANGES.boat.c, `${path}.c`),
      seatOffset: finiteRange(boat.seatOffset, RANGES.boat.seatOffset, `${path}.seatOffset`),
      s1: firstSeat,
      // V1 kept s2 in memory even for a single. Validate the input, but never
      // preserve that non-existent person's hidden rig as a 1x domain value.
      s2: PRESETS[preset]?.single === true ? firstSeat : secondSeat
    };
  }
  function v1CrewMember(member, rig, path) {
    exactRecord2(member, V1_CREW_FIELDS, path);
    const rower = v2Rower(Object.fromEntries(V1_PROFILE_FIELDS.map((key) => [key, member[key]])), path);
    const seat = v2Seat(Object.fromEntries(V1_SEAT_FIELDS.map((key) => [key, member[key]])), rig, path);
    return { rower, seat };
  }
  function workspaceBoat(state, preset, blade, s1, s2) {
    const single = PRESETS[preset]?.single === true;
    const secondSeat = single ? s1.seat : s2.seat;
    return {
      schemaVersion: 2,
      kind: "boat",
      name: `V1-Arbeitsstand ${preset}`,
      preset,
      blade,
      rig: state.rig,
      strokeSide: state.strokeSide,
      phiA: state.phiA,
      phiR: state.phiR,
      c: state.c,
      seatOffset: state.seatOffset,
      s1: s1.seat,
      s2: secondSeat
    };
  }
  function validateWorkspaceSource(value, path) {
    exactRecord2(value, V1_WORKSPACE_FIELDS, path);
    const state = exactRecord2(value.state, V1_STATE_FIELDS, `${path}.state`);
    const preset = oneOf(value.preset, V1_PRESETS, `${path}.preset`);
    const blade = oneOf(value.blade, BLADES2, `${path}.blade`);
    const rig = oneOf(state.rig, RIGS2, `${path}.state.rig`);
    if (PRESETS[preset]?.rig !== rig) fail(`${path}.state.rig`, "Rigg passt nicht zur Bootsklasse");
    if (state.strokeSide !== -1 && state.strokeSide !== 1) fail(`${path}.state.strokeSide`, "erwartet wurde -1 oder 1");
    finiteRange(state.phiA, RANGES.boat.phiA, `${path}.state.phiA`);
    finiteRange(state.phiR, RANGES.boat.phiR, `${path}.state.phiR`);
    finiteRange(state.t, RANGES.current.t, `${path}.state.t`);
    bool(state.recovery, `${path}.state.recovery`);
    finiteRange(state.c, RANGES.boat.c, `${path}.state.c`);
    finiteRange(state.kg, RANGES.current.kg, `${path}.state.kg`);
    oneOf(state.mode, MODES, `${path}.state.mode`);
    oneOf(state.heightRef, HEIGHT_REFS, `${path}.state.heightRef`);
    finiteRange(state.seatOffset, RANGES.boat.seatOffset, `${path}.state.seatOffset`);
    oneOf(state.editSeat, EDIT_SEATS, `${path}.state.editSeat`);
    exactRecord2(state.crew, ["s1", "s2"], `${path}.state.crew`);
    const db = array(state.db, `${path}.state.db`);
    const boats = array(state.boats, `${path}.state.boats`);
    safeInteger(state.dbIdx, -1, Math.max(-1, db.length - 1), `${path}.state.dbIdx`);
    return { state, preset, blade, rig, db, boats };
  }
  function adaptLegacyV1Boats(raw, { seed = "v1-boats" } = {}) {
    const stableSeed = requireSeed(seed);
    const value = array(parseJsonValue(raw, "rudertrimm_boats"), "rudertrimm_boats");
    const legacy = value.map((boat, index) => v2Boat(boat, `rudertrimm_boats[${index}]`));
    const migrated = legacy.map((boat, index) => migrateBoatToCurrent(boat, {
      storage: false,
      path: `rudertrimm_boats[${index}]`,
      record: { id: `${stableSeed}-${index + 1}` }
    }).value);
    return Object.freeze(migrated);
  }
  function adaptLegacyV1Workspace(raw, { seed = "v1-workspace" } = {}) {
    const stableSeed = requireSeed(seed);
    const source = parseJsonValue(raw, "rudertrimm");
    const { state, preset, blade, rig, db, boats } = validateWorkspaceSource(source, "rudertrimm");
    const first = v1CrewMember(state.crew.s1, rig, "rudertrimm.state.crew.s1");
    const second = v1CrewMember(state.crew.s2, rig, "rudertrimm.state.crew.s2");
    const single = PRESETS[preset]?.single === true;
    const legacyConfig = {
      schemaVersion: 2,
      kind: "currentConfig",
      boat: workspaceBoat(state, preset, blade, first, second),
      crew: { s1: first.rower, s2: single ? null : second.rower },
      editSeat: single ? "s1" : state.editSeat,
      mode: state.mode,
      heightRef: state.heightRef,
      kg: state.kg,
      t: state.t,
      recovery: state.recovery
    };
    const rowers = db.map((profile, index) => currentRower(profile, `rudertrimm.state.db[${index}]`));
    const legacyBoats = boats.map((boat, index) => v2Boat(boat, `rudertrimm.state.boats[${index}]`));
    const migratedBoats = legacyBoats.map((boat, index) => migrateBoatToCurrent(boat, {
      storage: false,
      path: `rudertrimm.state.boats[${index}]`,
      record: { id: `${stableSeed}-boat-${index + 1}` }
    }).value);
    const workspace = migrateCurrentConfigToCurrent(legacyConfig, {
      storage: false,
      path: "rudertrimm",
      record: { id: stableSeed }
    }).value;
    if (workspace.schemaVersion !== SCHEMA_VERSION) fail("rudertrimm", "Core-Migration lieferte nicht das aktuelle Schema");
    return Object.freeze({
      workspace,
      rowers: Object.freeze(rowers),
      boats: Object.freeze(migratedBoats)
    });
  }

  // js/history.mjs
  var HISTORY_OPERATIONS2 = Object.freeze([
    "baseline",
    "create",
    "update",
    "import",
    "migration",
    "delete"
  ]);
  var HISTORY_LIMITS = Object.freeze({
    maxDepth: 32,
    maxNodes: 2e4,
    maxSourceLength: 80,
    maxReasonLength: 500
  });
  var OPERATION_SET = new Set(HISTORY_OPERATIONS2);
  var ENTRY_FIELDS = Object.freeze([
    "entityId",
    "revision",
    "changedAt",
    "operation",
    "source",
    "reason",
    "snapshot"
  ]);
  var DANGEROUS_KEYS2 = /* @__PURE__ */ new Set(["__proto__", "prototype", "constructor"]);
  var UNSAFE_TEXT = /[<>\u0000-\u001f\u007f-\u009f\u202a-\u202e\u2066-\u2069]/u;
  var SAFE_ID = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;
  var HistoryValidationError = class extends TypeError {
    constructor(message, details = []) {
      super(message);
      this.name = "HistoryValidationError";
      this.code = "history-validation";
      this.details = Object.freeze(details.map((detail) => Object.freeze({ ...detail })));
    }
  };
  function fail2(path, code, message) {
    throw new HistoryValidationError(message, [{ path, code, message }]);
  }
  function isPlainObject2(value) {
    if (value === null || typeof value !== "object" || Array.isArray(value)) return false;
    const prototype = Object.getPrototypeOf(value);
    return prototype === Object.prototype || prototype === null;
  }
  function ownDataKeys(value, path) {
    const keys = Reflect.ownKeys(value);
    if (keys.some((key) => typeof key !== "string")) fail2(path, "symbol-key", `${path} must not contain symbol keys`);
    for (const key of keys) {
      const descriptor = Object.getOwnPropertyDescriptor(value, key);
      if (!descriptor || !Object.hasOwn(descriptor, "value") || !descriptor.enumerable) {
        fail2(path, "non-json-property", `${path} must contain enumerable data properties only`);
      }
    }
    return keys;
  }
  function exactEntryKeys(value) {
    if (!isPlainObject2(value)) fail2("entry", "plain-object", "History entry must be a plain object");
    const actual = ownDataKeys(value, "entry").sort();
    const expected = [...ENTRY_FIELDS].sort();
    if (actual.length !== expected.length || actual.some((key, index) => key !== expected[index])) {
      fail2("entry", "exact-fields", "History entry has unexpected or missing fields");
    }
  }
  function normalizeText(value, { path, maxLength }) {
    if (typeof value !== "string") fail2(path, "type", `${path} must be a string`);
    const normalized = value.trim();
    const length = [...normalized].length;
    if (length < 1 || length > maxLength) fail2(path, "length", `${path} must contain 1-${maxLength} characters`);
    if (UNSAFE_TEXT.test(normalized)) fail2(path, "unsafe-text", `${path} contains unsafe text characters`);
    return normalized;
  }
  function normalizeTimestamp(value, path) {
    const date = typeof value === "string" ? new Date(value) : null;
    if (!date || !Number.isFinite(date.getTime()) || date.toISOString() !== value) {
      fail2(path, "timestamp", `${path} must be a canonical ISO timestamp`);
    }
    return value;
  }
  function normalizeJson(value, { path = "snapshot", limits = HISTORY_LIMITS } = {}) {
    const ancestors = /* @__PURE__ */ new WeakSet();
    let nodes = 0;
    function visit(current, currentPath, depth) {
      nodes += 1;
      if (nodes > limits.maxNodes) fail2(path, "node-limit", `${path} contains too many values`);
      if (depth > limits.maxDepth) fail2(path, "depth-limit", `${path} exceeds the nesting limit`);
      if (current === null || typeof current === "string" || typeof current === "boolean") return current;
      if (typeof current === "number") {
        if (!Number.isFinite(current)) fail2(currentPath, "non-finite", `${currentPath} must be finite`);
        return Object.is(current, -0) ? 0 : current;
      }
      if (typeof current !== "object") fail2(currentPath, "json-safe", `${currentPath} is not JSON-safe`);
      if (ancestors.has(current)) fail2(currentPath, "cycle", `${currentPath} contains a cycle`);
      ancestors.add(current);
      let normalized;
      if (Array.isArray(current)) {
        const keys = Reflect.ownKeys(current);
        for (const key of keys) {
          if (typeof key !== "string") fail2(currentPath, "symbol-key", `${currentPath} must not contain symbol keys`);
          if (key === "length") continue;
          const descriptor = Object.getOwnPropertyDescriptor(current, key);
          if (!/^(0|[1-9][0-9]*)$/u.test(key) || Number(key) >= current.length || !descriptor || !Object.hasOwn(descriptor, "value") || !descriptor.enumerable) {
            fail2(currentPath, "array-shape", `${currentPath} must be a dense JSON array`);
          }
        }
        normalized = [];
        for (let index = 0; index < current.length; index += 1) {
          if (!Object.hasOwn(current, index)) fail2(`${currentPath}[${index}]`, "array-hole", `${currentPath} must be a dense JSON array`);
          normalized.push(visit(current[index], `${currentPath}[${index}]`, depth + 1));
        }
      } else {
        if (!isPlainObject2(current)) fail2(currentPath, "plain-object", `${currentPath} must be a plain object`);
        const keys = ownDataKeys(current, currentPath).sort();
        normalized = {};
        for (const key of keys) {
          if (DANGEROUS_KEYS2.has(key)) fail2(`${currentPath}.${key}`, "dangerous-key", `${currentPath} contains a forbidden key`);
          normalized[key] = visit(current[key], `${currentPath}.${key}`, depth + 1);
        }
      }
      ancestors.delete(current);
      return normalized;
    }
    return visit(value, path, 0);
  }
  function deepFreeze3(value) {
    if (value && typeof value === "object" && !Object.isFrozen(value)) {
      for (const child of Object.values(value)) deepFreeze3(child);
      Object.freeze(value);
    }
    return value;
  }
  function normalizeHistoryEntry(entry) {
    exactEntryKeys(entry);
    if (typeof entry.entityId !== "string" || !SAFE_ID.test(entry.entityId)) {
      fail2("entityId", "safe-id", "entityId must be a safe stable id");
    }
    if (!Number.isSafeInteger(entry.revision) || entry.revision < 1) {
      fail2("revision", "positive-integer", "revision must be a positive safe integer");
    }
    const changedAt = normalizeTimestamp(entry.changedAt, "changedAt");
    if (!OPERATION_SET.has(entry.operation)) fail2("operation", "enum", "operation is not supported");
    const source = normalizeText(entry.source, { path: "source", maxLength: HISTORY_LIMITS.maxSourceLength });
    const reason = normalizeText(entry.reason, { path: "reason", maxLength: HISTORY_LIMITS.maxReasonLength });
    if (entry.operation === "delete") {
      if (entry.snapshot !== null) fail2("snapshot", "delete-snapshot", "delete entries must have a null snapshot");
    } else if (!isPlainObject2(entry.snapshot)) {
      fail2("snapshot", "dto-snapshot", "non-delete entries require a complete DTO object snapshot");
    }
    const snapshot = entry.snapshot === null ? null : normalizeJson(entry.snapshot);
    return deepFreeze3({
      entityId: entry.entityId,
      revision: entry.revision,
      changedAt,
      operation: entry.operation,
      source,
      reason,
      snapshot
    });
  }
  function normalizeHistoryEntries(entries) {
    if (!Array.isArray(entries)) fail2("entries", "array", "entries must be an array");
    const normalized = [];
    for (let index = 0; index < entries.length; index += 1) {
      if (!Object.hasOwn(entries, index)) fail2(`entries[${index}]`, "array-hole", "entries must be a dense array");
      normalized.push(normalizeHistoryEntry(entries[index]));
    }
    return deepFreeze3(normalized);
  }
  function assertEntitySequence(entries, entityId) {
    for (let index = 1; index < entries.length; index += 1) {
      const previous = entries[index - 1];
      const current = entries[index];
      if (current.revision <= previous.revision) {
        fail2("entries", "revision-order", `History for ${entityId} must use strictly increasing revisions`);
      }
      if (current.changedAt < previous.changedAt) {
        fail2("entries", "timestamp-order", `History for ${entityId} must not move backwards in time`);
      }
      if (previous.operation === "delete") {
        fail2("entries", "after-delete", `History for ${entityId} must not continue after deletion`);
      }
    }
  }
  function historyForEntity(entries, entityId) {
    if (typeof entityId !== "string" || !SAFE_ID.test(entityId)) fail2("entityId", "safe-id", "entityId must be a safe stable id");
    const selected = normalizeHistoryEntries(entries).filter((entry) => entry.entityId === entityId).sort((left, right) => left.revision - right.revision || left.changedAt.localeCompare(right.changedAt));
    assertEntitySequence(selected, entityId);
    return deepFreeze3(selected);
  }
  function pointerSegment(value) {
    return String(value).replace(/~/gu, "~0").replace(/\//gu, "~1");
  }
  function childPath(path, key) {
    return `${path}/${pointerSegment(key)}`;
  }
  function sameContainer(left, right) {
    return Array.isArray(left) && Array.isArray(right) || isPlainObject2(left) && isPlainObject2(right);
  }
  function addChange(changes, path, type, beforePresent, afterPresent, before, after) {
    changes.push({
      path,
      type,
      beforePresent,
      afterPresent,
      before: beforePresent ? before : null,
      after: afterPresent ? after : null
    });
  }
  function stableSeatMap(value) {
    if (!Array.isArray(value)) return null;
    const seats = /* @__PURE__ */ new Map();
    for (const seat of value) {
      if (!isPlainObject2(seat) || typeof seat.id !== "string" || seat.id.length === 0 || seats.has(seat.id)) return null;
      seats.set(seat.id, seat);
    }
    return seats;
  }
  function diffSeatValues(before, after, path, changes) {
    const beforeSeats = stableSeatMap(before);
    const afterSeats = stableSeatMap(after);
    if (!beforeSeats || !afterSeats) return false;
    const ids = [.../* @__PURE__ */ new Set([...beforeSeats.keys(), ...afterSeats.keys()])].sort();
    for (const id of ids) {
      const beforePresent = beforeSeats.has(id);
      const afterPresent = afterSeats.has(id);
      const nextPath = childPath(path, `@${id}`);
      if (!beforePresent) addChange(changes, nextPath, "added", false, true, null, afterSeats.get(id));
      else if (!afterPresent) addChange(changes, nextPath, "removed", true, false, beforeSeats.get(id), null);
      else diffValues(beforeSeats.get(id), afterSeats.get(id), nextPath, changes);
    }
    return true;
  }
  function diffValues(before, after, path, changes) {
    if (Object.is(before, after)) return;
    if (sameContainer(before, after)) {
      if (Array.isArray(before)) {
        if (path === "/seats" && diffSeatValues(before, after, path, changes)) return;
        const length = Math.max(before.length, after.length);
        for (let index = 0; index < length; index += 1) {
          const beforePresent = index < before.length;
          const afterPresent = index < after.length;
          const nextPath = childPath(path, index);
          if (!beforePresent) addChange(changes, nextPath, "added", false, true, null, after[index]);
          else if (!afterPresent) addChange(changes, nextPath, "removed", true, false, before[index], null);
          else diffValues(before[index], after[index], nextPath, changes);
        }
        return;
      }
      const keys = [.../* @__PURE__ */ new Set([...Object.keys(before), ...Object.keys(after)])].sort();
      for (const key of keys) {
        const beforePresent = Object.hasOwn(before, key);
        const afterPresent = Object.hasOwn(after, key);
        const nextPath = childPath(path, key);
        if (!beforePresent) addChange(changes, nextPath, "added", false, true, null, after[key]);
        else if (!afterPresent) addChange(changes, nextPath, "removed", true, false, before[key], null);
        else diffValues(before[key], after[key], nextPath, changes);
      }
      return;
    }
    addChange(changes, path, "changed", true, true, before, after);
  }
  function diffHistoryEntries(previous, current) {
    const before = previous === null ? null : normalizeHistoryEntry(previous);
    const after = normalizeHistoryEntry(current);
    if (before) {
      if (before.entityId !== after.entityId) fail2("entityId", "entity-mismatch", "History entries must describe the same entity");
      if (after.revision <= before.revision) fail2("revision", "revision-order", "Current revision must be newer than previous revision");
      if (after.changedAt < before.changedAt) fail2("changedAt", "timestamp-order", "Current revision must not predate previous revision");
      if (before.operation === "delete") fail2("previous.operation", "after-delete", "A deleted entity cannot receive another revision");
    }
    const changes = [];
    let changeType = "update";
    if (!before) {
      if (after.snapshot === null) fail2("snapshot", "delete-without-before", "A delete comparison requires a previous snapshot");
      changeType = "create";
      addChange(changes, "", "added", false, true, null, after.snapshot);
    } else if (after.operation === "delete") {
      changeType = "delete";
      addChange(changes, "", "removed", true, false, before.snapshot, null);
    } else {
      diffValues(before.snapshot, after.snapshot, "", changes);
      if (changes.length === 0) changeType = "unchanged";
    }
    return deepFreeze3({
      entityId: after.entityId,
      fromRevision: before?.revision ?? null,
      toRevision: after.revision,
      changeType,
      changes
    });
  }

  // js/efa-csv.mjs
  var EFA_CSV_LIMITS = Object.freeze({
    maxBytes: 1048576,
    maxRows: 250,
    maxColumns: 64,
    maxCellCodePoints: 1e3
  });
  var EFA_CANDIDATE_SCHEMA_VERSION = 1;
  var EFA_CANDIDATE_KIND = "efaCandidate";
  var DELIMITERS = /* @__PURE__ */ new Set([";", ",", "	"]);
  var ENTITY_TYPES = /* @__PURE__ */ new Set(["person", "boat"]);
  var HASH_PATTERN = /^sha256-[0-9a-f]{64}$/u;
  var FORBIDDEN_TEXT = /[<>\u0000-\u001f\u007f\u202a-\u202e\u2066-\u2069]/u;
  var PROVENANCE_FIELDS = Object.freeze([
    "source",
    "importedAt",
    "fileSha256",
    "encoding",
    "delimiter",
    "mappingFingerprint",
    "efaVersion"
  ]);
  var CANDIDATE_FIELDS = Object.freeze([
    "schemaVersion",
    "kind",
    "entityType",
    "name",
    "externalRef",
    "provenance",
    "status"
  ]);
  var HEADER_ALIASES = Object.freeze({
    person: Object.freeze({
      displayName: Object.freeze(["anzeigename", "display name", "displayname", "fullname", "full name", "vollständiger name", "name", "person", "ruderer"]),
      firstName: Object.freeze(["vorname", "first name", "firstname", "given name", "givenname"]),
      lastName: Object.freeze(["nachname", "last name", "lastname", "surname", "family name", "familyname"]),
      affix: Object.freeze(["namenszusatz", "name affix", "affix", "name prefix", "nameprefix"]),
      id: Object.freeze(["efa id", "efaid", "person id", "personid", "mitgliedsnummer", "member id", "memberid", "id"])
    }),
    boat: Object.freeze({
      name: Object.freeze(["bootsname", "boat name", "boatname", "boot", "name", "bezeichnung"]),
      id: Object.freeze(["efa id", "efaid", "boat id", "boatid", "boots id", "bootsid", "bootsnummer", "id"])
    })
  });
  var EfaCsvValidationError = class extends TypeError {
    constructor(message, { code = "validation", details = [] } = {}) {
      super(message);
      this.name = "EfaCsvValidationError";
      this.code = code;
      this.details = Object.freeze([...details]);
    }
  };
  function isPlainRecord3(value) {
    if (value === null || typeof value !== "object" || Array.isArray(value)) return false;
    const prototype = Object.getPrototypeOf(value);
    return prototype === Object.prototype || prototype === null;
  }
  function exactFields(value, fields, label, errors) {
    if (!isPlainRecord3(value)) {
      errors.push(`${label} muss ein einfaches Objekt sein`);
      return false;
    }
    const actual = Object.keys(value).sort();
    const expected = [...fields].sort();
    if (actual.length !== expected.length || actual.some((field, index) => field !== expected[index])) {
      errors.push(`${label} enthält unerwartete oder fehlende Felder`);
      return false;
    }
    return true;
  }
  function hasInvalidUnicode(value) {
    for (let index = 0; index < value.length; index += 1) {
      const first = value.charCodeAt(index);
      let codePoint = first;
      if (first >= 55296 && first <= 56319) {
        const second = value.charCodeAt(index + 1);
        if (!(second >= 56320 && second <= 57343)) return true;
        codePoint = 65536 + (first - 55296) * 1024 + (second - 56320);
        index += 1;
      } else if (first >= 56320 && first <= 57343) {
        return true;
      }
      if (codePoint >= 64976 && codePoint <= 65007 || (codePoint & 65535) >= 65534) return true;
    }
    return false;
  }
  function validText(value, { max = 80 } = {}) {
    return typeof value === "string" && value === value.trim() && [...value].length >= 1 && [...value].length <= max && !FORBIDDEN_TEXT.test(value) && !hasInvalidUnicode(value);
  }
  function deepFreeze4(value) {
    if (value && typeof value === "object" && !Object.isFrozen(value)) {
      Object.freeze(value);
      for (const child of Object.values(value)) deepFreeze4(child);
    }
    return value;
  }
  function canonicalJson3(value) {
    if (value === null || typeof value !== "object") return JSON.stringify(value);
    if (Array.isArray(value)) return `[${value.map(canonicalJson3).join(",")}]`;
    return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonicalJson3(value[key])}`).join(",")}}`;
  }
  function utf8Bytes(value) {
    if (typeof TextEncoder !== "function") throw new EfaCsvValidationError("UTF-8-Codierung ist in dieser Laufzeit nicht verfügbar", { code: "encoding-unavailable" });
    return new TextEncoder().encode(value);
  }
  function normalizeDigest(result) {
    if (typeof result === "string") {
      const normalized = result.toLowerCase().replace(/^sha256[:-]/u, "");
      if (!/^[0-9a-f]{64}$/u.test(normalized)) throw new EfaCsvValidationError("Die Digest-Funktion lieferte keinen SHA-256-Wert", { code: "digest" });
      return `sha256-${normalized}`;
    }
    let bytes;
    if (result instanceof ArrayBuffer) bytes = new Uint8Array(result);
    else if (ArrayBuffer.isView(result)) bytes = new Uint8Array(result.buffer, result.byteOffset, result.byteLength);
    else throw new EfaCsvValidationError("Die Digest-Funktion lieferte keinen unterstützten Wert", { code: "digest" });
    if (bytes.byteLength !== 32) throw new EfaCsvValidationError("Die Digest-Funktion lieferte keinen SHA-256-Wert", { code: "digest" });
    return `sha256-${[...bytes].map((value) => value.toString(16).padStart(2, "0")).join("")}`;
  }
  async function sha256(value, digest) {
    const bytes = utf8Bytes(value);
    if (digest !== void 0) {
      if (typeof digest !== "function") throw new TypeError("digest muss eine Funktion sein");
      return normalizeDigest(await digest(bytes));
    }
    const subtle = globalThis.crypto?.subtle;
    if (!subtle || typeof subtle.digest !== "function") {
      throw new EfaCsvValidationError("SHA-256 ist nicht verfügbar; eine Digest-Funktion muss injiziert werden", { code: "digest-unavailable" });
    }
    return normalizeDigest(await subtle.digest("SHA-256", bytes));
  }
  function timestamp(clock) {
    if (typeof clock !== "function") throw new TypeError("clock muss eine Funktion sein");
    const raw = clock();
    const date = raw instanceof Date ? raw : new Date(raw);
    if (!Number.isFinite(date.getTime())) throw new RangeError("clock muss ein gültiges Datum liefern");
    return date.toISOString();
  }
  function normalizedHeaderKey(value) {
    return value.normalize("NFKC").trim().toLocaleLowerCase("de-DE").replace(/[_.-]+/gu, " ").replace(/\s+/gu, " ");
  }
  function assertDelimiter(delimiter) {
    if (!DELIMITERS.has(delimiter)) {
      throw new EfaCsvValidationError("Trennzeichen muss explizit Semikolon, Komma oder Tab sein", { code: "delimiter" });
    }
  }
  function rejectEncodingMarkers(text) {
    if (text.startsWith("￾") || text.startsWith("ÿþ") || text.startsWith("þÿ") || text.includes("\0") || text.slice(1).includes("\uFEFF")) {
      throw new EfaCsvValidationError("UTF-16-/NUL-Markierung erkannt; erwartet wird UTF-8", { code: "encoding" });
    }
    if (hasInvalidUnicode(text)) {
      throw new EfaCsvValidationError("CSV enthält ungültige Unicode-Codepunkte", { code: "encoding" });
    }
  }
  function parseEfaCsv(text, { delimiter, limits = {} } = {}) {
    if (typeof text !== "string") throw new TypeError("CSV muss Text sein");
    assertDelimiter(delimiter);
    const normalizedLimits = { ...EFA_CSV_LIMITS, ...limits };
    for (const field2 of ["maxBytes", "maxRows", "maxColumns", "maxCellCodePoints"]) {
      if (!Number.isSafeInteger(normalizedLimits[field2]) || normalizedLimits[field2] < 1) throw new RangeError(`${field2} muss eine positive Ganzzahl sein`);
      if (normalizedLimits[field2] > EFA_CSV_LIMITS[field2]) throw new RangeError(`${field2} darf die feste Importgrenze nicht erhöhen`);
    }
    rejectEncodingMarkers(text);
    const byteLength = utf8Bytes(text).byteLength;
    if (byteLength > normalizedLimits.maxBytes) {
      throw new EfaCsvValidationError(`CSV überschreitet ${normalizedLimits.maxBytes} UTF-8-Bytes`, { code: "limit", details: [{ byteLength, maxBytes: normalizedLimits.maxBytes }] });
    }
    const source = text.startsWith("\uFEFF") ? text.slice(1) : text;
    if (source.length === 0) throw new EfaCsvValidationError("CSV enthält keine Kopfzeile", { code: "empty" });
    const records = [];
    let record = [];
    let field = "";
    let quoted = false;
    let afterQuote = false;
    let endedWithRecordBreak = false;
    const pushField = () => {
      if (record.length >= normalizedLimits.maxColumns) {
        throw new EfaCsvValidationError(`CSV überschreitet ${normalizedLimits.maxColumns} Spalten`, { code: "limit" });
      }
      if ([...field].length > normalizedLimits.maxCellCodePoints) {
        throw new EfaCsvValidationError(`CSV-Zelle überschreitet ${normalizedLimits.maxCellCodePoints} Codepunkte`, { code: "limit" });
      }
      record.push(field);
      field = "";
      afterQuote = false;
    };
    const pushRecord = () => {
      pushField();
      records.push(record);
      record = [];
      if (records.length > normalizedLimits.maxRows + 1) {
        throw new EfaCsvValidationError(`CSV überschreitet ${normalizedLimits.maxRows} Datenzeilen`, { code: "limit" });
      }
    };
    for (let index = 0; index < source.length; index += 1) {
      const char = source[index];
      endedWithRecordBreak = false;
      if (quoted) {
        if (char === '"') {
          if (source[index + 1] === '"') {
            field += '"';
            index += 1;
          } else {
            quoted = false;
            afterQuote = true;
          }
        } else if (char === "\r") {
          if (source[index + 1] !== "\n") throw new EfaCsvValidationError("Alle CR-Zeilenumbrüche müssen als CRLF vorliegen", { code: "malformed", details: [{ offset: index }] });
          field += "\n";
          index += 1;
        } else {
          field += char;
        }
        continue;
      }
      if (afterQuote) {
        if (char === delimiter) {
          pushField();
          continue;
        }
        if (char === "\n") {
          pushRecord();
          endedWithRecordBreak = true;
          continue;
        }
        if (char === "\r") {
          if (source[index + 1] !== "\n") throw new EfaCsvValidationError("Alle CR-Zeilenumbrüche müssen als CRLF vorliegen", { code: "malformed", details: [{ offset: index }] });
          pushRecord();
          endedWithRecordBreak = true;
          index += 1;
          continue;
        }
        throw new EfaCsvValidationError("Nach einem schließenden Anführungszeichen ist nur Trennzeichen oder Zeilenende erlaubt", { code: "malformed", details: [{ offset: index }] });
      }
      if (char === '"') {
        if (field.length !== 0) throw new EfaCsvValidationError("Anführungszeichen darf nur am Feldanfang stehen", { code: "malformed", details: [{ offset: index }] });
        quoted = true;
        continue;
      }
      if (char === delimiter) {
        pushField();
        continue;
      }
      if (char === "\n") {
        pushRecord();
        endedWithRecordBreak = true;
        continue;
      }
      if (char === "\r") {
        if (source[index + 1] !== "\n") throw new EfaCsvValidationError("Alle CR-Zeilenumbrüche müssen als CRLF vorliegen", { code: "malformed", details: [{ offset: index }] });
        pushRecord();
        endedWithRecordBreak = true;
        index += 1;
        continue;
      }
      field += char;
    }
    if (quoted) throw new EfaCsvValidationError("Nicht geschlossenes Anführungszeichen in CSV", { code: "malformed" });
    if (!endedWithRecordBreak || record.length > 0 || field.length > 0 || afterQuote) pushRecord();
    const rawHeaders = records.shift();
    if (!rawHeaders || rawHeaders.length === 0) throw new EfaCsvValidationError("CSV enthält keine Kopfzeile", { code: "empty" });
    const headers = rawHeaders.map((header) => header.trim());
    const seenHeaders = /* @__PURE__ */ new Map();
    headers.forEach((header, index) => {
      if (!validText(header, { max: 1e3 })) throw new EfaCsvValidationError(`Kopfzeile ${index + 1} ist leer oder ungültig`, { code: "header" });
      const key = normalizedHeaderKey(header);
      if (seenHeaders.has(key)) {
        throw new EfaCsvValidationError(`Kopfzeile „${header}“ ist nicht eindeutig`, { code: "duplicate-header", details: [{ columns: [seenHeaders.get(key) + 1, index + 1] }] });
      }
      seenHeaders.set(key, index);
    });
    records.forEach((row, index) => {
      if (row.length !== headers.length) {
        throw new EfaCsvValidationError(`Datenzeile ${index + 2} hat ${row.length} statt ${headers.length} Spalten`, { code: "columns", details: [{ row: index + 2, actual: row.length, expected: headers.length }] });
      }
    });
    return deepFreeze4({ delimiter, headers, rows: records, byteLength });
  }
  function assertEntityType(entityType) {
    if (!ENTITY_TYPES.has(entityType)) throw new EfaCsvValidationError("entityType muss person oder boat sein", { code: "entity-type" });
  }
  function suggestEfaHeaderMapping(headers, { entityType } = {}) {
    assertEntityType(entityType);
    if (!Array.isArray(headers) || headers.some((header) => typeof header !== "string")) throw new TypeError("headers muss ein Text-Array sein");
    const normalized = headers.map((header) => normalizedHeaderKey(header));
    const suggestions = {};
    for (const [role, aliases] of Object.entries(HEADER_ALIASES[entityType])) {
      const matches = headers.filter((_header, index) => aliases.includes(normalized[index]));
      suggestions[role] = matches.length === 1 ? matches[0] : null;
    }
    return deepFreeze4(suggestions);
  }
  function normalizeMapping(mapping, headers, entityType) {
    if (!isPlainRecord3(mapping)) throw new EfaCsvValidationError("Mapping muss explizit angegeben werden", { code: "mapping" });
    const allowed = entityType === "person" ? /* @__PURE__ */ new Set(["displayName", "firstName", "lastName", "affix", "id"]) : /* @__PURE__ */ new Set(["name", "id"]);
    for (const key of Object.keys(mapping)) {
      if (!allowed.has(key)) throw new EfaCsvValidationError(`Unbekanntes Mapping-Feld: ${key}`, { code: "mapping" });
      if (typeof mapping[key] !== "string" || !headers.includes(mapping[key])) {
        throw new EfaCsvValidationError(`Mapping-Feld ${key} verweist nicht eindeutig auf eine Kopfzeile`, { code: "mapping" });
      }
    }
    if (entityType === "person") {
      const hasDisplay = Object.hasOwn(mapping, "displayName");
      const hasParts = Object.hasOwn(mapping, "firstName") || Object.hasOwn(mapping, "lastName");
      if (hasDisplay === hasParts) throw new EfaCsvValidationError("Personen benötigen entweder displayName oder firstName + lastName", { code: "mapping" });
      if (hasParts && (!Object.hasOwn(mapping, "firstName") || !Object.hasOwn(mapping, "lastName"))) {
        throw new EfaCsvValidationError("firstName und lastName müssen gemeinsam gemappt werden", { code: "mapping" });
      }
      if (hasDisplay && Object.hasOwn(mapping, "affix")) throw new EfaCsvValidationError("affix ist nur mit firstName + lastName zulässig", { code: "mapping" });
    } else if (!Object.hasOwn(mapping, "name")) {
      throw new EfaCsvValidationError("Boote benötigen ein name-Mapping", { code: "mapping" });
    }
    const columns = Object.values(mapping);
    if (new Set(columns).size !== columns.length) throw new EfaCsvValidationError("Eine CSV-Spalte darf nur einer Rolle zugeordnet werden", { code: "mapping" });
    return Object.freeze({ ...mapping });
  }
  function candidateErrors(value) {
    const errors = [];
    if (!exactFields(value, CANDIDATE_FIELDS, "candidate", errors)) return errors;
    if (value.schemaVersion !== EFA_CANDIDATE_SCHEMA_VERSION) errors.push("candidate.schemaVersion wird nicht unterstützt");
    if (value.kind !== EFA_CANDIDATE_KIND) errors.push("candidate.kind ist ungültig");
    if (!ENTITY_TYPES.has(value.entityType)) errors.push("candidate.entityType ist ungültig");
    if (!validText(value.name)) errors.push("candidate.name ist leer, zu lang oder enthält unsichere Zeichen");
    if (value.externalRef !== null) {
      if (exactFields(value.externalRef, ["system", "scope", "id"], "candidate.externalRef", errors)) {
        if (value.externalRef.system !== "efa2-csv") errors.push("candidate.externalRef.system muss efa2-csv sein");
        if (!validText(value.externalRef.scope)) errors.push("candidate.externalRef.scope ist ungültig");
        if (!validText(value.externalRef.id)) errors.push("candidate.externalRef.id ist ungültig");
      }
    }
    if (exactFields(value.provenance, PROVENANCE_FIELDS, "candidate.provenance", errors)) {
      if (value.provenance.source !== "efa-csv") errors.push("candidate.provenance.source ist ungültig");
      const date = typeof value.provenance.importedAt === "string" ? new Date(value.provenance.importedAt) : null;
      if (!date || !Number.isFinite(date.getTime()) || date.toISOString() !== value.provenance.importedAt) errors.push("candidate.provenance.importedAt ist ungültig");
      if (!HASH_PATTERN.test(value.provenance.fileSha256)) errors.push("candidate.provenance.fileSha256 ist ungültig");
      if (value.provenance.encoding !== "utf-8") errors.push("candidate.provenance.encoding muss utf-8 sein");
      if (!DELIMITERS.has(value.provenance.delimiter)) errors.push("candidate.provenance.delimiter ist ungültig");
      if (!HASH_PATTERN.test(value.provenance.mappingFingerprint)) errors.push("candidate.provenance.mappingFingerprint ist ungültig");
      if (value.provenance.efaVersion !== "unknown") errors.push("candidate.provenance.efaVersion muss ohne belegte Exportversion unknown bleiben");
    }
    if (value.status !== "incomplete") errors.push("candidate.status muss incomplete sein");
    return errors;
  }
  function validateEfaCandidate(value) {
    const errors = Object.freeze(candidateErrors(value));
    return Object.freeze({ ok: errors.length === 0, value: errors.length === 0 ? value : null, errors });
  }
  function rowValue(row, headers, column) {
    return row[headers.indexOf(column)].trim();
  }
  function nameFromRow(row, headers, mapping, entityType) {
    if (entityType === "boat") return rowValue(row, headers, mapping.name);
    if (mapping.displayName) return rowValue(row, headers, mapping.displayName);
    const firstName = rowValue(row, headers, mapping.firstName);
    const lastName = rowValue(row, headers, mapping.lastName);
    if (!firstName || !lastName) return "";
    const parts = [
      firstName,
      mapping.affix ? rowValue(row, headers, mapping.affix) : "",
      lastName
    ].filter(Boolean);
    return parts.join(" ");
  }
  function refKey(ref) {
    return ref === null ? null : canonicalJson3(ref);
  }
  function comparableCandidate(candidate) {
    return {
      entityType: candidate.entityType,
      name: candidate.name,
      externalRef: candidate.externalRef,
      status: candidate.status
    };
  }
  function normalizedName(name) {
    return name.normalize("NFKC").trim().replace(/\s+/gu, " ").toLocaleLowerCase("de-DE");
  }
  function classifyCandidate(candidate, pool) {
    const key = refKey(candidate.externalRef);
    const sameRef = key === null ? [] : pool.filter((existing) => refKey(existing.externalRef) === key);
    if (sameRef.some((existing) => canonicalJson3(comparableCandidate(existing)) !== canonicalJson3(comparableCandidate(candidate)))) return "idConflict";
    if (sameRef.some((existing) => canonicalJson3(comparableCandidate(existing)) === canonicalJson3(comparableCandidate(candidate)))) return "exactDuplicate";
    if (pool.some((existing) => existing.entityType === candidate.entityType && normalizedName(existing.name) === normalizedName(candidate.name))) return "nameReview";
    return "new";
  }
  function candidateFromRow({ row, headers, mapping, entityType, scope, provenance }) {
    const name = nameFromRow(row, headers, mapping, entityType);
    const externalId = mapping.id ? rowValue(row, headers, mapping.id) : "";
    const candidate = {
      schemaVersion: EFA_CANDIDATE_SCHEMA_VERSION,
      kind: EFA_CANDIDATE_KIND,
      entityType,
      name,
      externalRef: externalId ? { system: "efa2-csv", scope, id: externalId } : null,
      provenance: { ...provenance },
      status: "incomplete"
    };
    return deepFreeze4(candidate);
  }
  async function previewEfaCsv({
    text,
    delimiter,
    entityType,
    mapping,
    scope = null,
    existingCandidates = [],
    clock = () => /* @__PURE__ */ new Date(),
    digest,
    limits
  } = {}) {
    assertEntityType(entityType);
    const parsed = parseEfaCsv(text, { delimiter, limits });
    const normalizedMapping = normalizeMapping(mapping, parsed.headers, entityType);
    const mapsExternalId = Object.hasOwn(normalizedMapping, "id");
    if (mapsExternalId && !validText(scope)) {
      throw new EfaCsvValidationError("Ein ID-Mapping benötigt einen expliziten, sicheren eFa-Scope", { code: "scope" });
    }
    if (!mapsExternalId && scope !== null) {
      throw new EfaCsvValidationError("Ein Scope ist nur zusammen mit einem expliziten ID-Mapping zulässig", { code: "scope" });
    }
    if (!Array.isArray(existingCandidates)) throw new TypeError("existingCandidates muss ein Array sein");
    for (const [index, candidate] of existingCandidates.entries()) {
      const validation = validateEfaCandidate(candidate);
      if (!validation.ok) throw new EfaCsvValidationError(`Bestehender Kandidat ${index + 1} ist ungültig`, { code: "existing-candidate", details: validation.errors });
    }
    const importedAt = timestamp(clock);
    const fileSha256 = await sha256(text, digest);
    const mappingContract = { entityType, delimiter, mapping: normalizedMapping, scope: mapsExternalId ? scope : null };
    const mappingFingerprint = await sha256(canonicalJson3(mappingContract), digest);
    const provenance = Object.freeze({
      source: "efa-csv",
      importedAt,
      fileSha256,
      encoding: "utf-8",
      delimiter,
      mappingFingerprint,
      efaVersion: "unknown"
    });
    const pool = [...existingCandidates];
    const counts = { new: 0, exactDuplicate: 0, nameReview: 0, idConflict: 0, invalid: 0, incomplete: 0 };
    const items = parsed.rows.map((row, index) => {
      const candidate = candidateFromRow({ row, headers: parsed.headers, mapping: normalizedMapping, entityType, scope, provenance });
      const validation = validateEfaCandidate(candidate);
      if (!validation.ok) {
        counts.invalid += 1;
        return deepFreeze4({ rowNumber: index + 2, classification: "invalid", classifications: ["invalid"], candidate: null, errors: [...validation.errors] });
      }
      const classification = classifyCandidate(candidate, pool);
      counts[classification] += 1;
      counts.incomplete += 1;
      pool.push(candidate);
      return deepFreeze4({ rowNumber: index + 2, classification, classifications: [classification, "incomplete"], candidate, errors: [] });
    });
    const baseFingerprint = await sha256(canonicalJson3(existingCandidates.map(comparableCandidate)), digest);
    const planFingerprint = await sha256(canonicalJson3({
      fileSha256,
      mappingFingerprint,
      baseFingerprint,
      rows: items.map((item) => ({ classification: item.classification, candidate: item.candidate && comparableCandidate(item.candidate) }))
    }), digest);
    return deepFreeze4({
      schemaVersion: 1,
      kind: "efaCsvPreview",
      entityType,
      delimiter,
      headers: [...parsed.headers],
      mapping: { ...normalizedMapping },
      scope: mapsExternalId ? scope : null,
      fileSha256,
      mappingFingerprint,
      baseFingerprint,
      planFingerprint,
      importedAt,
      counts,
      items
    });
  }

  // js/app.mjs
  var BOOT_ROOT = document.documentElement;
  BOOT_ROOT.dataset.rudertrimmBoot = "loading";
  async function bootRudertrimm() {
    const $ = (id) => document.getElementById(id);
    const rad = (d) => d * Math.PI / 180;
    const deg = (r) => r * 180 / Math.PI;
    const lerp = (a, b, t) => a + (b - a) * t;
    const clamp2 = (v, a, b) => Math.max(a, Math.min(b, v));
    const smooth = (x) => {
      x = clamp2(x, 0, 1);
      return x * x * (3 - 2 * x);
    };
    const fmt = (v, dec = 1) => v.toLocaleString("de-DE", { minimumFractionDigits: 0, maximumFractionDigits: dec });
    const RELEASE = globalThis.RUDERTRIMM_RELEASE;
    if (!RELEASE) throw new TypeError("Zentrale Release-Metadaten fehlen.");
    const APP_VERSION = RELEASE.appVersion;
    const APP_BUILD_DATE = RELEASE.buildDate;
    const APP_BUILD_ID = RELEASE.buildId;
    const APP_SHELL_REVISION = RELEASE.shellRevision;
    $("buildState").textContent = RELEASE.label;
    const isDirectFile = globalThis.location?.protocol === "file:";
    const STORAGE_PROBE_KEY = "rudertrimm:v2:capability-probe";
    let storageMode = "local";
    let storageAdapter;
    let storageFallbackReason = "";
    function probeStorage(candidate) {
      candidate.setItem(STORAGE_PROBE_KEY, "1");
      candidate.removeItem(STORAGE_PROBE_KEY);
      return candidate;
    }
    try {
      if (isDirectFile) {
        const error = new Error("Direktstart verwendet keinen browserübergreifend verlässlichen Dauerspeicher.");
        error.code = "direct-file";
        throw error;
      }
      if (typeof navigator.locks?.request !== "function") {
        const error = new Error("Web Locks fehlen; persistente Mehrtab-Schreibvorgänge wären nicht atomar koordinierbar.");
        error.code = "coordination-unavailable";
        throw error;
      }
      storageAdapter = probeStorage(window.localStorage);
    } catch (localError) {
      try {
        storageAdapter = probeStorage(window.sessionStorage);
        storageMode = "session";
        storageFallbackReason = localError?.code === "direct-file" ? "Direktstart ohne verlässlichen Dauerspeicher" : localError?.code === "coordination-unavailable" ? "Web Locks fehlen" : "Persistenter Browser-Speicher ist nicht verfügbar";
        console.warn("Persistente, atomar koordinierte Mehrtab-Speicherung ist nicht verfügbar; V2 verwendet tabgebundenes sessionStorage.", localError);
      } catch (sessionError) {
        storageAdapter = createMemoryStorage();
        storageMode = "memory";
        storageFallbackReason = "Browser-Speicher ist vollständig blockiert";
        console.error("Browser-Speicher ist vollständig blockiert; V2 verwendet flüchtigen Arbeitsspeicher.", { localError, sessionError });
      }
    }
    let dirty = false;
    let workspaceChangeVersion = 0;
    let ephemeralDataPresent = false;
    let workspaceViewState = "new";
    let workflowGuideReady = false, workflowGuideSignature = "", workflowCurrentResultSignature = "", workflowReviewedResultSignature = "";
    let renderedActionPlanSignature = "";
    const limitName = (value) => truncateCodePoints(String(value ?? ""), MAX_NAME_LENGTH);
    const cleanName = (value, fallback = "Unbenannt") => limitName(String(value ?? "").replace(/\s+/g, " ").trim()) || fallback;
    const cleanOptionalName = (value) => limitName(String(value ?? "").replace(/\s+/g, " ").trim()) || null;
    function visualName(value, fallback = "Ruderer", maxLength = 24) {
      const name = cleanName(value, fallback);
      return [...name].length <= maxLength ? name : `${truncateCodePoints(name, maxLength - 1)}…`;
    }
    function clearErrorStatus() {
      const error = $("errorStatus");
      if (error) {
        error.textContent = "";
        error.hidden = true;
      }
    }
    function announce(message) {
      $("liveStatus").textContent = "";
      requestAnimationFrame(() => $("liveStatus").textContent = message);
    }
    function cleanStateLabel() {
      return workspaceStateLabel({ dirty, viewState: workspaceViewState, storageMode });
    }
    function markWorkspaceMutation() {
      workspaceChangeVersion += 1;
    }
    function setDirty(value = true) {
      if (value) markWorkspaceMutation();
      dirty = value;
      if ($("dirtyState")) $("dirtyState").textContent = cleanStateLabel();
      if (workflowGuideReady) renderWorkflowGuide();
    }
    function setPressed(id, on) {
      const button = $(id);
      button.classList.toggle("on", on);
      button.setAttribute("aria-pressed", String(on));
    }
    const PRESENTATION_MODES = Object.freeze(["compact", "details"]);
    function setPresentationMode(mode, { focus = false } = {}) {
      if (!PRESENTATION_MODES.includes(mode)) return false;
      document.body.dataset.presentation = mode;
      const compact = $("presentationCompact"), details = $("presentationDetails");
      for (const [button, active] of [[compact, mode === "compact"], [details, mode === "details"]]) {
        button.setAttribute("aria-pressed", String(active));
        button.classList.toggle("on", active);
      }
      placeCompactControls(mode);
      if (focus) (mode === "compact" ? compact : details).focus();
      return true;
    }
    function initPresentationMode() {
      $("presentationCompact").addEventListener("click", () => setPresentationMode("compact"));
      $("presentationDetails").addEventListener("click", () => setPresentationMode("details"));
      for (const button of document.querySelectorAll(".open-details")) {
        button.addEventListener("click", () => setPresentationMode("details", { focus: true }));
      }
      setPresentationMode(document.body.dataset.presentation === "details" ? "details" : "compact");
    }
    function updateContext() {
      if (!$("ctxBoat")) return;
      $("ctxBoat").textContent = cleanName($("boatName").value, "Ungespeichertes Boot");
      $("ctxMode").textContent = state.mode === "werkstatt" ? "Werkstatt/Böcke" : "Wasser/Steg";
      const seat = activeSeat(), assignment = activeAssignment();
      $("ctxSeat").textContent = seat ? `${seatLabel(seat)} · ${assignment ? cleanName(assignment.rower.name, "Ungespeichertes Profil") : "frei · Profil erforderlich"}` : "Kein Bootsplatz";
      $("dirtyState").textContent = cleanStateLabel();
    }
    function replaceOptions(select, placeholder, items) {
      const first = document.createElement("option");
      first.value = "";
      first.textContent = placeholder;
      const options = items.map(({ value, label }) => {
        const option = document.createElement("option");
        option.value = String(value);
        option.textContent = label;
        return option;
      });
      select.replaceChildren(first, ...options);
    }
    function downloadJson(data, filename) {
      const url = URL.createObjectURL(new Blob([JSON.stringify(data, null, 2)], { type: "application/json" }));
      const anchor = document.createElement("a");
      anchor.href = url;
      anchor.download = filename;
      anchor.hidden = true;
      document.body.appendChild(anchor);
      anchor.click();
      anchor.remove();
      setTimeout(() => URL.revokeObjectURL(url), 1e3);
    }
    function validationMessage2(result) {
      return result.errors.slice(0, 5).map((error) => `${error.path}: ${error.message}`).join("\n");
    }
    function reportError(error, prefix = "Aktion fehlgeschlagen") {
      console.error(prefix, error);
      const message = `${prefix}: ${error?.message || "Unbekannter Fehler"}`;
      const status = $("errorStatus");
      status.textContent = message;
      status.hidden = false;
    }
    function confirmCollectionImport(preview) {
      return confirm([
        `Importvorschau für ${preview.kind}:`,
        `• vorhanden: ${preview.existing}`,
        `• Datei enthält: ${preview.incoming}`,
        `• neu: ${preview.added}`,
        `• exakte Dubletten übersprungen: ${preview.duplicatesSkipped}`,
        `• ID-Konflikte werden neu vergeben: ${preview.remapped}`,
        `• unsichere Boots-Profilreferenzen werden gelöst: ${preview.referencesDetached ?? 0}`,
        `• Datensätze auf das aktuelle Schema migriert: ${preview.migrated}`,
        `• Ergebnis: ${preview.total} von maximal ${preview.maxRecords} Einträgen`,
        "",
        "Jetzt atomar übernehmen?"
      ].join("\n"));
    }
    function legacyCollectionImportMigrator(migrateRecord) {
      return (envelope, { expectedKind }) => {
        if (!envelope || typeof envelope !== "object" || Array.isArray(envelope)) throw new TypeError("Import muss ein Objekt sein.");
        if (Number.isSafeInteger(envelope.schemaVersion) && envelope.schemaVersion > SCHEMA_VERSION) {
          throw new UnsupportedSchemaVersionError("import", envelope.schemaVersion);
        }
        const keys = Object.keys(envelope).sort().join(",");
        if (keys !== "items,kind,schemaVersion" || envelope.kind !== expectedKind || !Array.isArray(envelope.items)) {
          throw new TypeError(`Erwartet wird eine versionierte Datei vom Typ ${expectedKind}.`);
        }
        return {
          schemaVersion: SCHEMA_VERSION,
          kind: expectedKind,
          items: envelope.items.map((item, index) => migrateRecord(item, { storage: false, path: `items[${index}]` }).value)
        };
      };
    }
    function approveFreshImportPreview(approved, fresh) {
      if (sameCollectionImportPreview(approved, fresh)) return true;
      const error = new Error("Die bestätigte Importvorschau ist nicht mehr aktuell. Bitte den Import erneut starten und die neue Vorschau prüfen.");
      error.code = "import-preview-changed";
      throw error;
    }
    function reportRepositoryLoad(result, label) {
      if (result.ok) return;
      if (result.quarantine?.stored === true) {
        announce(`${label} waren beschädigt. Die unveränderten Rohdaten wurden nachweislich gesichert; der Bereich startet leer.`);
        return;
      }
      reportError(result.error, `${label} konnten nicht sicher gelesen oder gesichert werden; Speichern und Import bleiben für diesen Bereich gesperrt`);
    }
    const canMigrateLegacy = (result) => result.rawPresent === false || !result.ok && result.quarantine?.stored === true;
    let legacyV1WorkspaceCache;
    function legacyV1WorkspaceCandidate() {
      if (legacyV1WorkspaceCache !== void 0) return legacyV1WorkspaceCache;
      const raw = storageAdapter.getItem("rudertrimm");
      legacyV1WorkspaceCache = raw === null ? null : adaptLegacyV1Workspace(raw, { seed: "stored-v1-workspace" });
      return legacyV1WorkspaceCache;
    }
    const PHI = { skull: { A: 66, R: 44 }, riemen: { A: 54, R: 36 } };
    function mkRower(name, o = {}) {
      return Object.assign({
        name,
        legLen: 90,
        torsoLen: 95,
        wingspan: 188,
        SB: 40,
        weight: 80,
        // Nur neue, ungespeicherte Skull-Arbeitsstände erhalten den Modellstandard;
        // geladene Profile/Workspaces behalten ihren explizit gespeicherten Fa-Wert.
        stemmX: defaultFaForRig("skull"),
        DA: 159,
        IH: 88,
        L: 288,
        d: 2,
        handGap: 18,
        // Hebel des Platzes
        a: 15,
        anlage: 4,
        aussen: 0,
        dBB: 0.5,
        // Dollenhöhe/-neigung; Δ BB−StB (Skull)
        stemmW: 42,
        rollL: 75,
        rueh: 5
        // Stemmbrett/Rollbahn (42° = WM-2017-Median)
      }, o);
    }
    const clone = (value) => JSON.parse(JSON.stringify(value));
    const localId = (prefix) => `${prefix}-${createStableId()}`;
    const seatLabel = (seat) => seat?.label ?? `Platz ${seat?.position ?? "?"}`;
    function presetSeatSource(presetKey, blade, position, previous = {}, seatCount = PRESETS[presetKey]?.seatCount ?? 2) {
      const preset = PRESETS[presetKey];
      return buildSeatDTO({
        ...previous,
        id: previous.id ?? localId("seat"),
        trimId: previous.trimId ?? localId("trim"),
        externalRef: previous.externalRef ?? null,
        position,
        role: seatRoleForPosition(position, seatCount),
        label: seatLabelForPosition(position, seatCount),
        rig: preset.rig,
        DA: preset.DA,
        IH: preset.IH,
        L: blade === "mac" ? preset.Lmac : preset.Lbig,
        d: previous.d ?? 2,
        handGap: previous.handGap ?? 18,
        a: preset.a,
        anlage: previous.anlage ?? 4,
        aussen: previous.aussen ?? 0,
        dBB: previous.dBB ?? 0.5,
        stemmW: previous.stemmW ?? 42,
        rollL: previous.rollL ?? 75,
        rueh: previous.rueh ?? 5,
        stemmX: defaultFaForRig(preset.rig),
        rowerRef: previous.rowerRef ?? null
      });
    }
    function initialSeats() {
      return [clone(presetSeatSource("1x", "big", 1, {}, 1))];
    }
    const state = {
      rig: "skull",
      strokeSide: 1,
      // Riemen: +1 = Schlagmann Steuerbord, −1 = Backbord
      phiA: 66,
      phiR: 44,
      t: 0,
      recovery: false,
      // Schlagbogen bootsweit (Handbuch 4.1: gleiche Ruderwinkel)
      c: 8,
      kg: 0,
      // Rollsitz über WL & Mannschaftsgewicht = Bootsebene
      mode: "werkstatt",
      // 'werkstatt' (Boot auf Böcken) | 'wasser' (Steg-Kontrolle)
      heightRef: "sitz",
      // Dollenhöhen-Referenz: 'sitz' (Rollsitz-Tiefpunkt) | 'schiene'
      seatOffset: 5,
      // Rollsitz-Tiefpunkt über Schienenoberkante (cm)
      seats: initialSeats(),
      crew: [],
      editSeatId: null,
      referenceSeatId: null,
      db: [],
      dbIdx: -1,
      boats: []
      // Boots-Datenbank (komplette Einstellungen)
    };
    state.editSeatId = state.seats[0].id;
    state.referenceSeatId = state.seats[0].id;
    const repositoryOptions = storageMode === "local" ? { storage: storageAdapter } : { storage: storageAdapter, channelFactory: false, storageEventTarget: null };
    const rowerRepository = createRowerRepository({ ...repositoryOptions, validateRecord: validateRower, migrateRecord: migrateRowerToCurrent });
    const boatRepository = createBoatRepository({ ...repositoryOptions, validateRecord: validateBoat, migrateRecord: migrateBoatToCurrent });
    const efaCandidateRepository = createEfaCandidateRepository({ ...repositoryOptions, validateRecord: validateEfaCandidate });
    const workspaceRepository = createWorkspaceRepository({ ...repositoryOptions, validateWorkspace: validateCurrentConfig, migrateWorkspace: migrateCurrentConfigToCurrent });
    const repositoryWrite = (repository, action) => withExclusiveRepositoryWrite(repository, () => {
      if (repository.migrationStatus().pending) {
        repository.commitPendingMigration({ expectedRevision: repository.snapshot().revision });
      }
      return action();
    }, { shared: storageMode === "local" });
    let selectedBoatId = "", selectedBoatRevision = null;
    let boatMetadata = { externalRef: null, capacityStatus: "preset", legacyRigTemplate: null };
    const rowerSelection = createContextualSelection();
    const workspaceRevision = createObservedRevision();
    const PROFILE_KEYS = /* @__PURE__ */ new Set(["name", "legLen", "torsoLen", "wingspan", "SB", "weight"]);
    const SEAT_KEYS = /* @__PURE__ */ new Set(["stemmX", "DA", "IH", "L", "d", "handGap", "a", "anlage", "aussen", "dBB", "stemmW", "rollL", "rueh"]);
    const profileDraftDirty = /* @__PURE__ */ Object.create(null);
    const profileDraftVersion = /* @__PURE__ */ Object.create(null);
    function ensureDraftState(seatId) {
      if (!Object.hasOwn(profileDraftDirty, seatId)) profileDraftDirty[seatId] = false;
      if (!Object.hasOwn(profileDraftVersion, seatId)) profileDraftVersion[seatId] = 0;
    }
    function markProfileDraftChanged(seatId) {
      ensureDraftState(seatId);
      profileDraftDirty[seatId] = true;
      profileDraftVersion[seatId] += 1;
    }
    const activeSeat = () => state.seats.find((seat) => seat.id === state.editSeatId) ?? state.seats[0] ?? null;
    const assignmentFor = (seatId) => state.crew.find((assignment) => assignment.seatId === seatId) ?? null;
    const activeAssignment = () => activeSeat() ? assignmentFor(activeSeat().id) : null;
    const activeRower = () => activeAssignment()?.rower ?? null;
    function runtimeForSeat(seat) {
      const assignment = seat ? assignmentFor(seat.id) : null;
      return assignment ? { ...assignment.rower, ...seat, stemmX: seat.stemmX } : null;
    }
    const isRowerKey = (k) => PROFILE_KEYS.has(k) || SEAT_KEYS.has(k);
    function getVal(k) {
      if (PROFILE_KEYS.has(k)) return activeRower()?.[k] ?? null;
      if (SEAT_KEYS.has(k)) return activeSeat()?.[k] ?? null;
      return state[k];
    }
    function setVal(k, v) {
      if (PROFILE_KEYS.has(k)) {
        const rower = activeRower();
        if (!rower) return;
        rower[k] = v;
        markProfileDraftChanged(state.editSeatId);
      } else if (SEAT_KEYS.has(k)) {
        const seat = activeSeat();
        if (!seat) return;
        seat[k] = v;
      } else state[k] = v;
      setDirty();
    }
    const CTLS = [
      { hdr: "Ruder & Rigg · gewählter Platz" },
      { k: "DA", lab: "Dollenabstand DA", unit: "cm", step: 0.5, hint: { skull: "Stift–Stift", riemen: "Bootsmitte–Stift" } },
      { k: "IH", lab: "Innenhebel IH", unit: "cm", step: 0.5, hint: { skull: "Faustformel: DA/2 + 8 cm", riemen: "Faustformel: DA + 30 cm" } },
      { k: "L", lab: "Ruderlänge L", unit: "cm", step: 0.5 },
      { k: "d", lab: "d · Stift→Klemmring", unit: "cm", min: 1, max: 3, step: 0.25 },
      { k: "handGap", lab: "Handabstand am Griff", unit: "cm", min: 14, max: 22, step: 0.5, hint: { both: "nur Riemen (~2 Handbreit)" } },
      { hdr: "Höhen & Anlage · gewählter Platz" },
      { k: "a", lab: "Dollenhöhe über Rollsitz", unit: "cm", min: 11, max: 21, step: 0.5, hint: { both: "Referenz Schiene = Wert + Sitz-Offset" } },
      { k: "dBB", lab: "Δ Dollenhöhe BB−StB", unit: "cm", min: -2, max: 3, step: 0.25, hint: { skull: "Ziel +0,5–1 cm (Backbord höher)", riemen: "nur Skull — Riemen: Vergleich über die Plätze" } },
      { k: "anlage", lab: "Dollen-Neigung (z. Heck)", unit: "°", min: 0, max: 8, step: 0.5, hint: { both: "Norm 4° · Big-Blades eher weniger" } },
      { k: "aussen", lab: "Außenneigung Dollenstift", unit: "°", min: 0, max: 3, step: 0.25 },
      { hdr: "Rollbahn & Stemmbrett · gewählter Platz" },
      { k: "stemmX", lab: "Stemmbrett längs (Fa)", unit: "cm", min: 26, max: 56, step: 0.5, hint: { both: "Fa + Körpermaße bestimmen die modellierte Ist-Auslage · unkalibriertes Prüfmodell" } },
      { k: "stemmW", lab: "Stemmbrett-Neigung", unit: "°", min: 36, max: 48, step: 0.5, hint: { both: "Norm 45° zum Kiel, −3° · unabhängig von der Längsposition" } },
      { k: "rollL", lab: "Rollbahnlänge", unit: "cm", min: 68, max: 80, step: 0.5 },
      { k: "rueh", lab: "Rollbahn-Überstand h. Dollenanlage", unit: "cm", min: -5, max: 12, step: 0.5 },
      { hdr: "Schlagbogen & Boot · gemeinsam" },
      { k: "phiA", lab: "Auslagewinkel Rigg-Ziel", unit: "°", min: 40, max: 85, step: 0.5, hint: { both: "Vergleichswert; die modellierte Ist-Auslage entsteht aus Fa + Körpermaßen" } },
      { k: "phiR", lab: "Rücklagewinkel", unit: "°", min: 20, max: 60, step: 0.5 },
      { k: "t", lab: "Durchzug", unit: "%", min: 0, max: 100, step: 1 },
      { k: "c", lab: "Rollsitz über Wasserlinie", unit: "cm", min: 5, max: 10, step: 0.5 },
      { k: "kg", lab: "Gewicht vs. Auslegung", unit: "kg", min: -20, max: 20, step: 1, hint: { both: "1 kg ≙ 1 mm Eintauchtiefe" } }
    ];
    const COMPACT_CONTROL_KEYS = Object.freeze([
      "IH",
      "stemmX"
    ]);
    const COMPACT_CONTROL_KEY_SET = new Set(COMPACT_CONTROL_KEYS);
    const ROWER_CTLS = [
      { k: "legLen", lab: "Beinlänge", unit: "cm", min: 70, max: 105, step: 1, hint: { both: "Sohle → Hüftgelenk" } },
      { k: "torsoLen", lab: "Rumpflänge", unit: "cm", min: 75, max: 110, step: 1, hint: { both: "Sitzhöhe: Sitz → Scheitel" } },
      { k: "wingspan", lab: "Spannweite", unit: "cm", min: 150, max: 215, step: 1, hint: { both: "Mittelfinger → Mittelfinger" } },
      { k: "SB", lab: "Schulterbreite", unit: "cm", min: 32, max: 48, step: 1, hint: { both: "Schulter → Schulter" } },
      { k: "weight", lab: "Körpergewicht", unit: "kg", min: 45, max: 120, step: 1 }
    ];
    function numericDraftError(raw, min, max, step2) {
      if (String(raw).trim() === "") return "Wert erforderlich.";
      const value = Number(raw);
      if (!Number.isFinite(value)) return "Bitte eine gültige Zahl eingeben.";
      if (value < min || value > max) return `Erlaubter Bereich: ${fmt(min)} bis ${fmt(max)}.`;
      const ticks = (value - min) / step2;
      if (Math.abs(ticks - Math.round(ticks)) > 1e-7) return `Erlaubte Schrittweite: ${fmt(step2)}.`;
      return "";
    }
    function setNumericDraftError(input, error, message) {
      input.setAttribute("aria-invalid", String(Boolean(message)));
      error.hidden = !message;
      error.textContent = message;
    }
    function restoreNumericDraft(input, error, value, unit) {
      if (input.getAttribute("aria-invalid") !== "true") return false;
      input.value = String(value);
      setNumericDraftError(input, error, "");
      announce(`Ungültige Eingabe verworfen · gültiger Wert ${fmt(value)} ${unit}.`);
      return true;
    }
    function buildInto(list, hostId) {
      const host = $(hostId);
      const grouped = hostId === "controls";
      const existingOpenGroups = new Set([...host.querySelectorAll?.("details.control-group[open]") ?? []].map((group) => group.dataset.group));
      const hadGroups = !!host.querySelector?.("details.control-group");
      host.replaceChildren();
      let destination = host, groupIndex = -1;
      for (const [itemIndex, c] of list.entries()) {
        if (c.hdr) {
          if (!grouped) {
            const h = document.createElement("div");
            h.className = "slab" + (host.children.length ? " grp" : "");
            h.textContent = c.hdr;
            host.appendChild(h);
            continue;
          }
          groupIndex += 1;
          const groupKey = String(groupIndex);
          const nextHeaderOffset = list.slice(itemIndex + 1).findIndex((item) => item.hdr);
          const controlCount = nextHeaderOffset < 0 ? list.length - itemIndex - 1 : nextHeaderOffset;
          const details = document.createElement("details");
          details.className = "control-group";
          details.dataset.group = groupKey;
          details.open = hadGroups ? existingOpenGroups.has(groupKey) : groupIndex === 0;
          const summary = document.createElement("summary");
          const title = document.createElement("span");
          title.textContent = c.hdr;
          const meta = document.createElement("span");
          meta.className = "control-group-meta";
          meta.textContent = `${controlCount} Werte`;
          summary.append(title, meta);
          destination = document.createElement("div");
          destination.className = "control-group-content";
          details.append(summary, destination);
          host.appendChild(details);
          continue;
        }
        const r = RANGES[state.rig][c.k];
        const min = r ? r[0] : c.min, max = r ? r[1] : c.max;
        const current = getVal(c.k);
        const unavailable = PROFILE_KEYS.has(c.k) && current === null;
        const hint = c.hint ? c.hint.both || c.hint[state.rig] || "" : "";
        const div = document.createElement("div");
        div.className = "ctl";
        const lab = document.createElement("div");
        lab.className = "lab";
        const label = document.createElement("label");
        label.htmlFor = `in_${c.k}`;
        label.textContent = c.lab;
        const value = document.createElement("span");
        value.className = "val";
        value.id = `v_${c.k}`;
        lab.append(label, value);
        const input = document.createElement("input");
        input.type = "range";
        input.id = `in_${c.k}`;
        input.min = String(min);
        input.max = String(max);
        input.step = String(c.step);
        input.value = String(unavailable ? min : current);
        input.disabled = unavailable;
        input.setAttribute("aria-valuetext", unavailable ? "Profil erforderlich" : `${fmt(current)} ${c.unit}`);
        const numberLabel = document.createElement("label");
        numberLabel.className = "sr-only";
        numberLabel.htmlFor = `num_${c.k}`;
        numberLabel.textContent = `${c.lab} als Zahl in ${c.unit}`;
        const number = document.createElement("input");
        number.type = "number";
        number.id = `num_${c.k}`;
        number.className = "ctl-number";
        number.min = String(min);
        number.max = String(max);
        number.step = String(c.step);
        number.required = true;
        number.inputMode = "decimal";
        number.value = unavailable ? "" : String(current);
        number.disabled = unavailable;
        number.placeholder = unavailable ? "Profil erforderlich" : "";
        const unit = document.createElement("span");
        unit.className = "ctl-unit";
        unit.textContent = c.unit;
        unit.setAttribute("aria-hidden", "true");
        const numberWrap = document.createElement("span");
        numberWrap.className = "ctl-number-wrap";
        numberWrap.append(numberLabel, number, unit);
        const pair = document.createElement("div");
        pair.className = "ctl-input-pair";
        pair.append(input, numberWrap);
        const mm = document.createElement("div");
        mm.className = "mm";
        const minLabel = document.createElement("span");
        minLabel.textContent = fmt(min);
        const maxLabel = document.createElement("span");
        maxLabel.textContent = fmt(max);
        mm.append(minLabel, maxLabel);
        const error = document.createElement("div");
        error.className = "ctl-error";
        error.id = `err_${c.k}`;
        error.setAttribute("aria-live", "polite");
        error.hidden = true;
        div.append(lab, pair, mm);
        const descriptions = [error.id];
        if (hint) {
          const hintEl = document.createElement("div");
          hintEl.className = "hint";
          hintEl.id = `hint_${c.k}`;
          hintEl.textContent = hint;
          input.setAttribute("aria-describedby", hintEl.id);
          descriptions.unshift(hintEl.id);
          div.appendChild(hintEl);
        }
        number.setAttribute("aria-describedby", descriptions.join(" "));
        div.appendChild(error);
        if (grouped && COMPACT_CONTROL_KEY_SET.has(c.k)) {
          const anchor = document.createElement("span");
          anchor.hidden = true;
          anchor.dataset.compactAnchor = c.k;
          destination.appendChild(anchor);
          div.classList.add("compact-core-control");
        }
        destination.appendChild(div);
        const commit = (value2) => {
          setNumericDraftError(number, error, "");
          setVal(c.k, value2);
          if (c.k === "t") state.recovery = false;
          render();
        };
        input.addEventListener("input", (e) => commit(Number(e.target.value)));
        number.addEventListener("input", (e) => {
          const message = numericDraftError(e.target.value, min, max, c.step);
          setNumericDraftError(number, error, message);
          if (!message) commit(Number(e.target.value));
        });
        const restoreNumberDraft = () => restoreNumericDraft(number, error, getVal(c.k), c.unit);
        number.addEventListener("change", restoreNumberDraft);
        number.addEventListener("blur", restoreNumberDraft);
        c._unit = c.unit;
      }
    }
    function placeCompactControls(mode) {
      const host = $("compactControlHost");
      if (!host) return;
      for (const key of COMPACT_CONTROL_KEYS) {
        const input = $(`in_${key}`), control = input?.closest(".ctl");
        if (!control) continue;
        if (mode === "compact") {
          if (control.parentElement !== host) host.appendChild(control);
          continue;
        }
        const anchor = document.querySelector(`[data-compact-anchor="${key}"]`);
        if (anchor) anchor.after(control);
      }
    }
    function buildControls() {
      $("compactControlHost")?.replaceChildren();
      buildInto(CTLS, "controls");
      buildInto(ROWER_CTLS, "rowerControls");
      placeCompactControls(document.body.dataset.presentation === "details" ? "details" : "compact");
    }
    function syncControls() {
      for (const c of [...CTLS, ...ROWER_CTLS]) {
        if (c.hdr) continue;
        const inp = $("in_" + c.k);
        if (!inp) continue;
        const current = getVal(c.k), unavailable = PROFILE_KEYS.has(c.k) && current === null;
        inp.disabled = unavailable;
        if (!unavailable) inp.value = current;
        inp.setAttribute("aria-valuetext", unavailable ? "Profil erforderlich" : `${fmt(current)} ${c._unit}`);
        const number = $("num_" + c.k);
        if (number) {
          number.disabled = unavailable;
          const editingInvalid = typeof document !== "undefined" && document.activeElement === number && number.getAttribute("aria-invalid") === "true";
          if (!editingInvalid) {
            number.value = unavailable ? "" : String(current);
            number.placeholder = unavailable ? "Profil erforderlich" : "";
            if (number.getAttribute("aria-invalid") === "true") setNumericDraftError(number, $("err_" + c.k), "");
          }
        }
        $("v_" + c.k).textContent = unavailable ? "Profil erforderlich" : fmt(current) + " " + c._unit;
      }
    }
    function applyPreset() {
      const p = PRESETS[$("preset").value];
      const presetKey = $("preset").value;
      const explicitCount = p.seatCount;
      const requested = explicitCount ?? Math.max(1, Math.min(8, Number.parseInt($("boatSeatCount").value, 10) || state.seats.length || 2));
      const prior = state.seats;
      const layout = planSeatLayoutByRole(prior, requested);
      const removed = layout.removed.filter((seat) => assignmentFor(seat.id) || seat.rowerRef);
      const moved = layout.sources.flatMap((seat, index) => {
        if (!seat || seat.position === index + 1 || !assignmentFor(seat.id) && !seat.rowerRef) return [];
        return [`${seatLabel(seat)} → ${seatLabelForPosition(index + 1, requested)}`];
      });
      if ((removed.length || moved.length) && !confirm([
        moved.length ? `Bestehende Belegung folgt ihrer Rolle: ${moved.join(", ")}.` : "",
        removed.length ? `Entfernt werden: ${removed.map((seat) => seatLabel(seat)).join(", ")} samt aktueller Zuordnung.` : "",
        "Profile in der Datenbank bleiben erhalten. Fortfahren?"
      ].filter(Boolean).join("\n"))) {
        return false;
      }
      state.rig = p.rig;
      boatMetadata.capacityStatus = p.seatCount === null ? "confirmed" : "preset";
      if (!p.single) boatMetadata.legacyRigTemplate = null;
      state.phiA = PHI[p.rig].A;
      state.phiR = PHI[p.rig].R;
      state.seats = Array.from({ length: requested }, (_, index) => clone(presetSeatSource(
        presetKey,
        $("blade").value,
        index + 1,
        layout.sources[index] ?? {},
        requested
      )));
      const validIds = new Set(state.seats.map((seat) => seat.id));
      state.crew = state.crew.filter((assignment) => validIds.has(assignment.seatId)).map((assignment) => ({
        ...assignment,
        trimId: state.seats.find((seat) => seat.id === assignment.seatId).trimId
      }));
      if (!validIds.has(state.editSeatId)) state.editSeatId = state.seats[state.seats.length - 1].id;
      state.referenceSeatId = state.seats[state.seats.length - 1].id;
      if (!p.coxed) $("coxName").value = "";
      syncSeatCountControl();
      updateCoxControl();
      setDirty();
      buildControls();
      updateRig();
      render();
      return true;
    }
    let lastPreset = $("preset").value, lastBlade = $("blade").value;
    function confirmOverwrite(message, hasUnsaved = dirty) {
      return !hasUnsaved || confirm(`${message}

Ungespeicherte Änderungen bleiben sonst nicht erhalten.`);
    }
    function applyBladeLength() {
      const p = PRESETS[$("preset").value];
      for (const seat of state.seats) seat.L = $("blade").value === "big" ? p.Lbig : p.Lmac;
      setDirty();
      buildControls();
      render();
    }
    function setRig(rig) {
      if (state.rig === rig) return;
      if (!confirmOverwrite("Der Riggwechsel überschreibt DA, IH, Ruderlänge, Dollenhöhe und Fa aller realen Plätze sowie den gemeinsamen Schlagbogen.")) return;
      const previousPreset = lastPreset;
      $("preset").value = rig === "skull" ? "1x" : "4-";
      if (applyPreset()) lastPreset = $("preset").value;
      else $("preset").value = previousPreset;
    }
    function updateRig() {
      setPressed("rigS", state.rig === "skull");
      setPressed("rigR", state.rig === "riemen");
      $("sideWrap").style.display = state.rig === "riemen" ? "" : "none";
      setPressed("sideB", state.strokeSide === -1);
      setPressed("sideS", state.strokeSide === 1);
    }
    $("preset").addEventListener("change", () => {
      if (!confirmOverwrite("Das neue Preset überschreibt DA, IH, Ruderlänge, Dollenhöhe und Fa aller realen Plätze sowie den gemeinsamen Schlagbogen.")) {
        $("preset").value = lastPreset;
        return;
      }
      const requestedPreset = $("preset").value;
      if (!applyPreset()) {
        $("preset").value = lastPreset;
        syncSeatCountControl();
        return;
      }
      lastPreset = requestedPreset;
    });
    $("blade").addEventListener("change", () => {
      if (!confirmOverwrite("Der Blatttyp ändert ausschließlich die Ruderlänge aller realen Plätze.")) {
        $("blade").value = lastBlade;
        return;
      }
      lastBlade = $("blade").value;
      applyBladeLength();
    });
    $("rigS").addEventListener("click", () => setRig("skull"));
    $("rigR").addEventListener("click", () => setRig("riemen"));
    $("sideB").addEventListener("click", () => {
      state.strokeSide = -1;
      setDirty();
      updateRig();
      render();
    });
    $("sideS").addEventListener("click", () => {
      state.strokeSide = 1;
      setDirty();
      updateRig();
      render();
    });
    function updateMode() {
      setPressed("modeW", state.mode === "werkstatt");
      setPressed("modeX", state.mode === "wasser");
      $("refWrap").style.display = state.mode === "werkstatt" ? "" : "none";
      setPressed("refSitz", state.heightRef === "sitz");
      setPressed("refSchiene", state.heightRef === "schiene");
      $("in_seatOffset").value = state.seatOffset;
      $("in_seatOffset").setAttribute("aria-valuetext", `${fmt(state.seatOffset)} cm`);
      const number = $("num_seatOffset");
      if (number && !(document.activeElement === number && number.getAttribute("aria-invalid") === "true")) number.value = String(state.seatOffset);
      $("v_seatOffset").textContent = fmt(state.seatOffset) + " cm";
    }
    $("modeW").addEventListener("click", () => {
      state.mode = "werkstatt";
      setDirty();
      updateMode();
      render();
    });
    $("modeX").addEventListener("click", () => {
      state.mode = "wasser";
      setDirty();
      updateMode();
      render();
    });
    $("refSitz").addEventListener("click", () => {
      state.heightRef = "sitz";
      setDirty();
      updateMode();
      render();
    });
    $("refSchiene").addEventListener("click", () => {
      state.heightRef = "schiene";
      setDirty();
      updateMode();
      render();
    });
    function commitSeatOffset(value) {
      state.seatOffset = value;
      setDirty();
      updateMode();
      render();
    }
    $("in_seatOffset").addEventListener("input", (e) => commitSeatOffset(Number(e.target.value)));
    $("num_seatOffset").addEventListener("input", (e) => {
      const [min, max] = RANGES.boat.seatOffset, step2 = Number(e.target.step);
      const message = numericDraftError(e.target.value, min, max, step2);
      setNumericDraftError(e.target, $("err_seatOffset"), message);
      if (!message) commitSeatOffset(Number(e.target.value));
    });
    const restoreSeatOffsetDraft = () => restoreNumericDraft($("num_seatOffset"), $("err_seatOffset"), state.seatOffset, "cm");
    $("num_seatOffset").addEventListener("change", restoreSeatOffsetDraft);
    $("num_seatOffset").addEventListener("blur", restoreSeatOffsetDraft);
    const BOAT_RIG_KEYS = ["DA", "IH", "L", "d", "handGap", "a", "anlage", "aussen", "dBB", "stemmW", "rollL", "rueh", "stemmX"];
    function updateCoxControl() {
      const coxed = PRESETS[$("preset").value]?.coxed === true;
      $("coxWrap").hidden = !coxed;
      $("coxName").disabled = !coxed;
    }
    function syncSeatCountControl() {
      const preset = PRESETS[$("preset").value];
      const input = $("boatSeatCount");
      const fixed = Number.isSafeInteger(preset?.seatCount);
      input.disabled = fixed;
      input.value = String(fixed ? preset.seatCount : state.seats.length);
      $("boatSeatCountHint").textContent = fixed ? `Bootsklasse gibt ${preset.seatCount} reale${preset.seatCount === 1 ? "n" : ""} Platz vor.` : "Gig-Boot: Platzanzahl bewusst festlegen; bestehende Belegung wird vor einer Verkleinerung bestätigt.";
    }
    $("boatSeatCount").addEventListener("change", (event) => {
      const previous = state.seats.length;
      if (!confirmOverwrite(
        "Die neue Gig-Platzanzahl wendet das aktuelle Preset erneut an und überschreibt DA, Innenhebel, Ruderlänge, Dollenhöhe und Stemmbrettposition aller Plätze.",
        true
      )) {
        event.target.value = String(previous);
        return;
      }
      if (!applyPreset()) event.target.value = String(previous);
    });
    function boatOf() {
      const coxName = cleanOptionalName($("coxName").value);
      return buildBoatDTO({
        name: cleanName($("boatName").value, "Boot"),
        preset: $("preset").value,
        blade: $("blade").value,
        externalRef: boatMetadata.externalRef,
        rig: state.rig,
        strokeSide: state.strokeSide,
        phiA: state.phiA,
        phiR: state.phiR,
        c: state.c,
        seatOffset: state.seatOffset,
        cox: PRESETS[$("preset").value]?.coxed && coxName ? { name: coxName } : null,
        capacityStatus: PRESETS[$("preset").value]?.seatCount === null ? boatMetadata.capacityStatus : "preset",
        seats: state.seats,
        legacyRigTemplate: PRESETS[$("preset").value]?.single ? boatMetadata.legacyRigTemplate : null
      });
    }
    function canonicalizeBoatName() {
      const name = cleanName($("boatName").value, "Boot");
      $("boatName").value = name;
      updateContext();
      return name;
    }
    function resolveBoatCrew() {
      const resolved = [];
      for (const seat of state.seats) {
        if (!seat.rowerRef) continue;
        const selected = rowerRepository.select(seat.rowerRef.id);
        if (!selected.ok || selected.record.revision !== seat.rowerRef.revision) continue;
        resolved.push({
          schemaVersion: SCHEMA_VERSION,
          kind: "crewAssignment",
          seatId: seat.id,
          trimId: seat.trimId,
          rowerRef: { ...seat.rowerRef },
          rower: clone(selected.record.value)
        });
      }
      state.crew = resolved;
    }
    function applyBoat(b, { resolveCrew = true } = {}) {
      clearRowerSelection();
      const replacedSeatIds = /* @__PURE__ */ new Set([...state.seats.map((seat) => seat.id), ...b.seats.map((seat) => seat.id)]);
      for (const seatId of replacedSeatIds) {
        ensureDraftState(seatId);
        profileDraftVersion[seatId] += 1;
      }
      $("preset").value = b.preset || "1x";
      $("blade").value = b.blade || "big";
      $("boatName").value = b.name || "";
      $("coxName").value = b.cox?.name ?? "";
      lastPreset = $("preset").value;
      lastBlade = $("blade").value;
      state.rig = b.rig || "skull";
      state.strokeSide = b.strokeSide ?? 1;
      state.phiA = b.phiA ?? state.phiA;
      state.phiR = b.phiR ?? state.phiR;
      state.c = b.c ?? state.c;
      state.seatOffset = b.seatOffset ?? state.seatOffset;
      boatMetadata = {
        externalRef: b.externalRef ? clone(b.externalRef) : null,
        capacityStatus: b.capacityStatus,
        legacyRigTemplate: b.legacyRigTemplate ? clone(b.legacyRigTemplate) : null
      };
      state.seats = clone(b.seats);
      if (!state.seats.some((seat) => seat.id === state.editSeatId)) state.editSeatId = state.seats[0].id;
      state.referenceSeatId = state.seats[state.seats.length - 1].id;
      if (resolveCrew) resolveBoatCrew();
      else state.crew = [];
      for (const seat of state.seats) {
        ensureDraftState(seat.id);
        profileDraftDirty[seat.id] = false;
      }
      syncSeatCountControl();
      updateCoxControl();
      buildControls();
      updateRig();
      updateMode();
      render();
    }
    function migrateBoatRecord(source) {
      return migrateBoatToCurrent(source).value;
    }
    async function loadBoats() {
      try {
        await repositoryWrite(boatRepository, () => {
          const loaded = boatRepository.load();
          reportRepositoryLoad(loaded, "V2-Bootsdaten");
          if (loaded.ok && loaded.migration?.migrated) {
            boatRepository.commitPendingMigration({ expectedRevision: loaded.state.revision });
            announce(loaded.migration.recordIds.length ? `${loaded.migration.recordIds.length} Bootseinträge wurden atomar auf das aktuelle Sitzplatzschema migriert.` : "Der lokale Bootsverlauf wurde atomar initialisiert; die Bootswerte blieben unverändert.");
          }
          if (canMigrateLegacy(loaded) && boatRepository.list().length === 0) {
            let candidates = [];
            const rawV2 = storageAdapter.getItem("rudertrimm_boats_v2");
            if (rawV2) {
              const parsed = JSON.parse(rawV2);
              if (parsed?.kind !== "rudertrimm.boats" || !Array.isArray(parsed.items)) throw new TypeError("Alte Bootsdatei hat kein gültiges Format.");
              candidates = parsed.items.map((item) => migrateBoatToCurrent(item).value);
            } else if (storageAdapter.getItem("rudertrimm_boats") !== null) {
              candidates = adaptLegacyV1Boats(storageAdapter.getItem("rudertrimm_boats"), { seed: "stored-v1-boats" });
            } else {
              candidates = legacyV1WorkspaceCandidate()?.boats ?? [];
            }
            if (migrateCollection(boatRepository, "rudertrimm.boats", candidates, buildBoatDTO)) {
              announce(`${candidates.length} Bootseinträge wurden in den versionierten V2-Speicher übernommen.`);
            }
          }
        });
      } catch (error) {
        console.error("Bootsdaten konnten nicht geladen werden", error);
        announce("Bootsdaten waren ungültig und wurden nicht geladen. Alte Speicherstände bleiben unverändert erhalten.");
      }
      state.boats = boatRepository.list().map((record) => record.value);
    }
    function updateMasterBoatSelection() {
      const select = $("masterBoatSelect");
      if (!select) return;
      const selected = select.value ? boatRepository.select(select.value) : { ok: false };
      $("masterBoatUse").disabled = !selected.ok;
      $("masterBoatStatus").textContent = selected.ok ? `${cleanName(selected.record.value.name, "Boot")} · ${selected.record.value.seats.length} reale Plätze · Revision ${selected.record.revision} · noch nicht geladen` : `${boatRepository.list().length} gespeicherte Boote · Auswahl allein verändert den Arbeitsstand nicht.`;
    }
    function refreshMasterBoatSelect() {
      const select = $("masterBoatSelect");
      if (!select) return;
      const previous = select.value;
      const records = boatRepository.list();
      replaceOptions(select, "— Boot auswählen —", records.map((record) => ({
        value: record.id,
        label: `${cleanName(record.value.name, "Boot")} · ${record.value.seats.length} Plätze · ${record.value.rig === "skull" ? "Skull" : "Riemen"} · r${record.revision}`
      })));
      select.value = records.some((record) => record.id === previous) ? previous : "";
      updateMasterBoatSelection();
    }
    const HISTORY_OPERATION_LABELS = Object.freeze({
      baseline: "Ausgangsstand",
      create: "angelegt",
      update: "geändert",
      import: "importiert",
      migration: "migriert",
      delete: "datenschutzkonform gelöscht"
    });
    const HISTORY_SOURCE_LABELS = Object.freeze({
      "local-ui": "lokale Bedienung",
      "json-import": "JSON-Import",
      "efa-csv": "eFa-CSV",
      migration: "Migration",
      system: "System"
    });
    const HISTORY_FIELD_LABELS = Object.freeze({
      "": "Gesamtdatensatz",
      "/name": "Name",
      "/legLen": "Beinlänge (cm)",
      "/torsoLen": "Rumpflänge (cm)",
      "/wingspan": "Spannweite (cm)",
      "/SB": "Schulterbreite (cm)",
      "/weight": "Gewicht (kg)",
      "/stemmX": "Stemmbrett längs (cm)",
      "/preset": "Bootsklasse",
      "/rig": "Rigg",
      "/blade": "Blatt",
      "/strokeSide": "Riemenseite",
      "/phiA": "Auslage Rigg-Ziel (°)",
      "/phiR": "Rücklagewinkel (°)",
      "/c": "Rollsitz über Wasserlinie (cm)",
      "/seatOffset": "Sitzbezug (cm)",
      "/externalRef": "Externe Referenz",
      "/IH": "Innenhebel (cm)",
      "/DA": "Dollenabstand (cm)",
      "/L": "Ruderlänge (cm)",
      "/a": "Dollenhöhe (cm)",
      "/anlage": "Dollenneigung (°)",
      "/stemmW": "Stemmbrettwinkel (°)",
      "/rollL": "Rollbahnlänge (cm)",
      "/rueh": "Überhöhung (cm)",
      "/position": "Platznummer",
      "/role": "Rolle",
      "/label": "Platzbezeichnung",
      "/rowerRef": "Ruderer-Zuordnung",
      "/trimId": "Trimmprofil-ID"
    });
    function historyRepository() {
      return $("historyKind").value === "boats" ? boatRepository : rowerRepository;
    }
    function historyValueText(value, present = true) {
      if (!present) return "—";
      if (value === null) return "leer";
      if (typeof value === "boolean") return value ? "ja" : "nein";
      if (typeof value === "object") {
        const text = JSON.stringify(value);
        return [...text].length > 260 ? truncateCodePoints(text, 259) + "…" : text;
      }
      return String(value);
    }
    function unescapePointerSegment(value) {
      return String(value).replace(/~1/gu, "/").replace(/~0/gu, "~");
    }
    function historyPathLabel(path, beforeSnapshot = null, afterSnapshot = null) {
      if (Object.hasOwn(HISTORY_FIELD_LABELS, path)) return HISTORY_FIELD_LABELS[path];
      const stableSeat = path.match(/^\/seats\/@([^/]+)(?:\/([^/]+))?$/u);
      if (stableSeat) {
        const seatId = unescapePointerSegment(stableSeat[1]);
        const before = beforeSnapshot?.seats?.find?.((seat2) => seat2.id === seatId);
        const after = afterSnapshot?.seats?.find?.((seat2) => seat2.id === seatId);
        const seat = after ?? before;
        const place = seat?.position ? `Platz ${seat.position}` : "Bootsplatz";
        if (!stableSeat[2]) return place + " · Sitzdatensatz";
        const field = unescapePointerSegment(stableSeat[2]);
        return place + " · " + (HISTORY_FIELD_LABELS["/" + field] ?? field);
      }
      const indexedSeat = path.match(/^\/seats\/(\d+)\/([^/]+)$/u);
      if (indexedSeat) return "Platz " + (Number(indexedSeat[1]) + 1) + " · " + (HISTORY_FIELD_LABELS["/" + indexedSeat[2]] ?? indexedSeat[2]);
      return path || "Gesamtdatensatz";
    }
    function renderHistoryRevision() {
      const repository = historyRepository();
      const id = $("historyEntity").value;
      const revision2 = Number($("historyRevision").value);
      const entries = id ? historyForEntity(repository.history(), id) : [];
      const index = entries.findIndex((entry) => entry.revision === revision2);
      const current = entries[index];
      const diff = $("historyDiff");
      diff.replaceChildren();
      if (!current) {
        $("historyMeta").textContent = "Eintrag und Revision wählen.";
        return;
      }
      const date = new Date(current.changedAt).toLocaleString("de-DE");
      const floor = (repository.historyFloors?.() ?? []).find((item) => item.entityId === id);
      const retention = floor ? ` · Ältere Revisionen bis r${floor.throughRevision} wurden nach der Speichergrenze verworfen.` : "";
      $("historyMeta").textContent = "Revision " + current.revision + " · " + (HISTORY_OPERATION_LABELS[current.operation] ?? current.operation) + " · " + date + " · " + (HISTORY_SOURCE_LABELS[current.source] ?? current.source) + " · Grund: " + current.reason + retention;
      if (current.operation === "delete") {
        const note = document.createElement("div");
        const term = document.createElement("dt");
        term.textContent = "Datenschutz-Löschung";
        const value = document.createElement("dd");
        value.textContent = "Personenbezogene Altsnapshots wurden entfernt; nur dieses datenarme Ereignis bleibt.";
        value.style.gridColumn = "span 2";
        note.append(term, value);
        diff.appendChild(note);
        return;
      }
      const previous = index > 0 ? entries[index - 1] : null;
      const comparison = diffHistoryEntries(previous, current);
      if (comparison.changes.length === 0) {
        const note = document.createElement("div");
        const term = document.createElement("dt");
        term.textContent = "Keine Feldänderung";
        const value = document.createElement("dd");
        value.textContent = "Der gespeicherte Fachdatensatz ist gegenüber der Vorrevision unverändert.";
        value.style.gridColumn = "span 2";
        note.append(term, value);
        diff.appendChild(note);
        return;
      }
      for (const change of comparison.changes) {
        const row = document.createElement("div");
        const term = document.createElement("dt");
        term.textContent = historyPathLabel(change.path, previous?.snapshot, current.snapshot);
        const before = document.createElement("dd");
        before.textContent = "Alt: " + historyValueText(change.before, change.beforePresent);
        const after = document.createElement("dd");
        after.textContent = "Neu: " + historyValueText(change.after, change.afterPresent);
        row.append(term, before, after);
        diff.appendChild(row);
      }
    }
    function refreshHistoryRevisions() {
      const id = $("historyEntity").value;
      const previous = $("historyRevision").value;
      const entries = id ? historyForEntity(historyRepository().history(), id) : [];
      replaceOptions($("historyRevision"), "— Revision wählen —", entries.map((entry) => ({
        value: entry.revision,
        label: "r" + entry.revision + " · " + (HISTORY_OPERATION_LABELS[entry.operation] ?? entry.operation) + " · " + new Date(entry.changedAt).toLocaleString("de-DE")
      })));
      const keep = entries.some((entry) => String(entry.revision) === previous);
      $("historyRevision").value = keep ? previous : entries.at(-1) ? String(entries.at(-1).revision) : "";
      renderHistoryRevision();
    }
    function refreshHistoryEntities() {
      if (!$("historyEntity")) return;
      const repository = historyRepository();
      const previous = $("historyEntity").value;
      const current = new Map(repository.list().map((record) => [record.id, record]));
      const entries = repository.history();
      const ids = [.../* @__PURE__ */ new Set([...current.keys(), ...entries.map((entry) => entry.entityId)])];
      const items = ids.map((id) => {
        const record = current.get(id);
        const lastSnapshot = [...entries].reverse().find((entry) => entry.entityId === id && entry.snapshot)?.snapshot;
        const name = record?.value?.name ?? lastSnapshot?.name;
        return {
          value: id,
          label: name ? cleanName(name, "Eintrag") + " · r" + (record?.revision ?? entries.filter((entry) => entry.entityId === id).at(-1)?.revision ?? "?") : "Gelöschter Eintrag · " + id.slice(0, 8)
        };
      }).sort((left, right) => left.label.localeCompare(right.label, "de"));
      replaceOptions($("historyEntity"), "— Eintrag wählen —", items);
      $("historyEntity").value = items.some((item) => item.value === previous) ? previous : "";
      refreshHistoryRevisions();
    }
    $("historyKind").addEventListener("change", refreshHistoryEntities);
    $("historyEntity").addEventListener("change", refreshHistoryRevisions);
    $("historyRevision").addEventListener("change", renderHistoryRevision);
    $("historySection").addEventListener("toggle", () => {
      if ($("historySection").open) refreshHistoryEntities();
    });
    let efaCsvText = "";
    let efaParsed = null;
    let efaApprovedPreview = null;
    let efaApprovedContract = null;
    function resetEfaPreview(message = "CSV und Mapping prüfen, dann Vorschau erstellen.") {
      efaApprovedPreview = null;
      efaApprovedContract = null;
      $("efaCommit").disabled = true;
      $("efaPreviewResult").hidden = true;
      $("efaPreviewList").replaceChildren();
      $("efaImportStatus").textContent = message;
    }
    function setEfaMappingVisibility() {
      const person = $("efaEntityType").value === "person";
      const parts = person && $("efaPersonNameMode").value === "parts";
      for (const element of document.querySelectorAll(".efa-person-only")) element.hidden = !person;
      for (const element of document.querySelectorAll(".efa-boat-only")) element.hidden = person;
      for (const element of document.querySelectorAll(".efa-display-only")) element.hidden = !person || parts;
      for (const element of document.querySelectorAll(".efa-parts-only")) element.hidden = !parts;
    }
    function setEfaColumnOptions(selectId, headers, placeholder) {
      replaceOptions($(selectId), placeholder, headers.map((header) => ({ value: header, label: header })));
    }
    function populateEfaMapping() {
      if (!efaParsed) return;
      const headers = efaParsed.headers;
      for (const id of ["efaDisplayNameColumn", "efaFirstNameColumn", "efaLastNameColumn", "efaAffixColumn", "efaBoatNameColumn", "efaIdColumn"]) {
        setEfaColumnOptions(id, headers, id === "efaAffixColumn" || id === "efaIdColumn" ? "— nicht übernehmen —" : "— Spalte wählen —");
      }
      const entityType = $("efaEntityType").value;
      const suggested = suggestEfaHeaderMapping(headers, { entityType });
      if (entityType === "person") {
        const useParts = !suggested.displayName && suggested.firstName && suggested.lastName;
        $("efaPersonNameMode").value = useParts ? "parts" : "display";
        $("efaDisplayNameColumn").value = suggested.displayName ?? "";
        $("efaFirstNameColumn").value = suggested.firstName ?? "";
        $("efaLastNameColumn").value = suggested.lastName ?? "";
        $("efaAffixColumn").value = suggested.affix ?? "";
        $("efaIdColumn").value = suggested.id ?? "";
      } else {
        $("efaBoatNameColumn").value = suggested.name ?? "";
        $("efaIdColumn").value = suggested.id ?? "";
      }
      $("efaMapping").disabled = false;
      $("efaPreview").disabled = false;
      setEfaMappingVisibility();
      resetEfaPreview("CSV gelesen · Spaltenvorschläge bewusst prüfen. Noch nichts importiert.");
    }
    function parseSelectedEfaText() {
      if (!efaCsvText) {
        efaParsed = null;
        $("efaMapping").disabled = true;
        $("efaPreview").disabled = true;
        resetEfaPreview("Noch keine CSV gewählt.");
        return;
      }
      efaParsed = parseEfaCsv(efaCsvText, { delimiter: $("efaDelimiter").value });
      populateEfaMapping();
    }
    function currentEfaMapping() {
      const person = $("efaEntityType").value === "person";
      const mapping = {};
      if (person && $("efaPersonNameMode").value === "display") {
        if ($("efaDisplayNameColumn").value) mapping.displayName = $("efaDisplayNameColumn").value;
      } else if (person) {
        if ($("efaFirstNameColumn").value) mapping.firstName = $("efaFirstNameColumn").value;
        if ($("efaLastNameColumn").value) mapping.lastName = $("efaLastNameColumn").value;
        if ($("efaAffixColumn").value) mapping.affix = $("efaAffixColumn").value;
      } else if ($("efaBoatNameColumn").value) {
        mapping.name = $("efaBoatNameColumn").value;
      }
      if ($("efaIdColumn").value) mapping.id = $("efaIdColumn").value;
      return mapping;
    }
    function currentEfaContract() {
      const mapping = currentEfaMapping();
      return {
        text: efaCsvText,
        delimiter: $("efaDelimiter").value,
        entityType: $("efaEntityType").value,
        mapping,
        scope: mapping.id ? $("efaScope").value.trim() : null
      };
    }
    const EFA_CLASS_LABELS = Object.freeze({
      new: "neu · unvollständig",
      exactDuplicate: "exakte Dublette · übersprungen",
      nameReview: "Namensgleichheit · manuell prüfen",
      idConflict: "ID-Konflikt · gesperrt",
      invalid: "ungültige Zeile · gesperrt"
    });
    function renderEfaPreview(preview) {
      const list = $("efaPreviewList");
      list.replaceChildren();
      for (const item of preview.items) {
        const row = document.createElement("li");
        const name = document.createElement("span");
        name.textContent = "Zeile " + item.rowNumber + " · " + (item.candidate?.name ?? item.errors.join("; ") ?? "ungültig");
        const status = document.createElement("strong");
        status.textContent = EFA_CLASS_LABELS[item.classification] ?? item.classification;
        row.append(name, status);
        list.appendChild(row);
      }
      const blocked = preview.counts.invalid + preview.counts.nameReview + preview.counts.idConflict;
      const capacity = efaCandidateRepository.list().length + preview.counts.new;
      const maxRecords = efaCandidateRepository.constraints().maxRecords;
      const overCapacity = capacity > maxRecords;
      $("efaPreviewResult").hidden = false;
      $("efaImportStatus").textContent = "Vorschau · " + preview.counts.new + " neu · " + preview.counts.exactDuplicate + " Dublette(n) · " + blocked + " zu prüfen · alle gültigen Zeilen bleiben unvollständige Vormerkungen" + (overCapacity ? " · Speichergrenze überschritten" : "");
      $("efaCommit").disabled = preview.counts.new === 0 || blocked > 0 || overCapacity;
      $("efaPreviewResult").focus({ preventScroll: true });
    }
    function refreshEfaCandidateList() {
      const list = $("efaCandidateList");
      list.replaceChildren();
      const records = efaCandidateRepository.list();
      if (records.length === 0) {
        const empty = document.createElement("li");
        const text = document.createElement("span");
        text.textContent = "Keine lokalen eFa-Vormerkungen.";
        empty.appendChild(text);
        list.appendChild(empty);
        return;
      }
      for (const record of records) {
        const row = document.createElement("li");
        const text = document.createElement("span");
        const type = record.value.entityType === "person" ? "Person" : "Boot";
        const ref = record.value.externalRef ? "externe ID vorhanden" : "ohne externe ID";
        text.textContent = type + " · " + record.value.name + " · unvollständig · " + ref + " · r" + record.revision;
        const remove = document.createElement("button");
        remove.type = "button";
        remove.textContent = "Vormerkung löschen";
        remove.addEventListener("click", async () => {
          if (!confirm("Unvollständige eFa-Vormerkung „" + record.value.name + "“ lokal löschen?")) return;
          try {
            await repositoryWrite(efaCandidateRepository, () => {
              const fresh = efaCandidateRepository.select(record.id);
              if (!fresh.ok || fresh.record.revision !== record.revision) {
                const error = new Error("Die Vormerkung wurde inzwischen geändert. Bitte Liste neu prüfen.");
                error.code = "candidate-stale";
                throw error;
              }
              return efaCandidateRepository.delete(record.id, {
                expectedRevision: efaCandidateRepository.snapshot().revision,
                expectedRecordRevision: record.revision,
                audit: { source: "local-ui", reason: "privacy-delete" }
              });
            });
            refreshEfaCandidateList();
            announce("eFa-Vormerkung samt personenbezogenen Altsnapshots lokal gelöscht.");
          } catch (error) {
            reportError(error, "eFa-Vormerkung konnte nicht gelöscht werden");
          }
        });
        row.append(text, remove);
        list.appendChild(row);
      }
    }
    async function loadEfaCandidates() {
      try {
        await repositoryWrite(efaCandidateRepository, () => {
          const loaded = efaCandidateRepository.load();
          reportRepositoryLoad(loaded, "eFa-Vormerkliste");
          if (loaded.ok && loaded.migration?.migrated) {
            efaCandidateRepository.commitPendingMigration({ expectedRevision: loaded.state.revision });
            announce("Die lokale eFa-Vormerkliste wurde atomar auf den Revisionsvertrag aktualisiert.");
          }
        });
      } catch (error) {
        reportError(error, "eFa-Vormerkliste konnte nicht sicher geladen werden");
      }
      refreshEfaCandidateList();
    }
    $("efaFile").addEventListener("change", async (event) => {
      const file = event.target.files?.[0];
      if (!file) {
        efaCsvText = "";
        parseSelectedEfaText();
        return;
      }
      try {
        if (file.size > EFA_CSV_LIMITS.maxBytes) throw new RangeError("CSV überschreitet die feste Grenze von 1 MiB.");
        const bytes = await file.arrayBuffer();
        efaCsvText = new TextDecoder("utf-8", { fatal: true }).decode(bytes);
        parseSelectedEfaText();
      } catch (error) {
        efaCsvText = "";
        efaParsed = null;
        $("efaMapping").disabled = true;
        $("efaPreview").disabled = true;
        resetEfaPreview("CSV wurde nicht übernommen · erwartet wird eine gültige UTF-8-Datei.");
        reportError(error, "eFa-CSV konnte nicht sicher gelesen werden");
      }
    });
    $("efaDelimiter").addEventListener("change", () => {
      try {
        parseSelectedEfaText();
      } catch (error) {
        resetEfaPreview("Trennzeichen passt nicht zur CSV · nichts importiert.");
        reportError(error, "eFa-CSV konnte nicht geparst werden");
      }
    });
    $("efaEntityType").addEventListener("change", () => {
      try {
        if (efaParsed) populateEfaMapping();
        else setEfaMappingVisibility();
      } catch (error) {
        reportError(error, "eFa-Mapping konnte nicht vorbereitet werden");
      }
    });
    $("efaPersonNameMode").addEventListener("change", () => {
      setEfaMappingVisibility();
      resetEfaPreview("Namensaufbau geändert · Vorschau erneut erstellen.");
    });
    $("efaMapping").addEventListener("input", () => {
      resetEfaPreview("Mapping geändert · Vorschau erneut erstellen.");
    });
    $("efaPreview").addEventListener("click", async () => {
      try {
        clearErrorStatus();
        const contract = currentEfaContract();
        const preview = await previewEfaCsv({
          ...contract,
          existingCandidates: efaCandidateRepository.list().map((record) => record.value)
        });
        efaApprovedPreview = preview;
        efaApprovedContract = { ...contract, mapping: { ...contract.mapping } };
        renderEfaPreview(preview);
      } catch (error) {
        resetEfaPreview("Vorschau fehlgeschlagen · Mapping, Scope und CSV korrigieren.");
        reportError(error, "eFa-Importvorschau konnte nicht erstellt werden");
      }
    });
    $("efaCommit").addEventListener("click", async () => {
      if (!efaApprovedPreview || !efaApprovedContract) return;
      const approved = efaApprovedPreview;
      const contract = { ...efaApprovedContract, mapping: { ...efaApprovedContract.mapping } };
      try {
        const result = await repositoryWrite(efaCandidateRepository, async () => {
          const fresh = await previewEfaCsv({
            ...contract,
            existingCandidates: efaCandidateRepository.list().map((record) => record.value)
          });
          if (fresh.planFingerprint !== approved.planFingerprint) {
            const error = new Error("Die bestätigte eFa-Vorschau ist nicht mehr aktuell. Bitte neu prüfen.");
            error.code = "efa-preview-changed";
            throw error;
          }
          if (fresh.counts.invalid + fresh.counts.nameReview + fresh.counts.idConflict > 0) {
            const error = new Error("Die frische Vorschau enthält Konflikte und bleibt gesperrt.");
            error.code = "efa-preview-blocked";
            throw error;
          }
          const candidates = fresh.items.filter((item) => item.classification === "new").map((item) => item.candidate);
          if (candidates.length === 0) return Object.freeze({ added: 0, cancelled: false });
          const exchange = collectionExchange(
            STORAGE_KINDS.efaCandidates,
            candidates,
            (value) => value,
            { clock: () => new Date(fresh.importedAt) }
          );
          return mergeCollectionImport({
            repository: efaCandidateRepository,
            text: JSON.stringify(exchange),
            validateRecord: validateEfaCandidate,
            builder: (value) => value,
            approve: (preview) => preview.incoming === candidates.length && preview.added === candidates.length,
            audit: { source: "efa-csv", reason: "efa-csv-import" }
          });
        });
        const imported = result.added;
        efaCsvText = "";
        efaParsed = null;
        $("efaFile").value = "";
        $("efaMapping").disabled = true;
        $("efaPreview").disabled = true;
        resetEfaPreview(imported + " unvollständige eFa-Vormerkung(en) lokal gespeichert · keine Zuordnung und kein Sync.");
        refreshEfaCandidateList();
        announce(imported + " eFa-Vormerkung(en) lokal gespeichert; Pflichtdaten müssen bewusst ergänzt werden.");
      } catch (error) {
        resetEfaPreview("Import nicht ausgeführt · Vorschau erneut prüfen.");
        reportError(error, "eFa-Vormerkungen konnten nicht atomar importiert werden");
      }
    });
    function refreshBoatSelect(selectedId = "", adoptRevision = false) {
      const records = boatRepository.list();
      state.boats = records.map((record) => record.value);
      replaceOptions($("boatSelect"), "— gespeichertes Boot laden —", records.map((record) => ({
        value: record.id,
        label: `${cleanName(record.value.name, "Boot")} · ${record.value.rig === "skull" ? "Skull" : "Riemen"} · r${record.revision} · ${record.id.slice(0, 6)}`
      })));
      const selection = selectedId ? boatRepository.select(selectedId) : { ok: false };
      $("boatSelect").value = selection.ok ? selectedId : "";
      $("boatUpdate").disabled = !selection.ok;
      $("boatDelete").disabled = !selection.ok;
      if (!selection.ok) {
        $("boatSelect").title = "";
        selectedBoatId = "";
        selectedBoatRevision = null;
      } else if (adoptRevision) {
        selectedBoatId = selection.record.id;
        selectedBoatRevision = selection.record.revision;
      }
      refreshMasterBoatSelect();
      refreshHistoryEntities();
    }
    function loadStoredBoatIntoWorkspace(id) {
      const selected = boatRepository.select(id);
      $("boatUpdate").disabled = !selected.ok;
      $("boatDelete").disabled = !selected.ok;
      if (!selected.ok) {
        refreshBoatSelect(selectedBoatId);
        announce("Das ausgewählte Boot ist nicht mehr verfügbar. Die aktuellen Arbeitswerte blieben unverändert.");
        return false;
      }
      if (dirty && !confirmOverwrite("Das gespeicherte Boot ersetzt Boot, Sitzwerte und gespeicherte Belegungsreferenzen des aktuellen Arbeitsstands")) {
        refreshBoatSelect(selectedBoatId);
        return false;
      }
      selectedBoatId = selected.record.id;
      selectedBoatRevision = selected.record.revision;
      applyBoat(selected.record.value);
      endDemoSession();
      refreshDBSelect("", true, state.editSeatId);
      setDirty(true);
      refreshBoatSelect(selected.record.id, true);
      const unresolved = state.seats.filter((seat) => seat.rowerRef && !assignmentFor(seat.id)).length;
      announce(`${cleanName(selected.record.value.name, "Boot")} mit realen Plätzen geladen · Arbeitsstand noch speichern${unresolved ? ` · ${unresolved} Profilreferenz(en) müssen bewusst neu zugeordnet werden` : ""}.`);
      return true;
    }
    $("boatSelect").addEventListener("change", (e) => {
      e.target.title = "";
      loadStoredBoatIntoWorkspace(e.target.value);
    });
    $("masterBoatSelect").addEventListener("change", updateMasterBoatSelection);
    $("masterBoatUse").addEventListener("click", () => {
      if (loadStoredBoatIntoWorkspace($("masterBoatSelect").value)) $("boatSelect").focus();
    });
    $("masterBoatManage").addEventListener("click", () => {
      setPresentationMode("details");
      $("boatSelect").focus();
      $("boatSelect").scrollIntoView({ block: "center" });
    });
    $("boatSaveAs").addEventListener("click", async () => {
      try {
        const operation = { changeVersion: workspaceChangeVersion, selectedId: $("boatSelect").value, selectedBoatId, selectedBoatRevision };
        canonicalizeBoatName();
        const value = boatOf();
        const record = await repositoryWrite(boatRepository, () => repositoryWrite(rowerRepository, () => {
          assertNoRetiredRowerReferences(value);
          return boatRepository.create(value, { expectedRevision: boatRepository.snapshot().revision });
        }));
        const unchanged = workspaceChangeVersion === operation.changeVersion && $("boatSelect").value === operation.selectedId && selectedBoatId === operation.selectedBoatId && selectedBoatRevision === operation.selectedBoatRevision;
        refreshBoatSelect(unchanged ? record.id : $("boatSelect").value, unchanged);
        announce(unchanged ? "Boot versioniert gespeichert." : "Boot gespeichert; die inzwischen aktive Auswahl blieb unverändert.");
      } catch (error) {
        reportError(error, "Boot konnte nicht gespeichert werden");
      }
    });
    $("boatUpdate").addEventListener("click", async () => {
      const id = $("boatSelect").value, selected = boatRepository.select(id);
      if (!selected.ok || id !== selectedBoatId || selectedBoatRevision === null) return;
      const observedRecordRevision = selectedBoatRevision;
      try {
        const operation = { changeVersion: workspaceChangeVersion, selectedId: id, selectedBoatId, selectedBoatRevision };
        canonicalizeBoatName();
        const value = boatOf();
        const updated = await repositoryWrite(boatRepository, () => repositoryWrite(rowerRepository, () => {
          assertNoRetiredRowerReferences(value);
          return boatRepository.update(id, value, {
            expectedRevision: boatRepository.snapshot().revision,
            expectedRecordRevision: observedRecordRevision
          });
        }));
        const unchanged = workspaceChangeVersion === operation.changeVersion && $("boatSelect").value === operation.selectedId && selectedBoatId === operation.selectedBoatId && selectedBoatRevision === operation.selectedBoatRevision;
        refreshBoatSelect(unchanged ? updated.id : $("boatSelect").value, unchanged);
        announce(unchanged ? "Boot als neue Revision gespeichert." : "Boot aktualisiert; die inzwischen aktive Auswahl blieb unverändert.");
      } catch (error) {
        reportError(error, "Boot konnte nicht überschrieben werden");
      }
    });
    $("boatDelete").addEventListener("click", async () => {
      const id = $("boatSelect").value, selected = boatRepository.select(id);
      if (!selected.ok || id !== selectedBoatId || selectedBoatRevision === null) return;
      const observedRecordRevision = selectedBoatRevision;
      const name = cleanName(selected.record.value.name, "Boot");
      if (confirm(`„${name}“ wirklich löschen?`)) {
        try {
          const operation = { changeVersion: workspaceChangeVersion, selectedId: id, selectedBoatId, selectedBoatRevision };
          await repositoryWrite(boatRepository, () => boatRepository.delete(id, {
            expectedRevision: boatRepository.snapshot().revision,
            expectedRecordRevision: observedRecordRevision
          }));
          const unchanged = workspaceChangeVersion === operation.changeVersion && $("boatSelect").value === operation.selectedId && selectedBoatId === operation.selectedBoatId && selectedBoatRevision === operation.selectedBoatRevision;
          refreshBoatSelect(unchanged ? "" : $("boatSelect").value, false);
          announce(unchanged ? `${name} gelöscht.` : `${name} gelöscht; die inzwischen aktive Auswahl blieb unverändert.`);
        } catch (error) {
          reportError(error, "Boot konnte nicht gelöscht werden");
        }
      }
    });
    $("boatExport").addEventListener("click", () => downloadJson(boatRepository.exportEnvelope(), "rudertrimm-v2-boote.json"));
    $("boatImport").addEventListener("click", () => $("boatFile").click());
    $("boatFile").addEventListener("change", async (e) => {
      const f = e.target.files[0];
      e.target.value = "";
      if (!f) return;
      try {
        if (f.size > 1e6) throw new RangeError("Datei ist größer als 1 MB");
        const text = await f.text();
        const importOptions = {
          repository: boatRepository,
          text,
          validateRecord: validateBoat,
          builder: buildBoatDTO,
          migrateRecord: migrateBoatToCurrent,
          migrateLegacyImport: legacyCollectionImportMigrator(migrateBoatToCurrent),
          transformIncomingValue: detachImportedBoatRowerReferences
        };
        const approvedPreview = previewCollectionImport(importOptions);
        if (!confirmCollectionImport(approvedPreview)) {
          announce("Bootsimport abgebrochen.");
          return;
        }
        const result = await repositoryWrite(boatRepository, () => mergeCollectionImport({
          ...importOptions,
          approve: (fresh) => approveFreshImportPreview(approvedPreview, fresh)
        }));
        refreshBoatSelect();
        announce(`${result.added} Bootseinträge atomar importiert${result.duplicatesSkipped ? ` · ${result.duplicatesSkipped} Dubletten übersprungen` : ""}${result.migrated ? ` · ${result.migrated} Datensätze auf das aktuelle Schema migriert` : ""}${result.remapped ? ` · ${result.remapped} ID-Konflikte aufgelöst` : ""}${result.referencesDetached ? ` · ${result.referencesDetached} unsichere Profilreferenzen gelöst` : ""}.`);
      } catch (error) {
        reportError(error, "Bootsimport abgelehnt");
      }
    });
    function derived(r, phiAeff) {
      const s = state;
      r = r || activeRower();
      const phiA = phiAeff ?? s.phiA;
      return derivedGeometry({
        rig: s.rig,
        DA: r.DA,
        IH: r.IH,
        L: r.L,
        d: r.d,
        a: r.a,
        phiA,
        phiR: s.phiR,
        t: s.t,
        c: s.c,
        kg: s.kg
      });
    }
    function band(v, gLo, gHi, yPad) {
      if (v >= gLo && v <= gHi) return "ok";
      if (v >= gLo - yPad && v <= gHi + yPad) return "warn";
      return "bad";
    }
    function SEG(r) {
      return deriveBodySegments(r);
    }
    function easeFromFloor(value, floor, width) {
      if (!(width > 0)) return Math.max(value, floor);
      if (value <= floor) return floor;
      if (value >= floor + width) return value;
      const u = (value - floor) / width;
      return floor + width * (2 * u * u - u * u * u);
    }
    function featherG(tPct, rec) {
      if (!rec) return 0;
      if (tPct > 96) return 0;
      if (tPct > 84) return (96 - tPct) / 12;
      if (tPct > 26) return 1;
      if (tPct > 10) return (tPct - 10) / 16;
      return 0;
    }
    function bladeLiftF(tPct, rec) {
      if (!rec) return -8;
      if (tPct > 96) return lerp(13, -8, smooth((tPct - 96) / 4));
      if (tPct > 8) return 13;
      return lerp(-8, 13, smooth(tPct / 8));
    }
    function ik2(A, B, l1, l2, pickLower) {
      let dx = B.x - A.x, dy = B.y - A.y, D = Math.hypot(dx, dy);
      const Dc = clamp2(D, Math.abs(l1 - l2) + 0.5, l1 + l2 - 0.2);
      if (D < 1e-6) {
        dx = 1;
        dy = 0;
        D = 1;
      }
      const ux = dx / D, uy = dy / D;
      const a = (l1 * l1 - l2 * l2 + Dc * Dc) / (2 * Dc);
      const h = Math.sqrt(Math.max(0, l1 * l1 - a * a));
      const J1 = { x: A.x + ux * a - uy * h, y: A.y + uy * a + ux * h };
      const J2 = { x: A.x + ux * a + uy * h, y: A.y + uy * a - ux * h };
      const J = pickLower ? J1.y < J2.y ? J1 : J2 : J1.y > J2.y ? J1 : J2;
      const ang = Math.acos(clamp2((l1 * l1 + l2 * l2 - Dc * Dc) / (2 * l1 * l2), -1, 1));
      return { J, ang };
    }
    function solveBody(dv, tPct, rec, r, phiAeff) {
      const s = state, seg = SEG(r);
      const t = clamp2(tPct, 0, 100) / 100;
      const wl = s.kg * 0.1, pinY = s.c + r.a;
      const g = featherG(tPct, rec), lift = bladeLiftF(tPct, rec);
      const phiA = phiAeff ?? s.phiA;
      const SW = phiA + s.phiR, theta = rad(phiA - t * SW);
      const foot = { x: r.stemmX, y: s.c - 18 };
      const hipY = s.c + 4, dyLeg = hipY - foot.y;
      const sinB = clamp2((pinY - wl - lift) / dv.outb, -0.5, 1);
      const hand = { x: dv.inb * Math.sin(theta), y: pinY + dv.inb * sinB };
      const knee0 = lerp(rad(CATCH_MODEL.kneeDeg), rad(172), smooth(t / 0.97));
      const lam0 = rec ? rad(lerp(CATCH_MODEL.leanDeg, -23, smooth((t - 0.35) / 0.65))) : rad(lerp(CATCH_MODEL.leanDeg, -23, smooth((t - 0.18) / 0.72)));
      const legD = (a) => Math.sqrt(seg.OS ** 2 + seg.US ** 2 - 2 * seg.OS * seg.US * Math.cos(a));
      const hipOf = (a) => foot.x - Math.sqrt(Math.max(1, legD(a) ** 2 - dyLeg * dyLeg));
      const hip0x = hipOf(knee0);
      const DAr = dv.DAr;
      const zW = DAr - dv.inb * Math.cos(theta);
      const rhoArm = dv.skull ? 0 : rad(CATCH_MODEL.sweepShoulderRotationDeg) * easeFromFloor(deg(theta) / Math.max(dv.phiA, 1), -0.5, 0.1);
      const dzArm = dv.skull ? Math.abs(zW - r.SB / 2) : Math.abs(zW + Math.cos(rhoArm) * r.SB / 2);
      const armLen3 = seg.OA + seg.UA;
      const armLen = Math.sqrt(Math.max((armLen3 * 0.55) ** 2, armLen3 * armLen3 - dzArm * dzArm));
      const flexOf = (l) => seg.T * 0.42 * smooth(l / rad(CATCH_MODEL.leanDeg));
      const shOf = (hx, l) => {
        const fx = flexOf(l);
        return { x: hx + seg.T * Math.sin(l) + 0.35 * fx, y: hipY + seg.T * Math.cos(l) - 0.9 * fx };
      };
      const armH = (sy) => {
        const verticalReach = dv.skull ? Math.max(Math.abs(sy - (hand.y - 1.5)), Math.abs(sy - (hand.y + 1.5))) : Math.abs(sy - hand.y);
        const q = armLen * armLen - verticalReach * verticalReach;
        return q <= 4 ? null : Math.sqrt(q);
      };
      const anatomyMin = hipOf(rad(172)), anatomyMax = hipOf(rad(45));
      const seatReferenceOffset = 5;
      const trackMin = r.rueh - r.rollL + seatReferenceOffset, trackMax = r.rueh + seatReferenceOffset;
      const rawHipMin = Math.max(anatomyMin, trackMin), rawHipMax = Math.min(anatomyMax, trackMax);
      const hipMin = Math.min(rawHipMin, rawHipMax), hipMax = Math.max(rawHipMin, rawHipMax);
      const hipTarget = hip0x;
      const trackEase = Math.min(4, Math.max(0, (hipMax - hipMin) / 4));
      const lowerEased = easeFromFloor(hipTarget, hipMin, trackEase);
      const hipX = -easeFromFloor(-lowerEased, -hipMax, trackEase);
      const trackLimited = rawHipMin > rawHipMax || Math.abs(hipX - hipTarget) > 0.05;
      const D = Math.hypot(foot.x - hipX, dyLeg);
      const knee = Math.acos(clamp2((seg.OS ** 2 + seg.US ** 2 - D * D) / (2 * seg.OS * seg.US), -1, 1));
      const hip = { x: hipX, y: hipY };
      const F = (l) => {
        const sh2 = shOf(hipX, l);
        const ah = armH(sh2.y);
        return ah === null ? null : sh2.x + ah - hand.x;
      };
      let lam = lam0, elbowBent = false, overreach = false;
      let lo = rad(-38), hi = rad(50), flo = F(lo), fhi = F(hi);
      if (fhi === null || fhi < 0) {
        lam = hi;
        overreach = true;
      } else {
        let lamStar;
        if (flo !== null && flo > 0) {
          const epsilon = rad(0.5), fNext = F(lo + epsilon);
          const slope = fNext === null ? 1 : (fNext - flo) / epsilon;
          lamStar = lo - flo / Math.max(slope, 0.05);
        } else {
          for (let i = 0; i < 28; i++) {
            const mid = (lo + hi) / 2, fm = F(mid);
            if (fm === null || fm > 0) hi = mid;
            else lo = mid;
          }
          lamStar = (lo + hi) / 2;
        }
        const d = lam0 - lamStar;
        const gate = smooth((t - 0.35) / 0.3);
        const bend = smooth((d - rad(3)) / rad(22)) * gate;
        elbowBent = bend > 0.02;
        lam = clamp2(lerp(lamStar, lam0, bend), rad(-35), rad(50));
      }
      const sh = shOf(hipX, lam);
      const flex = flexOf(lam);
      const kneeP = ik2(hip, foot, seg.OS, seg.US, false).J;
      const c7 = { x: hipX + seg.T * Math.sin(lam), y: hipY + seg.T * Math.cos(lam) };
      const tilt = lam + rad(22) * smooth(lam / rad(CATCH_MODEL.leanDeg));
      const headR = seg.HEAD * 0.42;
      const head = { x: sh.x + (seg.NECK + headR) * Math.sin(tilt), y: sh.y + (seg.NECK + headR) * Math.cos(tilt), r: headR };
      const reach3 = Math.hypot(sh.x - hand.x, sh.y - hand.y, dzArm);
      if (reach3 > armLen3 + 0.05 || reach3 < Math.abs(seg.OA - seg.UA) - 0.05) overreach = true;
      const armAng = deg(Math.atan2(sh.y - hand.y, hand.x - sh.x));
      return {
        seg,
        foot,
        hip,
        kneeP,
        sh,
        c7,
        hand,
        head,
        r,
        SB: r.SB,
        flex,
        knee: deg(knee),
        lam: deg(lam),
        armAng,
        progress: t,
        theta: deg(theta),
        g,
        lift,
        wl,
        pinY,
        sinB,
        overreach,
        trackLimited,
        reach3
      };
    }
    function solveArms(dv, b, r) {
      const s = state, seg = b.seg, SB = r.SB;
      const th = rad(b.theta), DAr = dv.DAr;
      const w = smooth((b.progress - 0.4) / 0.6);
      const OA = seg.OA, UA = seg.UA, L3 = OA + UA;
      const mk = (S, targetW, zside) => {
        let W = targetW;
        const dx = W.x - S.x, dy = W.y - S.y, dz = W.z - S.z;
        const D3 = Math.hypot(dx, dy, dz);
        const u = { x: dx / (D3 || 1), y: dy / (D3 || 1), z: dz / (D3 || 1) };
        let E, ang, reachable = true;
        if (D3 >= L3) {
          reachable = D3 <= L3 + 0.05;
          W = { x: S.x + u.x * L3, y: S.y + u.y * L3, z: S.z + u.z * L3 };
          E = { x: S.x + u.x * OA, y: S.y + u.y * OA, z: S.z + u.z * OA };
          ang = 180;
        } else {
          reachable = D3 >= Math.abs(OA - UA) - 0.05;
          const a = (OA * OA - UA * UA + D3 * D3) / (2 * D3);
          const h = Math.sqrt(Math.max(0, OA * OA - a * a));
          let e = { x: -0.85 * w, y: -(1 - w) - 0.2 * w, z: 0.5 * w * zside };
          const dot = e.x * u.x + e.y * u.y + e.z * u.z;
          e = { x: e.x - dot * u.x, y: e.y - dot * u.y, z: e.z - dot * u.z };
          const n = Math.hypot(e.x, e.y, e.z) || 1;
          E = { x: S.x + u.x * a + e.x / n * h, y: S.y + u.y * a + e.y / n * h, z: S.z + u.z * a + e.z / n * h };
          ang = deg(Math.acos(clamp2((OA * OA + UA * UA - D3 * D3) / (2 * OA * UA), -1, 1)));
        }
        return { S, E, W, targetW, ang, reachable };
      };
      const arms = [];
      if (dv.skull) {
        for (const sd of [1, -1]) {
          const S = { x: b.sh.x, y: b.sh.y, z: sd * SB / 2 };
          const W = { x: b.hand.x, y: b.hand.y + (sd < 0 ? 1.5 : -1.5), z: sd * (DAr - dv.inb * Math.cos(th)) };
          arms.push(mk(S, W, sd));
        }
      } else {
        const rho = rad(CATCH_MODEL.sweepShoulderRotationDeg) * easeFromFloor(b.theta / Math.max(dv.phiA, 1), -0.5, 0.1);
        const zC = Math.cos(rho) * SB / 2, xR = Math.sin(rho) * SB / 2;
        const W1 = { x: b.hand.x, y: b.hand.y, z: DAr - dv.inb * Math.cos(th) };
        const P = { x: 0, y: b.pinY, z: DAr };
        const dW = { x: P.x - W1.x, y: P.y - W1.y, z: P.z - W1.z };
        const dL = Math.hypot(dW.x, dW.y, dW.z) || 1;
        const W2 = { x: W1.x + dW.x / dL * r.handGap, y: W1.y + dW.y / dL * r.handGap, z: W1.z + dW.z / dL * r.handGap };
        arms.push(mk({ x: b.sh.x, y: b.sh.y, z: -zC }, W1, -1));
        arms.push(mk({ x: b.sh.x - 2 * xR, y: b.sh.y, z: +zC }, W2, 1));
      }
      return arms;
    }
    function bodyRefs(dv, r) {
      const phiA = dv.phiA ?? state.phiA;
      const b0 = solveBody(dv, 0, false, r, phiA), b1 = solveBody(dv, 100, false, r, phiA);
      const t90 = clamp2(phiA / (phiA + state.phiR) * 100, 0, 100);
      const b90 = solveBody(dv, t90, false, r, phiA);
      const span = b0.hip.x - b1.hip.x;
      const rollAt = (b) => span > 1 ? clamp2((b0.hip.x - b.hip.x) / span * 100, 0, 100) : 0;
      return { b0, b1, b90, t90, rollAt };
    }
    const CATCH_CACHE_LIMIT = 64;
    const catchSolutionCache = /* @__PURE__ */ new Map();
    function catchCacheKey(dv, r) {
      return [
        state.rig,
        dv.DA,
        dv.inb,
        dv.outb,
        r.a,
        state.c,
        state.kg,
        r.legLen,
        r.torsoLen,
        r.wingspan,
        r.SB,
        r.handGap,
        r.stemmX,
        r.rollL,
        r.rueh
      ].join("|");
    }
    function resolveCatchAngle(r) {
      const baseDv = derived(r);
      const key = catchCacheKey(baseDv, r);
      const cached = catchSolutionCache.get(key);
      if (cached) return cached;
      const natural = solveNaturalCatchAngle({
        rig: state.rig,
        DA: baseDv.DA,
        inboardFromPin: baseDv.inb,
        outboardFromPin: baseDv.outb,
        a: r.a,
        c: state.c,
        kg: state.kg,
        rower: r
      });
      const isReachable = (angle) => {
        const candidateDv = derived(r, angle);
        const body = solveBody(candidateDv, 0, false, r, angle);
        if (body.overreach) return false;
        return solveArms(candidateDv, body, r).every((arm) => arm.reachable);
      };
      let poseAngleDeg = natural.angleDeg;
      let reachable = isReachable(poseAngleDeg);
      let limitedByReach = false;
      if (natural.bracketed && !reachable && natural.angleDeg > CATCH_MODEL.search.minDeg) {
        const fallback = findHighestReachableAngle({
          minDeg: CATCH_MODEL.search.minDeg,
          maxDeg: natural.angleDeg,
          stepDeg: CATCH_MODEL.search.stepDeg,
          isReachable
        });
        poseAngleDeg = fallback.angleDeg;
        reachable = fallback.reachable;
        limitedByReach = fallback.limited;
      }
      const naturalResolved = natural.bracketed;
      const result = Object.freeze({
        poseAngleDeg,
        actualAngleDeg: naturalResolved && reachable ? poseAngleDeg : null,
        naturalAngleDeg: naturalResolved ? natural.angleDeg : null,
        naturalCandidateAngleDeg: natural.angleDeg,
        naturalResolved,
        reachable,
        limitedByReach,
        trackLimited: natural.trackLimited,
        residualCm: natural.residualCm,
        evaluations: natural.evaluations,
        modelStatus: "needsCalibration"
      });
      catchSolutionCache.set(key, result);
      if (catchSolutionCache.size > CATCH_CACHE_LIMIT) catchSolutionCache.delete(catchSolutionCache.keys().next().value);
      return result;
    }
    const STTXT = { ok: "Im Zielkorridor", warn: "Prüfen", bad: "Außerhalb", info: "Modellwert" };
    const STICON = { ok: "✓", warn: "!", bad: "×", info: "i" };
    function statusCardElement(card) {
      const root = document.createElement("article");
      root.className = `scard ${card.st}`;
      const title = document.createElement("div");
      title.className = "ico";
      const icon = document.createElement("span");
      icon.className = "status-symbol";
      icon.textContent = STICON[card.st] || "i";
      icon.setAttribute("aria-hidden", "true");
      title.append(icon, document.createTextNode(card.n));
      const value = document.createElement("div");
      value.className = "big";
      value.textContent = card.v;
      const stateLabel = document.createElement("div");
      stateLabel.className = "st";
      stateLabel.textContent = card.statusText ?? STTXT[card.st] ?? "Prüfwert";
      const range = document.createElement("div");
      range.className = "rng";
      range.textContent = card.rng;
      root.append(title, value, stateLabel, range);
      return root;
    }
    function chipElement(chip) {
      const root = document.createElement("div");
      root.className = `chip ${chip.st || ""} ${chip.compact ? "compact-critical" : "details-only"}`.trim();
      if (chip.st) {
        const icon = document.createElement("span");
        icon.className = "status-symbol";
        icon.textContent = STICON[chip.st] || "i";
        icon.setAttribute("aria-hidden", "true");
        root.appendChild(icon);
      }
      root.append(document.createTextNode(`${chip.n} `));
      const value = document.createElement("b");
      value.textContent = chip.v;
      root.appendChild(value);
      return root;
    }
    function actualCatchStatusCard(dv, targetDeg, catchSolution) {
      const nominalRange = dv.skull ? "65 – 75°" : "~54° (Riemen)";
      if (catchSolution.naturalResolved === false) {
        return {
          n: "Ist-Auslagewinkel",
          v: "nicht bestimmbar",
          st: "bad",
          rng: `Natural-Catch ohne Lösung · Suchgrenze ${fmt(catchSolution.naturalCandidateAngleDeg)}° · Restfehler ${fmt(catchSolution.residualCm)} cm · Rigg-Ziel ${fmt(targetDeg)}°`
        };
      }
      if (catchSolution.reachable === false) {
        return {
          n: "Ist-Auslagewinkel",
          v: "nicht erreichbar",
          st: "bad",
          rng: `Natural-Catch ${fmt(catchSolution.naturalAngleDeg)}° · Rigg-Ziel ${fmt(targetDeg)}° · 3D-Prüfung fehlgeschlagen`
        };
      }
      const limited = catchSolution.limitedByReach;
      return {
        n: "Ist-Auslagewinkel",
        v: fmt(dv.phiA) + "°" + (limited ? " ⚠" : ""),
        st: limited ? "warn" : "info",
        rng: `${nominalRange} · Rigg-Ziel ${fmt(targetDeg)}° · aus Fa + Körper (58°/16°-Prüfmodell, unkalibriert)`
      };
    }
    function renderStatus(dv, refs, r, catchSolution, seats = []) {
      const s = state;
      const uLo = dv.skull ? 18 : 30, uHi = dv.skull ? 23 : 34;
      const dIH = r.IH - dv.IHsoll;
      const aNorm = dv.skull ? 15 : 17;
      let dolleCard;
      if (s.mode === "werkstatt") {
        const off = s.heightRef === "schiene" ? s.seatOffset : 0;
        dolleCard = {
          n: s.heightRef === "schiene" ? "Dollenhöhe ü. Schiene" : "Dollenhöhe ü. Rollsitz",
          v: fmt(r.a + off) + " cm",
          st: band(r.a, aNorm - 2, aNorm + 2, 2),
          rng: `Norm ${fmt(aNorm + off)} ± 2 cm`
        };
      } else {
        dolleCard = { n: "Dollenhöhe ü. W.", v: fmt(dv.dWL) + " cm", st: band(dv.dWL, 22, 26, 2), rng: "22 – 26 cm" };
      }
      const roll90 = refs.rollAt(refs.b90);
      const kneeState = band(refs.b90.knee, 160, 170, 6), rollState = band(roll90, 70, 80, 5);
      const stemmState = kneeState === "ok" && rollState === "ok" ? "ok" : kneeState === "bad" || rollState === "bad" ? "bad" : "warn";
      const cards = [
        { n: "Übergriff", v: fmt(dv.U) + " cm", st: band(dv.U, uLo, uHi, 2), rng: `${uLo} – ${uHi} cm` },
        actualCatchStatusCard(dv, s.phiA, catchSolution),
        { n: "Schlagweite", v: fmt(dv.SW) + "°", st: band(dv.SW, dv.skull ? 105 : 85, dv.skull ? 115 : 95, 5), rng: dv.skull ? "110° (Skull)" : "90° (Riemen)" },
        dolleCard,
        { n: "Dollen-Neigung", v: fmt(r.anlage) + "°", st: band(r.anlage, 4, 6, 2), rng: "Arbeitsziel 4–6° · 2–8° nur nach Prüfung" },
        { n: "Innenhebel", v: fmt(r.IH) + " cm", st: Math.abs(dIH) <= 1 ? "ok" : Math.abs(dIH) <= 2 ? "warn" : "bad", rng: (dv.skull ? "DA/2 + 8 cm" : "DA + 30 cm") + ` = ${fmt(dv.IHsoll)}` },
        { n: "Stemmbrett / 90°", v: `${fmt(refs.b90.knee, 0)}° · ${fmt(roll90, 0)} %`, st: stemmState, rng: "Beide Kriterien: Knie 160–170° und Rollweg 70–80 %" }
      ];
      $("cards").replaceChildren(...cards.map(statusCardElement));
      const chips = [
        { n: "Schulter–Hand zur Horizontalen", v: fmt(refs.b0.armAng, 0) + "° · Zieldefinition vor Trainerkalibrierung offen", st: "info" },
        { n: "Anteil vor Orthogonale", v: fmt(dv.antV, 0) + " %", st: band(dv.antV, 55, 65, 5) },
        // Gegenstück zur Dollenhöhen-Karte als Info; plus BB/StB-Vergleich (euer Schienen-Messgerät)
        s.mode === "werkstatt" ? { n: "Dollenhöhe ü. Wasser (berechnet)", v: fmt(dv.dWL) + " cm · Soll 22–26", st: "" } : { n: "Dollenhöhe ü. Rollsitz", v: fmt(r.a) + " cm", st: "" },
        dv.skull ? { n: "Δ Dolle BB−StB", v: fmt(r.dBB) + " cm · derzeit Messwert, noch nicht in Körpermodell gekoppelt", st: "info" } : seats.length < 2 ? null : { n: "Δ Dolle Vergleich", v: fmt(seats[0].r.a - seats[1].r.a) + " cm (Ziel 0)", st: band(seats[0].r.a - seats[1].r.a, -0.25, 0.25, 0.5) },
        { n: "Außenneigung", v: fmt(r.aussen) + "°", st: r.aussen <= 2 ? "ok" : "warn" },
        { n: "Rollbahn-Überstand", v: fmt(r.rueh) + " cm", st: band(r.rueh, 3, 7, 3) },
        { n: "Stemmbrettwinkel", v: fmt(r.stemmW) + "°", st: band(r.stemmW, 42, 45, 2) },
        { n: "Außenhebel", v: fmt(dv.AH) + " cm", st: "" },
        { n: "Momentarmverhältnis", v: "1 : " + fmt(dv.outb / dv.inb, 2), st: "" },
        { n: "Ruderwinkel θ", v: fmt(dv.theta) + "°", st: "" }
      ];
      const crewAnlage = state.seats.map(runtimeForSeat).filter(Boolean).map((item) => item.anlage);
      if (crewAnlage.length > 1) {
        const spread = Math.max(...crewAnlage) - Math.min(...crewAnlage);
        if (spread > 1e-9) chips.unshift({
          n: "Crew-Anlage",
          v: `Δ ${fmt(spread)}° · platzbezogen gespeichert, als Crewwert gleich halten`,
          st: "warn",
          compact: true
        });
      }
      if (catchSolution.naturalResolved === false) chips.unshift({ n: "Natural Catch", v: "keine Lösung im 20–88°-Prüfbereich · Fa/Körper/Rollbahn prüfen", st: "bad", compact: true });
      if (refs.b0.overreach || catchSolution.reachable === false) chips.unshift({ n: "Auslage erreichbar", v: "nein — Stemmbrett/Körpermaße prüfen", st: "bad", compact: true });
      if (refs.b0.trackLimited || refs.b1.trackLimited || catchSolution.trackLimited) chips.unshift({ n: "Rollbahn-Modell", v: "Pose berührt die vorläufige Sitz-/Schienen-Grenze · kalibrieren", st: "info", compact: true });
      const visibleChips = chips.filter(Boolean), host = $("chips");
      host.replaceChildren(...visibleChips.map(chipElement));
      host.classList.toggle("compact-has-warnings", visibleChips.some((chip) => chip.compact));
    }
    let lastActionPlan = buildTrimActionPlan({ seats: [], totalSeatCount: 1 });
    const actionRound = (value, digits = 1) => +Number(value).toFixed(digits);
    function buildActionDiagnostics() {
      return state.seats.flatMap((seat) => {
        const r = runtimeForSeat(seat);
        if (!r) return [];
        const catchSolution = resolveCatchAngle(r);
        const dv = derived(r, catchSolution.poseAngleDeg);
        const refs = bodyRefs(dv, r);
        return [{
          seatId: seat.id,
          position: seat.position,
          label: seatLabel(seat),
          rowerName: r.name,
          rig: r.rig,
          IH: actionRound(r.IH),
          IHsoll: actionRound(dv.IHsoll),
          anlage: actionRound(r.anlage),
          stemmX: actionRound(r.stemmX),
          knee90: actionRound(refs.b90.knee),
          roll90: actionRound(refs.rollAt(refs.b90)),
          naturalResolved: catchSolution.naturalResolved,
          reachable: catchSolution.reachable && !refs.b0.overreach,
          trackLimited: !!(catchSolution.trackLimited || refs.b0.trackLimited || refs.b1.trackLimited)
        }];
      });
    }
    function formatActionMeasure(measure) {
      if (!measure) return "—";
      if (Number.isFinite(measure.value)) return `${fmt(measure.value)}${measure.unit ? ` ${measure.unit}` : ""}`;
      if (Number.isFinite(measure.min) && Number.isFinite(measure.max)) return `${fmt(measure.min)}–${fmt(measure.max)}${measure.unit ? ` ${measure.unit}` : ""}`;
      return measure.text || "—";
    }
    function formatActionTarget(target) {
      if (!target) return "prüfen";
      if (Number.isFinite(target.min) && Number.isFinite(target.max)) return `${fmt(target.min)}–${fmt(target.max)}${target.unit ? ` ${target.unit}` : ""}`;
      if (Number.isFinite(target.value)) return `${fmt(target.value)}${target.unit ? ` ${target.unit}` : ""}${target.text ? ` · ${target.text}` : ""}`;
      return target.text || "prüfen";
    }
    function workflowResultSignature(plan, diagnostics, totalSeatCount, referenceSeatId) {
      if (plan.status === "missing") return "";
      return JSON.stringify({
        diagnostics,
        totalSeatCount,
        referenceSeatId,
        status: plan.status,
        actionIds: plan.actions.map((action) => action.id)
      });
    }
    function renderWorkflowGuide() {
      if (!workflowGuideReady) return;
      if (workflowReviewedResultSignature && workflowReviewedResultSignature !== workflowCurrentResultSignature) {
        workflowReviewedResultSignature = "";
      }
      const model = workflowGuideState({
        boatReady: !!PRESETS[$("preset").value] && state.seats.length > 0,
        seatReady: !!activeSeat(),
        profileReady: !!activeAssignment(),
        resultReady: workflowCurrentResultSignature !== "" && workflowReviewedResultSignature === workflowCurrentResultSignature,
        dirty,
        viewState: workspaceViewState
      });
      const signature = model.steps.map((step2) => `${step2.id}:${step2.status}`).join("|");
      if (signature === workflowGuideSignature) return;
      workflowGuideSignature = signature;
      const labels = { done: "erledigt", current: "jetzt", open: "offen" };
      for (const step2 of model.steps) {
        const item = $("workflowGuide").querySelector(`[data-guide-step="${step2.id}"]`);
        const link = item.querySelector("[data-guide-target]");
        item.dataset.guideStatus = step2.status;
        item.querySelector("[data-guide-state]").textContent = labels[step2.status];
        if (step2.status === "current") link.setAttribute("aria-current", "step");
        else link.removeAttribute("aria-current");
      }
      $("workflowGuideStatus").textContent = model.currentStep ? `${model.completedCount} von 5 erledigt · nächster sicherer Schritt markiert` : `5 von 5 erledigt · ${cleanStateLabel()}`;
    }
    function markWorkflowResultReviewed() {
      if (workflowCurrentResultSignature === "") return false;
      workflowReviewedResultSignature = workflowCurrentResultSignature;
      workflowGuideSignature = "";
      renderWorkflowGuide();
      return true;
    }
    function workflowGuideTarget(step2) {
      if (step2 === "seat") return [...$("seattabs").querySelectorAll("button")].find((button) => button.dataset.seat === state.editSeatId) ?? $("seattabs");
      return { boat: $("preset"), profile: $("dbSelect"), result: $("actionResult"), save: $("bSave") }[step2] ?? null;
    }
    function focusWorkflowGuideTarget(step2) {
      const target = workflowGuideTarget(step2);
      if (!target) return false;
      target.focus?.();
      target.scrollIntoView?.({ block: "center" });
      const messages = {
        boat: "Bootsklasse und Ausgangspreset prüfen.",
        seat: "Aktiver realer Bootsplatz.",
        profile: "Profil wählen oder neu anlegen; ohne Maße bleibt die Körperrechnung aus.",
        result: "Ergebnis prüfen; kein Wert wird automatisch verändert.",
        save: "Arbeitsstand bewusst speichern."
      };
      announce(messages[step2] ?? "Schritt geöffnet.");
      return true;
    }
    function initWorkflowGuide() {
      const guide = $("workflowGuide");
      guide.addEventListener("click", (event) => {
        const link = event.target.closest("a[data-guide-target]");
        if (!link || !guide.contains(link)) return;
        event.preventDefault();
        focusWorkflowGuideTarget(link.dataset.guideTarget);
      });
      $("actionResult").addEventListener("focusin", markWorkflowResultReviewed);
      guide.open = workspaceViewState === "new" && !activeAssignment();
      workflowGuideReady = true;
      renderWorkflowGuide();
    }
    function renderActionPlan() {
      const diagnostics = buildActionDiagnostics();
      lastActionPlan = buildTrimActionPlan({
        seats: diagnostics,
        referenceSeatId: state.referenceSeatId,
        totalSeatCount: state.seats.length,
        maxActions: 3
      });
      workflowCurrentResultSignature = workflowResultSignature(lastActionPlan, diagnostics, state.seats.length, state.referenceSeatId);
      const statusText = { missing: "Daten fehlen", ok: "kein akuter Handlungsbedarf", check: "prüfen", change: "ändern" };
      const badge = $("actionStatus");
      badge.dataset.status = lastActionPlan.status;
      badge.textContent = statusText[lastActionPlan.status] || "prüfen";
      $("actionResult").dataset.status = lastActionPlan.status;
      $("actionSummary").textContent = lastActionPlan.summary;
      const resultUnavailable = lastActionPlan.status === "missing";
      $("actionMissingFocus").hidden = !resultUnavailable;
      $("resultPrivacy").disabled = resultUnavailable;
      for (const id of ["resultExport", "resultPrint"]) {
        const button = $(id);
        button.disabled = resultUnavailable;
        button.title = resultUnavailable ? "Erst verfügbar, sobald mindestens ein vollständiges Profil ausgewertet werden kann." : "";
      }
      const renderSignature = JSON.stringify(lastActionPlan);
      if (renderSignature !== renderedActionPlanSignature) {
        renderedActionPlanSignature = renderSignature;
        $("actionList").replaceChildren(...lastActionPlan.actions.map((action) => {
          const item = document.createElement("li");
          item.className = "action-item";
          item.dataset.actionId = action.id;
          const main = document.createElement("div");
          main.className = "action-item-main";
          const scope = document.createElement("span");
          scope.className = "action-scope";
          scope.textContent = action.scope.type === "crew" ? action.scope.label : `${action.scope.label} · ${cleanName(action.scope.rowerName, "Profil")}`;
          const title = document.createElement("h3");
          title.textContent = action.parameter;
          const values = document.createElement("p");
          values.className = "action-values action-problem";
          const problemLabel = document.createElement("strong");
          problemLabel.textContent = "Problem: ";
          values.append(problemLabel, document.createTextNode(`${action.reason} · Ist ${formatActionMeasure(action.current)} · Ziel ${formatActionTarget(action.target)}`));
          const change = document.createElement("p");
          change.className = "action-change";
          const changeLabel = document.createElement("strong");
          changeLabel.textContent = "Jetzt ändern: ";
          change.append(changeLabel, document.createTextNode(action.direction));
          const effect = document.createElement("p");
          effect.className = "action-effect";
          const effectLabel = document.createElement("strong");
          effectLabel.textContent = "Erwartete Wirkung: ";
          effect.append(effectLabel, document.createTextNode(action.effect));
          const uncertainty = document.createElement("p");
          uncertainty.className = "action-uncertainty details-only";
          uncertainty.textContent = `Unsicherheit: ${action.uncertainty}`;
          main.append(scope, document.createTextNode(" · "), title, values, change, effect, uncertainty);
          const focus = document.createElement("button");
          focus.type = "button";
          focus.className = "action-focus";
          focus.dataset.actionId = action.id;
          focus.textContent = "Zum Wert";
          focus.setAttribute("aria-label", `${action.parameter} für ${action.scope.label} öffnen`);
          item.append(main, focus);
          return item;
        }));
      }
    }
    function actionPlanForExport(plan, includeNames) {
      return {
        ...plan,
        actions: plan.actions.map((action) => ({
          ...action,
          scope: { ...action.scope, rowerName: includeNames ? action.scope.rowerName : action.scope.position ? `Profil Platz ${action.scope.position}` : null }
        }))
      };
    }
    function anonymizeCurrentConfig(config) {
      const seats = config.boat.seats.map((seat) => ({ ...seat, externalRef: null, rowerRef: null }));
      const crew = config.crew.map((assignment) => {
        const position = seats.find((seat) => seat.id === assignment.seatId)?.position ?? 0;
        return { ...assignment, rowerRef: null, rower: { ...assignment.rower, externalRef: null, name: `Profil Platz ${position}` } };
      });
      return buildCurrentConfigDTO({
        ...config,
        boatRef: null,
        boat: {
          ...config.boat,
          externalRef: null,
          name: `Boot ${config.boat.preset}`,
          cox: config.boat.cox ? { name: "Steuerperson (anonymisiert)" } : null,
          seats
        },
        crew
      });
    }
    function confirmResultShare(kind, includeNames) {
      return confirm([
        `${kind}-Vorschau:`,
        `• ${lastActionPlan.actions.length} priorisierte Maßnahme(n)`,
        `• ${lastActionPlan.evaluatedSeats} Profil(e) ausgewertet`,
        `• Namen werden ${includeNames ? "ausdrücklich mitgegeben" : "anonymisiert"}`,
        "• Modellgrenzen und App-Build werden mitgegeben",
        "",
        "Jetzt fortfahren?"
      ].join("\n"));
    }
    function resultSnapshot(includeNames) {
      const rawConfig = currentConfigOf();
      return {
        format: "rudertrimm.action-result",
        schemaVersion: 1,
        domainSchemaVersion: SCHEMA_VERSION,
        appVersion: APP_VERSION,
        buildDate: APP_BUILD_DATE,
        buildId: APP_BUILD_ID,
        shellRevision: APP_SHELL_REVISION,
        createdAt: (/* @__PURE__ */ new Date()).toISOString(),
        privacy: includeNames ? "names-included" : "anonymized",
        config: includeNames ? rawConfig : anonymizeCurrentConfig(rawConfig),
        result: actionPlanForExport(lastActionPlan, includeNames),
        modelLimits: [
          "Rigg-Zielkorridore und Modellvorschläge, keine automatische Änderung.",
          "Körperkinematik, Natural Catch und 90°-Prüfung sind unkalibriert.",
          "Trainer-, Messprotokoll- und Golden-Daten-Freigabe stehen aus."
        ]
      };
    }
    function renderPrintResult(snapshot) {
      const host = $("printResult");
      host.replaceChildren();
      const add = (tag, text) => {
        const node = document.createElement(tag);
        node.textContent = text;
        host.appendChild(node);
        return node;
      };
      add("h1", "Rudertrimm · Ergebnis & Handlungsbedarf");
      add("p", `${snapshot.appVersion} · Build ${snapshot.buildDate} · ${snapshot.buildId} · ${new Date(snapshot.createdAt).toLocaleString("de-DE")}`);
      add("p", `Boot: ${snapshot.config.boat.name} · ${snapshot.config.boat.preset} · ${snapshot.config.boat.rig === "skull" ? "Skull" : "Riemen"} · Datenschutz: ${snapshot.privacy === "anonymized" ? "anonymisiert" : "Namen enthalten"}`);
      add("h2", { ok: "Kein akuter Handlungsbedarf", check: "Prüfen", change: "Ändern", missing: "Daten fehlen" }[snapshot.result.status]);
      add("p", snapshot.result.summary);
      const list = document.createElement("ol");
      host.appendChild(list);
      for (const action of snapshot.result.actions) {
        const item = document.createElement("li");
        const heading = document.createElement("h3");
        heading.textContent = `${action.scope.label}${action.scope.rowerName ? ` · ${action.scope.rowerName}` : ""}: ${action.parameter}`;
        const body = document.createElement("p");
        body.textContent = `Ist ${formatActionMeasure(action.current)} · Ziel ${formatActionTarget(action.target)} · ${action.direction}. ${action.reason} Erwartete Wirkung: ${action.effect}`;
        const limit = document.createElement("p");
        limit.textContent = `Unsicherheit: ${action.uncertainty}`;
        item.append(heading, body, limit);
        list.appendChild(item);
      }
      add("h2", "Modellgrenzen");
      const limits = document.createElement("ul");
      host.appendChild(limits);
      for (const text of snapshot.modelLimits) {
        const item = document.createElement("li");
        item.textContent = text;
        limits.appendChild(item);
      }
    }
    function initActionResult() {
      $("actionMissingFocus").addEventListener("click", () => {
        const target = activeSeat() ? $("dbSelect") : $("preset");
        target.focus();
        target.scrollIntoView?.({ block: "center", behavior: preferredScrollBehavior() });
        announce(activeSeat() ? "Nächster Schritt: für den aktiven Bootsplatz ein gespeichertes Profil wählen oder ein neues Profil anlegen." : "Nächster Schritt: zuerst eine gültige Bootsklasse wählen.");
      });
      $("actionList").addEventListener("click", (event) => {
        const button = event.target.closest("button[data-action-id]");
        if (!button) return;
        const action = lastActionPlan.actions.find((candidate) => candidate.id === button.dataset.actionId);
        if (!action) return;
        const seatId = action.focusSeatId ?? action.scope.seatId ?? state.referenceSeatId;
        if (seatId && state.seats.some((seat) => seat.id === seatId) && state.editSeatId !== seatId) {
          state.editSeatId = seatId;
          clearRowerSelection();
          buildControls();
          refreshDBSelect();
          render();
        }
        if (!COMPACT_CONTROL_KEY_SET.has(action.field)) setPresentationMode("details");
        requestAnimationFrame(() => {
          const input = $(`in_${action.field}`);
          if (!input) {
            announce("Das betroffene Eingabefeld ist im aktuellen Arbeitsstand nicht verfügbar.");
            return;
          }
          input.focus();
          input.scrollIntoView?.({ block: "center", behavior: preferredScrollBehavior() });
        });
      });
      $("resultExport").addEventListener("click", () => {
        if (lastActionPlan.status === "missing") {
          announce("Ergebnisexport ist erst mit mindestens einem vollständigen Profil verfügbar.");
          return;
        }
        const includeNames = $("resultPrivacy").value === "names";
        if (!confirmResultShare("Ergebnisexport", includeNames)) return;
        downloadJson(resultSnapshot(includeNames), `rudertrimm-ergebnis-${$("preset").value}.json`);
        announce(`Ergebnis exportiert · ${includeNames ? "Namen enthalten" : "Namen anonymisiert"}.`);
      });
      $("resultPrint").addEventListener("click", () => {
        if (lastActionPlan.status === "missing") {
          announce("Druckbericht ist erst mit mindestens einem vollständigen Profil verfügbar.");
          return;
        }
        const includeNames = $("resultPrivacy").value === "names";
        if (!confirmResultShare("Druckbericht", includeNames)) return;
        renderPrintResult(resultSnapshot(includeNames));
        document.body.classList.add("print-result-mode");
        const cleanup = () => document.body.classList.remove("print-result-mode");
        window.addEventListener("afterprint", cleanup, { once: true });
        window.print();
        setTimeout(cleanup, 1e3);
      });
    }
    const NS = "http://www.w3.org/2000/svg";
    let visualContentWidth = 0;
    function visualCanvasWidth(designWidth) {
      return visualContentWidth >= 240 ? Math.min(designWidth, visualContentWidth) : designWidth;
    }
    function recordVisualContentWidth(value) {
      const next = Math.floor(Number(value));
      if (!Number.isFinite(next) || next < 240 || Math.abs(next - visualContentWidth) < 2) return false;
      visualContentWidth = next;
      return true;
    }
    function initVisualSizing() {
      const container = $("viewPanels");
      recordVisualContentWidth(container.clientWidth);
      const rerenderIfWidthChanged = (width) => {
        if (recordVisualContentWidth(width) && !playing) render();
      };
      if (typeof ResizeObserver === "function") {
        const observer = new ResizeObserver((entries) => rerenderIfWidthChanged(entries[0]?.contentRect?.width));
        observer.observe(container);
      } else {
        window.addEventListener("resize", () => rerenderIfWidthChanged(container.clientWidth), { passive: true });
      }
    }
    function el(svg, tag, at) {
      const e = document.createElementNS(NS, tag);
      for (const k in at) e.setAttribute(k, at[k]);
      svg.appendChild(e);
      return e;
    }
    function txt(svg, x, y, str, at = {}) {
      const e = el(svg, "text", { x, y, ...at });
      e.textContent = str;
      return e;
    }
    function newSVG(host, W, H, label, description = "") {
      host.replaceChildren();
      const svg = document.createElementNS(NS, "svg");
      svg.setAttribute("viewBox", `0 0 ${W} ${H}`);
      svg.setAttribute("role", "img");
      const idBase = `${host.id || "rudertrimmVisual"}Svg`;
      const titleId = `${idBase}Title`;
      const title = el(svg, "title", { id: titleId });
      title.textContent = label;
      svg.setAttribute("aria-labelledby", titleId);
      if (description) {
        const descriptionId = `${idBase}Description`;
        const desc = el(svg, "desc", { id: descriptionId });
        desc.textContent = description;
        svg.setAttribute("aria-describedby", descriptionId);
      }
      host.appendChild(svg);
      return svg;
    }
    function plateLabel(svg, x, y, label, { anchor = "start", leader = null, className = "" } = {}) {
      const width = Math.max(48, [...String(label)].length * 6.2 + 14), height = 20;
      const left = anchor === "end" ? x - width : anchor === "middle" ? x - width / 2 : x;
      const lineClass = className ? { class: `${className}-leader` } : {}, plateClass = className ? { class: `${className}-plate` } : {};
      const textClass = className ? `svg-label ${className}` : "svg-label";
      if (leader) el(svg, "line", { ...lineClass, x1: leader.x, y1: leader.y, x2: x, y2: y + 2, stroke: "var(--fg-mut)", "stroke-width": 1, "stroke-dasharray": "2 3" });
      el(svg, "rect", { ...plateClass, x: left, y: y - 14, width, height, rx: 4, fill: "rgba(7,17,10,.9)", stroke: "var(--v2-panel-line)", "stroke-width": 0.8 });
      txt(svg, x, y, label, { "text-anchor": anchor, fill: "var(--fg)", class: textClass });
    }
    function dim(svg, x1, y1, x2, y2, label, opts = {}) {
      const col = opts.col || "#7f92ac";
      el(svg, "line", { x1, y1, x2, y2, stroke: col, "stroke-width": 1.25 });
      for (const [px, py, qx, qy] of [[x1, y1, x2, y2], [x2, y2, x1, y1]]) {
        const ang = Math.atan2(qy - py, qx - px), s = 6;
        el(svg, "path", { d: `M${px},${py} L${px + s * Math.cos(ang - 0.4)},${py + s * Math.sin(ang - 0.4)} L${px + s * Math.cos(ang + 0.4)},${py + s * Math.sin(ang + 0.4)} Z`, fill: col });
      }
      const tx = (x1 + x2) / 2 + (opts.dx || 0), ty = (y1 + y2) / 2 + (opts.dy || -5);
      if (label) {
        if (opts.plate) plateLabel(svg, tx, ty, label, { anchor: opts.anchor || "middle" });
        else txt(svg, tx, ty, label, { "text-anchor": opts.anchor || "middle", fill: opts.tcol || col });
      }
    }
    function arrow(svg, x1, y1, x2, y2, col, w) {
      el(svg, "line", { x1, y1, x2, y2, stroke: col, "stroke-width": w, "stroke-linecap": "round" });
      const ang = Math.atan2(y2 - y1, x2 - x1), s = w * 3.2;
      el(svg, "path", { d: `M${x2},${y2} L${x2 - s * Math.cos(ang - 0.45)},${y2 - s * Math.sin(ang - 0.45)} L${x2 - s * Math.cos(ang + 0.45)},${y2 - s * Math.sin(ang + 0.45)} Z`, fill: col });
    }
    const BLADE_BIG = [[0, 0.08], [0.2, 0.2], [0.5, 0.26], [0.85, 0.28], [1, 0.26], [1, -0.55], [0.93, -0.7], [0.55, -0.66], [0.2, -0.42], [0, -0.1]];
    const BLADE_MAC = [
      [0, 0.06],
      [0.15, 0.22],
      [0.35, 0.38],
      [0.6, 0.47],
      [0.8, 0.48],
      [0.93, 0.42],
      [1, 0.25],
      [1.03, 0],
      [1, -0.25],
      [0.93, -0.42],
      [0.8, -0.48],
      [0.6, -0.47],
      [0.35, -0.38],
      [0.15, -0.22],
      [0, -0.06]
    ];
    function bladeDims(type, skull) {
      return type === "mac" ? { BL: skull ? 50 : 58, BW: skull ? 17 : 20 } : { BL: skull ? 46 : 54, BW: skull ? 21 : 25 };
    }
    function bladePts(type) {
      return type === "mac" ? BLADE_MAC : BLADE_BIG;
    }
    function drawBlade(svg, base, dirU, len, dirV, wide, type, flip, attributes = {}) {
      const f = flip ? -1 : 1;
      const d = bladePts(type).map((p, i) => {
        const gx = base.x + dirU.x * p[0] * len + dirV.x * p[1] * wide * f;
        const gy = base.y + dirU.y * p[0] * len + dirV.y * p[1] * wide * f;
        return (i ? "L" : "M") + gx.toFixed(1) + "," + gy.toFixed(1);
      }).join(" ") + " Z";
      el(svg, "path", { ...attributes, d, fill: "var(--blade)", stroke: "#a87b28", "stroke-width": 1.1, opacity: 0.95 });
    }
    function renderTop(dv, seats, primary) {
      const s = state, W = visualCanvasWidth(1180), body = primary.b;
      const DAr = dv.DAr;
      const K = 0.55;
      const rOut = dv.outb * K, rIn = dv.inb;
      const DArMax = Math.max(...seats.map((x) => x.dv.DAr)), outMax = Math.max(...seats.map((x) => x.dv.outb)) * K;
      const phiAmax = Math.max(s.phiA, ...seats.map((seat) => seat.dv.phiA));
      const latMax = DArMax + outMax + 34;
      const lat = Math.max(latMax, DArMax + rIn * 0.4 + 60, 150);
      const sc = Math.min((W - 40) / 560, 250 / lat);
      const plotH = 2 * lat * sc + 46, compactLegend = W < 700;
      const H = plotH + (compactLegend ? 118 : 0);
      const cx = W / 2, cy = plotH / 2 + 8;
      const X = (u) => cx + u * sc, Y = (v) => cy - v * sc;
      const fullNames = seats.map((seat) => cleanName(seat.name, "Ruderer")).join(" und ");
      const svg = newSVG(
        $("vTop"),
        W,
        H,
        `Draufsicht des Ruderriggs für ${cleanName(primary.name, "Ruderer")}`,
        `${dv.skull ? "Skull" : "Riemen"}, Phase ${PHASES[currentPhase()].n}. Dargestellte Profile: ${fullNames}. Freie Bootsplätze werden nicht simuliert.`
      );
      const th = rad(body.theta), g = body.g;
      const blade = $("blade").value;
      const { BL, BW } = bladeDims(blade, dv.skull);
      el(svg, "rect", { x: X(-240), y: Y(body.SB / 2), width: 480 * sc, height: body.SB * sc, fill: "rgba(46,204,113,.07)", stroke: "rgba(46,204,113,.3)", "stroke-dasharray": "3 4" });
      el(svg, "line", { x1: X(-275), y1: cy, x2: X(275), y2: cy, stroke: "var(--fg-mut)", "stroke-dasharray": "6 5" });
      txt(svg, cx, 16, "ORTHOGONALSTELLUNG", { class: "cap", "text-anchor": "middle", fill: "var(--fg)" });
      txt(svg, X(-272), cy - 6, "Bug", { class: "cap" });
      txt(svg, X(244), cy - 6, "Heck", { class: "cap", "text-anchor": "end" });
      const hw = 13;
      el(svg, "path", {
        d: `M${X(-250)},${cy} Q${X(-150)},${Y(hw)} ${X(0)},${Y(hw + 1)} Q${X(150)},${Y(hw)} ${X(250)},${cy} Q${X(150)},${Y(-hw)} ${X(0)},${Y(-hw - 1)} Q${X(-150)},${Y(-hw)} ${X(-250)},${cy} Z`,
        fill: "var(--hull)",
        stroke: "#42597a",
        "stroke-width": 1
      });
      el(svg, "rect", { x: X(-65), y: Y(9), width: 120 * sc, height: 18 * sc, rx: 8, fill: "#0b1420", stroke: "#42597a", "stroke-width": 0.8 });
      const RS = dv.skull ? 1 : primary.side;
      const angBand = dv.skull ? { lo: 65, hi: 75, pad: 5 } : { lo: 50, hi: 60, pad: 5 };
      const zoneMax = Math.max(angBand.hi + angBand.pad + 6, phiAmax + 6);
      const zones = [
        [0, Math.max(0, angBand.lo - angBand.pad), "rgba(248,81,73,.09)", "rgba(248,81,73,.3)"],
        [Math.max(0, angBand.lo - angBand.pad), angBand.lo, "rgba(240,169,46,.12)", "rgba(240,169,46,.4)"],
        [angBand.lo, angBand.hi, "rgba(46,204,113,.14)", "rgba(46,204,113,.5)"],
        [angBand.hi, angBand.hi + angBand.pad, "rgba(240,169,46,.12)", "rgba(240,169,46,.4)"],
        [angBand.hi + angBand.pad, zoneMax, "rgba(248,81,73,.09)", "rgba(248,81,73,.3)"]
      ];
      function sectorPath(ox, pv, sd, rk, aLoDeg, aHiDeg) {
        const N = 16;
        let d = `M${X(ox)},${Y(pv)} `;
        for (let i = 0; i <= N; i++) {
          const a = rad(lerp(aLoDeg, aHiDeg, i / N));
          d += `L${X(-rk * Math.sin(a) + ox)},${Y(pv + sd * rk * Math.cos(a))} `;
        }
        return d + "Z";
      }
      function drawSeatArcs(seat, isRef) {
        const ox = seat.ox, sdv = seat.dv, sOut = sdv.outb * K, sIn = sdv.inb, sDAr = sdv.DAr;
        const seatSides = dv.skull ? [1, -1] : [seat.side];
        const rk = sOut * 0.92;
        const phiASeat = sdv.phiA;
        el(svg, "line", { x1: X(ox), y1: Y(lat - 4), x2: X(ox), y2: Y(-lat + 4), stroke: "#fff", "stroke-dasharray": "5 5", opacity: isRef ? 0.45 : 0.28 });
        for (const sd of seatSides) {
          const pv = sd * sDAr;
          for (const [aLo, aHi, fill, stroke] of zones) {
            if (aHi <= aLo) continue;
            el(svg, "path", { d: sectorPath(ox, pv, sd, rk, aLo, aHi), fill, stroke, "stroke-width": 1 });
          }
          for (const [r, col, sgn] of [[sOut, "rgba(63,162,255,.55)", 1], [sIn, "rgba(232,176,75,.5)", -1]]) {
            let dstr = "";
            for (let i = 0; i <= 44; i++) {
              const a = rad(lerp(phiASeat, -s.phiR, i / 44));
              dstr += (i ? "L" : "M") + X(-sgn * r * Math.sin(a) + ox) + "," + Y(pv + sd * sgn * r * Math.cos(a)) + " ";
            }
            el(svg, "path", { d: dstr, fill: "none", stroke: col, "stroke-width": 1.6, "stroke-dasharray": "5 4" });
          }
          for (const A of [phiASeat, -s.phiR]) {
            const a = rad(A);
            el(svg, "line", { x1: X(ox), y1: Y(pv), x2: X(-rk * Math.sin(a) + ox), y2: Y(pv + sd * rk * Math.cos(a)), stroke: "var(--fg-mut)", "stroke-width": 1, "stroke-dasharray": "2 4" });
          }
        }
        const lblSide = seatSides[0], pvLbl = lblSide * sDAr;
        const targetDiffers = Math.abs(phiASeat - s.phiA) > 1.5;
        const naturalMissing = seat.catchSolution.naturalResolved === false;
        const actualMissing = seat.catchSolution.actualAngleDeg === null;
        const modelTxt = compactLegend ? "" : naturalMissing ? ` (keine Natural-Catch-Lösung · Rigg-Ziel ${fmt(s.phiA)}°)` : seat.reachAvailable === false ? ` (3D nicht erreichbar; Natural ${fmt(seat.catchSolution.naturalAngleDeg)}°)` : seat.catchSolution.limitedByReach ? ` (3D-Limit; Natural ${fmt(seat.catchSolution.naturalAngleDeg)}° · Ziel ${fmt(s.phiA)}°)` : targetDiffers ? ` (Rigg-Ziel ${fmt(s.phiA)}°)` : "";
        const angleLabel = compactLegend ? `${actualMissing ? "Prüf" : "Ist"} ${fmt(phiASeat)}°` : actualMissing ? seats.length > 1 ? `${visualName(seat.name)}: Prüfpose ${fmt(phiASeat)}°` : `Prüfpose ${fmt(phiASeat)}°` : seats.length > 1 ? `${visualName(seat.name)}: Ist ${fmt(phiASeat)}°` : `Ist-Auslage ${fmt(phiASeat)}°`;
        const lblText = angleLabel + modelTxt;
        txt(
          svg,
          X(-rk * 0.7 * Math.sin(rad(phiASeat)) + ox) - 8,
          Y(pvLbl + lblSide * rk * 0.75 * Math.cos(rad(phiASeat))) - 6,
          lblText,
          { fill: actualMissing ? "var(--bad)" : targetDiffers ? "var(--warn)" : "var(--fg)", class: "cap", "text-anchor": "end" }
        );
        txt(svg, X(ox) + 8, Y(pvLbl) - 8, "Dolle", { class: "cap" });
      }
      function drawSeatTop(seat, isRef) {
        const b = seat.b, ox = seat.ox, th2 = rad(b.theta), g2 = b.g, zf = dv.skull ? 1 : seat.side;
        const sdv = seat.dv;
        const sOut = sdv.outb * K, sIn = sdv.inb, sDAr = sdv.DAr;
        const XL = (u) => X(u + ox), YZ = (z) => Y(zf * z);
        const seatSides = dv.skull ? [1, -1] : [seat.side];
        const bodyCol = isRef ? "var(--body)" : "var(--accent2)", jointCol = isRef ? "var(--accent2)" : "var(--accent)";
        for (const sd of seatSides) {
          const pv = sd * sDAr;
          const tip = { x: XL(-sOut * Math.sin(th2)), y: Y(pv + sd * sOut * Math.cos(th2)) };
          const neckR = sOut - BL * K, neck = { x: XL(-neckR * Math.sin(th2)), y: Y(pv + sd * neckR * Math.cos(th2)) };
          const hnd = { x: XL(sIn * Math.sin(th2)), y: Y(pv - sd * sIn * Math.cos(th2)) };
          el(svg, "line", { class: "top-oar-shaft", "data-oar-side": sd, x1: hnd.x, y1: hnd.y, x2: neck.x, y2: neck.y, stroke: "var(--wood)", "stroke-width": 3, "stroke-linecap": "round" });
          const dir = { x: (tip.x - neck.x) / (BL * K * sc), y: (tip.y - neck.y) / (BL * K * sc) };
          drawBlade(
            svg,
            neck,
            dir,
            BL * K * sc,
            { x: -dir.y, y: dir.x },
            BW * sc * K * lerp(0.16, 1, g2),
            blade,
            sd > 0,
            { class: "top-oar-blade", "data-oar-side": sd }
          );
          el(svg, "circle", { cx: hnd.x, cy: hnd.y, r: 4, fill: jointCol });
          el(svg, "circle", { cx: XL(0), cy: Y(pv), r: 4.5, fill: "#e4ecf7", stroke: "#42597a" });
        }
        const S1 = b.arms[0].S, S2 = b.arms[b.arms.length - 1].S;
        el(svg, "path", { d: `M${XL(b.hip.x)},${Y(11)} L${XL(S1.x)},${YZ(S1.z)} L${XL(S2.x)},${YZ(S2.z)} L${XL(b.hip.x)},${Y(-11)} Z`, fill: bodyCol, opacity: 0.28 });
        el(svg, "line", { x1: XL(S1.x), y1: YZ(S1.z), x2: XL(S2.x), y2: YZ(S2.z), stroke: bodyCol, "stroke-width": 5, "stroke-linecap": "round", opacity: 0.85 });
        el(svg, "circle", { cx: XL(b.head.x), cy, r: b.head.r * sc, fill: bodyCol });
        for (const A of b.arms) {
          el(svg, "path", { d: `M${XL(A.S.x)},${YZ(A.S.z)} L${XL(A.E.x)},${YZ(A.E.z)} L${XL(A.W.x)},${YZ(A.W.z)}`, fill: "none", stroke: bodyCol, "stroke-width": 3, "stroke-linecap": "round", "stroke-linejoin": "round", opacity: 0.85 });
          el(svg, "circle", { cx: XL(A.W.x), cy: YZ(A.W.z), r: 3.4, fill: jointCol });
        }
        if (seats.length > 1) txt(svg, XL(b.head.x), cy - b.head.r * sc - 4, visualName(seat.name, "Ruderer", 18), { "text-anchor": "middle", fill: bodyCol, class: "cap" });
      }
      const sortedSeats = [...seats].sort((a, b) => a.ox - b.ox);
      sortedSeats.forEach((seat) => drawSeatArcs(seat, seat === primary));
      sortedSeats.forEach((seat) => drawSeatTop(seat, seat === primary));
      if (dv.U > 0) {
        const y1 = Y(dv.skull ? DAr - dv.inb : 0), y2 = Y(dv.skull ? -(DAr - dv.inb) : RS * (DAr - dv.inb));
        dim(svg, X(95), y1, X(95), y2, `Ü = ${fmt(dv.U)} cm`, { col: "var(--accent2)", tcol: "var(--accent2)", anchor: "start", dx: 8, dy: 3 });
      }
      const strokeSummary = compactLegend ? `SW ${fmt(dv.SW)}° · θ ${fmt(body.theta, 0)}° · ${body.g > 0.5 ? "flach" : "auf"}` : `Schlagweite ${fmt(dv.SW)}°  ·  θ = ${fmt(body.theta)}°  ·  Blatt ${body.g > 0.5 ? "flach (abgedreht)" : "aufgedreht"}`;
      txt(svg, X(-255), Y(-lat + 14), strokeSummary, { class: "big" });
      const lx = compactLegend ? 16 : W - 208, ly = compactLegend ? plotH + 20 : 26;
      const leg = [
        ["rgba(63,162,255,.8)", "Schlagbogen (Blattweg)", "5 4"],
        ["rgba(232,176,75,.8)", "Griffweg", "5 4"],
        ["rgba(46,204,113,.8)", "Zugbereich", "3 4"],
        ["#ffffff88", "Orthogonalstellung", "5 5"]
      ];
      leg.forEach((L, i) => {
        el(svg, "line", { x1: lx, y1: ly + i * 17, x2: lx + 26, y2: ly + i * 17, stroke: L[0], "stroke-width": 2, "stroke-dasharray": L[2] });
        txt(svg, lx + 33, ly + i * 17 + 4, L[1], {});
      });
      const legZ = [["rgba(46,204,113,.6)", "Rigg-Zielkorridor"], ["rgba(240,169,46,.6)", "… Toleranz"], ["rgba(248,81,73,.6)", "… außerhalb"]];
      const zy = ly + leg.length * 17 + 6;
      legZ.forEach((L, i) => {
        el(svg, "rect", { x: lx, y: zy + i * 15 - 6, width: 14, height: 9, fill: L[0], stroke: "none" });
        txt(svg, lx + 21, zy + i * 15 + 1, L[1], {});
      });
    }
    function renderCross(dv, primary) {
      const s = state, W = visualCanvasWidth(560), body = primary.b, r = primary.r;
      const RS = dv.skull ? 1 : primary.side;
      const DAr = dv.DAr;
      const wl = body.wl, pinY = body.pinY;
      const { top: topY, bottom: botY } = stableVerticalViewBounds([primary]);
      const extX = DAr + dv.outb + 12;
      const sc = (W - 24) / (2 * extX);
      const labelTop = 54, labelBottom = 14;
      const H = labelTop + (topY - botY) * sc + labelBottom;
      const cx = W / 2, oy = labelTop + topY * sc;
      const X = (x) => cx + x * sc, Y = (y) => oy - y * sc, XZ = (z) => X(RS * z);
      const svg = newSVG(
        $("vCross"),
        W,
        H,
        `Querschnitt des Ruderriggs für ${cleanName(primary.name, "Ruderer")}`,
        `${dv.skull ? "Skull mit zwei sichtbaren Rudern" : "Riemen mit einem sichtbaren Ruder"}, Phase ${PHASES[currentPhase()].n}. Dollenhöhe und Neigung sind als Prüfwerte beschriftet.`
      );
      const blade = $("blade").value;
      const { BL, BW } = bladeDims(blade, dv.skull);
      const g = body.g;
      if (s.mode === "wasser") {
        el(svg, "rect", { x: 0, y: Y(wl), width: W, height: H - Y(wl), fill: "var(--water)", opacity: 0.6 });
        el(svg, "line", { x1: 0, y1: Y(wl), x2: W, y2: Y(wl), stroke: "var(--waterline)", "stroke-width": 1.5, opacity: 0.8 });
        txt(svg, 8, Y(wl) + 14, s.kg ? `Wasserlinie (${s.kg > 0 ? "+" : ""}${s.kg} kg → ${s.kg > 0 ? "+" : ""}${fmt(s.kg, 0)} mm)` : "Wasserlinie", { fill: "var(--waterline)" });
      } else {
        el(svg, "path", { d: `M${X(0)},${Y(-13)} L${X(-30)},${Y(-30)} M${X(0)},${Y(-13)} L${X(30)},${Y(-30)} M${X(-19)},${Y(-24)} L${X(19)},${Y(-24)}`, stroke: "#7a6a4f", "stroke-width": 4, fill: "none", "stroke-linecap": "round" });
        txt(svg, 8, 16, "Werkstatt-Modus · Boot auf Böcken", { class: "cap", fill: "var(--fg-dim)" });
      }
      const hb = s.c + 6.5;
      el(svg, "path", {
        d: `M${X(-27)},${Y(hb)} Q${X(-25)},${Y(-14)} ${X(0)},${Y(-14.5)} Q${X(25)},${Y(-14)} ${X(27)},${Y(hb)} L${X(-27)},${Y(hb)} Z`,
        fill: "var(--hull)",
        stroke: "#42597a"
      });
      el(svg, "rect", { x: X(-14), y: Y(s.c), width: 28 * sc, height: 3.2 * sc, rx: 2, fill: "#9aa7b5" });
      const sides = dv.skull ? [1, -1] : [1];
      const cosT = Math.cos(rad(body.theta));
      const tipY = wl + body.lift;
      for (const sd of sides) {
        el(svg, "line", { class: "cross-rigger", x1: XZ(sd * 26), y1: Y(hb), x2: XZ(sd * DAr), y2: Y(pinY), stroke: "#6d83a0", "stroke-width": 2 });
        el(svg, "line", { class: "cross-rigger", x1: XZ(sd * 17), y1: Y(hb - 4), x2: XZ(sd * DAr), y2: Y(pinY - 1.5), stroke: "#6d83a0", "stroke-width": 1.1 });
        const tilt = rad(r.aussen * 4) * sd * RS;
        el(svg, "line", { x1: XZ(sd * DAr), y1: Y(pinY - 2), x2: XZ(sd * DAr) + Math.sin(tilt) * 10, y2: Y(pinY + 7), stroke: "#e4ecf7", "stroke-width": 3 });
        const arm = body.arms[dv.skull ? sd > 0 ? 0 : 1 : 0];
        const zN = sd * (DAr + (dv.outb - BL) * cosT), zTip = sd * (DAr + dv.outb * cosT);
        const yN = lerp(pinY, tipY, (dv.outb - BL) / dv.outb);
        el(svg, "line", { class: "cross-oar-shaft", "data-oar-side": sd, x1: XZ(arm.W.z), y1: Y(arm.W.y), x2: XZ(sd * DAr), y2: Y(pinY), stroke: "var(--wood)", "stroke-width": 2.5, "stroke-linecap": "round" });
        el(svg, "line", { class: "cross-oar-shaft", "data-oar-side": sd, x1: XZ(sd * DAr), y1: Y(pinY), x2: XZ(zN), y2: Y(yN), stroke: "var(--wood)", "stroke-width": 2.5, "stroke-linecap": "round" });
        const neck = { x: XZ(zN), y: Y(yN) }, tip = { x: XZ(zTip), y: Y(tipY) };
        const projectedLength = BL * sc;
        const dirU = { x: (tip.x - neck.x) / projectedLength, y: (tip.y - neck.y) / projectedLength };
        drawBlade(svg, neck, dirU, projectedLength, { x: 0, y: -1 }, BW * sc * lerp(1, 0.16, g), blade, false, {
          class: "cross-oar-blade",
          "data-oar-side": sd
        });
        el(svg, "circle", { class: "cross-oar-pin", "data-oar-side": sd, cx: XZ(sd * DAr), cy: Y(pinY), r: 4, fill: "#e4ecf7", stroke: "#42597a" });
      }
      {
        for (const sd of [1, -1])
          el(svg, "path", {
            d: `M${X(sd * 6)},${Y(body.hip.y)} L${X(sd * 9)},${Y(body.kneeP.y)} L${X(sd * 11)},${Y(body.foot.y)}`,
            fill: "none",
            stroke: "var(--body)",
            "stroke-width": 4,
            opacity: 0.45,
            "stroke-linecap": "round",
            "stroke-linejoin": "round"
          });
        const S1 = body.arms[0].S, S2 = body.arms[body.arms.length - 1].S;
        el(svg, "path", {
          d: `M${X(-10)},${Y(s.c + 3)} L${XZ(S2.z)},${Y(S2.y)} Q${X(0)},${Y(body.sh.y + 5)} ${XZ(S1.z)},${Y(S1.y)} L${X(10)},${Y(s.c + 3)} Z`,
          fill: "var(--body)",
          opacity: 0.3
        });
        el(svg, "circle", { cx: X(0), cy: Y(body.head.y), r: body.head.r * sc, fill: "var(--body)", opacity: 0.75 });
        for (const A of body.arms) {
          el(svg, "path", {
            d: `M${XZ(A.S.z)},${Y(A.S.y)} L${XZ(A.E.z)},${Y(A.E.y)} L${XZ(A.W.z)},${Y(A.W.y)}`,
            fill: "none",
            stroke: "var(--body)",
            "stroke-width": 3,
            "stroke-linecap": "round",
            "stroke-linejoin": "round",
            opacity: 0.75
          });
          el(svg, "circle", { cx: XZ(A.W.z), cy: Y(A.W.y), r: 3.2, fill: "var(--accent2)" });
        }
      }
      if (r.aussen > 0) txt(svg, XZ(DAr), Y(pinY + 10), W < 460 ? `Außen ${fmt(r.aussen)}° · 4×` : `Außenneigung ${fmt(r.aussen)}° (4× überhöht)`, { "text-anchor": RS > 0 ? "end" : "start" });
      const mx = dv.skull ? X(-DAr - 16) : XZ(DAr + 16);
      if (s.mode === "werkstatt") {
        const baseY = s.heightRef === "schiene" ? s.c - s.seatOffset : s.c;
        const off = s.heightRef === "schiene" ? s.seatOffset : 0;
        dim(svg, mx, Y(baseY), mx, Y(pinY), `${fmt(r.a + off)} cm`, { col: "var(--ok)", tcol: "var(--ok)", anchor: "middle", dy: -7 });
        txt(svg, mx, Y(pinY) - 20, "Dollenhöhe", { "text-anchor": "middle", fill: "var(--ok)", class: "cap" });
        txt(svg, mx, Y(baseY) + 16, s.heightRef === "schiene" ? "über Schiene" : "über Rollsitz", { "text-anchor": "middle", fill: "var(--ok)" });
      } else {
        dim(svg, mx, Y(wl), mx, Y(pinY), `${fmt(dv.dWL)} cm`, { col: "var(--ok)", tcol: "var(--ok)", anchor: "middle", dy: -7 });
        txt(svg, mx, Y(pinY) - 20, "Dollenhöhe", { "text-anchor": "middle", fill: "var(--ok)", class: "cap" });
        txt(svg, mx, Y(wl) + 16, "über Wasser", { "text-anchor": "middle", fill: "var(--ok)" });
      }
      const m3 = Y(pinY + 13);
      const daStart = dv.skull ? X(-DAr) : X(0), daEnd = dv.skull ? X(DAr) : XZ(DAr);
      dim(svg, daStart, m3, daEnd, m3, "");
      plateLabel(svg, 24, 42, `DA ${fmt(dv.DA)} cm`, { leader: { x: (daStart + daEnd) / 2, y: m3 }, className: "cross-da-label" });
      const pinScreen = { x: XZ(DAr), y: Y(pinY) };
      plateLabel(svg, W - 24, 42, `Neigung ${fmt(r.anlage)}°`, { anchor: "end", leader: pinScreen, className: "cross-pitch-label" });
    }
    function stableVerticalViewBounds(seats, mode = state.mode, seatHeight = state.c) {
      let top = seatHeight + 32;
      for (const seat of seats) {
        const seg = SEG(seat.r), headRadius = seg.HEAD * 0.42;
        const pinY = seatHeight + seat.r.a;
        const headTop = seatHeight + 4 + seg.T + seg.NECK + 2 * headRadius + 12;
        top = Math.max(top, pinY + 20, headTop);
      }
      return Object.freeze({ top, bottom: mode === "werkstatt" ? -34 : -22 });
    }
    function renderSide(dv, seats, primary, refs) {
      const s = state, W = visualCanvasWidth(760);
      const { BL, BW } = bladeDims($("blade").value, dv.skull);
      const blade = $("blade").value;
      let x0 = -115, x1 = 140;
      for (const seat of seats) {
        const b2 = seat.b, ox = seat.ox;
        x0 = Math.min(x0, ox - seat.dv.outb * Math.sin(rad(Math.max(s.phiA, seat.dv.phiA))) - 14);
        x1 = Math.max(x1, ox + b2.foot.x + 70, ox + seat.dv.outb * Math.sin(rad(s.phiR)) + 14);
      }
      const vertical = stableVerticalViewBounds(seats);
      const ext = { x0, x1, y0: vertical.bottom, y1: vertical.top };
      const sc = (W - 20) / (ext.x1 - ext.x0);
      const narrowLabels = W < 740, labelTop = narrowLabels ? 86 : 38, labelBottom = 36;
      const H = labelTop + (ext.y1 - ext.y0) * sc + labelBottom;
      const X = (x) => 10 + (x - ext.x0) * sc, Y = (y) => labelTop + (ext.y1 - y) * sc;
      const sideNames = seats.map((seat) => cleanName(seat.name, "Ruderer")).join(" und ");
      const svg = newSVG(
        $("vSide"),
        W,
        H,
        `Seitenansicht und Körpermodell für ${cleanName(primary.name, "Ruderer")}`,
        `Phase ${PHASES[currentPhase()].n}. Sitz, Beine, Rumpf, Arme und Ruder der dargestellten Profile ${sideNames} verwenden denselben Schlagzustand.`
      );
      const wl = primary.b.wl;
      if (s.mode === "wasser") {
        el(svg, "rect", { x: 0, y: Y(wl), width: W, height: H - Y(wl), fill: "var(--water)", opacity: 0.6 });
        el(svg, "line", { x1: 0, y1: Y(wl), x2: W, y2: Y(wl), stroke: "var(--waterline)", "stroke-width": 1.5, opacity: 0.8 });
      } else {
        for (const bx of [ext.x0 + 45, ext.x1 - 55]) {
          el(svg, "path", { d: `M${X(bx)},${Y(-11)} L${X(bx - 16)},${Y(-30)} M${X(bx)},${Y(-11)} L${X(bx + 16)},${Y(-30)} M${X(bx - 10)},${Y(-25)} L${X(bx + 10)},${Y(-25)}`, stroke: "#7a6a4f", "stroke-width": 4, fill: "none", "stroke-linecap": "round" });
        }
        txt(svg, X(ext.x0 + 8), Y(-26), "Werkstatt-Modus · Bewegung simuliert", { class: "cap", fill: "var(--fg-dim)" });
      }
      const hb = s.c + 6.5;
      el(svg, "path", {
        d: `M${X(ext.x0 + 2)},${Y(hb)} L${X(ext.x1 - 18)},${Y(hb)} Q${X(ext.x1 - 4)},${Y(hb - 4)} ${X(ext.x1 - 9)},${Y(-7)} L${X(ext.x0 + 16)},${Y(-12)} Q${X(ext.x0 + 2)},${Y(-11)} ${X(ext.x0 + 2)},${Y(hb)} Z`,
        fill: "var(--hull)",
        opacity: 0.55,
        stroke: "#42597a"
      });
      txt(svg, X(ext.x0 + 6), Y(hb) - 6, "← Bug", { class: "cap" });
      txt(svg, X(ext.x1 - 20), Y(hb) - 6, "Heck →", { class: "cap", "text-anchor": "end" });
      function drawSeat(seat, isRef) {
        const b2 = seat.b, ox = seat.ox, th = rad(b2.theta), rr = seat.r, sdv = seat.dv;
        const XL = (x) => X(x + ox);
        const pinY = b2.pinY, bladeX = -sdv.outb * Math.sin(th), bladeY = wl + b2.lift;
        const bodyCol = isRef ? "#dbe4ee" : "var(--accent2)";
        const jointCol = isRef ? "var(--accent2)" : "var(--accent)";
        const trackEnd = rr.rueh, track0 = trackEnd - rr.rollL;
        el(svg, "line", { x1: XL(track0), y1: Y(s.c - 4), x2: XL(trackEnd), y2: Y(s.c - 4), stroke: "#9aa7b5", "stroke-width": 3 });
        for (const e of [track0, trackEnd]) el(svg, "line", { x1: XL(e), y1: Y(s.c - 6.5), x2: XL(e), y2: Y(s.c - 1.5), stroke: "#9aa7b5", "stroke-width": 2 });
        const tA = rad(rr.anlage * 4);
        el(svg, "line", { x1: XL(0), y1: Y(pinY - 3), x2: XL(Math.sin(tA) * 10), y2: Y(pinY + 7), stroke: "#e4ecf7", "stroke-width": 3 });
        el(svg, "circle", { cx: XL(0), cy: Y(pinY), r: 4, fill: "#e4ecf7", stroke: "#42597a" });
        if (isRef) {
          el(svg, "line", { x1: XL(0), y1: Y(-8), x2: XL(0), y2: Y(pinY + 14), stroke: "#fff", "stroke-dasharray": "4 5", opacity: 0.4 });
          plateLabel(svg, W - 16, narrowLabels ? 50 : 24, "Dollenlot", { anchor: "end", leader: { x: XL(0), y: Y(-8) }, className: "side-dollenlot-label" });
          plateLabel(svg, W / 2, 24, `Anlage ${fmt(rr.anlage)}° · 4× dargestellt`, { anchor: "middle", leader: { x: XL(0), y: Y(pinY) } });
          dim(svg, XL(0), Y(s.c - 10), XL(trackEnd), Y(s.c - 10), "");
          plateLabel(svg, W - 16, H - 14, `Überstand ${fmt(rr.rueh)} cm`, { anchor: "end", leader: { x: (XL(0) + XL(trackEnd)) / 2, y: Y(s.c - 10) }, className: "side-track-label" });
        }
        const bw = rad(rr.stemmW), bTop = { x: b2.foot.x + 30 * Math.cos(bw), y: b2.foot.y + 30 * Math.sin(bw) };
        el(svg, "line", { x1: XL(b2.foot.x), y1: Y(b2.foot.y), x2: XL(bTop.x), y2: Y(bTop.y), stroke: "var(--wood)", "stroke-width": 4, "stroke-linecap": "round" });
        if (isRef) {
          txt(svg, XL(bTop.x) + 6, Y(bTop.y) + 2, `Stemmbrett ${fmt(rr.stemmW)}°`, {});
          dim(svg, XL(0), Y(b2.foot.y - 4), XL(b2.foot.x), Y(b2.foot.y - 4), "");
          plateLabel(svg, 16, H - 14, `Fa ${fmt(b2.r.stemmX)} cm`, { leader: { x: (XL(0) + XL(b2.foot.x)) / 2, y: Y(b2.foot.y - 4) }, className: "side-fa-label" });
        }
        el(svg, "line", { x1: XL(b2.hand.x), y1: Y(b2.hand.y), x2: XL(0), y2: Y(pinY), stroke: "var(--wood)", "stroke-width": 2.6 });
        const neckF = (sdv.outb - BL) / sdv.outb;
        const neck = { x: XL(bladeX * neckF), y: Y(lerp(pinY, bladeY, neckF)) };
        el(svg, "line", { x1: XL(0), y1: Y(pinY), x2: neck.x, y2: neck.y, stroke: "var(--wood)", "stroke-width": 2.6 });
        const cosB = Math.sqrt(Math.max(0, 1 - b2.sinB * b2.sinB));
        drawBlade(svg, neck, { x: -Math.sin(th) * cosB, y: b2.sinB }, BL * sc, { x: -b2.g * Math.cos(th), y: -(1 - b2.g) }, BW * sc, blade, false);
        if (isRef && s.mode === "wasser" && !state.recovery && s.t > 4 && s.t < 98) {
          const len = 16 + 34 * Math.sin(Math.PI * s.t / 100);
          arrow(svg, XL(bladeX), Y(bladeY), XL(bladeX - len * 0.9), Y(bladeY - len * 0.4), "var(--accent)", 3);
          txt(svg, XL(bladeX - len) - 4, Y(bladeY - len * 0.45) - 6, "Blattdruck (schematisch)", { fill: "var(--accent2)", class: "cap", "text-anchor": "end" });
        }
        el(svg, "rect", { x: XL(b2.hip.x - 11), y: Y(s.c), width: 22 * sc, height: 3.2 * sc, rx: 2, fill: "#9aa7b5" });
        const st = { stroke: bodyCol, "stroke-width": 3.4, "stroke-linecap": "round", "stroke-linejoin": "round", fill: "none" };
        el(svg, "path", { d: `M${XL(b2.hip.x)},${Y(b2.hip.y)} L${XL(b2.kneeP.x)},${Y(b2.kneeP.y)} L${XL(b2.foot.x)},${Y(b2.foot.y)}`, ...st });
        {
          const dxs = b2.sh.x - b2.hip.x, dys = b2.sh.y - b2.hip.y, dl = Math.hypot(dxs, dys) || 1;
          let nx = -dys / dl, ny = dxs / dl;
          if (nx > 0) {
            nx = -nx;
            ny = -ny;
          }
          const bulge = (b2.flex || 0) * 0.6;
          const mxp = (b2.hip.x + b2.sh.x) / 2 + nx * bulge, myp = (b2.hip.y + b2.sh.y) / 2 + ny * bulge;
          el(svg, "path", { d: `M${XL(b2.hip.x)},${Y(b2.hip.y)} Q${XL(mxp)},${Y(myp)} ${XL(b2.sh.x)},${Y(b2.sh.y)}`, ...st, "stroke-width": 4 });
        }
        b2.arms.forEach((A, i) => {
          const op = i === 0 ? 1 : 0.6;
          el(svg, "path", { d: `M${XL(A.S.x)},${Y(A.S.y)} L${XL(A.E.x)},${Y(A.E.y)} L${XL(A.W.x)},${Y(A.W.y)}`, ...st, "stroke-width": 2.6, opacity: op });
          el(svg, "circle", { cx: XL(A.E.x), cy: Y(A.E.y), r: 2.4, fill: jointCol, opacity: op });
          el(svg, "circle", { cx: XL(A.W.x), cy: Y(A.W.y), r: 3.4, fill: jointCol, opacity: op });
        });
        for (const p of [b2.hip, b2.kneeP, b2.sh]) el(svg, "circle", { cx: XL(p.x), cy: Y(p.y), r: 2.6, fill: jointCol });
        el(svg, "circle", { cx: XL(b2.head.x), cy: Y(b2.head.y), r: b2.head.r * sc, fill: "none", stroke: bodyCol, "stroke-width": 2.8 });
        if (seats.length > 1) txt(svg, XL(b2.head.x), Y(b2.head.y + b2.head.r + 3), visualName(seat.name, "Ruderer", 18), { "text-anchor": "middle", fill: bodyCol, class: "cap" });
        if (isRef && s.t < 10 && !state.recovery) {
          el(svg, "line", { x1: XL(b2.sh.x), y1: Y(b2.sh.y), x2: XL(b2.sh.x + 70), y2: Y(b2.sh.y - 70 * Math.tan(rad(6))), stroke: "#70b9e8", "stroke-dasharray": "4 4", "stroke-width": 1.4 });
          const armLabel = narrowLabels ? `Arme ${fmt(b2.armAng, 0)}° · Ref. 6° offen` : `Armmodell ${fmt(b2.armAng, 0)}° · Referenz 6° offen`;
          plateLabel(svg, 12, narrowLabels ? 76 : 24, armLabel, { leader: { x: XL(b2.sh.x), y: Y(b2.sh.y) } });
        }
      }
      [...seats].sort((a, b2) => a.ox - b2.ox).forEach((seat) => drawSeat(seat, seat === primary));
      const b = primary.b, ref = seats.find((x) => x.ref) || seats[0], rb = ref.b;
      const dcmp = (a, c, u = "") => {
        const dd = a - c;
        return seats.length > 1 && !primary.ref ? ` (${dd >= 0 ? "+" : ""}${fmt(dd, 0)}${u})` : "";
      };
      const rows = [
        ["Körperwinkel", fmt(b.lam, 0) + "° " + (b.lam > 2 ? "Vorlage" : b.lam < -2 ? "Rücklage" : "aufrecht")],
        ["Kniewinkel", fmt(b.knee, 0) + "°" + dcmp(b.knee, rb.knee, "°")],
        ["Ellbogen", fmt(b.arms[0].ang, 0) + "°"],
        ["Griffhöhe ü. W.", fmt(b.hand.y - wl, 0) + " cm"],
        ["Reichweite Auslage", fmt(refs.b0.hand.x, 0) + " cm" + dcmp(refs.b0.hand.x, bodyRefs(ref.dv, ref.r).b0.hand.x, "")],
        ["Rollweg", fmt(refs.rollAt(b), 0) + " %"],
        ["Blatt", b.g > 0.5 ? "flach" : "aufgedreht"]
      ];
      const table = $("bodyTab"), caption = table.querySelector("caption");
      const body = document.createElement("tbody");
      for (const [label, value] of rows) {
        const tr = document.createElement("tr");
        const th = document.createElement("th");
        th.scope = "row";
        th.textContent = label;
        const td = document.createElement("td");
        td.className = "v";
        td.textContent = value;
        tr.append(th, td);
        body.appendChild(tr);
      }
      table.replaceChildren(caption, body);
    }
    const PHASES = [
      { n: "Auslage", t: 0, rec: false },
      { n: "Vord. Zug", t: 25, rec: false },
      { n: "Zugmitte", t: 50, rec: false },
      { n: "Hinterer Zug", t: 75, rec: false },
      { n: "Ausheben", t: 100, rec: false },
      { n: "Rückführung", t: 50, rec: true }
    ];
    function currentPhase() {
      if (state.recovery) return 5;
      const t = state.t;
      return t <= 10 ? 0 : t < 40 ? 1 : t < 62 ? 2 : t < 92 ? 3 : 4;
    }
    function renderPhases() {
      const cur = currentPhase();
      const host = $("phases");
      if (host.children.length !== PHASES.length) {
        host.replaceChildren(...PHASES.map((p, i) => {
          const button = document.createElement("button");
          button.type = "button";
          button.className = "phase";
          button.dataset.i = String(i);
          const number = document.createElement("span");
          number.className = "n";
          number.textContent = String(i + 1);
          const title = document.createElement("span");
          title.className = "t";
          title.textContent = p.n;
          button.append(number, title);
          button.addEventListener("click", () => {
            stopPlay();
            state.t = p.t;
            state.recovery = p.rec;
            setDirty();
            render();
          });
          return button;
        }));
      }
      for (const [index, button] of [...host.children].entries()) {
        button.classList.toggle("on", index === cur);
        button.setAttribute("aria-pressed", String(index === cur));
      }
      $("phN").textContent = cur + 1 + "/6";
      $("phName").textContent = PHASES[cur].n;
    }
    const VISUAL_VIEWS = Object.freeze(["top", "side", "cross"]);
    let activeVisualView = "top";
    function visualViewFromKey(current, key) {
      const index = VISUAL_VIEWS.indexOf(current);
      if (index < 0) return null;
      if (key === "Home") return VISUAL_VIEWS[0];
      if (key === "End") return VISUAL_VIEWS.at(-1);
      if (key === "ArrowRight") return VISUAL_VIEWS[(index + 1) % VISUAL_VIEWS.length];
      if (key === "ArrowLeft") return VISUAL_VIEWS[(index - 1 + VISUAL_VIEWS.length) % VISUAL_VIEWS.length];
      return null;
    }
    function activateVisualView(view, { focus = false } = {}) {
      if (!VISUAL_VIEWS.includes(view)) return false;
      activeVisualView = view;
      for (const candidate of VISUAL_VIEWS) {
        const selected = candidate === view;
        const tab = $(`viewTab${candidate[0].toUpperCase()}${candidate.slice(1)}`);
        const panel = $(`viewPanel${candidate[0].toUpperCase()}${candidate.slice(1)}`);
        tab.setAttribute("aria-selected", String(selected));
        tab.tabIndex = selected ? 0 : -1;
        panel.hidden = !selected;
      }
      if (focus) $(`viewTab${view[0].toUpperCase()}${view.slice(1)}`).focus();
      return true;
    }
    function initVisualViewTabs() {
      for (const view of VISUAL_VIEWS) {
        const tab = $(`viewTab${view[0].toUpperCase()}${view.slice(1)}`);
        tab.addEventListener("click", () => activateVisualView(view));
        tab.addEventListener("keydown", (event) => {
          const target = visualViewFromKey(view, event.key);
          if (!target) return;
          event.preventDefault();
          activateVisualView(target, { focus: true });
        });
      }
      activateVisualView(activeVisualView);
    }
    const FOLD_STORAGE_KEY = "rudertrimm:v2:fold";
    let foldState = {};
    try {
      const parsed = JSON.parse(storageAdapter.getItem(FOLD_STORAGE_KEY) || "{}");
      foldState = parsed && typeof parsed === "object" && !Array.isArray(parsed) ? parsed : {};
    } catch (e) {
      foldState = {};
    }
    const isFolded = (id) => !!foldState[id];
    function initFold() {
      for (const card of document.querySelectorAll(".card[data-fold]")) {
        const id = card.dataset.fold;
        card.classList.toggle("closed", isFolded(id));
        const heading = card.querySelector("h2");
        const button = document.createElement("button");
        button.type = "button";
        button.className = "fold-toggle";
        button.setAttribute("aria-expanded", String(!isFolded(id)));
        button.append(...heading.childNodes);
        heading.appendChild(button);
        const toggle = () => {
          foldState[id] = !isFolded(id);
          storageAdapter.setItem(FOLD_STORAGE_KEY, JSON.stringify(foldState));
          card.classList.toggle("closed", foldState[id]);
          button.setAttribute("aria-expanded", String(!foldState[id]));
          render();
        };
        button.addEventListener("click", toggle);
      }
    }
    function isSingle() {
      return state.seats.length === 1;
    }
    function assignmentResolution(seat) {
      const assignment = assignmentFor(seat.id);
      if (!assignment) return seat.rowerRef ? "missing" : "free";
      if (!assignment.rowerRef) return "unsaved";
      const selected = rowerRepository.select(assignment.rowerRef.id);
      if (!selected.ok) return "snapshot-missing";
      return selected.record.revision === assignment.rowerRef.revision ? "resolved" : "stale";
    }
    function crewScopeLabel(presetKey) {
      const boatClass = String(presetKey).replace(/^wm[MW]/u, "");
      const occupied = state.seats.filter((seat) => assignmentFor(seat.id)).length;
      const unresolved = state.seats.filter((seat) => seat.rowerRef && !assignmentFor(seat.id)).length;
      return `${occupied}/${state.seats.length} belegt · ${boatClass}${unresolved ? ` · ${unresolved} Referenz${unresolved === 1 ? "" : "en"} ungeklärt` : ""}`;
    }
    function compactDollenSummary(dv, r) {
      if (state.mode === "wasser") return `Dollenhöhe ü. Wasser ${fmt(dv.dWL)} cm`;
      const fromTrack = state.heightRef === "schiene";
      const reference = fromTrack ? "Schiene" : "Rollsitz";
      const height = r.a + (fromTrack ? state.seatOffset : 0);
      return `Dollenhöhe ü. ${reference} ${fmt(height)} cm`;
    }
    function renderCompactSummaries(dv, r, catchSolution, modelSeat = activeSeat()) {
      const seat = seatLabel(modelSeat);
      const preview = modelSeat?.id !== activeSeat()?.id;
      const rig = dv.skull ? "Skull" : "Riemen";
      const dolle = compactDollenSummary(dv, r);
      const catchText = catchSolution.actualAngleDeg !== null ? `Auslage Ist ${fmt(dv.phiA)}°` : `${catchSolution.naturalResolved ? "Auslage 3D nicht erreichbar" : "Auslage nicht bestimmbar"} · Prüfpose ${fmt(dv.phiA)}°`;
      $("compactRigSummary").textContent = `${preview ? "Referenzvorschau: " : ""}${seat} · ${rig} · DA ${fmt(r.DA)} cm · IH ${fmt(r.IH)} cm · ${dolle} · Fa ${fmt(r.stemmX)} cm · ${catchText} / Rigg-Ziel ${fmt(state.phiA)}°.`;
      $("compactProfileSummary").textContent = `${cleanName(r.name, seat)} · ${fmt(SEG(r).height, 0)} cm · Spannweite ${fmt(r.wingspan, 0)} cm · ${fmt(r.weight, 0)} kg`;
    }
    function renderAssignmentStatus() {
      const seat = activeSeat(), assignment = activeAssignment();
      const status = $("seatAssignmentStatus");
      if (!seat) {
        if (status.textContent !== "Kein Bootsplatz verfügbar. Zuerst eine gültige Bootsklasse wählen.") status.textContent = "Kein Bootsplatz verfügbar. Zuerst eine gültige Bootsklasse wählen.";
        $("seatCreateDraft").disabled = true;
        $("quickEditPerson").disabled = true;
        $("seatUnassign").disabled = true;
        $("quickEditBoat").disabled = true;
        return;
      }
      $("seatCreateDraft").disabled = false;
      $("quickEditPerson").disabled = !assignment;
      $("quickEditBoat").disabled = false;
      $("seatUnassign").disabled = !assignment && !seat.rowerRef;
      const resolution = assignmentResolution(seat);
      let message;
      if (resolution === "free") {
        message = `${seatLabel(seat)} ist frei. Ohne Körperprofil bleibt die Körperrechnung aus; jetzt ein gespeichertes Profil wählen oder „Neues Profil“ anlegen.`;
      } else if (resolution === "missing") {
        message = `${seatLabel(seat)} verweist auf ein nicht verfügbares Profil. Die Körperrechnung bleibt aus; Profil bewusst neu zuordnen oder den Platz freigeben.`;
      } else if (resolution === "snapshot-missing") {
        message = `${seatLabel(seat)} zeigt den gespeicherten Snapshot von „${cleanName(assignment.rower.name, "Profil")}“. Der verknüpfte Datenbankeintrag fehlt; Rechnung ist möglich, aber die Zuordnung muss bewusst repariert oder freigegeben werden.`;
      } else if (resolution === "stale") {
        message = `${seatLabel(seat)} nutzt den gespeicherten Snapshot von „${cleanName(assignment.rower.name, "Profil")}“. Die Datenbank hat eine andere Revision; neu auswählen, um sie bewusst zu übernehmen.`;
      } else if (resolution === "unsaved") {
        message = `${seatLabel(seat)}: „${cleanName(assignment.rower.name, "Ungespeichertes Profil")}“ ist nur im Arbeitsstand zugeordnet und noch nicht als Profil gespeichert.`;
      } else {
        message = `${seatLabel(seat)}: „${cleanName(assignment.rower.name, "Profil")}“ ist mit der gespeicherten Profilrevision verbunden.`;
      }
      if (status.textContent !== message) status.textContent = message;
      const externalStatus = $("externalLinkStatus");
      if (externalStatus) {
        const profileRef = assignment?.rower?.externalRef;
        const boatRef = boatMetadata.externalRef;
        const parts = [];
        if (boatRef) parts.push(`Boot: ${boatRef.system} · ${boatRef.scope} · ${boatRef.id}`);
        if (profileRef) parts.push(`Profil: ${profileRef.system} · ${profileRef.scope} · ${profileRef.id}`);
        externalStatus.textContent = parts.length ? `${parts.join(" · ")} · schreibgeschützte Herkunft; kein Rückschreiben in V2.` : "Keine externe Verknüpfung · eFa-Leseadapter noch nicht angebunden.";
      }
    }
    function renderSeatTabs() {
      if (!state.seats.some((seat) => seat.id === state.editSeatId)) state.editSeatId = state.seats[0]?.id ?? null;
      if (rowerSelection.snapshot().id && !rowerSelection.isFor(state.editSeatId)) clearRowerSelection();
      const tabs = state.seats.map((seat) => [seat.id, seatLabel(seat)]);
      const host = $("seattabs");
      const existing = [...host.children].map((button) => button.dataset.seat);
      if (existing.length !== tabs.length || tabs.some(([id], index) => existing[index] !== id)) {
        host.replaceChildren(...tabs.map(([id, sub]) => {
          const button = document.createElement("button");
          button.type = "button";
          button.dataset.seat = id;
          button.append(document.createTextNode(""));
          const detail = document.createElement("span");
          detail.className = "sub";
          detail.textContent = sub;
          button.appendChild(detail);
          button.addEventListener("click", () => {
            if (state.editSeatId === button.dataset.seat) return;
            state.editSeatId = button.dataset.seat;
            clearRowerSelection();
            buildControls();
            refreshDBSelect();
            render();
          });
          return button;
        }));
      }
      for (const [index, [id, sub]] of tabs.entries()) {
        const button = host.children[index];
        const seat = state.seats[index], assignment = assignmentFor(id), resolution = assignmentResolution(seat);
        const status = assignment ? resolution : seat.rowerRef ? "missing" : "free";
        const problematic = status === "missing" || status === "snapshot-missing" || status === "stale";
        button.className = `${state.editSeatId === id ? "on is-active" : ""} ${problematic ? "is-missing" : assignment ? "is-occupied" : "is-free"}`;
        button.dataset.seatStatus = status === "free" ? "" : status === "missing" ? "Referenz fehlt" : status === "snapshot-missing" ? "Snapshot · Profil fehlt" : status === "stale" ? "Snapshot · Stand veraltet" : status === "unsaved" ? "ungespeichert" : "belegt";
        button.setAttribute("aria-pressed", String(state.editSeatId === id));
        button.setAttribute("aria-label", `${sub}: ${assignment ? cleanName(assignment.rower.name, "Profil") : "frei"}${state.editSeatId === id ? " · aktiv" : ""}`);
        button.firstChild.nodeValue = assignment ? cleanName(assignment.rower.name, "Ungespeichertes Profil") : sub;
        button.querySelector(".sub").textContent = assignment ? sub : `frei${state.editSeatId === id ? " · aktiv" : ""}`;
      }
      $("stepper").hidden = !activeAssignment();
      $("stepper").setAttribute("aria-hidden", String($("stepper").hidden));
      $("rowerName").disabled = !activeAssignment();
      $("rowerName").value = activeRower()?.name ?? "";
      $("rowerName").placeholder = activeAssignment() ? "Name oder Pseudonym" : "Zuerst Profil zuordnen oder neu anlegen";
      $("stepLbl").textContent = state.dbIdx >= 0 && state.db[state.dbIdx] ? state.db[state.dbIdx].name : "Datenbank";
      renderAssignmentStatus();
      renderBMI();
    }
    function renderBMI() {
      const r = activeRower(), el2 = $("bmiBadge");
      if (!r) {
        el2.className = "bmi";
        el2.textContent = "Körperrechnung aus · Profil erforderlich";
        el2.title = "Ohne zugeordnetes Körperprofil wird keine Körperrechnung ausgeführt.";
        return;
      }
      const h = SEG(r).height / 100, bmi = r.weight / (h * h);
      const cat = bmi < 18.5 ? ["lo", "schlank"] : bmi < 25 ? ["ok", "normal"] : bmi < 30 ? ["hi", "kräftig"] : ["vhi", "schwer"];
      el2.className = "bmi " + cat[0];
      const strong = document.createElement("b");
      strong.textContent = fmt(bmi, 1);
      el2.replaceChildren(document.createTextNode("BMI "), strong, document.createTextNode(` · ${cat[1]}`));
      el2.title = `Body-Mass-Index = ${fmt(r.weight, 0)} kg / (${fmt(SEG(r).height / 100, 2)} m)²`;
    }
    $("rowerName").addEventListener("input", (e) => {
      const rower = activeRower();
      if (!rower) return;
      const name = limitName(e.target.value);
      e.target.value = name;
      rower.name = name;
      markProfileDraftChanged(state.editSeatId);
      setDirty();
      renderSeatTabs();
      render();
    });
    function renderForceCompare(seats) {
      const box = $("forceCmp");
      if (seats.length < 2) {
        box.style.display = "none";
        return;
      }
      box.style.display = "";
      const ref = seats.find((x) => x.ref), other = seats.find((x) => !x.ref);
      const ratio = (seat) => seat.dv.outb / seat.dv.inb;
      const rRef = ratio(ref), rOther = ratio(other);
      const d = rOther - rRef;
      for (const [host, seat, value] of [[$("fcS1"), ref, rRef], [$("fcS2"), other, rOther]]) {
        const strong = document.createElement("b");
        strong.textContent = `1 : ${fmt(value, 2)}`;
        host.replaceChildren(document.createTextNode(`${cleanName(seat.name, "Ruderer")} `), strong);
      }
      const dEl = $("fcDelta");
      const closeEnough = Math.abs(d) <= 0.03;
      const solution = solveInboardForRatio({
        L: other.r.L,
        d: other.r.d,
        targetRatio: rRef,
        range: RANGES[state.rig].IH,
        step: 0.5
      });
      const noFurtherChange = Math.abs(other.r.IH - solution.IH) < 1e-9;
      dEl.textContent = closeEnough ? "✓ angeglichen" : `Δ ${d > 0 ? "+" : ""}${fmt(d, 2)}`;
      dEl.className = "fc-delta " + (closeEnough ? "" : Math.abs(d) <= 0.1 ? "warn" : "bad");
      const equalize = $("fcEqualize"), status = $("fcEqualizeStatus");
      equalize.disabled = closeEnough || noFurtherChange;
      const explanation = closeEnough ? "Hebelverhältnisse stimmen bereits überein" : noFurtherChange && solution.clamped ? `Zielverhältnis liegt außerhalb des zulässigen Innenhebelbereichs; Grenze ${fmt(solution.IH)} cm, verbleibendes Δ ${fmt(solution.delta, 2)}` : noFurtherChange ? `Mit 0,5-cm-Schritten nicht genauer erreichbar; verbleibendes Δ ${fmt(solution.delta, 2)}` : "";
      equalize.title = explanation || `Innenhebel von ${other.name} so anpassen, dass sein Hebelverhältnis dem von ${ref.name} entspricht (Ruderlänge bleibt)`;
      if (status.textContent !== explanation) status.textContent = explanation;
      status.hidden = !explanation;
    }
    $("fcEqualize").addEventListener("click", () => {
      const seats = buildSeats(), ref = seats.find((x) => x.ref), other = seats.find((x) => !x.ref);
      if (!ref || !other) return;
      const targetRatio = ref.dv.outb / ref.dv.inb;
      const solution = solveInboardForRatio({
        L: other.r.L,
        d: other.r.d,
        targetRatio,
        range: RANGES[state.rig].IH,
        step: 0.5
      });
      const targetSeat = state.seats.find((seat) => seat.id === other.seatId);
      if (!targetSeat) return;
      targetSeat.IH = solution.IH;
      setDirty();
      if (state.editSeatId === targetSeat.id) buildControls();
      render();
      announce(solution.clamped ? `Innenhebel auf ${fmt(solution.IH)} cm begrenzt; Zielverhältnis nicht vollständig erreichbar, verbleibendes Δ ${fmt(solution.delta, 2)}.` : `Innenhebel auf ${fmt(solution.IH)} cm gesetzt; erreichtes Verhältnis 1 : ${fmt(solution.achievedRatio, 2)}.`);
    });
    function renderVitruv(r) {
      const W = 220, Hd = 224, cx = 110, cy = 112;
      const seg = SEG(r), col = state.editSeatId === state.referenceSeatId ? "#f2e9d8" : "var(--accent2)";
      const guideCol = "#8f7255", secondaryCol = "#c9925a";
      const height = seg.height;
      const rawScale = Math.max(seg.height, r.wingspan) * (160 / 215);
      const S = Math.min(160, rawScale);
      const half = S / 2, sqTop = cy - half, sqBot = cy + half;
      const navelY = sqBot - 0.615 * S, R = S * 0.575;
      const svg = newSVG($("vitruv"), W, Hd, `Proportionsgrafik für ${cleanName(r.name, "Ruderer")}: ${fmt(seg.height, 0)} cm Größe, ${fmt(r.wingspan, 0)} cm Spannweite und ${fmt(r.weight, 0)} kg`);
      const description = el(svg, "desc", { id: "vitruvSvgDescription" });
      description.textContent = `Technische Proportions-Doppelpose von ${cleanName(r.name, "Ruderer")}. Die helle Primärpose zeigt Körpergröße und Spannweite; die bronzefarbene Sekundärpose dient als Konstruktionsreferenz.`;
      svg.setAttribute("aria-describedby", "vitruvSvgDescription");
      const guides = el(svg, "g", { class: "vitruv-guides", "aria-hidden": "true" });
      el(guides, "circle", { cx, cy: navelY, r: R, fill: "none", stroke: guideCol, "stroke-width": 0.85, opacity: 0.42, "stroke-dasharray": "5 5" });
      el(guides, "rect", { x: cx - half, y: sqTop, width: S, height: S, fill: "none", stroke: guideCol, "stroke-width": 0.75, opacity: 0.34, "stroke-dasharray": "3 5" });
      const sw = S / Math.max(seg.height, r.wingspan);
      const figTop = sqBot - height * sw;
      const yAt = (v) => sqBot - v * sw;
      const yFoot = sqBot, yKnee = yAt(seg.US), yHip = yAt(seg.US + seg.OS), ySh = yAt(seg.US + seg.OS + seg.T), yNeck = yAt(seg.US + seg.OS + seg.T + seg.NECK), yCrown = figTop;
      const shH = r.SB / 2 * sw, hipH = shH * 0.7, arm = (seg.OA + seg.UA) * sw;
      const primary = el(svg, "g", { class: "vitruv-primary", "aria-hidden": "true" });
      const secondary = el(svg, "g", { class: "vitruv-secondary", "aria-hidden": "true" });
      const ink = { stroke: col, fill: "none", "stroke-linecap": "round", "stroke-linejoin": "round" };
      const faint = { stroke: secondaryCol, fill: "none", "stroke-linecap": "round", "stroke-linejoin": "round", "stroke-width": 1.15, opacity: 0.38 };
      const limb = { ...ink, "stroke-width": 4.2 };
      const joint = { fill: col, opacity: 0.92 };
      const waistY = lerp(ySh, yHip, 0.55), waistH = hipH * 0.76;
      el(primary, "path", {
        class: "vitruv-torso",
        d: `M${cx - shH},${ySh} C${cx - shH * 0.92},${ySh + 7} ${cx - waistH},${waistY} ${cx - hipH},${yHip} Q${cx},${yHip + 5} ${cx + hipH},${yHip} C${cx + waistH},${waistY} ${cx + shH * 0.92},${ySh + 7} ${cx + shH},${ySh} Q${cx},${ySh + 3} ${cx - shH},${ySh} Z`,
        fill: col,
        opacity: 0.24,
        stroke: col,
        "stroke-width": 2.15,
        "stroke-linejoin": "round"
      });
      el(primary, "path", { class: "vitruv-neck", d: `M${cx - shH * 0.28},${ySh + 1} L${cx - shH * 0.18},${yNeck} M${cx + shH * 0.28},${ySh + 1} L${cx + shH * 0.18},${yNeck}`, ...ink, "stroke-width": 2.2 });
      const headRy = Math.max(6, (yNeck - yCrown) / 2), headRx = headRy * 0.78;
      el(primary, "ellipse", { class: "vitruv-head", cx, cy: (yNeck + yCrown) / 2, rx: headRx, ry: headRy, fill: col, opacity: 0.16, stroke: col, "stroke-width": 2.3 });
      el(primary, "path", { class: "vitruv-pelvis", d: `M${cx - hipH},${yHip} Q${cx},${yHip + 5} ${cx + hipH},${yHip}`, ...ink, "stroke-width": 2.4 });
      const legSpread = rad(23), footR = { x: cx + R * Math.sin(legSpread), y: navelY + R * Math.cos(legSpread) };
      const footL = { x: cx - R * Math.sin(legSpread), y: navelY + R * Math.cos(legSpread) };
      for (const F of [footR, footL]) {
        const sgn = Math.sign(F.x - cx) || 1;
        const knee = { x: (cx + F.x) / 2 + sgn * 4, y: (yHip + F.y) / 2 };
        el(secondary, "path", { d: `M${cx + sgn * hipH * 0.58},${yHip} Q${knee.x},${knee.y - 2} ${knee.x},${knee.y} Q${F.x - sgn * 3},${F.y - 3} ${F.x},${F.y}`, ...faint });
        el(secondary, "circle", { cx: knee.x, cy: knee.y, r: 1.25, fill: secondaryCol, opacity: 0.42 });
      }
      const armRaise = rad(38);
      for (const sgn of [1, -1]) {
        const H2 = { x: cx + sgn * R * Math.sin(armRaise), y: navelY - R * Math.cos(armRaise) };
        const elbow = { x: cx + sgn * (shH + arm * 0.5), y: ySh - arm * 0.35 };
        el(secondary, "path", { d: `M${cx + sgn * shH},${ySh} Q${elbow.x - sgn * 3},${elbow.y + 2} ${elbow.x},${elbow.y} Q${H2.x - sgn * 2},${H2.y + 2} ${H2.x},${H2.y}`, ...faint });
        el(secondary, "circle", { cx: elbow.x, cy: elbow.y, r: 1.2, fill: secondaryCol, opacity: 0.42 });
      }
      for (const sgn of [1, -1]) {
        const hipX = cx + sgn * hipH * 0.58, kneeX = cx + sgn * S * 0.045, ankleX = cx + sgn * S * 0.052, footX = cx + sgn * S * 0.075;
        el(primary, "path", { class: "vitruv-limb", d: `M${hipX},${yHip} Q${kneeX - sgn * 2},${yKnee - 2} ${kneeX},${yKnee} Q${ankleX},${yFoot - 8} ${ankleX},${yFoot}`, ...limb });
        el(primary, "line", { class: "vitruv-foot", x1: ankleX, y1: yFoot, x2: footX, y2: yFoot, ...ink, "stroke-width": 3.5 });
        el(primary, "circle", { class: "vitruv-joint", cx: kneeX, cy: yKnee, r: 1.75, ...joint });
      }
      const elbowY = ySh + arm * 0.06, handX = r.wingspan / 2 * sw;
      for (const sgn of [1, -1]) {
        const shoulderX = cx + sgn * shH, elbowX = cx + sgn * (shH + arm * 0.5), handPosX = cx + sgn * handX;
        el(primary, "path", { class: "vitruv-limb", d: `M${shoulderX},${ySh} Q${elbowX - sgn * 2},${elbowY - 1} ${elbowX},${elbowY} Q${handPosX - sgn * 2},${ySh + 1} ${handPosX},${ySh}`, ...limb });
        el(primary, "circle", { class: "vitruv-joint", cx: elbowX, cy: elbowY, r: 1.75, ...joint });
        el(primary, "line", { class: "vitruv-hand", x1: handPosX - sgn * 3, y1: ySh, x2: handPosX + sgn * 2, y2: ySh, ...ink, "stroke-width": 3.4 });
      }
      txt(svg, cx, Hd - 6, `${fmt(seg.height, 0)} cm · Spannweite ${fmt(r.wingspan, 0)} cm · ${fmt(r.weight, 0)} kg`, { "text-anchor": "middle", fill: col, class: "vitruv-caption" });
    }
    async function loadDB() {
      try {
        await repositoryWrite(rowerRepository, () => {
          const loaded = rowerRepository.load();
          reportRepositoryLoad(loaded, "V2-Rudererdaten");
          if (loaded.ok && loaded.migration?.migrated) {
            rowerRepository.commitPendingMigration({ expectedRevision: loaded.state.revision });
            announce(loaded.migration.recordIds.length ? `${loaded.migration.recordIds.length} Rudererprofile wurden atomar auf das aktuelle Schema migriert.` : "Der lokale Personenverlauf wurde atomar initialisiert; die Profilwerte blieben unverändert.");
          }
          if (canMigrateLegacy(loaded) && rowerRepository.list().length === 0) {
            let candidates = [];
            const rawV2 = storageAdapter.getItem("rudertrimm_rowers_v2");
            if (rawV2) {
              const parsed = JSON.parse(rawV2);
              if (parsed?.kind !== "rudertrimm.rowers" || !Array.isArray(parsed.items)) throw new TypeError("Alte Profildatei hat kein gültiges Format.");
              candidates = parsed.items.map((item) => migrateRowerToCurrent(item).value);
            } else if (storageAdapter.getItem("rudertrimm_db") !== null) {
              const legacy = JSON.parse(storageAdapter.getItem("rudertrimm_db"));
              if (Array.isArray(legacy)) {
                const rejected = [];
                candidates = legacy.flatMap((item, index) => {
                  try {
                    return [profileOf(migrate(item))];
                  } catch (error) {
                    rejected.push({ index, error: error.message });
                    return [];
                  }
                });
                if (rejected.length) announce(`${rejected.length} ungültige alte Rudererprofile wurden übersprungen.`);
              }
            } else {
              candidates = legacyV1WorkspaceCandidate()?.rowers ?? [];
            }
            if (migrateCollection(rowerRepository, "rudertrimm.rowers", candidates, buildRowerDTO)) {
              announce(`${candidates.length} Rudererprofile wurden in den versionierten V2-Speicher übernommen.`);
            }
          }
        });
      } catch (error) {
        console.error("Rudererprofile konnten nicht geladen werden", error);
        announce("Rudererprofile waren ungültig und wurden nicht geladen. Alte Speicherstände bleiben unverändert erhalten.");
      }
      state.db = rowerRepository.list().map((record) => record.value);
    }
    function profileOf(r) {
      return buildRowerDTO({
        externalRef: r.externalRef ?? null,
        name: cleanName(r.name, "Profil"),
        legLen: r.legLen,
        torsoLen: r.torsoLen,
        wingspan: r.wingspan,
        SB: r.SB,
        weight: r.weight,
        stemmX: r.stemmX
      });
    }
    function migrate(p) {
      if (!p || typeof p !== "object" || Array.isArray(p)) throw new TypeError("Profil muss ein Objekt sein");
      if (p.schemaVersion != null) return migrateRowerToCurrent(p).value;
      if (p.legLen != null) return buildRowerDTO({ ...p, externalRef: null });
      const H = p.hgt || 185;
      return buildRowerDTO({
        externalRef: null,
        name: p.name || "Profil",
        legLen: Math.round(0.49 * H),
        torsoLen: Math.round(0.51 * H),
        wingspan: Math.round(H * 1.01),
        SB: p.SB || 40,
        weight: p.weight || 80,
        stemmX: p.stemmX || 42
      });
    }
    function applyProfile(seatId, p, { rowerRef = null, draftDirty = false } = {}) {
      const seat = state.seats.find((candidate) => candidate.id === seatId);
      if (!seat) throw new RangeError("Bootsplatz existiert nicht");
      const profile = profileOf(migrate(p));
      if (rowerRef && state.seats.some((item) => item.id !== seatId && item.rowerRef?.id === rowerRef.id)) {
        throw new RangeError("Dieses Rudererprofil ist bereits einem anderen Platz zugeordnet.");
      }
      const assignment = {
        schemaVersion: SCHEMA_VERSION,
        kind: "crewAssignment",
        seatId,
        trimId: seat.trimId,
        rowerRef: rowerRef ? { ...rowerRef } : null,
        rower: clone(profile)
      };
      seat.rowerRef = assignment.rowerRef ? { ...assignment.rowerRef } : null;
      state.crew = state.crew.filter((item) => item.seatId !== seatId);
      state.crew.push(assignment);
      state.crew.sort((left, right) => state.seats.find((seat2) => seat2.id === left.seatId).position - state.seats.find((seat2) => seat2.id === right.seatId).position);
      ensureDraftState(seatId);
      profileDraftVersion[seatId] += 1;
      profileDraftDirty[seatId] = draftDirty;
      return assignment;
    }
    function canonicalizeRowerName(seatId) {
      const assignment = assignmentFor(seatId);
      if (!assignment) return null;
      const seat = state.seats.find((candidate) => candidate.id === seatId);
      const previous = assignment.rower.name;
      assignment.rower.name = cleanName(previous, seat?.role === "stroke" ? "Schlagprofil" : `Ruderer Platz ${seat?.position ?? ""}`);
      if (assignment.rower.name !== previous) markProfileDraftChanged(seatId);
      if (state.editSeatId === seatId) $("rowerName").value = assignment.rower.name;
      return assignment.rower.name;
    }
    function confirmProfileReplacement(seatId, message) {
      return !profileDraftDirty[seatId] || confirm(`${message}

Ungespeicherte Rudererwerte dieses Platzes gehen sonst verloren.`);
    }
    function clearRowerSelection() {
      rowerSelection.clear();
      state.dbIdx = -1;
      const select = $("dbSelect");
      if (select) {
        select.value = "";
        select.title = "";
      }
      if ($("dbUpdate")) $("dbUpdate").disabled = true;
      if ($("dbDelete")) $("dbDelete").disabled = true;
    }
    function updateMasterPersonSelection() {
      const select = $("masterPersonSelect");
      if (!select) return;
      const selected = select.value ? rowerRepository.select(select.value) : { ok: false };
      $("masterPersonUse").disabled = !selected.ok || !activeSeat();
      $("masterPersonDelete").disabled = !selected.ok;
      $("masterPersonStatus").textContent = selected.ok ? `${cleanName(selected.record.value.name, "Profil")} · ${fmt(selected.record.value.legLen + selected.record.value.torsoLen, 0)} cm · Revision ${selected.record.revision} · noch nicht zugeordnet` : `${rowerRepository.list().length} gespeicherte Personenprofile · Auswahl allein verändert keine Bootsbelegung.`;
    }
    function refreshMasterPersonSelect() {
      const select = $("masterPersonSelect");
      if (!select) return;
      const previous = select.value;
      const records = rowerRepository.list();
      replaceOptions(select, "— Person auswählen —", records.map((record) => ({
        value: record.id,
        label: `${cleanName(record.value.name, "Profil")} · ${fmt(record.value.legLen + record.value.torsoLen, 0)} cm · r${record.revision}`
      })));
      select.value = records.some((record) => record.id === previous) ? previous : "";
      updateMasterPersonSelection();
    }
    function refreshDBSelect(selectedId = "", adoptRevision = false, selectionSeat = state.editSeatId) {
      const records = rowerRepository.list();
      state.db = records.map((record) => record.value);
      replaceOptions($("dbSelect"), "— Datenbank wählen —", records.map((record) => ({
        value: record.id,
        label: `${cleanName(record.value.name, "Profil")} · ${fmt(record.value.legLen + record.value.torsoLen, 0)} cm · r${record.revision} · ${record.id.slice(0, 6)}`
      })));
      const assignedId = activeAssignment()?.rowerRef?.id ?? "";
      if (!selectedId) selectedId = assignedId;
      const selection = selectedId ? rowerRepository.select(selectedId) : { ok: false };
      $("dbSelect").value = selection.ok ? selectedId : "";
      if (!selection.ok) clearRowerSelection();
      else if (adoptRevision && assignmentFor(selectionSeat)?.rowerRef?.id === selection.record.id && assignmentFor(selectionSeat)?.rowerRef?.revision === selection.record.revision) {
        rowerSelection.adopt({ id: selection.record.id, revision: selection.record.revision, context: selectionSeat });
        state.dbIdx = records.findIndex((record) => record.id === selection.record.id);
      } else if (adoptRevision) {
        rowerSelection.clear();
        state.dbIdx = records.findIndex((record) => record.id === selection.record.id);
      }
      const writable = selection.ok && rowerSelection.matches({
        id: selection.record.id,
        revision: selection.record.revision,
        context: state.editSeatId
      });
      $("dbUpdate").disabled = !writable;
      $("dbDelete").disabled = !writable;
      refreshMasterPersonSelect();
      refreshHistoryEntities();
    }
    function sameRowerSelection(left, right) {
      return left.id === right.id && left.revision === right.revision && left.context === right.context;
    }
    function applyCurrentConfig(config, { draftsDirty = false } = {}) {
      markWorkspaceMutation();
      applyBoat(config.boat, { resolveCrew: false });
      state.crew = clone(config.crew);
      state.editSeatId = state.seats.some((seat) => seat.id === config.editSeatId) ? config.editSeatId : state.seats[0].id;
      state.referenceSeatId = state.seats.some((seat) => seat.id === config.referenceSeatId) ? config.referenceSeatId : state.seats[0].id;
      state.mode = config.mode;
      state.heightRef = config.heightRef;
      state.kg = config.kg;
      state.t = config.t;
      state.recovery = config.recovery;
      for (const seat of state.seats) {
        ensureDraftState(seat.id);
        const assignment = assignmentFor(seat.id);
        const linked = assignment?.rowerRef ? rowerRepository.select(assignment.rowerRef.id) : { ok: false };
        const exactStored = linked.ok && linked.record.revision === assignment.rowerRef.revision && JSON.stringify(profileOf(assignment.rower)) === JSON.stringify(linked.record.value);
        profileDraftDirty[seat.id] = !!assignment && (draftsDirty || !exactStored);
      }
    }
    function anyProfileDraftDirty() {
      return Object.values(profileDraftDirty).some(Boolean);
    }
    let demoRestorePoint = null;
    function captureDemoRestorePoint() {
      if (demoRestorePoint) return demoRestorePoint;
      demoRestorePoint = {
        config: currentConfigOf(),
        dirty,
        workspaceRevision: workspaceRevision.snapshot(),
        presentationMode: document.body.dataset.presentation === "details" ? "details" : "compact",
        activeVisualView,
        wasPlaying: playing,
        speed: $("speed").value,
        selectedBoatId,
        selectedBoatRevision,
        boatSelect: $("boatSelect").value,
        rowerSelection: rowerSelection.snapshot(),
        dbIdx: state.dbIdx,
        profileDraftDirty: { ...profileDraftDirty },
        profileDraftVersion: { ...profileDraftVersion }
      };
      return demoRestorePoint;
    }
    function showDemoIndicator(label) {
      $("demoBadge").hidden = false;
      $("demoBadge").textContent = `Synthetische Demo · ${label}`;
      $("removeDemo").hidden = false;
      document.body.dataset.demo = "synthetic";
    }
    function clearDemoIndicator() {
      $("demoBadge").hidden = true;
      $("demoBadge").textContent = "Synthetische Demo";
      $("removeDemo").hidden = true;
      delete document.body.dataset.demo;
    }
    function endDemoSession() {
      if (!demoRestorePoint) return false;
      demoRestorePoint = null;
      clearDemoIndicator();
      return true;
    }
    function loadTestielDemo() {
      const hasUnsaved = dirty || anyProfileDraftDirty();
      if (!confirmOverwrite("Die synthetische Demo „Testiel“ ersetzt die aktuellen Boot-, Ruderer- und Arbeitswerte.", hasUnsaved)) return false;
      captureDemoRestorePoint();
      const config = buildTestielDemoConfig();
      stopPlay();
      clearRowerSelection();
      refreshBoatSelect();
      applyCurrentConfig(config, { draftsDirty: true });
      lastPreset = config.boat.preset;
      lastBlade = config.boat.blade;
      buildControls();
      updateRig();
      updateMode();
      setDirty(true);
      render();
      $("loadTestielDemo").focus();
      showDemoIndicator("Testiel · Einer");
      $("testielDemoNote").textContent = "Demo „Testiel“ geladen – synthetisch; nichts wurde automatisch gespeichert.";
      return true;
    }
    $("loadTestielDemo").addEventListener("click", loadTestielDemo);
    function loadComparisonDemo() {
      const hasUnsaved = dirty || anyProfileDraftDirty();
      if (!confirmOverwrite("Die synthetische Vergleichsdemo ersetzt die aktuellen Boot-, Ruderer- und Arbeitswerte.", hasUnsaved)) return false;
      captureDemoRestorePoint();
      const config = buildTestielComparisonDemoConfig();
      stopPlay();
      clearRowerSelection();
      refreshBoatSelect();
      applyCurrentConfig(config, { draftsDirty: true });
      lastPreset = config.boat.preset;
      lastBlade = config.boat.blade;
      buildControls();
      updateRig();
      updateMode();
      setDirty(true);
      render();
      $("loadComparisonDemo").focus();
      showDemoIndicator("Testiel + Testiel 2 · 4x");
      $("testielDemoNote").textContent = "Synthetische Vergleichsdemo geladen – noch nicht gespeichert.";
      return true;
    }
    $("loadComparisonDemo").addEventListener("click", loadComparisonDemo);
    function restoreBeforeDemo() {
      if (!demoRestorePoint) return false;
      const previous = demoRestorePoint;
      try {
        const currentWorkspaceRevision = workspaceRevision.snapshot();
        const persistenceUnchanged = currentWorkspaceRevision.observedRevision === previous.workspaceRevision.observedRevision && currentWorkspaceRevision.externalRevision === previous.workspaceRevision.externalRevision && currentWorkspaceRevision.stale === previous.workspaceRevision.stale;
        stopPlay();
        clearRowerSelection();
        applyCurrentConfig(previous.config, { draftsDirty: false });
        lastPreset = previous.config.boat.preset;
        lastBlade = previous.config.boat.blade;
        for (const key of Object.keys(profileDraftDirty)) delete profileDraftDirty[key];
        Object.assign(profileDraftDirty, previous.profileDraftDirty);
        for (const [seatId, version] of Object.entries(previous.profileDraftVersion)) {
          profileDraftVersion[seatId] = Math.max(profileDraftVersion[seatId] ?? 0, version + 1);
        }
        const selected = previous.selectedBoatId ? boatRepository.select(previous.selectedBoatId) : { ok: false };
        const boatSelectionCurrent = selected.ok && selected.record.revision === previous.selectedBoatRevision;
        refreshBoatSelect(boatSelectionCurrent ? previous.boatSelect : "", boatSelectionCurrent);
        state.dbIdx = previous.dbIdx;
        buildControls();
        updateRig();
        updateMode();
        updateCoxControl();
        $("speed").value = previous.speed;
        updateSpeed();
        setPresentationMode(previous.presentationMode);
        activateVisualView(previous.activeVisualView);
        render();
        if (previous.rowerSelection.id) {
          refreshDBSelect(previous.rowerSelection.id, true, previous.rowerSelection.context);
        } else clearRowerSelection();
        dirty = persistenceUnchanged ? previous.dirty : true;
        demoRestorePoint = null;
        clearDemoIndicator();
        updateWorkspaceSaveState();
        updateContext();
        if (previous.wasPlaying) startPlay({ markDirty: false });
        dirty = persistenceUnchanged ? previous.dirty : true;
        updateContext();
        $("loadComparisonDemo").focus();
        announce(!persistenceUnchanged ? "Synthetische Demo entfernt · vorherige Werte wiederhergestellt; ein externer Arbeitsstandkonflikt bleibt gesperrt und ungespeichert." : boatSelectionCurrent || !previous.selectedBoatId ? "Synthetische Demo entfernt · vorheriger Arbeitsstand vollständig wiederhergestellt." : "Synthetische Demo entfernt · vorherige Werte wiederhergestellt; die damalige Bootsdatenbank-Revision ist inzwischen veraltet.");
        return true;
      } catch (error) {
        reportError(error, "Vorheriger Arbeitsstand konnte nach der Demo nicht wiederhergestellt werden");
        return false;
      }
    }
    $("removeDemo").addEventListener("click", restoreBeforeDemo);
    function finishProfileCommit(record, { saveSeat, draftVersion, selectionAtStart }) {
      const selectionBeforeFinish = rowerSelection.snapshot();
      const selectedIdBeforeFinish = $("dbSelect").value;
      const policy = profileCommitPolicy({
        savedContext: saveSeat,
        activeContext: state.editSeatId,
        selectionUnchanged: sameRowerSelection(selectionAtStart, selectionBeforeFinish),
        savedVersion: draftVersion,
        currentVersion: profileDraftVersion[saveSeat]
      });
      if (policy.adoptSelection) {
        applyProfile(saveSeat, record.value, { rowerRef: { id: record.id, revision: record.revision }, draftDirty: false });
        refreshDBSelect(record.id, true, saveSeat);
        state.dbIdx = rowerRepository.list().findIndex((entry) => entry.id === record.id);
        setDirty(true);
      } else {
        refreshDBSelect(selectedIdBeforeFinish);
      }
      if (policy.clearDraft) profileDraftDirty[saveSeat] = false;
      renderSeatTabs();
      updateContext();
      return policy;
    }
    function assignStoredProfileToSeat(id, seatId = state.editSeatId) {
      const selected = rowerRepository.select(id);
      if (!selected.ok) {
        refreshDBSelect(activeAssignment()?.rowerRef?.id ?? "");
        announce("Zum Freigeben bitte „Platz freigeben“ verwenden; die Leerauswahl überschreibt nichts.");
        return false;
      }
      const seat = state.seats.find((candidate) => candidate.id === seatId);
      if (!seat || seat.id !== state.editSeatId) {
        announce("Der Zielplatz hat sich geändert. Profil bitte für den jetzt aktiven Platz erneut auswählen.");
        return false;
      }
      const previous = rowerSelection.snapshot();
      if (!confirmProfileReplacement(seat.id, "Das gewählte Profil ersetzt Name und Körpermaße des aktiven Platzes; sein Trimmprofil bleibt erhalten")) {
        refreshDBSelect(previous.id);
        return false;
      }
      try {
        applyProfile(seat.id, selected.record.value, { rowerRef: { id: selected.record.id, revision: selected.record.revision } });
        refreshDBSelect(selected.record.id, true, seat.id);
        state.dbIdx = rowerRepository.list().findIndex((record) => record.id === selected.record.id);
        buildControls();
        renderSeatTabs();
        setDirty();
        render();
        announce(`${cleanName(selected.record.value.name, "Profil")} wurde ${seatLabel(seat)} zugeordnet · Arbeitsstand noch speichern.`);
        return true;
      } catch (error) {
        refreshDBSelect(previous.id || (activeAssignment()?.rowerRef?.id ?? ""));
        reportError(error, "Rudererprofil konnte dem Platz nicht zugeordnet werden");
        return false;
      }
    }
    $("dbSelect").addEventListener("change", (e) => {
      e.target.title = "";
      assignStoredProfileToSeat(e.target.value);
    });
    $("masterPersonSelect").addEventListener("change", updateMasterPersonSelection);
    $("masterPersonUse").addEventListener("click", () => {
      if (assignStoredProfileToSeat($("masterPersonSelect").value)) $("dbSelect").focus();
    });
    $("masterPersonManage").addEventListener("click", () => {
      setPresentationMode("details");
      $("dbSelect").focus();
      $("dbSelect").scrollIntoView({ block: "center" });
    });
    $("masterPersonDelete").addEventListener("click", async (event) => {
      const selected = rowerRepository.select($("masterPersonSelect").value);
      if (!selected.ok) {
        updateMasterPersonSelection();
        return;
      }
      await deleteStoredRowerProfile({
        id: selected.record.id,
        expectedRecordRevision: selected.record.revision,
        trigger: event.currentTarget
      });
    });
    $("seatCreateDraft").addEventListener("click", () => {
      const seat = activeSeat();
      if (!seat) return;
      if (!confirmProfileReplacement(seat.id, "Ein neues ungespeichertes Profil ersetzt die aktuelle Zuordnung dieses Platzes")) return;
      const position = seat.position;
      applyProfile(seat.id, mkRower(`Neues Profil Platz ${position}`, { stemmX: seat.stemmX }), { draftDirty: true });
      clearRowerSelection();
      setDirty();
      setPresentationMode("details");
      buildControls();
      render();
      $("rowerName").focus();
      $("rowerName").select();
      announce(`${seatLabel(seat)}: neues Profil angelegt · Maße prüfen und bewusst speichern.`);
    });
    $("seatUnassign").addEventListener("click", () => {
      const seat = activeSeat(), assignment = activeAssignment();
      if (!seat || !assignment && !seat.rowerRef) return;
      ensureDraftState(seat.id);
      if (profileDraftDirty[seat.id] && !confirm("Ungespeicherte Profiländerungen dieses Platzes verwerfen und den Platz freigeben?")) return;
      state.crew = state.crew.filter((item) => item.seatId !== seat.id);
      seat.rowerRef = null;
      profileDraftDirty[seat.id] = false;
      profileDraftVersion[seat.id] += 1;
      if (!state.seats.some((candidate) => assignmentFor(candidate.id))) stopPlay();
      clearRowerSelection();
      setDirty();
      buildControls();
      render();
      announce(`${seatLabel(seat)} ist frei. Das gespeicherte Körperprofil wurde nicht gelöscht.`);
    });
    $("dbSaveAs").addEventListener("click", async () => {
      try {
        const saveSeat = state.editSeatId;
        canonicalizeRowerName(saveSeat);
        const assignment = assignmentFor(saveSeat);
        if (!assignment) throw new TypeError("Zuerst ein Profil für den aktiven Platz anlegen.");
        const value = profileOf(assignment.rower);
        const draftVersion = profileDraftVersion[saveSeat];
        const selectionAtStart = rowerSelection.snapshot();
        const record = await repositoryWrite(rowerRepository, () => rowerRepository.create(value, { expectedRevision: rowerRepository.snapshot().revision }));
        const policy = finishProfileCommit(record, { saveSeat, draftVersion, selectionAtStart });
        announce(policy.adoptSelection ? "Rudererprofil versioniert gespeichert." : "Rudererprofil gespeichert; die inzwischen aktive Auswahl blieb unverändert.");
      } catch (error) {
        reportError(error, "Rudererprofil konnte nicht gespeichert werden");
      }
    });
    $("dbUpdate").addEventListener("click", async () => {
      const id = $("dbSelect").value, selected = rowerRepository.select(id);
      if (!selected.ok || !rowerSelection.matches({ id, revision: selected.record.revision, context: state.editSeatId })) return;
      const observed = rowerSelection.snapshot();
      try {
        const saveSeat = state.editSeatId;
        canonicalizeRowerName(saveSeat);
        const assignment = assignmentFor(saveSeat);
        if (!assignment) throw new TypeError("Aktiver Platz hat kein Profil.");
        const value = profileOf(assignment.rower);
        const draftVersion = profileDraftVersion[saveSeat];
        const selectionAtStart = observed;
        const updated = await repositoryWrite(rowerRepository, () => rowerRepository.update(id, value, {
          expectedRevision: rowerRepository.snapshot().revision,
          expectedRecordRevision: observed.revision
        }));
        const policy = finishProfileCommit(updated, { saveSeat, draftVersion, selectionAtStart });
        announce(policy.adoptSelection ? "Rudererprofil als neue Revision gespeichert." : "Rudererprofil aktualisiert; die inzwischen aktive Auswahl blieb unverändert.");
      } catch (error) {
        reportError(error, "Rudererprofil konnte nicht überschrieben werden");
      }
    });
    function storedRowerReferences(id) {
      const savedWorkspace = workspaceRepository.get();
      return [.../* @__PURE__ */ new Set([
        ...state.seats.filter((seat) => seat.rowerRef?.id === id).map((seat) => `aktueller ${seatLabel(seat)}`),
        ...(savedWorkspace?.boat?.seats ?? []).filter((seat) => seat.rowerRef?.id === id).map((seat) => `gespeicherter Arbeitsstand, ${seatLabel(seat)}`),
        ...boatRepository.list().flatMap((record) => record.value.seats.filter((seat) => seat.rowerRef?.id === id).map((seat) => `Boot „${cleanName(record.value.name, "Boot")}“, ${seatLabel(seat)}`))
      ])];
    }
    function assertNoRetiredRowerReferences(subject) {
      const seats = Array.isArray(subject?.boat?.seats) ? subject.boat.seats : Array.isArray(subject?.seats) ? subject.seats : [];
      const retiredIds = new Set(rowerRepository.reservedIds().filter((recordId) => !rowerRepository.select(recordId).ok));
      const retiredReference = seats.find((seat) => seat.rowerRef && retiredIds.has(seat.rowerRef.id));
      if (!retiredReference) return;
      const error = new Error(`${seatLabel(retiredReference)} verweist auf ein inzwischen datenschutzkonform gelöschtes Profil. Platz freigeben oder ein aktuelles Profil zuordnen.`);
      error.code = "profile-reference-retired";
      throw error;
    }
    async function deleteStoredRowerProfile({ id, expectedRecordRevision, trigger = null }) {
      const selected = rowerRepository.select(id);
      if (!selected.ok || selected.record.revision !== expectedRecordRevision) return false;
      const name = cleanName(selected.record.value.name, "Profil");
      const references = storedRowerReferences(id);
      if (references.length) {
        const message = `Profil ist noch zugeordnet: ${references.slice(0, 3).join("; ")}. Erst Plätze freigeben, dann löschen.`;
        announce(message);
        if ($("masterPersonSelect").value === id) $("masterPersonStatus").textContent = message;
        return false;
      }
      if (!confirm(`Profil „${name}“ wirklich datenschutzkonform löschen?`)) return false;
      const focusAtStart = document.activeElement === trigger;
      try {
        await repositoryWrite(workspaceRepository, () => repositoryWrite(boatRepository, () => repositoryWrite(rowerRepository, () => {
          const live = rowerRepository.select(id);
          if (!live.ok) throw new NoSelectionError("Profil wurde inzwischen gelöscht.");
          const liveReferences = storedRowerReferences(id);
          if (liveReferences.length) {
            const error = new Error("Profil wurde inzwischen einem Bootsplatz zugeordnet. Zuordnung zuerst lösen.");
            error.code = "profile-in-use";
            throw error;
          }
          return rowerRepository.delete(id, {
            expectedRevision: rowerRepository.snapshot().revision,
            expectedRecordRevision
          });
        })));
        const dbSelection = $("dbSelect").value;
        const masterSelection = $("masterPersonSelect").value;
        if (dbSelection === id) state.dbIdx = -1;
        refreshDBSelect(dbSelection === id ? "" : dbSelection);
        if (focusAtStart && document.activeElement === trigger && masterSelection === id) {
          $("masterPersonSelect").focus();
        }
        announce(`${name} datenschutzkonform gelöscht.`);
        return true;
      } catch (error) {
        reportError(error, "Rudererprofil konnte nicht gelöscht werden");
        return false;
      }
    }
    $("dbDelete").addEventListener("click", async () => {
      const id = $("dbSelect").value, selected = rowerRepository.select(id);
      if (!selected.ok || !rowerSelection.matches({ id, revision: selected.record.revision, context: state.editSeatId })) return;
      await deleteStoredRowerProfile({ id, expectedRecordRevision: selected.record.revision, trigger: $("dbDelete") });
    });
    $("dbExport").addEventListener("click", () => downloadJson(rowerRepository.exportEnvelope(), "rudertrimm-v2-rudererprofile.json"));
    $("dbImport").addEventListener("click", () => $("dbFile").click());
    $("dbFile").addEventListener("change", async (e) => {
      const f = e.target.files[0];
      e.target.value = "";
      if (!f) return;
      try {
        if (f.size > 1e6) throw new RangeError("Datei ist größer als 1 MB");
        const text = await f.text();
        const importOptions = {
          repository: rowerRepository,
          text,
          validateRecord: validateRower,
          builder: buildRowerDTO,
          migrateRecord: migrateRowerToCurrent,
          migrateLegacyImport: legacyCollectionImportMigrator(migrateRowerToCurrent)
        };
        const approvedPreview = previewCollectionImport(importOptions);
        if (!confirmCollectionImport(approvedPreview)) {
          announce("Profilimport abgebrochen.");
          return;
        }
        const result = await repositoryWrite(rowerRepository, () => mergeCollectionImport({
          ...importOptions,
          approve: (fresh) => approveFreshImportPreview(approvedPreview, fresh)
        }));
        state.dbIdx = -1;
        refreshDBSelect();
        announce(`${result.added} Rudererprofile atomar importiert${result.duplicatesSkipped ? ` · ${result.duplicatesSkipped} Dubletten übersprungen` : ""}${result.migrated ? ` · ${result.migrated} Datensätze auf das aktuelle Schema migriert` : ""}${result.remapped ? ` · ${result.remapped} ID-Konflikte aufgelöst` : ""}.`);
      } catch (error) {
        reportError(error, "Profilimport abgelehnt");
      }
    });
    const QUICK_PERSON_FIELDS = Object.freeze([
      { id: "quickPersonLegLen", key: "legLen", min: RANGES.rower.legLen[0], max: RANGES.rower.legLen[1], step: 1 },
      { id: "quickPersonTorsoLen", key: "torsoLen", min: RANGES.rower.torsoLen[0], max: RANGES.rower.torsoLen[1], step: 1 },
      { id: "quickPersonWingspan", key: "wingspan", min: RANGES.rower.wingspan[0], max: RANGES.rower.wingspan[1], step: 1 },
      { id: "quickPersonShoulder", key: "SB", min: RANGES.rower.SB[0], max: RANGES.rower.SB[1], step: 1 },
      { id: "quickPersonWeight", key: "weight", min: RANGES.rower.weight[0], max: RANGES.rower.weight[1], step: 1 }
    ]);
    let personQuickEditContext = null, boatQuickEditContext = null;
    function setQuickEditError(id, message = "") {
      const error = $(id);
      error.hidden = !message;
      error.textContent = message;
    }
    function readQuickNumber(field) {
      const input = $(field.id);
      const message = numericDraftError(input.value, field.min, field.max, field.step);
      input.setAttribute("aria-invalid", String(Boolean(message)));
      return message ? { ok: false, message } : { ok: true, value: Number(input.value) };
    }
    function clearQuickFieldErrors(ids) {
      for (const id of ids) $(id).setAttribute("aria-invalid", "false");
    }
    function updateQuickPersonHeight() {
      const leg = Number($("quickPersonLegLen").value), torso = Number($("quickPersonTorsoLen").value);
      $("quickPersonHeight").textContent = Number.isFinite(leg) && Number.isFinite(torso) ? `Abgeleitete Körpergröße: ${fmt(leg + torso, 0)} cm (Bein- + Rumpflänge)` : "Abgeleitete Körpergröße: gültige Bein- und Rumpflänge erforderlich.";
    }
    function openPersonQuickEdit() {
      const seat = activeSeat(), assignment = activeAssignment();
      if (!seat || !assignment) {
        announce("Zuerst dem aktiven Platz ein Profil zuordnen oder neu anlegen.");
        return false;
      }
      ensureDraftState(seat.id);
      personQuickEditContext = {
        seatId: seat.id,
        trimId: seat.trimId,
        assignment,
        draftVersion: profileDraftVersion[seat.id],
        changeVersion: workspaceChangeVersion,
        opener: document.activeElement
      };
      $("quickPersonName").value = assignment.rower.name;
      for (const field of QUICK_PERSON_FIELDS) $(field.id).value = String(assignment.rower[field.key]);
      clearQuickFieldErrors(["quickPersonName", ...QUICK_PERSON_FIELDS.map((field) => field.id)]);
      setQuickEditError("personQuickEditError");
      updateQuickPersonHeight();
      $("personQuickEditDialog").showModal();
      $("quickPersonName").focus();
      return true;
    }
    function applyPersonQuickEdit() {
      const context = personQuickEditContext;
      const seat = context ? state.seats.find((candidate) => candidate.id === context.seatId) : null;
      const assignment = context ? assignmentFor(context.seatId) : null;
      if (!context || !seat || seat.id !== state.editSeatId || seat.trimId !== context.trimId || assignment !== context.assignment || profileDraftVersion[context.seatId] !== context.draftVersion || workspaceChangeVersion !== context.changeVersion) {
        setQuickEditError("personQuickEditError", "Der aktive Platz oder Arbeitsstand hat sich geändert. Dialog schließen und die Bearbeitung neu starten.");
        return false;
      }
      const name = cleanOptionalName($("quickPersonName").value);
      $("quickPersonName").setAttribute("aria-invalid", String(!name));
      const values = {}, errors = [];
      if (!name) errors.push("Name oder Pseudonym ist erforderlich.");
      for (const field of QUICK_PERSON_FIELDS) {
        const result = readQuickNumber(field);
        if (result.ok) values[field.key] = result.value;
        else errors.push(`${$(`${field.id}`).labels?.[0]?.firstChild?.textContent?.trim() || field.key}: ${result.message}`);
      }
      if (errors.length) {
        setQuickEditError("personQuickEditError", errors.join(" "));
        return false;
      }
      try {
        const candidate = profileOf({ ...assignment.rower, name, ...values });
        applyProfile(context.seatId, candidate, { rowerRef: assignment.rowerRef, draftDirty: true });
        setDirty();
        buildControls();
        render();
        $("personQuickEditDialog").close("apply");
        announce(`${name} im Arbeitsstand geändert · gespeicherte Stammdaten bleiben bis zum bewussten Überschreiben unverändert.`);
        return true;
      } catch (error) {
        setQuickEditError("personQuickEditError", `Eingaben konnten nicht übernommen werden: ${error.message}`);
        return false;
      }
    }
    function openBoatQuickEdit() {
      const seat = activeSeat();
      if (!seat) {
        announce("Zuerst ein Boot mit realem Platz wählen.");
        return false;
      }
      boatQuickEditContext = {
        seatId: seat.id,
        trimId: seat.trimId,
        seat,
        changeVersion: workspaceChangeVersion,
        selectedBoatId,
        selectedBoatRevision,
        opener: document.activeElement
      };
      const [ihMin, ihMax] = RANGES[state.rig].IH;
      $("quickBoatName").value = cleanName($("boatName").value, "Ungespeichertes Boot");
      $("quickBoatInboard").min = String(ihMin);
      $("quickBoatInboard").max = String(ihMax);
      $("quickBoatInboard").value = String(seat.IH);
      $("quickBoatFootboard").value = String(seat.stemmX);
      clearQuickFieldErrors(["quickBoatName", "quickBoatInboard", "quickBoatFootboard"]);
      setQuickEditError("boatQuickEditError");
      $("boatQuickEditDialog").showModal();
      $("quickBoatName").focus();
      return true;
    }
    function applyBoatQuickEdit() {
      const context = boatQuickEditContext;
      const seat = context ? state.seats.find((candidate) => candidate.id === context.seatId) : null;
      if (!context || !seat || seat !== context.seat || seat.id !== state.editSeatId || seat.trimId !== context.trimId || workspaceChangeVersion !== context.changeVersion || selectedBoatId !== context.selectedBoatId || selectedBoatRevision !== context.selectedBoatRevision) {
        setQuickEditError("boatQuickEditError", "Boot, Platz oder Arbeitsstand hat sich geändert. Dialog schließen und die Bearbeitung neu starten.");
        return false;
      }
      const name = cleanOptionalName($("quickBoatName").value);
      $("quickBoatName").setAttribute("aria-invalid", String(!name));
      const [ihMin, ihMax] = RANGES[state.rig].IH;
      const ih = readQuickNumber({ id: "quickBoatInboard", min: ihMin, max: ihMax, step: 0.5 });
      const fa = readQuickNumber({ id: "quickBoatFootboard", min: RANGES.rower.stemmX[0], max: RANGES.rower.stemmX[1], step: 0.5 });
      const errors = [];
      if (!name) errors.push("Bootsname ist erforderlich.");
      if (!ih.ok) errors.push(`Innenhebel: ${ih.message}`);
      if (!fa.ok) errors.push(`Stemmbrettposition: ${fa.message}`);
      if (errors.length) {
        setQuickEditError("boatQuickEditError", errors.join(" "));
        return false;
      }
      try {
        const current = boatOf();
        const candidate = buildBoatDTO({ ...current, name, seats: current.seats.map((item) => item.id === seat.id ? { ...item, IH: ih.value, stemmX: fa.value } : item) });
        const validatedSeat = candidate.seats.find((item) => item.id === seat.id);
        $("boatName").value = candidate.name;
        seat.IH = validatedSeat.IH;
        seat.stemmX = validatedSeat.stemmX;
        setDirty();
        buildControls();
        render();
        $("boatQuickEditDialog").close("apply");
        announce(`${candidate.name}, ${seatLabel(seat)} im Arbeitsstand geändert · gespeicherte Bootsrevision bleibt bis zum bewussten Überschreiben unverändert.`);
        return true;
      } catch (error) {
        setQuickEditError("boatQuickEditError", `Eingaben konnten nicht übernommen werden: ${error.message}`);
        return false;
      }
    }
    function restoreDialogFocus(context) {
      const opener = context?.opener;
      if (opener && typeof opener.focus === "function" && opener.isConnected !== false) opener.focus();
    }
    $("quickEditPerson").addEventListener("click", openPersonQuickEdit);
    $("quickPersonLegLen").addEventListener("input", updateQuickPersonHeight);
    $("quickPersonTorsoLen").addEventListener("input", updateQuickPersonHeight);
    $("quickPersonApply").addEventListener("click", applyPersonQuickEdit);
    $("quickPersonCancel").addEventListener("click", () => $("personQuickEditDialog").close("cancel"));
    $("personQuickEditDialog").addEventListener("close", () => {
      const context = personQuickEditContext;
      personQuickEditContext = null;
      restoreDialogFocus(context);
    });
    $("quickEditBoat").addEventListener("click", openBoatQuickEdit);
    $("quickBoatApply").addEventListener("click", applyBoatQuickEdit);
    $("quickBoatCancel").addEventListener("click", () => $("boatQuickEditDialog").close("cancel"));
    $("boatQuickEditDialog").addEventListener("close", () => {
      const context = boatQuickEditContext;
      boatQuickEditContext = null;
      restoreDialogFocus(context);
    });
    function stepActiveRower(dir) {
      const records = rowerRepository.list();
      if (!records.length) {
        alert("Datenbank ist leer — erst Profile speichern.");
        return;
      }
      const seat = activeSeat();
      if (!seat || !confirmProfileReplacement(seat.id, "Das nächste Datenbankprofil ersetzt Name und Körpermaße des aktiven Platzes")) return;
      const base = state.dbIdx < 0 ? dir > 0 ? -1 : 0 : state.dbIdx;
      const nextIndex = (base + dir + records.length) % records.length;
      const record = records[nextIndex];
      applyProfile(seat.id, record.value, { rowerRef: { id: record.id, revision: record.revision } });
      refreshDBSelect(record.id, true, seat.id);
      state.dbIdx = nextIndex;
      setDirty();
      buildControls();
      renderSeatTabs();
      render();
    }
    $("rPrev").addEventListener("click", () => stepActiveRower(-1));
    $("rNext").addEventListener("click", () => stepActiveRower(1));
    const SEATGAP = 128;
    function buildSeats() {
      const ss = state.strokeSide;
      const reference = state.seats.find((seat) => seat.id === state.referenceSeatId && runtimeForSeat(seat));
      const active = state.seats.find((seat) => seat.id === state.editSeatId && runtimeForSeat(seat));
      const fallbackOther = state.seats.find((seat) => seat.id !== reference?.id && runtimeForSeat(seat));
      const chosen = [];
      for (const seat of [reference, active, active?.id === reference?.id ? fallbackOther : null]) {
        if (seat && !chosen.some((candidate) => candidate.id === seat.id)) chosen.push(seat);
        if (chosen.length === 2) break;
      }
      if (!chosen.length) {
        const first = state.seats.find((seat) => runtimeForSeat(seat));
        if (first) chosen.push(first);
      }
      const defs = chosen.map((seat) => {
        const r = runtimeForSeat(seat);
        return {
          seatId: seat.id,
          position: seat.position,
          r,
          ox: -(state.seats.length - seat.position) * SEATGAP,
          ref: seat.id === state.referenceSeatId,
          side: ss * ((state.seats.length - seat.position) % 2 === 0 ? 1 : -1),
          name: r.name,
          resolution: assignmentResolution(seat)
        };
      });
      return defs.map((d) => {
        const catchSolution = resolveCatchAngle(d.r);
        const effPhiA = catchSolution.poseAngleDeg;
        const dv = derived(d.r, effPhiA);
        const b = solveBody(dv, state.t, state.recovery, d.r, effPhiA);
        b.arms = solveArms(dv, b, d.r);
        if (b.arms.some((arm) => !arm.reachable)) b.overreach = true;
        return {
          ...d,
          dv,
          b,
          catchSolution,
          reachAvailable: catchSolution.reachable,
          capped: !catchSolution.naturalResolved || catchSolution.limitedByReach || Math.abs(effPhiA - state.phiA) > 1.5
        };
      });
    }
    function render() {
      const seats = buildSeats();
      const primary = seats.find((x) => x.seatId === state.editSeatId) || seats.find((x) => x.ref) || seats[0];
      syncControls();
      renderSeatTabs();
      if (seats.length === 0 && playing) stopPlay();
      $("play").disabled = seats.length === 0;
      if (!primary) {
        renderEmptyModelState();
        renderActionPlan();
        renderWorkflowGuide();
        renderPhases();
        $("crewScope").textContent = crewScopeLabel($("preset").value);
        updateContext();
        return;
      }
      const dv = primary.dv;
      const refs = bodyRefs(dv, primary.r);
      renderStatus(dv, refs, primary.r, primary.catchSolution, seats);
      renderActionPlan();
      renderWorkflowGuide();
      renderCompactSummaries(dv, primary.r, primary.catchSolution, state.seats.find((seat) => seat.id === primary.seatId));
      if (!activeAssignment()) {
        $("compactProfileSummary").textContent = `${seatLabel(activeSeat())} ist frei · Körperrechnung aus · Visualisierung zeigt ${seatLabel(state.seats.find((seat) => seat.id === primary.seatId))}.`;
        $("cards").prepend(statusCardElement({ n: "Aktiver Platz", v: "Profil erforderlich", st: "info", statusText: "Nicht berechenbar", rng: "Profil wählen oder neu anlegen; keine Ersatzperson wird berechnet" }));
      }
      if (primary.resolution === "snapshot-missing" || primary.resolution === "stale") {
        $("cards").prepend(statusCardElement({
          n: "Profilverknüpfung",
          v: primary.resolution === "stale" ? "Snapshot veraltet" : "Datenbankeintrag fehlt",
          st: "bad",
          rng: "Berechnung nutzt den gespeicherten Snapshot · Profil bewusst neu zuordnen oder Platz freigeben"
        }));
      }
      renderForceCompare(seats);
      if (!isFolded("rower")) {
        if (activeRower()) renderVitruv(activeRower());
        else $("vitruv").replaceChildren(Object.assign(document.createElement("p"), { className: "hint", textContent: "Für diesen freien Platz gibt es noch keine Proportionsgrafik." }));
      }
      renderTop(dv, seats, primary);
      renderCross(dv, primary);
      renderSide(dv, seats, primary, refs);
      renderPhases();
      $("crewScope").textContent = crewScopeLabel($("preset").value);
      updateContext();
    }
    function renderEmptyModelState() {
      $("cards").replaceChildren(statusCardElement({
        n: "Körperrechnung",
        v: "Profil erforderlich",
        st: "info",
        statusText: "Nicht berechenbar",
        rng: "Ursache: kein belegter Platz · nächster Schritt: Profil wählen oder neu anlegen"
      }));
      $("chips").replaceChildren(chipElement({ n: "Datenqualität", v: "Keine Person erfunden · Visualisierung bleibt bis zur bewussten Zuordnung leer", st: "info" }));
      $("compactProfileSummary").textContent = `${seatLabel(activeSeat())} ist frei · Körpermaße fehlen.`;
      $("compactRigSummary").textContent = "Boot und Trimmplatz sind angelegt; Körper-, Reichweiten- und Mannschaftsvergleich starten erst nach einer Profilzuordnung.";
      $("forceCmp").style.display = "none";
      $("vitruv").replaceChildren(Object.assign(document.createElement("p"), { className: "hint", textContent: "Profil zuordnen, um die Proportionsgrafik zu sehen." }));
      for (const [hostId, label] of [["vTop", "Draufsicht"], ["vSide", "Seitenansicht"], ["vCross", "Querschnitt"]]) {
        const svg = newSVG($(hostId), 900, 260, `${label}: Profil erforderlich`, `Leerer ${label}-Prüfstand ohne erfundene Person.`);
        txt(svg, 450, 118, label, { "text-anchor": "middle", fill: "#f2e9d8", "font-size": 18, "font-weight": 750 });
        txt(svg, 450, 148, "Profil wählen oder neu anlegen · Körperrechnung bleibt bis dahin aus", { "text-anchor": "middle", fill: "#a8b6ae", "font-size": 12 });
      }
    }
    let playing = false, raf = null, animationProgress = 0, lastAnimationFrameTime = null;
    function stopPlay() {
      if (!playing) return;
      playing = false;
      cancelAnimationFrame(raf);
      lastAnimationFrameTime = null;
      $("play").classList.remove("on");
      $("play").textContent = "▶ Durchzug starten";
      $("play").setAttribute("aria-pressed", "false");
    }
    function startPlay({ markDirty = true } = {}) {
      if (playing) return true;
      if (motionPreference.matches) {
        announce("Animation ist wegen „Bewegung reduzieren“ deaktiviert.");
        return;
      }
      if ($("play").disabled) return false;
      if (markDirty) setDirty();
      animationProgress = cycleProgressFromStrokePose({ t: state.t, recovery: state.recovery });
      lastAnimationFrameTime = performance.now();
      playing = true;
      $("play").classList.add("on");
      $("play").textContent = "⏸ Stopp";
      $("play").setAttribute("aria-pressed", "true");
      raf = requestAnimationFrame(step);
      return true;
    }
    $("play").addEventListener("click", () => {
      if (playing) {
        stopPlay();
        return;
      }
      startPlay();
    });
    const motionPreference = window.matchMedia("(prefers-reduced-motion: reduce)");
    function preferredScrollBehavior() {
      return motionPreference.matches ? "auto" : "smooth";
    }
    const handleMotionPreference = (event) => {
      if (event.matches) stopPlay();
    };
    if (typeof motionPreference.addEventListener === "function") motionPreference.addEventListener("change", handleMotionPreference);
    else motionPreference.addListener?.(handleMotionPreference);
    function updateSpeed() {
      const speed = parseFloat($("speed").value);
      $("speedV").textContent = fmt(speed, 2) + "×";
      $("speed").setAttribute("aria-valuetext", `${fmt(speed, 2)}-fache Geschwindigkeit`);
    }
    $("speed").addEventListener("input", updateSpeed);
    updateSpeed();
    $("boatName").addEventListener("input", (e) => {
      e.target.value = limitName(e.target.value);
      setDirty();
      updateContext();
    });
    $("coxName").addEventListener("input", (e) => {
      e.target.value = limitName(e.target.value);
      setDirty();
    });
    function step(now) {
      if (!playing) return;
      const elapsed = lastAnimationFrameTime === null ? 0 : Math.max(0, now - lastAnimationFrameTime);
      lastAnimationFrameTime = now;
      animationProgress = advanceStrokeCycle(animationProgress, elapsed, parseFloat($("speed").value));
      const pose = strokePoseAtCycleProgress(animationProgress);
      state.t = pose.t;
      state.recovery = pose.recovery;
      render();
      raf = requestAnimationFrame(step);
    }
    function currentConfigOf() {
      return buildCurrentConfigDTO({
        boat: boatOf(),
        boatRef: selectedBoatId && selectedBoatRevision !== null ? { id: selectedBoatId, revision: selectedBoatRevision } : null,
        crew: state.crew.map((assignment) => ({
          seatId: assignment.seatId,
          trimId: assignment.trimId,
          rowerRef: assignment.rowerRef ? { ...assignment.rowerRef } : null,
          rower: profileOf(assignment.rower)
        })),
        editSeatId: state.editSeatId,
        referenceSeatId: state.referenceSeatId,
        mode: state.mode,
        heightRef: state.heightRef,
        kg: state.kg,
        t: state.t,
        recovery: state.recovery
      });
    }
    function scrubSingleSeatWorkspaceInLock() {
      const stored = workspaceRepository.get();
      const beforeRevision = workspaceRepository.snapshot().revision;
      if (!hasHiddenSingleSeatProfile(stored)) return Object.freeze({ changed: false, revision: beforeRevision });
      const minimized = minimizeHiddenSingleSeatProfile(stored);
      workspaceRepository.save(minimized.config, { expectedRevision: beforeRevision });
      return Object.freeze({ changed: true, revision: workspaceRepository.snapshot().revision });
    }
    async function loadWorkspaceStore() {
      try {
        await repositoryWrite(workspaceRepository, () => {
          const loaded = workspaceRepository.load();
          reportRepositoryLoad(loaded, "Der V2-Arbeitsstand");
          if (loaded.ok && loaded.migration?.migrated) {
            workspaceRepository.commitPendingMigration({ expectedRevision: loaded.state.revision });
            announce(loaded.migration.workspace ? "Der Arbeitsstand wurde atomar auf reale Sitzplätze migriert; zusätzliche Plätze bleiben bewusst frei." : "Der lokale Speichervertrag des Arbeitsstands wurde atomar aktualisiert; Fachwerte blieben unverändert.");
          }
          const allowLegacyMigration = canMigrateLegacy(loaded);
          const scrubbed = scrubSingleSeatWorkspaceInLock();
          if (scrubbed.changed) {
            announce("Veraltete versteckte Zweitplatzdaten wurden aus dem gespeicherten Einer-Arbeitsstand entfernt.");
          }
          if (allowLegacyMigration && workspaceRepository.get() === null) {
            const legacyRaw = storageAdapter.getItem("rudertrimm_current_v2");
            if (legacyRaw) {
              const parsed = JSON.parse(legacyRaw);
              if (parsed?.kind !== "rudertrimm.current-config" || !parsed.config) throw new TypeError("Alter Arbeitsstand hat kein gültiges Format.");
              const canonicalLegacy = migrateCurrentConfigToCurrent(parsed.config).value;
              workspaceRepository.save(canonicalLegacy, { expectedRevision: workspaceRepository.snapshot().revision });
              announce("Der bisherige V2-Arbeitsstand wurde kanonisch und datenminimiert in den getrennten Workspace-Speicher übernommen.");
            } else {
              const candidate = legacyV1WorkspaceCandidate();
              if (candidate) {
                workspaceRepository.save(candidate.workspace, { expectedRevision: workspaceRepository.snapshot().revision });
                announce("Der V1-Arbeitsstand wurde atomar auf reale Sitzplätze migriert; eingebettete Datenbanken bleiben strikt getrennt.");
              }
            }
          }
        });
      } catch (error) {
        console.error("Arbeitsstand konnte nicht initialisiert werden", error);
        reportError(error, "Arbeitsstand konnte nicht vollständig initialisiert oder datenminimiert werden");
      }
      workspaceViewState = workspaceRepository.get() === null ? "new" : "available";
      workspaceRevision.adopt(workspaceRepository.snapshot().revision);
      updateWorkspaceSaveState();
    }
    function updateWorkspaceSaveState() {
      const policy = workspaceSavePolicy({ viewState: workspaceViewState, canCommit: workspaceRevision.snapshot().canCommit });
      const button = $("bSave"), notice = $("workspaceNotice");
      button.setAttribute("aria-disabled", String(!policy.canAttempt));
      button.title = policy.message;
      notice.hidden = !policy.message;
      notice.textContent = policy.message;
    }
    $("bSave").addEventListener("click", async () => {
      const observed = workspaceRevision.snapshot();
      const policy = workspaceSavePolicy({ viewState: workspaceViewState, canCommit: observed.canCommit });
      if (!policy.canAttempt) {
        announce(policy.message);
        return;
      }
      if (policy.requiresConfirmation && !confirm(`${policy.message}

Mit Speichern wird er durch die aktuell sichtbaren Eingaben ersetzt. Fortfahren?`)) return;
      try {
        canonicalizeBoatName();
        for (const assignment of state.crew) canonicalizeRowerName(assignment.seatId);
        const config = currentConfigOf();
        const savedChangeVersion = workspaceChangeVersion;
        const demoAtSave = demoRestorePoint;
        await repositoryWrite(workspaceRepository, () => repositoryWrite(rowerRepository, () => {
          assertNoRetiredRowerReferences(config);
          return workspaceRepository.save(config, { expectedRevision: observed.observedRevision });
        }));
        const completion = workspaceSaveCompletionPolicy({
          savedChangeVersion,
          currentChangeVersion: workspaceChangeVersion,
          demoAtStart: demoAtSave,
          currentDemo: demoRestorePoint,
          playing
        });
        workspaceRevision.adopt(workspaceRepository.snapshot().revision);
        workspaceViewState = "synced";
        if (completion.clearDemo) endDemoSession();
        updateWorkspaceSaveState();
        render();
        if (storageMode !== "local") ephemeralDataPresent = true;
        setDirty(completion.dirty);
        announce(storageMode === "local" ? "Arbeitsstand persistent gespeichert." : storageMode === "session" ? "Arbeitsstand nur in diesem Tab gespeichert. Vor dem Schließen bitte exportieren." : "Arbeitsstand nur flüchtig gespeichert. Er geht bereits bei einem Reload verloren; bitte jetzt exportieren.");
        const sp = $("bSave").querySelector("span");
        sp.textContent = storageMode === "local" ? "Gespeichert ✓" : storageMode === "session" ? "Nur Tab ✓" : "Flüchtig ✓";
        setTimeout(() => sp.textContent = "Arbeitsstand speichern", 1400);
      } catch (error) {
        if (error?.code === "revision-conflict") {
          workspaceRepository.reloadFromExternal("save-conflict");
          workspaceRevision.markExternal(workspaceRepository.snapshot().revision);
          updateWorkspaceSaveState();
          setDirty();
        }
        reportError(error, "Arbeitsstand konnte nicht gespeichert werden");
      }
    });
    $("bLoad").addEventListener("click", () => {
      try {
        const config = workspaceRepository.get();
        if (!config) {
          if (workspaceRevision.snapshot().stale) {
            if (!confirm("Der Arbeitsstand wurde in einem anderen Tab entfernt oder war dort nicht mehr gültig. Lokale Eingaben beibehalten und als neuen Stand freigeben?")) return;
            workspaceRevision.adopt(workspaceRepository.snapshot().revision);
            workspaceViewState = "new";
            updateWorkspaceSaveState();
            setDirty();
            announce("Die externe Entfernung wurde übernommen. Lokale Eingaben bleiben ungespeichert und können jetzt als neuer Arbeitsstand gespeichert werden.");
          } else announce("Kein V2-Arbeitsstand vorhanden.");
          return;
        }
        if (dirty && !confirmOverwrite("Der gespeicherte Arbeitsstand ersetzt alle aktuellen Eingaben")) return;
        stopPlay();
        applyCurrentConfig(config);
        const linkedBoat = config.boatRef ? boatRepository.select(config.boatRef.id) : { ok: false };
        const linkIsCurrent = linkedBoat.ok && linkedBoat.record.revision === config.boatRef.revision;
        refreshBoatSelect(linkIsCurrent ? config.boatRef.id : "", linkIsCurrent);
        refreshDBSelect();
        lastPreset = config.boat.preset;
        lastBlade = config.boat.blade;
        endDemoSession();
        workspaceRevision.adopt(workspaceRepository.snapshot().revision);
        workspaceViewState = "synced";
        updateWorkspaceSaveState();
        buildControls();
        updateRig();
        updateMode();
        render();
        setDirty(false);
        announce(linkedBoat.ok && !linkIsCurrent ? "Arbeitsstand geladen · die gespeicherte Bootsbasis hat inzwischen eine andere Revision; der Snapshot bleibt unverändert." : "Arbeitsstand geladen.");
      } catch (error) {
        reportError(error, "Arbeitsstand konnte nicht geladen werden");
      }
    });
    $("bExport").addEventListener("click", () => {
      try {
        const includeNames = $("resultPrivacy").value === "names";
        if (!confirmResultShare("Messbericht", includeNames)) return;
        const rawConfig = currentConfigOf();
        const config = includeNames ? rawConfig : anonymizeCurrentConfig(rawConfig);
        const results = {};
        for (const seat of state.seats) {
          const assignment = assignmentFor(seat.id);
          if (!assignment) {
            results[seat.id] = {
              position: seat.position,
              label: seatLabel(seat),
              status: seat.rowerRef ? "profile-reference-unresolved" : "free",
              rowerRef: includeNames ? seat.rowerRef : null,
              modelStatus: "notCalculated"
            };
            continue;
          }
          const r = runtimeForSeat(seat), catchSolution = resolveCatchAngle(r), poseAngle = catchSolution.poseAngleDeg, dv = derived(r, poseAngle), refs = bodyRefs(dv, r), catchBody = solveBody(dv, 0, false, r, poseAngle), catchArms = solveArms(dv, catchBody, r);
          const actualAngle = catchSolution.actualAngleDeg;
          results[seat.id] = {
            position: seat.position,
            label: seatLabel(seat),
            name: includeNames ? assignment.rower.name : `Profil Platz ${seat.position}`,
            rowerRef: includeNames ? assignment.rowerRef : null,
            resolution: assignmentResolution(seat),
            uebergriff_cm: +dv.U.toFixed(1),
            momentarmverhaeltnis: +(dv.outb / dv.inb).toFixed(3),
            schlagweite_grad: +dv.SW.toFixed(1),
            dollenhoehe_ueber_wasser_cm: +dv.dWL.toFixed(1),
            knie_bei_90grad: +refs.b90.knee.toFixed(1),
            rollweg_bei_90grad_prozent: +refs.rollAt(refs.b90).toFixed(1),
            armwinkel_modell_grad: +refs.b0.armAng.toFixed(1),
            auslage_rigg_ziel_grad: +state.phiA.toFixed(1),
            auslage_ist_grad: actualAngle === null ? null : +actualAngle.toFixed(1),
            auslage_natural_catch_grad: catchSolution.naturalResolved ? +catchSolution.naturalAngleDeg.toFixed(1) : null,
            auslage_modell_pose_grad: +poseAngle.toFixed(1),
            natural_catch_aufgeloest: catchSolution.naturalResolved,
            natural_catch_restfehler_cm: +catchSolution.residualCm.toFixed(3),
            // Rückwärtskompatible Berichtsschlüssel; ihre Ziel-/Ist-Bedeutung ist oben explizit.
            auslage_angefordert_grad: +state.phiA.toFixed(1),
            auslage_effektiv_grad: actualAngle === null ? null : +actualAngle.toFixed(1),
            auslage_reichweitenbegrenzt: catchSolution.limitedByReach,
            auslage_reichweite_verfuegbar: catchSolution.reachable,
            griffziele_bei_auslage_erreichbar: catchArms.every((arm) => arm.reachable),
            auslage_limit_grund: !catchSolution.naturalResolved ? "Natural-Catch-Gleichung ohne Nullstelle im 20-88-Grad-Pruefbereich" : catchSolution.limitedByReach ? "3D-Griffreichweite des unkalibrierten Modells" : "keines",
            natural_catch_modell: "Fa + Koerper; Knie 58 Grad; Vorlage 16 Grad; unkalibriert",
            modelStatus: "needsCalibration"
          };
        }
        const report = {
          format: "rudertrimm-report",
          schemaVersion: SCHEMA_VERSION,
          appVersion: APP_VERSION,
          buildDate: APP_BUILD_DATE,
          buildId: APP_BUILD_ID,
          shellRevision: APP_SHELL_REVISION,
          exportedAt: (/* @__PURE__ */ new Date()).toISOString(),
          privacy: includeNames ? "names-included" : "anonymized",
          config,
          results,
          result: actionPlanForExport(lastActionPlan, includeNames),
          sourceStatus: { worldRowing2017Presets: "verified", bodyKinematics: "needsCalibration", armAngle: "needsCalibration" },
          privacyNotice: "Enthält nur den aktuellen Arbeitsstand; keine lokalen Boots- oder Rudererdatenbanken."
        };
        downloadJson(report, `rudertrimm-v2-bericht-${$("preset").value}.json`);
        announce("Messbericht exportiert.");
      } catch (error) {
        reportError(error, "Messbericht konnte nicht exportiert werden");
      }
    });
    $("bReset").addEventListener("click", () => {
      if (confirm("Aktuelles Preset erneut auf alle realen Plätze anwenden?")) {
        if (applyPreset()) announce("Presetwerte für alle Plätze wiederhergestellt.");
      }
    });
    await loadDB();
    refreshDBSelect();
    await loadBoats();
    refreshBoatSelect();
    await loadEfaCandidates();
    await loadWorkspaceStore();
    if (storageMode !== "local" && (rowerRepository.list().length > 0 || boatRepository.list().length > 0 || efaCandidateRepository.list().length > 0 || workspaceRepository.get() !== null)) {
      ephemeralDataPresent = true;
    }
    $("storageState").textContent = storageMode === "local" ? "Persistenter lokaler Speicher · atomare Mehrtab-Schreibvorgänge aktiv" : storageMode === "session" ? `Tab-Sitzungsspeicher · ${storageFallbackReason} · Export vor dem Schließen` : "Flüchtiger Speicher · Verlust bei Reload";
    $("storageState").classList.toggle("storage-warning", storageMode !== "local");
    if (storageMode !== "local") announce(storageMode === "session" ? `Daten bleiben nur in diesem Tab erhalten (${storageFallbackReason}). Vor dem Schließen bitte exportieren.` : "Browser-Speicher ist blockiert. Daten gehen bei Reload oder Schließen verloren; bitte sofort exportieren.");
    rowerRepository.subscribe((event) => {
      if (event.type === "commit" && storageMode !== "local") ephemeralDataPresent = true;
      if (event.type === "external-sync-error") {
        reportError(event.error, "Ruderer-Datenbank konnte extern nicht gelesen werden");
        return;
      }
      if (event.type !== "external-sync") return;
      const selectedId = $("dbSelect").value;
      const current = selectedId ? rowerRepository.select(selectedId) : { ok: false };
      const observed = rowerSelection.snapshot();
      const stale = current.ok && observed.id === selectedId && current.record.revision !== observed.revision;
      refreshDBSelect(selectedId);
      state.dbIdx = selectedId ? rowerRepository.list().findIndex((record) => record.id === selectedId) : -1;
      if (stale) {
        $("dbUpdate").disabled = true;
        $("dbDelete").disabled = true;
        $("dbSelect").title = "Datensatz wurde in einem anderen Tab geändert · bitte neu auswählen";
      }
      if (event.result?.ok === false) {
        reportRepositoryLoad(event.result, "Externe Rudererdaten");
        return;
      }
      announce(stale ? "Das gewählte Rudererprofil wurde in einem anderen Tab geändert. Zum Übernehmen bitte neu auswählen." : "Ruderer-Datenbank wurde aus einem anderen Tab synchronisiert.");
    });
    boatRepository.subscribe((event) => {
      if (event.type === "commit" && storageMode !== "local") ephemeralDataPresent = true;
      if (event.type === "external-sync-error") {
        reportError(event.error, "Boots-Datenbank konnte extern nicht gelesen werden");
        return;
      }
      if (event.type !== "external-sync") return;
      const selectedId = $("boatSelect").value;
      const current = selectedId ? boatRepository.select(selectedId) : { ok: false };
      const stale = current.ok && selectedBoatRevision !== null && current.record.revision !== selectedBoatRevision;
      refreshBoatSelect(selectedId);
      if (stale) {
        $("boatUpdate").disabled = true;
        $("boatDelete").disabled = true;
        $("boatSelect").title = "Datensatz wurde in einem anderen Tab geändert · bitte neu auswählen";
      }
      if (event.result?.ok === false) {
        reportRepositoryLoad(event.result, "Externe Bootsdaten");
        return;
      }
      announce(stale ? "Das gewählte Boot wurde in einem anderen Tab geändert. Zum Übernehmen bitte neu auswählen." : "Boots-Datenbank wurde aus einem anderen Tab synchronisiert.");
    });
    efaCandidateRepository.subscribe((event) => {
      if (event.type === "commit" && storageMode !== "local") ephemeralDataPresent = true;
      if (event.type === "external-sync-error") {
        reportError(event.error, "eFa-Vormerkliste konnte extern nicht gelesen werden");
        return;
      }
      if (event.type !== "external-sync") return;
      refreshEfaCandidateList();
      resetEfaPreview("eFa-Vormerkliste wurde in einem anderen Tab geändert · Vorschau erneut erstellen.");
      if (event.result?.ok === false) {
        reportRepositoryLoad(event.result, "Externe eFa-Vormerkliste");
        return;
      }
      announce("Lokale eFa-Vormerkliste wurde aus einem anderen Tab synchronisiert.");
    });
    workspaceRepository.subscribe(async (event) => {
      if (event.type === "commit" && storageMode !== "local") ephemeralDataPresent = true;
      if (event.type === "external-sync-error") {
        reportError(event.error, "Arbeitsstand konnte extern nicht gelesen werden");
        return;
      }
      if (event.type === "external-sync") {
        workspaceViewState = workspaceRepository.get() === null ? "new" : "available";
        workspaceRevision.markExternal(event.revision);
        updateWorkspaceSaveState();
        setDirty();
        if (event.result?.ok === false) {
          reportRepositoryLoad(event.result, "Der externe Arbeitsstand");
          return;
        }
        try {
          const scrubbed = await repositoryWrite(workspaceRepository, scrubSingleSeatWorkspaceInLock);
          if (scrubbed.changed) {
            workspaceRevision.markExternal(scrubbed.revision);
            updateWorkspaceSaveState();
            announce("Externe versteckte Zweitplatzdaten wurden aus dem Einer-Arbeitsstand entfernt. Der neue Stand bleibt bis zum bewussten Laden gesperrt.");
            return;
          }
          announce("Der gespeicherte Arbeitsstand wurde in einem anderen Tab geändert. Speichern ist gesperrt; lokale Eingaben zuerst exportieren oder den neuen Stand laden.");
        } catch (error) {
          reportError(error, "Externe Einer-Daten konnten nicht minimiert werden; der Arbeitsstand bleibt gesperrt");
        }
      }
    });
    window.addEventListener("beforeunload", (event) => {
      if (!dirty && !(storageMode !== "local" && ephemeralDataPresent)) return;
      event.preventDefault();
      event.returnValue = "";
    });
    setEfaMappingVisibility();
    buildControls();
    updateRig();
    updateMode();
    updateCoxControl();
    initPresentationMode();
    initFold();
    initVisualViewTabs();
    initVisualSizing();
    initActionResult();
    initWorkflowGuide();
    render();
    setDirty(false);
    function updateNetworkStatus() {
      if (isDirectFile) {
        $("offlineState").textContent = "Direktstart · PWA/Offline-Cache nicht verfügbar";
        return;
      }
      $("offlineState").textContent = navigator.onLine ? "Online · Offline-Cache wird geprüft" : "Offline · lokale Funktionen verfügbar";
    }
    window.addEventListener("online", updateNetworkStatus);
    window.addEventListener("offline", updateNetworkStatus);
    updateNetworkStatus();
    function serviceWorkerMode(protocol, supported) {
      if (protocol === "file:") return "direct-file";
      if ((protocol === "http:" || protocol === "https:") && supported) return "register";
      return "unsupported";
    }
    function scheduleServiceWorkerRegistration(task) {
      if (document.readyState === "complete") {
        void task();
        return;
      }
      window.addEventListener("load", () => void task(), { once: true });
    }
    async function registerServiceWorker() {
      try {
        const registration = await navigator.serviceWorker.register("sw.js", { scope: "./" });
        $("offlineState").textContent = navigator.onLine ? "Online · PWA registriert" : "Offline · PWA aktiv";
        registration.addEventListener("updatefound", () => {
          const worker = registration.installing;
          worker?.addEventListener("statechange", () => {
            if (worker.state === "installed" && navigator.serviceWorker.controller) {
              $("offlineState").textContent = "Update bereit · beim nächsten Start aktiv";
              announce("Eine neue App-Version ist für den nächsten Start bereit.");
            }
          });
        });
      } catch (error) {
        console.error("Service Worker konnte nicht registriert werden", error);
        $("offlineState").textContent = "Online · Offline-Modus nicht bereit";
      }
    }
    const swMode = serviceWorkerMode(globalThis.location?.protocol, "serviceWorker" in navigator);
    if (swMode === "register") scheduleServiceWorkerRegistration(registerServiceWorker);
    else if (swMode === "direct-file") $("offlineState").textContent = "Direktstart · PWA/Offline-Cache nicht verfügbar";
    else $("offlineState").textContent = "Offline-Modus nicht unterstützt";
  }
  function reportBootFailure(error) {
    BOOT_ROOT.dataset.rudertrimmBoot = "failed";
    console.error("Rudertrimm konnte nicht initialisiert werden", error);
    const release = globalThis.RUDERTRIMM_RELEASE;
    const buildState = document.getElementById("buildState");
    if (buildState) buildState.textContent = `Startfehler · ${release?.appVersion ?? "Version unbekannt"}`;
    const status = document.getElementById("errorStatus");
    if (status) {
      status.hidden = false;
      status.textContent = "Rudertrimm konnte nicht initialisiert werden. Bitte den App-Ordner vollständig entpacken und erneut öffnen.";
    }
  }
  globalThis.RUDERTRIMM_BOOT_PROMISE = bootRudertrimm().then(() => {
    BOOT_ROOT.dataset.rudertrimmBoot = "ready";
    const status = document.getElementById("errorStatus");
    if (status?.dataset.bootFallback === "true") {
      status.hidden = true;
      status.textContent = "";
      delete status.dataset.bootFallback;
    }
  }).catch(reportBootFailure);
})();
